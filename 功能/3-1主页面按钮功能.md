# 主页面按钮功能（代码现状）

引用需求真源：`REQ-DOC-SINGLE-SOURCE`、`REQ-UI-RUNTIME-REFRESH-RATE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-SHM-DISPLAY-PROJECTION`、`REQ-NATIVE-OWNER-FIRST`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-LINUXCNC-COMMAND-GATE`、`REQ-G92-NATIVE-RUNTIME`、`REQ-WCS-NATIVE-OWNER`、`REQ-ROTARY-UNWRAP-ABSOLUTE-PROOF`、`REQ-ROTARY-REBASE-NATIVE-GATE`、`REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-RTCP-NATIVE-STATUS-SOURCE`、`REQ-START-RUN-HOT-PATH`、`REQ-MAIN-ESTOP-LATENCY`、`REQ-MICROKERNEL-MOTION-LOOKAHEAD`。

## AI 阅读入口

- 本文定位：主页面显示、按钮语义、Start 分支、急停/取消急停和主页面状态消费边界。
- Start 先读 `REQ-START-RUN-HOT-PATH`，急停先读 `REQ-MAIN-ESTOP-LATENCY`，刷新频率先读 `REQ-UI-RUNTIME-REFRESH-RATE`，显示状态先读 SHM/State Publisher 相关 REQ。
- 普通控制 UI 只发 native command gate/owner 请求；VPS/授权/下载和设置长流程才按对应 owner 走 Command Broker/后台 job。UI 只消费允许的 SHM 同帧显示投影，不得用本地视觉状态、按钮 dirty、Broker result 文件或旧缓存替代 native/microkernel 回读。
- 本文消费 `功能/0-1开机参数入内存.md` 的最高启动内存架构：UI 运行闭包、产品自写运行代码闭包、按钮表、页面资源、动作登记表和 provider 在开机阶段进入 RAM；Start/Resume、急停、点动、WCS、RTCP 等热路径不得临时读盘、懒导入脚本、扫描目录或读取 UI 资源兜底。
- G-code 打开、关闭、程序切换和 UI 刀路资源释放必须服从 `功能/0-1开机参数入内存.md` 的 mlockall 资源预算：Start/Resume 热路径不得新增大块分配、释放扫描或 `malloc_trim(0)`；`program_close`、程序切换和页面销毁等非实时边界才允许释放资源域并执行受控 trim，最终以板端 RSS/Locked 水位验收。

本文记录当前 `lvgl_app` 主页面已经接入的按钮、状态面板和动作分发现状。本文是现状说明，不把未接入目标写成已实现功能。

## 代码依据

- `lvgl_app/src/v3_page_main.c`：主页面布局、速度面板、跟随误差面板、CPU 面板注册。
- `lvgl_app/src/v3_ui_model.c`：`RE_V3_PAGE_MAIN` 可见按钮表。
- `lvgl_app/src/v3_ui_app.c`：按钮 `shell_action` 分发。
- `lvgl_app/src/v3_ui_command.c`：回零、启动、暂停、点动、归零、WCS、倍率、急停等 command 请求。
- `lvgl_app/src/v3_ui_first_point.c`：首点按钮 command 请求。
- `lvgl_app/src/v3_widget_coordinates.c`：主页面坐标显示和坐标点选。
- `lvgl_app/src/v3_ui_state.c`：状态帧刷新到 UI。

## 主页面组成

`create_main_shell()` 当前创建以下区域：

1. 左侧刀路视图：`re_v3_toolpath_view_create(page)`。
2. 坐标显示控件：`create_main_coordinate_widget(page)`。
3. 手动输入/程序预览模块：`create_main_mdi_module(page)`。
4. 右侧速度面板：主轴转速、主轴倍率、进给速度、进给倍率。
5. 右侧跟随误差面板：X/Y/Z/A/C 五轴。
6. 右下 CPU 面板：`cpu0`、`cpu1`。
7. 主页面按钮：`create_buttons_for_page(page, RE_V3_PAGE_MAIN)`。

## v3 视觉布局继承规则

主页面及由主页面按钮进入的本地 UI 页面（程序页、MDI 页、刀具补偿、探测、偏置、IO、网络、设置页）必须保持冻结 v3 视觉参照的屏幕尺寸、区域位置、表格列、键盘、底部按钮、右侧导航按钮、颜色和文案；v5 只能替换底层 owner 和运行语义，不能把这些页面改成自造的标题+单行+返回、说明文字、占位提示或另一套布局。

本文只维护主页面入口、主页面按钮语义、Start/急停 owner 和主页面状态消费边界。设置页具体布局、轴参数表、G53/B 中心、驱动动作行和 `保存并重启` 口径以 `功能/2-2设置页总线模式设置项作用说明.md`、`功能/2-3设置页每个按钮作用和目的.md` 及对应 owner 为准，本文不重复维护设置页坐标细节，避免形成第二套设置页需求。

实现前必须先查对应冻结 v3 视觉参照和当前 v5 owner，按源码坐标、控件层级、文案和颜色落地；不得凭截图印象近似重画。每次改 UI 布局前必须能指出当前 v5 owner 与冻结 v3 参照的源码函数或 `object_tree.txt` 坐标锚点；验收时至少保留当前页面截图和关键控件坐标锚点，未对齐前不能写“v3 一致”。v3 有专用页面的入口（例如程序、MDI、刀具补偿、偏置、网络、设置页）按专用源码恢复；v3 本身只走 `create_aux_shell()` 的入口（例如探测、IO）按 v3 通用辅助页框架恢复，不得新增另一套自造页面。

`打开程序` 页必须保持 v3 程序页格局：左上搜索框、`本机程序文件` 表格、名称/大小/创建日期/修改日期列、右侧 CNC 键盘、顶部 `刷新` 和底部 `本机/删除/打开修改/返回` 按钮。程序行第一次触摸只选中，同一行第二次触摸打开运行；显示可以来自 v5 当前程序目录和 runtime metadata，但前台布局不得偏离 v3。

`手动输入` 页必须保持 v3 MDI 格局：左侧 MDI 编辑区、行号、`MDI>` 输入文本、右侧 CNC 键盘、底部 `清空/发送/保存/关闭` 按钮。主页面 `手动输入` 进入时创建空白 MDI 文本；只有 `发送`/`启动` 路径才按 Start/MDI owner 下发，普通键盘输入只改 UI 内存中的可见文本。

主页面 MDI/程序预览区必须保持 v3 `create_main_program_edit_hit_area()` 的透明命中区语义：坐标 `x=0,y=441,w=560,h=154`，单击只记录双击起点，不发送 MDI、Start、运动或控制命令；550ms 内第二次点击才进入 MDI 编辑页。双击进入前必须把当前可编辑对象载入 MDI 编辑缓冲：当前 runtime 为 MDI 时载入当前 MDI 文本；当前 runtime 为已打开程序时优先使用运行内存中的原始 G-code 文本和源路径作为修改对象；当前 UI 已有未下发 MDI 编辑文本时保持该文本；没有已打开程序或文本、文本超出 MDI 编辑缓冲、路径不是本机 G-code 目录内可写回文件时 fail-closed 并停留当前页，不得默认加载 `cc-ac.ngc`、`cc-bc.ngc` 或其它 fallback 程序。程序页 `打开修改` 与主页面预览区双击必须复用同一条“打开到 MDI 修改区”语义；MDI 页 `保存` 只允许在存在有效原始 G-code 文件元数据时写回该文件，不能替代 `发送`/`启动` 运行路径。

MDI/主页面程序预览高亮行必须跟随 LinuxCNC/微内核 native actual 的当前执行行：MDI 运行态优先使用 native MDI run line/command；暂停态优先显示 native paused line，其次使用 fresh `current_line`，最后才使用 fresh `motion_line`；非暂停程序运行态必须优先使用 fresh `motion_line` 作为实际队列执行行，只有没有 fresh `motion_line` 时才允许退回 fresh `current_line`，不得把解释器预读到后续行的 `current_line` 当成正在执行行。高亮行必须始终显示在主页面 MDI/程序预览区域内：当 native actual 行号不在当前 4 行可见窗口内时，预览窗口必须随行号滚动/换页，让 actual 行落入可见行并高亮；不得停留在文件首行、上次可见窗口或打开程序时的缓存窗口。程序打开但还没开始运行、还没有同一程序 native actual 行历史时，默认高亮第 1 行，并让第 1 行显示在预览区内；操作员在空闲/非执行态手指拖动查看文本时，可高亮当前 4 行可见窗口第一行作为阅读光标，但这只表达查看位置，不得作为 native execution 证明。开始运行后，高亮行视觉位置默认固定在 MDI/程序预览 4 行窗口的第一行：当前 native actual 行号变化时，窗口起始行跟着变为该行号，让当前执行行出现在第一行；运行过程中的每个 fresh native actual 行号变化都必须驱动下一帧预览滚屏，不得只在程序结束、暂停、急停或慢速轮询后跳到最终行；只有执行到 G-code 最后几行、后续行数不足以填满 4 行窗口时，窗口固定到文件最后一屏，高亮才从第一行向下移动，直到最后一行停在窗口最后一行。高亮表达“当前或最近一次 native actual 执行/暂停所在行”：同一已打开程序内一旦收到 fresh native actual 行号，UI 必须保存该 native 行号并持续高亮、持续让该行落在 4 行预览窗口内；运行结束、Stop、Abort、急停或 native 后续行号暂时清零后，必须停留并高亮急停/停止前保存的当前执行行，不得回到第 1 行，也不得只保留滚动位置而清掉高亮。打开新程序、载入新 MDI、当前可见文本无法映射到该 native 行号时，才清掉保存的 native 高亮行。若已存在保存的高亮行或运行/暂停时出现 fresh native actual 行号，则 native actual 行号优先，必须强制滚回当前/最近执行行所在窗口。

## 通用分发规则

1. 主页面可见按钮来自 `re_v3_buttons[]` 中 `page == RE_V3_PAGE_MAIN` 的记录。
2. 按钮事件进入 `handle_button_press()` 后按 `shell_action` 分发。
3. 急停按钮走最高优先级本地路径，不受普通 `command_busy` 拦截。
4. 普通按钮只保留 50ms 重复点击去重；`command_busy` 只能作为显示/诊断字段，不得让非 VPS 普通按钮在 UI 本地拒绝下发。
4a. UI 级交互防误触只能作为视觉提示或真实 owner 返回后的状态表达，不能成为新的产品层 gate。按钮可根据 native/microkernel actual 做状态弱化、图标变化或提示，但除急停直通、真实不适用、资源确实不存在或 native owner 明确 fail-closed 外，不得用 UI 置灰替代动作打通；强制触发时仍应进入对应 native owner 并取得结构化结果。
5. `*_LOCAL` 类动作只改 UI 本地状态或页面，不向后端下发运动命令。
6. 非 VPS/授权/下载链路的普通 `*_COMMAND` 不得再通过 C 端 JSON/Broker 请求构造器发送；已具备 native/LinuxCNC gate 的动作由 UI C 直接调用 canonical native gate，未补回 native gate 的动作保持真实缺口记录，不恢复旧 C JSON 中介。
7. C 端新增或改动的普通控制请求不得手写 `snprintf` JSON、不得恢复 `v3_json_utils`、不得新增 `re_v3_product_command_broker_request_direct` 或等价 Broker direct 胶水；VPS/授权/下载链路如需 payload，只能留在对应身份链路 owner 内。
8. 主页面运动动作失败不再弹全屏窗口。启动、回零、点动、连续点动、首点、暂停/继续、急停/取消急停、RTCP 开关、加工坐标系切换和加工坐标归零只写顶部短提示与事件日志；不得在这些动作分支里保留 `re_v3_ui_show_error_popup()`、`show_command_result_failure_popup()` 或专用 popup 格式化。
9. 顶部短提示和事件日志不得出现底层控制内核品牌或项目字眼，只显示面向操作者的中文原因、结果码和可判断的直接原因。

## 主页面显示总规则

主页坐标/轨迹、转速、进给速度和主轴/进给倍率这些允许字段只能来自 State Publisher 发布到 typed SHM 的同一帧运行内存投影；当前模态、WCS、RTCP、tool/TLO、安全、Home、active mode、CPU/网络和动作结果必须直连微内核/native/provider 或诊断 owner。UI 不能直接读取 `/proc`、`/run` JSON、Broker result、settings/runtime 文件、旧本地缓存或按钮 dirty 值作为显示真源。SHM 缺帧、typed group 缺失、版本不匹配或字段降级时，主页对应显示必须进入 `--`/degraded 状态，不得继续显示旧帧数值。

主页面刷新频率必须分层：坐标大字、轨迹当前位置、红/绿点、刀柄/刀尖动态线、转速、进给速度、主轴/进给倍率和 MDI/程序预览 native actual 行号滚屏按 30Hz 目标刷新；普通按钮状态轮询按 10Hz；右下急停按键的显示状态、急停 latch 和 Machine Enable native 回读刷新按 10Hz；CPU/网络、普通诊断文字、后台信息、非运动状态摘要和其它不影响运行连续感的 UI 显示按 5Hz 即可。运行期刷新禁止用周期性整屏刷新或整页重建兜底，必须按坐标数字、按钮、急停、轨迹动态图层、程序预览行、诊断文字等 dirty 对象/dirty 区域分块按需刷新；页面切换首帧 cache/blit 不是稳定刷新循环。急停按下事件本身是 edge 直通 native safety owner，不能等待下一次 10Hz 轮询；刷新节流只影响显示和普通轮询，不能影响命令下发 freshness、急停 0.1s 生效链路、动作证明或安全/运动门禁。

G-code 运行中，主页坐标大字、刀路当前位置、红/绿点和实际刀尖/刀柄反馈点必须继续消费 State Publisher `motion_fast` 的 30Hz 级 fresh native 状态 帧。CPU 优化只能删除/降频非必要诊断、远程转发、重复 LVGL 写入、隐藏分区刷新或慢投影重算；不得把坐标/刀路主刷新降到低频、不得跳过实际位置帧、不得用旧坐标缓存或命令坐标冒充 30Hz actual。

主页普通按钮控制链为 UI C -> 已登记 native command gate -> LinuxCNC/HAL/EtherCAT/native owner；VPS/授权/下载和设置长流程按对应 owner 使用 Command Broker UDS/后台 job。按钮表、动作登记表和 provider 必须来自开机 RAM 闭包；运行期按钮下发不得通过硬盘脚本发现、动态导入 action、读取 wrapper 或读取 UI 资源兜底。按钮成功只允许由 native/对应 owner 触发结果和 microkernel/native 回读确认，不允许用主页本地视觉状态替代后端执行结果。

主页面按钮高亮只允许表达“当前已生效/当前选中/当前实际模态”状态，不能表达“可点击”“已提交请求”或“默认猜测”。WCS 组只能高亮 native actual 当前 WCS；`X1/X10/X100` 只能高亮当前点动步距；`机械全轴` 只能在当前确实选择 MCS 全轴时高亮；RTCP 只能在 fresh native actual 为 ON 时高亮；视图按钮只能高亮当前视图。启动、暂停、回零、归零、点动、首点、打开程序、手动输入等动作按钮没有同源已生效状态时必须保持非高亮底色，即使按钮可点击或命令已提交，也不得提前用高亮冒充已生效。

所有页面、所有可见按钮必须复用同一个中央按钮视觉入口，不得由主页面、设置页、弹窗或局部页面各自保留不同的 pressed 清理实现。黄色只允许表达真实触摸/远程输入按下期间的 LVGL `PRESSED` 瞬时反馈；收到 `RELEASED`、`PRESS_LOST` 或输入取消后，必须在该输入事件事务内立即清除 pressed 状态、立即刷新，并恢复为原底色或对应的非黄色 actual 状态色，不能等待按钮命令、Home、驱动动作、弹窗动作或下一帧 UI 刷新结束。任何按钮都不得把黄色用于“命令已提交”“等待结果”“执行中”“当前选中”“当前生效”或错误提示后的残留状态。

动作按钮在松手后的执行态也统一遵守同一规则：只有对应 command gate/native/provider owner 能证明本次 transaction 已进入执行中，按钮才允许使用统一的非黄色 active 状态色；transaction 成功、失败、取消或失去 fresh owner 证明时必须立即恢复原底色或对应 actual 状态色。`回零` 的 `机械全轴` 对应 Home transaction，MCS/WCS 单轴对应 positioning transaction；执行中可显示非黄色 active 高亮，但 pressed 黄色必须在手指/远程指针松开时先立刻消失，不能一直保持到回零结束。没有可回读执行态的普通命令按钮松手后立即恢复，不得自造执行中高亮；命令已提交但未执行、旧 result、旧 `all_homed=true`、当前位置接近 0、pressed 状态残留或本地缓存都不得维持黄色/高亮。

## 状态面板现状

| 区域 | 当前行为 |
|---|---|
| 机械坐标大字 | X/Y/Z 来源为 SHM `mcs` 显示投影；另外两行旋转轴的字母和槽位必须来自 fresh Active Model descriptor/readback，AC 显示 A/C、BC 显示 B/C，后续模型显示自己的登记轴组，未知模型不得回退 A/C。对应旋转轴必须显示同一 generation 中由 SHM 机械坐标输入和 native rotary/count-domain 解释共同生成的精确等效角，不得显示原始多圈机械角，也不得对等效角做 0 点吸附、包角、取整、缓存平滑或旧值兜底。主页面机械坐标和加工坐标数值、设置页右上角小“机械坐标/MCS”面板必须复用 v3 `v3_widget_coordinates_canvas.inc` 的七段数码字 canvas 视觉：固定宽度数字区域、固定小数点锚点、上下行小数点垂直对齐、整数向小数点左侧扩展、小数向小数点右侧扩展、同款数字/负号/小数点绘制、同款颜色和选中高亮；普通 LVGL label 只能作为透明点击/测试后备，不得成为操作者可见坐标字体。 |
| 加工坐标 | WCS、G54-G59.3、WCS offset 和 active WCS 必须直连 LinuxCNC interpreter/native/WCS owner readback；SHM 只承载允许的坐标显示投影，不保存、不证明、不发布 WCS 真源。坐标点选只改变后续点动、归零和 Active Model 对应旋转轴等效回零的 UI 目标。 |
| 主轴转速 rpm | 来自 State Publisher typed SHM 的 `spindle_speed_rpm`，由 LinuxCNC/native spindle 状态进入运行内存后投影；无效或缺帧显示 `--`。 |
| 主轴倍率 | 来自 `status->spindle_override` 的 typed native/运行内存回读或允许的 SHM 显示投影；拖动滑条释放后发送 `spindle_override` 目标倍率，请求不得携带 SHM/display `status_epoch` 作为 native 准入或执行证明，点击数值区复位为 100%。显示值只在 native 回读有效时更新，不能用滑条 dirty 值、`/run` 结果或 `config_cache` 作为稳定显示。 |
| 进给速度 | `status->linear_velocity_mm_per_min` 有效时显示绝对值，小于 0.05 显示 0.0，无效显示 `--`。 |
| 进给倍率 | 来自 `status->feedrate_override` 的 typed native/运行内存回读或允许的 SHM 显示投影；拖动滑条释放后发送 `feed_override` 目标倍率，请求不得携带 SHM/display `status_epoch` 作为 native 准入或执行证明，点击数值区复位为 100%。显示值只在 native 回读有效时更新，不能用滑条 dirty 值、`/run` 结果或 `config_cache` 作为稳定显示。 |
| 跟随误差 | 五轴常驻显示，计算为 `status->mcs[i] - status->cmd_mcs[i]`，格式 `%s: %+.3f`，无效时显示 `--.---`。 |
| CPU | 不属于 `/dev/shm/v3_status_shm` 允许字段；没有单独 OS/诊断 provider 时显示 `--%`，不得从 SHM `system_metrics`、心跳 JSON 或 ready 文件补成有效。 |

## 主页面按钮清单

### 右侧导航与运行控制

| 按钮 | action | 当前分发 | 当前行为 |
|---|---|---|---|
| 主页面 | `page_main` | `NAV_MAIN` | 本地切回主页面，清除顶部消息，不发后端命令。 |
| 刀具补偿 | `popup_tool` | `NAV_TOOL` | 本地进入刀具补偿页。 |
| 探测 | `popup_probe` | `NAV_PROBE` | 本地进入探测页。 |
| 偏置 | `popup_offset` | `NAV_OFFSET` | 本地进入偏置页。 |
| IO设置 | `popup_io` | `NAV_IO` | 本地进入 IO 页。 |
| 系统设置 | `page_settings` | `NAV_SETTINGS` | 本地进入设置页。 |
| 网络 | `popup_network` | `NAV_NETWORK` | 本地进入网络只读详情页。 |
| 暂停 | `popup_pause` | `PAUSE_COMMAND` | **暂停状态完全依赖微内核原生态判断**：UI C 端必须直连回读微内核解释器原生状态（例如 `interp_state == INTERP_PAUSED`）。若微内核原生状态为已暂停，则下发 `Set Resume` 恢复运行；若微内核原生状态为运行中，则下发 `Set Pause` 暂停运行。不得使用任何 UI 本地标志位、Broker 缓存或其它自造中间状态作为判断依据。 |
| 启动 | `start` | `START_COMMAND` | **启动决策与执行完全基于微内核原生态状态**：按下启动按钮时，必须直连回读微内核原生解释器状态（`interp_state`）。如果是微内核原生态暂停（`INTERP_PAUSED`），UI C 端直接下发 `Set Resume` 恢复运行，由微内核从底层保存的暂停点继续向下执行；如果不是微内核原生暂停，UI C 端直接下发运行指令：程序模式只使用 Program Open 阶段已经登记的原始程序身份，`runtime_program_path` 必须等于操作员打开的原始 G-code 文件路径，再 `Set Mode Auto`、`Set Run 0`、`Set Resume` 从第一行连续向下执行；MDI 模式直接发送已在 UI 内存中的 MDI 行。只读 resident identity/预览内存可以存在，但运行副本、过滤文件、sidecar 或预览产物不得成为 `runtime_program_path`。Start/Resume 热路径不得读程序文件、计算 SHA、重新生成 runtime 文件、扫描脚本、读配置或等待 proof。启动判定不得引入任何 UI 本地标志、Broker 缓存、文件缓存行号或外部 V3 自造信标。真实运动与安全门禁均由微内核/Realtime 核心底层负责。 |
| 急停 | `estop` | `ESTOP_PRIORITY_LOCAL` | **急停与恢复完全依赖微内核原生安全/使能状态**：按钮状态及执行分支必须与微内核原生 Machine Enable 状态一致，并同时消费原生急停锁存状态。若微内核回读 `enabled=false` 或 `stat.estop == 1`，按钮显示为“取消急停”，点击发送 `estop_or_reset` Urgent 请求，执行微内核 reset/cancel-estop 并发起上使能；只有微内核回读 `enabled=true` 且急停锁存已清时，按钮才显示“急停”，点击发送 `estop_force` 直通微内核安全层触发急停。微内核使能状态未知但急停锁存已知为急停时仍显示“取消急停”；两者均未知或仅确认未急停时保持急停直通语义。不得将驱动 Operation Enabled、HAL helper 写 pin、自造急停循环状态当做急停/使能真源，按钮底色始终保持红色，在取消急停等待期间显示黄色 stop 图标，状态以微内核回读真值为唯一源。 |

急停实时下沉硬边界：`estop_force` 的执行 owner 必须下沉到 LinuxCNC/微内核/HAL/EtherCAT realtime/native safety 层，形成常驻 emergency latch/gate；UI、remote input、Command Broker 和已登记 urgent/native adapter 只能直通触发该 latch/gate、记录一次动作证据和读取 microkernel/native 结果，不得在 UI/Python/linuxcncrsh 普通命令链里承担急停生效职责。急停按下后不允许任何 V3 产品门禁挡在微内核急停之前：不得等待或检查 `command_busy`、backend-ready、status epoch、状态 fresh、身份/DNA/授权、motion 状态、CPU isolation、当前是否已急停、后台 job cancel、弹窗状态、页面状态或任何普通动作锁。急停直通下发后必须回读微内核/native memory 状态确认是否生效；确认前 UI 只能显示急停待确认并允许再次直通急停，确认生效后 UI 必须切换为取消急停入口，并可在同一确认阶段同步清 UI 侧 pending 流程、输入队列、弹窗/进度队列、未开始的本地动作队列和 UI 自己创建的短生命周期辅助流程；该清理只限控制态和临时流程，不得清 G-code 缓存、程序页选择/列表缓存、主页面当前显示、刀路预览/投影缓存、页面可见数据或其它显示态缓存。急停热路径不得依赖 Python handler、普通 TCP/text gate、临时脚本、文件/JSON 轮询或 HAL helper 写 pin 作为最终执行 owner；这些只能在急停直通下发之后作为触发 adapter、异步清理或诊断证据，目标闭环必须由微内核 realtime/servo cycle、HAL realtime component、EtherCAT/drive safety 或等价 native owner 立即禁止运动输出。
C UI 急停实现边界：`execute_estop_priority_command()` 只能直接调用 native safety gate，由该 gate 更新微内核 realtime emergency latch/epoch 并等待同源 native ack/actual；`estop_force` 路径不得打开或复用 linuxcncrsh 普通文本控制链，不得发送 `Set Machine Off`、`Set EStop On`、MDI、HAL helper 写 pin 或其它用户态替代命令。UI 只读取 native safety actual 改变按钮和顶部显示，不写本地急停真值。`execute_estop_reset_gate_command()` 同样先直通 native safety reset/cancel-estop 并回读 latch 已清；恢复上使能是 latch 清除后的独立低频动作，可以由已登记 command gate 发送 `Set Machine On`，但该文本动作不属于急停生效链，也不能反向证明 force 成功。这两条路径不得恢复 Broker/Python/JSON/product action 中介、HAL helper、UI 本地急停真值或 removed-route 占位；请求已发送只作为一次触发证据，按钮语义和最终成功必须由 microkernel/native memory readback 驱动。禁止 `RE_V3_SHELL_FAULT_RESET_COMMAND` 入口；设置页“清除故障”只保留 `drive_fault_reset`/驱动映射表 owner，不走急停 safety 分支。

急停输入与上层状态边界：按键、触摸和远程输入只允许产生 `estop_force requested` 的 edge event，不能成为急停 owner，不能执行门禁、排队、状态证明、上层锁存写入或恢复判断。上层急停状态直接读取微内核内存急停状态；V3 不再维护 `V3 上层急停请求锁存` 或 software emergency latch 作为产品状态源。按下瞬间不得由按键驱动、UI 回调或 remote input 写上层急停状态；UI 最多显示瞬时按压/发送中视觉，最终急停中、取消急停、待确认和恢复为急停都必须由 microkernel/native/microkernel 回读驱动。V3 不得直接改 microkernel/native 状态真值来伪造 `estop=true`、`enabled=false` 或运动输出禁止。

急停记录边界：急停按下必须记录，但记录不得由按键驱动直接写成产品状态，也不得挡在急停直通前面。输入事件只随直通请求携带 request_id、来源和 monotonic 时间给微内核/急停 adapter；上层内存记录由状态同步链直接读取微内核内存急停状态和 request/ack 时间自动生成。持久化日志、结果文件、审计明细、队列清理摘要和耗时统计只能在微内核急停请求已下发后异步写入，或在 microkernel/native 回读确认后补齐；写文件失败、日志失败或审计失败不得影响急停下发、不得影响 0.1s 生效窗口，也不得被当作急停失败或成功证明。

急停时限硬边界：从 UI/remote input 确认急停按下或 Broker 收到 `estop_force` urgent 请求开始计时，0.1s 内必须让 LinuxCNC/微内核/HAL/EtherCAT 急停 owner 完成真实急停生效，并能从 native/microkernel 回读证明 `estop=true` 或 `enabled=false`、运动输出已被禁止且速度进入急停停止边界。UI 本地视觉状态、事件日志、命令已发送、Broker accepted 或结果文件写入都不能单独算作 0.1s 急停成功证明；取得真实急停证明后，UI 必须立即把右下按钮和顶部恢复入口切换为取消急停语义。超过 0.1s 未取得真实急停证明时必须 fail-closed 并保持急停待确认视觉和控制语义。

急停生效后 UI 清理边界：微内核/native 回读确认急停生效的同一阶段，可以同步清 UI 侧流程和队列，包括未开始的 UI motion/start/jog/home 意图、按钮输入队列、弹窗/进度队列、UI 轮询任务和 UI 自己创建的短生命周期辅助流程。该清理不能前置到急停直通下发之前，不能作为急停下发门禁；不得停止 LVGL 主进程、remote relay/input、Command Broker、State Publisher、LinuxCNC/微内核或其它产品常驻安全/状态/显示链路；不得清除 G-code 文件、G-code 缓存、当前加载/选中的程序身份、程序页选择/列表缓存、主页面和程序页当前显示、刀路预览/投影缓存、坐标显示缓存、坐标系、刀补、G92、RTCP actual、runtime INI、驱动 profile、授权状态、故障证据、急停日志或任何 owner 真源。

急停取消时限硬边界：从 UI/顶部状态条/右下恢复入口确认取消急停点击或 Broker 收到 `estop_or_reset` urgent 请求开始计时，1s 内只执行并确认这条链路：直通微内核 reset/cancel-estop、读取微内核内存急停状态、确认底层 latch 已清、再发起上使能并读取微内核 Machine Enable 状态。右下按钮是否恢复为“急停”必须跟随微内核 `enabled=true` 且急停锁存已清；若 latch 已清但仍 `enabled=false`，按钮继续显示“取消急停”作为上使能/恢复入口。1s 窗口不得等待或检查解释器 idle、底层 motion-ready、5 轴 Operation Enabled、急停源/硬故障、队列/缓存等其它条件；这些状态只在顶部状态/问题区域显示。`BROKER_JOB_ACCEPTED`、后台 job 已提交或 machine-on 命令已发送不能单独算作按钮恢复完成；未读到微内核 latch 已清和 `enabled=true` 时不得把按钮提前恢复为“急停”。

急停按钮颜色边界：右下按钮底色始终保持红色。急停已下发但微内核/native memory 未确认生效时，右下按钮显示“急停中”并保持急停直通语义；微内核/native memory 确认急停生效或确认 `enabled=false` 后，右下按钮显示“取消急停”并发送 `estop_or_reset`；`estop_or_reset` 等待微内核 latch 清除和 Machine Enable 回读期间，用黄色 stop 方块/图标作为统一视觉提醒；读到微内核 latch 已清且 `enabled=true` 后，右下按钮恢复红色“急停”和白色 stop 方块/图标，其它未 ready 状态只在顶部显示。

急停取消后的顶部状态边界：`estop_or_reset` 读取微内核 latch 已清且 Machine Enable 已恢复后，按钮才恢复为“急停”；顶部状态/问题区域再显示其它未 ready 条件，包括解释器 idle、底层 motion-ready、BUS/EtherCAT 5 轴 Operation Enabled、急停源/硬故障、队列、暂停、行号、运行文件或 active MDI 等。顶部问题可以阻止后续运动动作，但不得替代右下按钮的微内核使能状态判断。V3 不得用 SHM 速度、JSON 或私有容差自造 motion-ready。

急停恢复 disabled-only 边界：`enabled=false` 是右下急停按钮的核心显示/分支输入，不是特殊快路径。所有取消急停入口都走同一条最小链路：直通微内核 reset/cancel-estop、读取微内核内存急停状态、上使能、读取微内核 Machine Enable 状态；只要微内核仍回读 `enabled=false`，按钮继续显示“取消急停”并继续走恢复入口。非 idle、速度、队列或驱动 Operation Enabled 等其它状态只在顶部显示。

急停恢复前台响应边界：`estop_or_reset` 按钮/顶部状态条前台不得在 UI 主线程同步等待 native machine-on 完整确认。前台只允许提交 Broker 后台 job 并立即进入 pending 显示，保持界面可刷新、可继续响应急停；后台 job 完成后按微内核急停 latch 与 Machine Enable 回读决定按钮是否从“取消急停”恢复为“急停”。`BROKER_JOB_ACCEPTED` 只表示恢复任务已提交，不能当作取消动作已经完成；取消动作完成必须来自微内核 latch 已清且 `enabled=true` 的回读。fresh native 状态/status 和驱动 Operation Enabled 证明只用于顶部 ready/问题显示和后续运动准入。

急停恢复结果结构边界：`estop_or_reset` 的 `ok`、`code` 和 `RESET_LATCH_CLEARED` 只能表示微内核 latch 已清；上使能请求必须用独立的 `machine_on_requested`、`on_result`、`machine_on_error` 等字段表达。Broker 紧凑响应不得丢弃这些字段，也不得把 machine-on 失败反向改写为取消急停失败；后续 machine-ready、顶部状态和运动准入必须读取这些独立字段和 fresh microkernel/native 回读继续判断。

急停后台清理边界：`estop_force` urgent 请求不得为了身份校验、job ownership 或后台清理而延迟急停直通微内核。Broker 管理的可取消产品后台 job 可以在微内核 emergency latch/gate 已触发之后异步 `cancel_all()`，取消原因记录为 `estop_force:<request_id>`，并在后续结果或诊断中带回取消摘要；确认急停生效时，UI 侧流程/队列清理可以与 UI 切换取消急停同步执行。取消后台 job 和清 UI 队列都不是急停下发门禁，也不能挡在急停生效前。这个清理只覆盖 Broker 管理的可取消产品后台 job，例如服务器下载、授权下载、驱动设置/复位/读写、回零等已接入 `job_token` 的长任务；不得杀掉 LinuxCNC/微内核、State Publisher、Command Broker、remote relay/input、LVGL 主进程等产品常驻安全/状态/显示链路，也不得新建第二套进程表或用 shell `kill` 扫描替代 Broker job 取消。`estop_or_reset` 恢复上使能不触发后台 job 清理。

急停恢复显示边界：右下按钮状态必须与微内核 Machine Enable 状态一致，并同时受微内核急停 latch 约束。`enabled=true` 必须来自 LinuxCNC/HAL/native 实际使能输出的回读，例如同一 native owner 暴露的 Machine Enable actual、`joint.*.amp-enable-out`/等价 HAL native 使能输出或微内核常驻内存中的同源 actual；`Set Machine On` 已发送、`linuxcncrsh Get Machine` 文本返回、UI 事件 `machine_on_status`、命令 accepted、旧缓存或本地按钮状态都不能单独把按钮恢复成“急停”。只要微内核/native actual 回读 `enabled=false` 或 `estop=true`，右下按钮必须显示“取消急停”并点击进入恢复/上使能链路；只有 actual 回读 `enabled=true` 且 `estop=false` 时，按钮才显示“急停”并点击进入急停直通链路。急停已下发但尚未被微内核/native memory 确认生效的 pending 窗口，右下按钮保持急停直通语义；一旦真实 `estop=true` 或 `enabled=false` 停止证明确认，右下按钮必须切换到取消急停。若 `linuxcncrsh` 失联、返回空响应或只读到 `Get Machine` 但实际 HAL/native 使能输出仍为 false，按钮必须按 `enabled=false` 处理，不能沿用上一帧已使能状态。
### WCS 与坐标/回零/归零

| 按钮 | action | 当前分发 | 当前行为 |
|---|---|---|---|
| G54 | `wcs_g54` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G54`，并设置 5s pending 视觉状态。 |
| G55 | `wcs_g55` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G55`。 |
| G56 | `wcs_g56` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G56`。 |
| G57 | `wcs_g57` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G57`。 |
| G58 | `wcs_g58` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G58`。 |
| G59 | `wcs_g59` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G59`。 |
| G59.1 | `wcs_g591` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G59.1`。 |
| G59.2 | `wcs_g592` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G59.2`。 |
| G59.3 | `wcs_g593` | `WCS_SWITCH_COMMAND` | 发送 `wcs_switch`，目标 `G59.3`。 |
| 机械全轴 | `all_axes` | `AXIS_ALL_LOCAL` | 主页面按钮必须单行显示 `机械全轴`；本地恢复全轴机械选择，不发运动命令。恢复后点动仍需重新点选单个坐标轴。 |
| RTCP | `rtcp_toggle` | `RTCP_TOGGLE_COMMAND` | 主页面按钮文字固定显示 `RTCP`，不再显示 `刀尖关/开`；C UI 属于 UI RTCP 链路，只在 fresh native RTCP actual provider 写入 `rtcp_valid/rtcp_active` 时按 actual 反向选择目标 ON/OFF，并通过 `native_rtcp_control` 直通微内核 RTCP 控制 latch；微内核 RTCP owner 消费 latch 后直接写 HAL/native `re-switchkins-select` / `motion.switchkins-type` actual，并回填 native ack/readback。G-code 程序 RTCP 链路由程序文本内 `M64/M65` 已登记输出直驱微内核，不由 UI 按钮代发，UI 按钮也不消费程序请求值；两条链路最后只通过同一个 fresh native actual 合流。RTCP 按钮底色必须跟随同一个 fresh native actual：`rtcp_active=true` 时高亮，`rtcp_active=false` 或 unknown/degraded 时保持非高亮；不得用按钮请求值、点击事件、本地缓存、命令已提交或 SHM modal 提前染成 ON。RTCP 按钮不得把点击转义、转换或降级为 G-code 程序输出、历史 M-code 文件、MDI 文本、linuxcncrsh 普通文本命令或 Python product action；不得保留历史 M-code 文件、部署入口或测试断言作为兼容路径。当前 provider 是登记的二进制 `v5_native_rtcp_status.bin` 状态块，由常驻 native owner 直连 HAL/native actual 后写入，C UI 只读 fresh timestamp/CRC/valid/active；不得恢复 Broker/Python `target_rtcp_active`/`target_mcode` 中介，不得用 SHM `runtime_modal_text`、按钮文字、默认 OFF、旧状态文件、`linuxcnc.stat().mcodes` 或本地缓存推导目标。native actual provider 缺失、过期或 command gate 下发后未回读到目标状态时保持真实缺口并 fail-closed，不伪造 toggle 成功。 |
| 回零 | `home` / native positioning | `HOME_COMMAND` / native single-axis-zero gate | 当前选择为 `机械全轴` 时，C UI 进入 mode-aware native Home gate，按 fresh active BUS/Pulse 模式执行正常全轴 Home。选择 MCS 单轴时，所选机械轴真实移动到机械坐标 0；选择 WCS 单轴时，所选轴真实移动到当前 active 加工坐标 0，且 WCS offset 不变。三种分支都必须取得本次真实运动/到位/readback，不能只改显示、offset、G92、homed 或驱动位置；单轴能力缺失时不得回退为全轴 Home。旋转轴机械 0 使用 active model 对应轴的 native 最近等效角语义。 |

主页面当前选择为 `机械全轴` 时，`回零` 必须在任何当前运行态下进入可回零的机器域：若上一次 G-code/急停恢复后解释器仍处于 Auto/程序上下文，Home owner 必须先通过已登记 LinuxCNC/native gate 中止当前 Auto 运行，再切入不依赖 world/switchkins 姿态的 native joint/Home 域执行真实 Home。MCS/WCS 单轴选择则进入 native positioning 域，不能进入全轴 Home；WCS 单轴运动的 RTCP ON/OFF 补偿由微内核/LinuxCNC/switchkins 按 actual 实现。回零 owner 不拥有 RTCP 运行输入：不得发送 `M65 P0`、清 `v5-rtcp-gcode-request`、通过 `native_rtcp_control` 请求 OFF，或修改 `[RTCP].DEFAULT_ACTIVE`。RTCP actual 前后继续由原有 UI latch/程序输出合成并回读。三种回零分支都不得因为 RTCP ON、程序暂停/急停后残留状态、Auto 模态、旧 homed、当前位置接近 0 或显示坐标为 0 而在 UI 本地空跑成功；无法进入对应 native 运动域或无法取得真实运动证明时必须结构化 fail-closed。
| 归零 | `popup_zero` | `WORK_ZERO_COMMAND` | 归零语义以 `REQ-WCS-NATIVE-OWNER` 和 `功能/1-1加工坐标系偏移值保存与读取实现.md` 为准。MCS/WCS 任一坐标列点选单轴都只确定目标轴；C UI 经 native/linuxcncrsh gate 下发 `G10 L20` 等价 Work Zero，由 owner 取所选轴 fresh 机械坐标关系写入当前 active WCS 对应轴偏移，使该轴当前加工坐标 native readback 为 `0`。全轴选择不得执行。 |

RTCP 按钮的开关执行不再增加产品层 backend-ready 门禁。产品启动脚本必须使用随 `lvgl_app/scripts` 一起部署的 `scripts/re-v3-8ax-backend.sh` 作为 backend starter；该路径缺失时不得把 UI 按钮问题误判为点击失效，应修启动/部署路径并让 backend-ready 恢复。

RTCP Toggle 最终口径：禁止旧 UI/Broker `target_rtcp_active`、`target_mcode`、`enabled`、`rtcp_enabled` 字段和 Python product action 作为兼容入口。C UI 入口只允许消费同源 native actual provider 给出的 fresh `rtcp_valid/rtcp_active`，并通过 `native_rtcp_control` 递交 ON/OFF 目标到微内核 RTCP 控制 latch；G-code 程序入口独立由解释器执行程序内 `M64/M65` 已登记输出，不能由 UI 按钮代发，也不能被 runner 在 Start 前外部预置。两条链路都直驱微内核，互不覆盖请求源，最终只以同一个 native/HAL/switchkins actual 和 ack/readback 为准。RTCP 按钮不得发送或生成 G-code 程序输出、历史 M-code 控制文件、MDI 文本、linuxcncrsh 普通文本命令，历史 M-code 控制文件和部署项必须删除；最终 RTCP actual 必须由微内核/native memory/API 或登记 native provider 回读，板端闭环必须证明该块由真实 LinuxCNC/native 运行态刷新，且不得用成功提示或 SHM modal 冒充已确认。

RTCP actual provider 的当前 v5 BUS 判定规则固定为：HAL/native `motion.switchkins-type` / `re-switchkins-select` 读回大于等于 `0.5` 时 `rtcp_valid=true/rtcp_active=true`，小于 `0.5` 时 `rtcp_valid=true/rtcp_active=false`；读不到 HAL/native actual、状态块过期或结构错误时 `rtcp_valid=false` 并 fail-closed。不能把按钮请求、程序 `M64/M65` 已发送、历史 M-code 已发送、默认 OFF、旧缓存或 `linuxcnc.stat().mcodes` 当作 provider actual。

RTCP 按钮显示、点击后的下一次目标选择和下发后的 UI 状态都必须跟随同一个 fresh native actual：按钮文字固定为 `RTCP`，不再用文字表达开关态；按钮底色在 fresh `rtcp_active=true` 时高亮，在 `rtcp_active=false`、unknown 或 degraded 时保持非高亮；点击前先刷新 `rtcp_valid/rtcp_active`，按实际状态反向递交 ON/OFF 到 `native_rtcp_control`；command gate 成功返回后必须重新读取 native actual 并刷新 UI，不得把本次请求值、命令已提交或本地缓存直接写成按钮状态。无论 actual 来自 UI latch 还是 G-code 程序输出，主页面当前模态区域都必须在刀长下一行显示同一个 RTCP 实际模态：`RTCP ON`、`RTCP OFF` 或 `RTCP --`；该行来源同为 native actual，不得由按钮文字、请求值、SHM modal 或旧缓存推导。

### 点动与视图

| 按钮 | action | 当前分发 | 当前行为 |
|---|---|---|---|
| X1 | `jog_step_1` | `JOG_STEP_1_LOCAL` | 本地选择单次增量点动距离/角度 `0.001`：直线轴为 `0.001 mm`，旋转轴为 `0.001 deg`；不发运动命令。 |
| X10 | `jog_step_10` | `JOG_STEP_10_LOCAL` | 本地选择单次增量点动距离/角度 `0.01`：直线轴为 `0.01 mm`，旋转轴为 `0.01 deg`；不发运动命令。 |
| X100 | `jog_step_100` | `JOG_STEP_100_LOCAL` | 本地选择单次增量点动距离/角度 `0.1`：直线轴为 `0.1 mm`，旋转轴为 `0.1 deg`；不发运动命令。 |
| 点动+ | `popup_jog_plus` | `JOG_PLUS_COMMAND` | 短按一次按当前 X1/X10/X100 选择值执行一次正向增量点动；长按超过 500ms 直接进入正向持续运动，release/lost/超时必须立即下发 native jog stop。长按释放时不得再补发一次增量点动。 |
| 点动- | `popup_jog_minus` | `JOG_MINUS_COMMAND` | 短按一次按当前 X1/X10/X100 选择值执行一次反向增量点动；长按超过 500ms 直接进入反向持续运动，release/lost/超时必须立即下发 native jog stop。长按释放时不得再补发一次增量点动。 |
| XY | `view_xy` | `VIEW_LOCAL` | 本地切换刀路视图为 XY，不改变后端状态。 |
| XZ | `view_xz` | `VIEW_LOCAL` | 本地切换刀路视图为 XZ，不改变后端状态。 |
| YZ | `view_yz` | `VIEW_LOCAL` | 本地切换刀路视图为 YZ，不改变后端状态。 |
| 3D | `view_3d` | `VIEW_LOCAL` | 本地切换刀路视图为 3D，使用独立 3D 投影，不改变后端状态。 |

点动 UI 本地只允许保留输入合法性检查：必须点选单个受支持坐标轴，空间只能是 `mcs` 或 `wcs`，点动数值必须有限。受支持轴只能来自 fresh active model descriptor/readback：X/Y/Z 加当前模型旋转轴（例如 AC 的 A/C、BC 的 B/C），不得硬编码 A/C 或把未知模型回退为 AC。`command_busy`、急停、使能、回零、程序运行、暂停、速度、授权和其它运动准入不得由 UI 自造分支挡在点动下发前；这些只能由 native/LinuxCNC/HAL/EtherCAT 硬安全、登记 native gate 和后续 microkernel/native readback 决定。

增量点动和连续点动的实际速度必须统一取所选轴当前已生效机械输出最大速度的 `1/2`。最大速度真源为 LinuxCNC/runtime INI 与微内核/native owner 的 fresh readback：X/Y/Z 按 `mm/min`，当前 active model 对应旋转轴按 `deg/min`；进入 LinuxCNC 原生命令时只做其接口要求的单位换算。`X1/X10/X100` 只表示单次短按的增量点动距离/角度 `0.001/0.01/0.1`，不得作为连续点动速度，也不得参与最大速度计算；长按只按最大速度的 `1/2` 持续运动，直到 release/lost/超时 stop。UI 点动请求只携带所选轴、方向和增量意图；不得继续发送固定速度 `100`，不得使用设置页 dirty 值、UI 缓存、默认值、SHM 显示投影或电机侧 RPM 计算点动速度。所选轴最大速度 native readback 缺失或无效时必须由 native gate fail-closed，不得用固定低速兜底。

增量点动和连续点动的加速度必须使用所选轴参数表中当前已保存、已生效的 `max_acceleration`。其运行真源仍为参数表提交后写入的 LinuxCNC/runtime INI 与微内核/native owner fresh readback；UI 不得另设“点动加速度”，不得使用设置页尚未保存的 dirty 值、UI 缓存、固定常量、SHM 显示投影或电机侧加速度代替。所选轴 `max_acceleration` readback 缺失或无效时必须由 native gate fail-closed。

点动执行进入 joint jog 域时必须使用受控 native gate，退出任意路径都要发送 jog stop 并由 native owner 恢复 World/Teleop 或等价安全域。增量点动或连续点动的 stale、急停、下使能、状态不可读、限位、未授权等失败只能来自 native/LinuxCNC/HAL/EtherCAT owner 的拒绝或回读结果，不得恢复 Broker/Python/JSON product action 中介、SHM 显示投影门禁或 UI 本地运动准入分支。C UI 必须直接调用 `re_v3_product_native_jog_increment()`、`re_v3_product_native_jog_continuous()` 和 `re_v3_product_native_jog_stop()`；命令已发送只作为触发证据，运动成功仍需板端 native/microkernel 回读证明。

### 主轴/程序/MDI

| 按钮 | action | 当前分发 | 当前行为 |
|---|---|---|---|
| 打开程序 | `page_program` | `NAV_PROGRAM` | 本地进入程序页；程序页负责选择/打开程序并生成运行内存副本和 runtime metadata，主页面此按钮本身不直接打开 `cc-ac.ngc` 或 `cc-bc.ngc`。 |
| 手动输入 | `popup_mdi` | `NAV_MDI` | 本地新建空白 MDI 文本并进入 MDI 页。 |
| 首点 | `popup_first_point` | `FIRST_POINT_COMMAND` | 由 `native_first_point` owner 执行。Program Open/Load 阶段必须从 RAM 中的只读程序模型提取首个可执行目标点和 axis mask；点击 `首点` 时只消费该 RAM 模型、当前程序路径、sha256、loaded_epoch 和序列 `AC_XY_Z`，不得重新读 G-code 文件、计算 SHA、走旧 v3 空函数、Broker/Python/JSON 中介或向 linuxcncrsh 发送伪 `FirstPoint ...` 文本。native 执行顺序为 A/C -> X/Y -> Z 的受控 MDI 子命令；缺少已加载程序、首点坐标或 native owner 时必须 fail-closed。 |

当前代码里存在 `program_open_cc` command 函数，但主页面 `打开程序` 按钮并没有绑定到该 command；主页面只跳转到程序页。
程序页触摸打开规则：第一次点某一行只选中该程序；同一行已经选中后，再次触摸同一行必须直接打开/加载该程序用于运行，即使触摸 relay 或事件间隔慢于快速双击窗口也不能只重复选中。默认文件夹仍是产品 G-code 目录；主页面 `打开程序` 按钮仍只进入程序页，不直接绑定 `program_open_cc` 或固定打开 `cc-ac.ngc`/`cc-bc.ngc`。
程序页双击前台完成边界：同一行第二次触摸确认后，UI C 端必须完成本地安全文件名/路径/存在性校验，并把 Start 热路径需要的最小 resident runtime identity 原子登记好，至少包括当前程序身份、`runtime_program_path` 或等价已驻留运行句柄、generation/epoch 和可执行入口状态；这些最小字段缺失时不能显示为“已打开可启动”。完成最小登记后应立即切回主页面；不得再恢复 `program_open_cc` Broker/Python/JSON 中介，不得在程序页事件回调或成功返回路径里等待 LinuxCNC `program_open` 完成、State Publisher SHM 文件回读、Start 预热、黄色刀路解析/投影缓存、MDI 文本全量读取、重复 SHA256 计算或其它大 G-code 预览缓存完成。真正的 LinuxCNC Auto/Open/Run 只能由 Start 分支在启动时按“暂停续跑 / 非暂停首行”规则执行。预览、诊断和刀路缓存可以后台补齐，不能反向阻塞已完成最小 runtime identity 登记后的程序页切回。

## Start 分支规则

`REQ-START-RUN-HOT-PATH` 最终口径：启动按钮不是综合准入门禁，也不是程序文件续跑入口。按下启动按钮时，产品层只做一个分支判断：之前停下来是不是因为暂停；除此之外，UI 不得再设置 V3 自写准入门禁，Broker 和 Python product action 不得再作为启动/暂停中间层。启动、暂停、恢复必须由 UI C 端直接向 LinuxCNC/微内核 native command gate 下发。微内核/LinuxCNC/HAL/EtherCAT 的硬安全、实时限位、急停、伺服/驱动保护，以及 VPS/授权/下载身份链不属于本条删除范围，仍由各自 owner 执行。

启动分支：

- 如果当前/最近停下状态是暂停：启动按钮直接下发 native `Set Resume`，由 LinuxCNC/native 从暂停 MDI 行继续执行后续内容。
- 如果不是暂停且处于程序文件模式：启动按钮只使用 Program Open/Load 阶段已登记的原始程序身份/`runtime_program_path`，执行链路下发的 `Set Open` 路径必须仍指向操作员打开的原始 G-code 文件，不得指向 UI 预览、黄色轨迹、等效角改写、sidecar、临时副本或其它显示链路产物；随后下发 native `Set Mode Auto`、`Set Run 0`、`Set Resume`，从该原始程序第 1 行开始执行；不得在 Start/Resume 热路径重新读程序文件、计算 SHA、生成 runtime 文件或读取脚本/配置。
- 非暂停程序启动的 native handoff 一次启动事务里最多只能出现一次 `Set Open <runtime_program_path>`；`program_open` 工具/验收 helper 如果已经单独打开程序，不能再用带 `Set Open` 的 Start 事务重复打开同一文件。需要直接启动时，应使用 Start 事务一次性完成 `Set Open`、Auto、Run/Resume；需要只验证打开时，才单独发送 Program Open。
- 如果不是暂停且处于可见 MDI 模式：启动按钮直接下发 native `Set Mode MDI`，再把当前可见 MDI 文本的可执行行交给 LinuxCNC/微内核执行。
- 非暂停启动不得根据当前坐标、当前行号、上一次停止行号或 native 当前已加载程序行号决定入口；不得从中间行继续。
- 暂停恢复不得重新从 MDI 首行开始；必须使用 native 暂停恢复语义继续暂停行之后的内容。
- 暂停按钮直接下发 native `Set Pause`；暂停态再次按暂停按钮直接下发 native `Set Resume`。
- `start_run`、`pause_resume_program`、`product_handler_start_run.py`、`product_handler_pause_resume.py`、`actions_program_start.py` 和 `actions_controls.run_pause_resume` 不再是启动/暂停支持路径；同一 slice 必须从 UI/Broker/action registry、能力清单、测试和发布面删除这些自造入口，不能保留为兼容、fallback、诊断或隐藏分支。

启动按钮禁止新增门禁：

- 不检查当前坐标。
- 不以 CPU isolation beacon、motion ready latch、参数/驱动 proof、runtime INI/native 投影、program manifest、loaded_epoch、source/runtime SHA256、native 已加载文件回读、status epoch、backend-ready、速度证明、行号推进、encoder delta 或 motion proof 作为是否允许下发启动的前置门禁。
- 不在 Start 中重新读取 G-code 文件、计算 SHA256、重新过滤 G-code、重写 runtime 程序或补齐后台 proof；Program Open 已准备好的 runtime 程序身份有效但 native 当前未加载时，Start 只能由 UI C 端做一次最小 native runtime handoff，不得回到 Broker/Python action。
- 上述状态允许作为诊断字段和后台记录，但不能阻止启动按钮按“暂停续跑 / 非暂停首行”的规则下发；不能把诊断结果反向改写成 `START_*_UNAVAILABLE`、`START_*_NOT_SEEN`、`BROKER_START_RUN_*` 或其它产品层失败门禁。

启动结果边界：

- `Set Run`、`Set Pause` 或 `Set Resume` 下发后，UI 只记录 native command gate 返回的最小结构化结果；不得再由 UI/Broker/product action 自写运行证明门禁。
- 产品层启动前置门禁全部退役。Program Open 已准备 runtime 程序但 native 当前未加载或加载为 `NONE` 时，Start 只提交已准备好的 runtime 程序并继续下发运行，不得在下发前阻断。
- 速度、running、位置、行号、encoder delta、状态 fresh、status epoch 和 `/run` 结果文件只能作为诊断显示或事后记录；不得成为启动是否允许、是否成功返回、是否弹失败窗的 V3 自写判定。
- 真正的运动保护、急停、限位、驱动故障和硬实时联锁由 LinuxCNC/微内核/HAL/EtherCAT owner 负责；VPS/授权/下载身份链仍归 `REQ-DRIVE-PROFILE-AUTH-CHAIN`，不得因本条删除。

## 坐标点选对按钮的影响

坐标列本身不是 `re_v3_buttons[]` 按钮，但它会改变后续按钮目标：

1. 点选机械坐标单轴：设置 `home_select_space="mcs"`、`home_select_axis=X/Y/Z/A/C`。
2. 点选加工坐标单轴：设置 `home_select_space="wcs"`、`home_select_axis=X/Y/Z/A/C`。
3. 点选 WCS 全轴：设置 `home_select_space="wcs"`、`home_select_axis="*"`。
4. 回零按钮严格按当前选择分支：`机械全轴` 提交 BUS/Pulse 正常全轴 Home；MCS 单轴提交该机械轴真实移动到 MCS 0；WCS 单轴提交该轴真实移动到当前 active WCS 0。每次按下都必须有新的 native transaction 和真实运动/到位/readback；不得因坐标接近 0 或旧 homed 空跑成功，单轴分支不得回退全轴 Home。
5. 归零按钮支持从 MCS 或 WCS 坐标列选定的单轴；列别只确定选中位置，写入源始终是该轴 fresh 机械坐标关系，目标始终是当前 active WCS 对应轴偏移。全轴选择不能执行 `work_zero`。
6. 点动按钮当前只支持单个坐标轴选择；全轴选择不能点动。
7. 点选任意 MCS/WCS 单轴后必须启动 3 秒 UI 空闲计时。从最后一次重新点选单轴，或执行使用当前轴选择的点动、归零、回机械 0、回加工 0 等有效操作起重新计时；连续按压操作尚未释放时不得判为空闲。连续 3 秒没有上述操作时，UI 必须自动执行与点击 `机械全轴` 相同的本地选择恢复：清除 MCS/WCS 单轴目标和对应坐标行高亮，恢复为 MCS 全轴选择；超时后再按 `回零` 应进入 BUS/Pulse 正常全轴 Home，后续点动必须重新点选单轴。该超时只改变 UI 选择状态，不发送运动、回零、归零或 Jog Stop 命令；`机械全轴` 按钮只按现有已选定状态色显示，不得使用或残留黄色 pressed 状态。
8. 点选、改选或取消 MCS/WCS 单轴选择后，选中色和坐标数码字必须在同一个 UI 事件事务内完成局部重绘；3 秒空闲计时自动取消选择也遵守同一规则。受影响数值单元格必须立即进入 LVGL invalidated rect/remote dirty rect，不能先变成黑块或空白后等待下一次 30Hz 状态帧、坐标值变化、页面重建或取消选择才恢复。仅颜色变化时必须保留原坐标文本和同一 owner 真值，不得依赖整屏刷新兜底。

## 当前限制

- `打开程序` 只进入程序页，不直接打开程序文件。
- `program_open_cc` command 当前不是主页面可见按钮的直接绑定动作。

## Broker 响应边界（非 Start/Pause）

- Start/Pause/Resume 不再经过 Broker/Python product action；不得保留旧的 Start 经 Broker 诊断规则作为启动路径要求。
- VPS/授权/下载、设置保存、设置驱动等仍归 Broker/后台 job 的长流程，UI direct Broker client 必须接受 Broker framed-response 上限内的有界结构化响应；响应缓冲区过小只能作为该长流程客户端实现缺陷处理，不能反向恢复 Start/Pause 的 Broker 中介。
