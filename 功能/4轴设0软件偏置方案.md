# 轴设0双模式方案

引用需求真源：`REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-DOC-SINGLE-SOURCE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-NATIVE-OWNER-FIRST`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION`、`REQ-LINUXCNC-COMMAND-GATE`、`REQ-G92-NATIVE-RUNTIME`、`REQ-WCS-NATIVE-OWNER`、`REQ-ROTARY-UNWRAP-ABSOLUTE-PROOF`、`REQ-ROTARY-REBASE-NATIVE-GATE`、`REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-MOTION-STILLNESS-TOLERANCE`。

## AI 阅读入口

- 本文同样遵守 `功能/0开机参数入内存.md` 的最高启动内存架构：LinuxCNC/微内核、UI 和产品自写运行代码闭包开机全量入 RAM；运行期热路径不得临时读盘、懒导入、扫描目录、读取旧 JSON/result 或用 SHM 显示投影冒充 owner 真源。
-  本文定位：设置页单轴设零、BUS/Pulse 双模式设 0、G92/WCS/Rebase 边界。
- 先读 `REQ-G92-NATIVE-RUNTIME`、`REQ-WCS-NATIVE-OWNER`、`REQ-ROTARY-REBASE-NATIVE-GATE` 和 `REQ-BUS-PULSE-MODE-DIFFERENCE`。
- 不得把 BUS 设 0、Pulse HOME_OFFSET、Work Zero、Rotary Equiv Zero、Rebase、Drive Position Preset 写成同一个能力。

## P0 BUS/EtherCAT 单轴设 0 最终规则

- BUS/EtherCAT 单轴设0的保存通过条件只有五项：驱动 count-domain 位置读数成功、LinuxCNC raw 机械坐标读数成功、active runtime INI `SCALE` 可用、`zero_model` 成功写入硬盘、按新 0 位和 UI 限位距离计算出的 raw limit 成功写入硬盘。除此之外的 RTCP/G53 geometry、静止窗口、UI/SHM 显示归零等待、live raw limit 读回、live runtime raw 归零、memory/UI 归零、自动上使能/idle 判定都不得作为本次 `设0` 按钮保存通过门禁。
- BUS/EtherCAT 单轴设0时，0 位一变，硬盘 runtime INI/native owner 中的 raw 软限位必须在同一次产品动作里跟随新 0 位计算并写入；否则最终重启后底层仍可能按旧 raw 限位判定越界。
- UI 软限位字段仍表示相对 0 位的物理距离；State Publisher/设置页必须继续显示这个距离，不得把已同步到 native/runtime 的 raw 限位坐标反向显示成 UI 距离。
- raw 限位落盘公式固定为 `raw_min_limit = raw_zero_position + ui_min_limit_distance`、`raw_max_limit = raw_zero_position + ui_max_limit_distance`。其中 `raw_zero_position` 对 BUS count-domain 零点等于 `zero_counts / active runtime INI SCALE`；螺距、减速比、目标精度、轴方向等比例链字段改变并导致 `SCALE` 改变时，必须用新 `SCALE` 同事务重算硬盘 raw 限位。
- 单次设0按钮的通过条件是当前 raw 机械位置/count-domain 零点证据和按 UI 限位距离计算出的 raw 限位值已成功写入硬盘 owner，且公式合法、写盘无错误；不要求同次校验 LinuxCNC/HAL/EtherCAT live raw 限位，也不要求等待 SHM/UI 已按新零点投影为 0。
- 设0或比例链保存只完成硬盘写入时，响应证明必须标明 live raw limit 未验证、`backend_restart_required=true`、`restart_deferred=true`；LinuxCNC/HAL/EtherCAT live raw 限位、允许的 SHM 坐标/UI 投影归零和运动闭环只能在最后统一保存重启后验证。
- 编码器 raw 位置允许轻微跳动；BUS 设0不得用前后 raw 双采样静止窗口作为阻塞门禁。可保留 raw delta 诊断，但不得因为微小编码器跳动返回 `SETTINGS_ZERO_AXIS_MOTION_ACTIVE`。

本文只维护设置页单轴 `设0/零位` 的本地请求契约、owner 和验收边界。BUS/Pulse 模式差异、模式切换闭环、Pulse 轴参数 `设零` 键盘和 `回零方向` 列的总口径以 `REQ-BUS-PULSE-MODE-DIFFERENCE` 及 `功能/总线脉冲区别.md` 为准。

## 1. 接手边界

先确认三件事：

- 当前 active mode 是 BUS/EtherCAT 还是 Pulse/StepDir；`motion_driver.conf` 只能作为 requested/持久 owner，真实 active mode 必须按 `功能/总线脉冲区别.md` 的 SelectLink/运行 INI/LinuxCNC/RT-HAL 四项并联证明进入 native/microkernel 运行内存。
- 本次动作是设置页单轴 `设0`，不是主页面 `回零`、Work Zero、驱动硬件 preset 或 position rebase。
- 当前字段是否会触碰 native/microkernel truth；若触碰，先按 `功能/需求真源索引.md` 找唯一 owner。

控制链固定为：

```text
UI C -> Command Broker UDS framed socket -> product action/settings apply -> LinuxCNC/HAL/EtherCAT/native owner
```

UI 不直接读 raw、不写 `settings_runtime.json`、不改 HAL/INI/驱动器对象。

## 2. 核心语义

`设0/零位` 在两种模式下不是同一个动作。

| 模式 | UI 文案 | 用户输入 | 后端动作 | 当前坐标是否立刻清零 | 是否写驱动 preset |
| --- | --- | --- | --- | --- | --- |
| BUS/EtherCAT | `设0` / `当前位置设为0` | 不输入数值 | 读取当前 LinuxCNC/HAL/EtherCAT/native raw 机械坐标和驱动 count-domain 位置，保存 Settings Drive 私域零点证据；按新 0 位和 UI 限位距离计算硬盘 raw 限位并写入 runtime INI/native owner | 零点证据和硬盘 raw 限位写入成功即为本次设0保存通过；最终 live 坐标归零、raw 限位装载和允许的 SHM 坐标/UI 投影显示闭环等最后统一保存重启后闭合 | 禁止普通设0直接写驱动 preset；raw 限位不是 preset，必须走受控 runtime/native owner 落盘 |
| Pulse/StepDir | `设零` / `当前轴0位` | 弹出键盘，输入坐标值作为当前轴 0 位；轴参数表另有 `回零方向` 列下拉选择回零方向 | 写 runtime INI/native owner 的当前轴 `HOME_OFFSET`；`回零方向` 列写独立回零参数 owner | 禁止只靠 UI 立即改坐标；必须经 owner 写入和后续回零/native 证明 | 禁止 |

BUS 模式设置页轴参数表里的单轴 `设0` 不是回零动作，也不生成 Pulse homing 参数；Pulse 模式轴参数表里的单轴 `设零` 是把键盘输入的坐标值写成当前行当前轴 0 位对应的 `HOME_OFFSET`，不读取或复用 BUS count-domain 零点证据。两种模式的请求字段、后端 owner 和验收闭环必须分开。

危险动作分类（2026-07-03）：设置页 BUS 单轴设0是硬盘零点证据和 raw 限位保存动作，不执行运动、不执行驱动 preset、不要求自动上使能，也不得把上使能、idle、stillness 或 SHM ready 当成本次保存通过门禁。只有转动轴相关的人机确认仍按危险动作处理：A/C 旋转轴设0、A/C 轴坐标写入、会导致旋转轴实际运动或相位重定的动作必须二次确认；X/Y/Z 直线轴设0不是危险动作，不得因“可能需要上使能”而弹危险确认。

硬规则：

- BUS 设0先保存 count-domain 证据，例如 `zero_counts`、A/C `zero_abs_turns`、`zero_abs_single_turn_counts`、`zero_abs_counts64`、驱动读回来源和 profile/hash 证据；同一次产品动作还必须按当前零点和 UI 限位距离写入硬盘 raw 限位。允许的 SHM 坐标/UI 等效坐标归零属于最后统一保存重启后的 live 显示闭环，不作为单次设0按钮保存通过门禁。
- `旧浮点零位字段`、`旧倒数比例字段` 只能作为内存诊断表达式，不得重新落盘，不得作为 UI/State Publisher/product action 的产品输入。
- BUS 坐标显示和 BUS Home 必须使用 count-domain 证据 + active runtime INI/native `SCALE` 回读 + native/API 回读。UI 坐标值不是独立真相，必须由 LinuxCNC/HAL/EtherCAT 的真实 raw 机械坐标经 State Publisher 按当前零点、`SCALE`、A/C 等效相位规则计算得到；只改 UI 本地状态或旧快照不能算通过。
- Pulse 不把 `home_offset` 或 `pulse_home_model` 写入 Settings Drive 私域持久文件；它们属于 runtime INI/native owner 的一次性 apply 请求。
- Pulse 禁止普通 `设零` 路径写 EtherCAT SDO `0x607C`、保存对象 `0x1010`、驱动侧零位改写、position preset 或 drive rebase。BUS 普通 `设0` 不写驱动 preset、不改写驱动 raw 位置；但 0 位改变后硬盘 raw 软限位必须由受控 runtime/native owner 同次计算写入，禁止 UI、Broker、Python 临时脚本、旧兼容路径或静默 preset/静默限位写入。

## 3. 真源分层

| 数据 | owner | 说明 |
| --- | --- | --- |
| 驱动模式 | `motion_driver.conf` + 运行态回读 | `settings_runtime.json` 不能决定系统拓扑 |
| BUS 驱动 profile | 开机装载或受控刷新后的 public/private 驱动映射内存快照 | 通过 `功能/1驱动命令映射表命令需求.md` 选择；运行期设 0 不得重新读 profile 文件、旧合成表或源码 fallback |
| BUS 零点证据 | Settings Drive 私域 | 只保存 drive/count-domain 证据和必要诊断 |
| `SCALE`、螺距、减速比、电子齿轮等 | runtime INI/native/受控 settings apply owner | 不复制成旧兼容字段 |
| Pulse `HOME_OFFSET` | runtime INI/native owner | 设置页 `设零` 键盘输入的坐标值作为当前轴 0 位，需要 owner 写入和后续回零/native 证明 |
| UI 显示 | State Publisher typed SHM | UI 只渲染 State Publisher 发布的等效值；该值必须由 LinuxCNC/HAL/EtherCAT 真实 raw 机械坐标、当前零点证据、`SCALE` 和 A/C 等效相位规则计算得到，不得自行读取磁盘补洞或私自造 0 |

## 4. BUS/EtherCAT 设0

BUS 模式普通 `设0` 是“保存零点证据 + 硬盘 raw 限位同次写入”的保存动作，不是 Pulse 回零，也不得把当前 UI 等效坐标立即归零作为按钮通过门禁。X/Y/Z 直线轴按下设0后直接提交正式 product action；A/C 转动轴按下设0并通过转动轴危险确认后提交正式 product action。系统必须读取当前 raw 机械坐标和驱动 count-domain 位置写入硬盘，并按新零点和 UI 限位距离计算写入硬盘 raw 限位；LinuxCNC/HAL/EtherCAT live raw 限位装载、允许的 SHM 坐标/UI 等效坐标 `0.000` 显示闭环在最后统一保存重启后完成。

基本模型：

```text
pre_zero_machine_position = 设0瞬间 LinuxCNC/HAL/EtherCAT/native 产品可见机械坐标
zero_shift                = pre_zero_machine_position
current_counts            = 当前驱动/编码器反馈计数
zero_counts               = 设0瞬间保存的 current_counts
active_runtime_SCALE      = active runtime INI/native owner 回读的 count/单位
post_raw_machine_position  = pre_zero_machine_position
post_zero_memory_position  = 最后统一保存重启后由 native/owner 证明的 0.000 等效投影值
ui_equivalent_position     = equivalent_projection(post_raw_machine_position, zero_model, SCALE, rotary_phase_rule)，按钮保存阶段不作为通过门禁
ui_min_limit_distance      = UI 设置的负向限位物理距离，坐标值相对 0 位
ui_max_limit_distance      = UI 设置的正向限位物理距离，坐标值相对 0 位
post_min_limit_raw         = zero_shift + ui_min_limit_distance
post_max_limit_raw         = zero_shift + ui_max_limit_distance
```

UI 显示值必须是 `ui_equivalent_position`，不是 UI 本地状态、旧快照、硬盘字段或弹窗状态。对 A/C 旋转轴，UI 显示的是从真实 raw 机械坐标/count-domain 位置投影得到的等效角；按钮保存阶段只证明新零点和硬盘 raw 限位已写入，等效角归零显示闭环必须在最后统一保存重启后由允许的 SHM 坐标/UI 投影闭合。

UI 里的限位设置只是相对于 0 位的物理距离，不是 LinuxCNC/HAL/EtherCAT 的绝对 raw 限位坐标。零位改变时，UI 限位距离值不自动改写；后端必须按新 0 位和这些距离计算 raw 限位，并在同一次设0动作内由受控 runtime/native owner 写入硬盘 `MIN_LIMIT/MAX_LIMIT`、`soft_minus/soft_plus` 或等价 raw limit 字段。只改 UI 显示限位、只改旧快照、只记录计划但不改硬盘 raw 限位，或让最终重启后的底层限位仍停留在旧 raw 坐标系，都不是闭合。

A/C 绝对多圈轴还必须保存：

```text
zero_abs_turns
zero_abs_single_turn_counts
zero_abs_counts64
raw_bits/raw_modulus/raw_value_type/raw_modulus_trust
```

驱动 profile 必须提供：

- `drive.read_actual_position`
- A/C 绝对多圈轴还必须提供 `drive.read_abs_multi_turn`
- A/C 绝对多圈轴还必须提供 `drive.read_abs_single_turn`
- A/C 绝对多圈轴还必须提供 `drive.read_abs_position64`，可由 Adapter 用 split 读数合成

X/Y/Z 线性轴设 0 的驱动读回只能读取本轴 `drive.read_actual_position` 这一条 count-domain 锚点，不得为了普通线性设 0 串行读取 statusword、mode、绝对编码器、电齿轮等全量诊断字段；缺 profile、缺轴-从站绑定、缺 `drive.read_actual_position` 或单条读回失败时直接 fail-closed，不得退回全量读回、扫描全站或旧兼容路径。A/C 旋转轴设 0 仍必须读取绝对多圈 basis；RTCP/G53 geometry 可在可用时保存为诊断证据，但缺失、不新鲜或不可证明时不得阻塞本次设0保存。

split 合成不得把 `uint16` 圈数直接强转有符号值；运行时必须使用相对零点差分取模公式。缺 profile、缺轴-从站绑定、缺读回命令、读回失败、`SCALE` 未经 active owner 回读、状态 stale 或机器忙时，必须 fail-closed。

BUS 设0不得把前后 raw 双采样静止窗口作为阻塞门禁。编码器位置允许轻微跳动，后端可以记录设0前后的 raw delta 作为诊断证据，但不得因为微小跳动返回 `SETTINGS_ZERO_AXIS_MOTION_ACTIVE` 或拒绝保存 `zero_model`。请求契约校验通过后，本次保存是否成功只看驱动 count-domain 读数、LinuxCNC raw 读数、active runtime INI `SCALE`、`zero_model` 写盘和 raw limit 写盘这五项。

A/C 绝对旋转轴的 `zero_model` 不以 RTCP/G53 回转几何身份作为设0保存门禁。若设0瞬间同一帧状态已经提供可信 geometry，可把 `G53_A_Y/G53_A_Z/G53_C_X/G53_C_Y` 所在 owner 的源 generation/hash 或规范化 geometry hash 作为可选诊断证据保存；若缺失、不新鲜或来源不可证明，本次设0保存仍按五项通过条件继续。后续 unwrap、checkpoint 恢复、runtime rebase 或 BUS Home 若自身需要 geometry 证明，必须在使用该能力前单独比对当前 RTCP/G53 geometry hash；该后续能力可以 fail-closed，但不得反向阻塞设置页本次设0保存。

BUS 保存成功不能只表示零点源数据已保存；同次产品动作必须完成硬盘 raw 限位写入。按钮成功响应必须诚实标明 live raw 限位和 UI/SHM 等效归零尚未验证，并要求最后统一保存重启后补齐 live 证明：

```text
code=SETTINGS_COUNT_DOMAIN_ZERO_SAVED_RESTART_REQUIRED
zero_applied=true
zero_display_verified=false
raw_limit_disk_saved=true
raw_limit_live_verified=false
raw_runtime_zero_verified=false
memory_zero_verified=false
pending_runtime_proof=true
backend_restart_required=true
restart_deferred=true
```

本次单轴设0保存通过必须来自同一次 settings_axis_zero 产品动作内的硬盘零点证据写入和硬盘 raw 限位写入。只改 UI 本地值、只写 `/run` result、只保存零点证据但未写硬盘 raw 限位，或缺 raw 读回、缺 `SCALE`、缺驱动 count-domain 读回、状态 stale/机器忙时仍必须 fail-closed。允许的 SHM 坐标/UI 等效坐标不是 `0.000` 不得阻塞本次按钮保存，但必须作为最后统一保存重启后的 live 显示闭环条件。

## 5. Pulse/StepDir 设0

Pulse 模式按轴参数表当前行的单轴 `设零` 时必须弹出键盘；用户输入坐标值，这个值作为当前行当前轴 0 位写入 native/runtime owner：

```text
home_offset      = 键盘输入的当前轴 0 位坐标值，直线轴 mm，旋转轴 deg
```

规则：

- Pulse 普通设置页轴参数表 `设零` 请求必须带 finite `home_offset`，该值来自本次键盘输入，表示当前行当前轴 0 位坐标值。
- Pulse 普通设置页轴参数表 `设零` 不再要求用户输入 `home_direction`；回零方向、搜索速度、锁存速度和回零顺序属于独立回零参数 owner，不能由本次设零键盘临时改写。
- Pulse 轴参数表必须有独立 `回零方向` 列，通过下拉选择回零方向；该列写入 `home_direction` 对应 owner，供后续 LinuxCNC/HAL Home 使用。
- Pulse 写入后只可返回 pending restart/homing 或 owner write/readback 状态，不得声称当前 LinuxCNC 坐标已被 UI 立即改成 0。
- Pulse 不发送 EtherCAT SDO read/write，不读取 BUS zero_model，不写 `motion_driver.conf`。
- Pulse `home_offset` 必须和当前 runtime/native 软限位区间一起校验。后端提交前要读取目标 joint 当前 `MIN_LIMIT/MAX_LIMIT` 或对应 `soft_minus/soft_plus` owner 投影；当该轴软限位有效时，`home_offset` 必须落在允许区间内。若软限位 owner 缺失、区间非法、单位不可信，或 `home_offset` 超出有效软限位范围，必须返回 `SETTINGS_PULSE_HOME_OFFSET_OUT_OF_LIMITS`，不得写 runtime INI。软限位被明确配置为该方向不生效时，才能按 owner 语义跳过该方向限制，不能用 `±1_000_000` 宽限值替代真实软限位。

成功响应口径：

```text
code=SETTINGS_PULSE_HOME_OFFSET_RUNTIME_INI_WRITTEN
home_offset_saved=true
zero_captured=false
zero_applied=false
backend_restart_required=true
```

## 6. 请求契约

两种模式复用 `settings_axis_zero` action，但必须用不同 scope。

| 字段 | BUS/EtherCAT | Pulse/StepDir |
| --- | --- | --- |
| `action/native_action` | `settings_axis_zero` | `settings_axis_zero` |
| `request_id` / `idempotency_key` | 必填 | 必填 |
| `status_epoch` | 不提交；SHM/display epoch 不得作为设0保存准入或证明 | 不提交；SHM/display epoch 不得作为设0保存准入或证明 |
| `driver_mode` | `ethercat` / `bus` | `pulse` |
| `axis` / `joint` | 必须匹配 | 必须匹配 |
| `target_scope` | `bus_count_domain_zero` | `pulse_mechanical_home_offset` |
| `apply_mode` | `count_domain_zero` | `persist_home_offset` |
| `home_offset` | 禁止作为请求源 | 必填 finite；来自键盘输入，表示当前轴 0 位坐标值 |
| `home_direction` | 不参与 BUS 设0或 BUS Home | 不由普通设置页 `设零` 键盘提交；由 Pulse 轴参数表 `回零方向` 列下拉选择并写独立回零参数 owner |

模式、scope 或字段冲突必须 fail-closed；不得尝试“兼容猜测”。

## 7. Apply 路线

### 7.1 BUS

BUS apply 必须在同一次设0动作里保存 count-domain 零点证据，并以 UI 限位距离为输入，同次计算写入新 0 位对应的硬盘 raw 限位坐标。UI 的 `0.000` 不能由 UI 独立置零；最终是否已经由 raw 机械坐标回读经 State Publisher 等效投影得到 `0.000`，属于最后统一保存重启后的 live 闭环证明，不作为单次设0按钮保存通过条件。

允许状态：

- `disk_zero_saved_restart_required`：硬盘零点证据和硬盘 raw 限位已写入；需要最后统一保存重启后由允许的 SHM 坐标/UI 投影证明当前等效坐标归零。
- `zero_saved_but_not_applied`：只保存证据或显示诊断，必须作为失败/阻塞返回，不得作为成功。

禁止把 Route A 当生产路线：

```text
UI 本地私自置零，或 State Publisher 没有使用 LinuxCNC/HAL/EtherCAT raw 机械坐标和新零点证据计算
```

当前单轴设0生产路线是保存 count-domain 零点证据后，同次计算并写入硬盘 raw 限位坐标；最后统一保存重启路线负责让 LinuxCNC/HAL/EtherCAT 装载这些硬盘 raw 限位，并由允许的 SHM 坐标/UI 投影证明等效坐标归零。不能只保存零点证据而把 raw 限位硬盘更新遗漏到最后。

### 7.2 Pulse

Pulse apply 固定为 homing 参数生成：

```text
settings_axis_zero request
  -> settings_apply/native owner
  -> 8ax_bus5_runtime_settings.ini
  -> [JOINT_N] HOME_OFFSET
  -> backend restart
  -> LinuxCNC homing
  -> homed 后建立机械坐标
```

Pulse 关闭条件不是字段写入成功，而是 runtime INI、重启后 un-homed、homing 后坐标和 native active mode 与 UI 显示 都闭合。

### 7.3 硬件 preset

驱动侧 position-basis mutation 不属于普通 BUS 单轴 `设0` 成功条件。若产品需要真正改写 EtherCAT 产品可见 raw 机械坐标，该能力必须有明确 profile/对象字典 allowlist、机器安全空闲、写入/保存/回读/必要重启校验闭环；不得作为独立隐藏维护 preset、临时脚本、旧路径或失败兜底保留。

## 8. Owner 文件

| 层级 | owner/锚点 | 责任 |
| --- | --- | --- |
| UI | `v3_ui_command_axis_zero.c::execute_settings_axis_zero_gate_command()`、`v3_ui_command_axis_zero_pulse_home_offset_command()`、`v3_ui_keyboard_settings.inc` | BUS 不输入值；Pulse 弹软键盘输入当前轴 0 位坐标值；请求带强类型字段 |
| Broker | `broker_action_handlers_settings.py::_handle_settings_axis_zero()`、`broker_identity_script_actions.py` | 校验 action、mode/scope、axis/joint、Pulse 字段、幂等 TTL/LRU；不得把 SHM/display `status_epoch` 作为设0准入或证明 |
| Product action | `product_handler_settings_axis_zero.py`、`product_actions/actions_axis_zero.py`、`product_actions/actions_axis_zero_bus_runtime.py`、`product_actions/actions_axis_zero_pulse_runtime.py` | BUS 读当前 raw 机械坐标、驱动 actual/split absolute，写 count-domain 证据，计算并写入硬盘 raw 限位，返回待统一重启；Pulse 构造 HOME apply 请求 |
| Settings apply | `v3_settings_apply.py`、`settings_apply_pulse_home_offset.py`、`settings_apply_axis_ini_generation.py` | BUS 不恢复旧字段；Pulse 写入当前轴 `HOME_OFFSET`；Pulse 轴参数表 `回零方向` 列写 `home_direction` owner，速度/顺序走各自 owner |
| SHM/UI 显示投影 | 状态显示 owner | 只发布允许的坐标、模态、转速、进给速度和主轴/进给倍率显示投影；active mode、count-domain、raw zero applied proof 和 degraded control state 必须直连 native/microkernel owner |

大文件只保留调度/glue/兼容转发；新增业务 helper、状态机、解析器、表驱动或参数迁移写路径必须放到合适 owner 或新 sub-owner。

## 9. Fail-closed

以下情况必须拒绝：

- native owner 状态不可用、状态来自 SHM/display epoch、或把 SHM 显示投影 stale/seq 校验失败当作设0保存准入。
- 缺 `axis`、`joint` 或轴关节不匹配。
- 缺 `driver_mode`、`motion_driver.conf` 损坏、模式与 scope 冲突。
- 机器运行、MDI 忙、Jog 活跃、Start 活跃、Homing 活跃。
- BUS 设置页设0路径出现上使能、idle、stillness、UI/SHM 显示归零等待、live raw limit 读回或 RTCP/G53 geometry fresh 要求等额外保存门禁。
- BUS 缺 public/private profile、轴-从站绑定、读回命令、驱动 actual/split absolute 读回或 active `SCALE`。
- BUS 状态 stale、机器正在运行、MDI/Jog/Start/Homing 活跃、`SCALE` 不可信、驱动 count-domain 读回失败，或 raw 读回完全不可用；前后 raw 微小 delta 只能作为诊断，不得单独作为阻塞条件。
- 后续 unwrap、checkpoint 恢复、runtime rebase 或 BUS Home 使用旋转 geometry 能力时，若当前 RTCP/G53 geometry hash 与可用零点诊断证据不一致，可在该后续能力内 fail-closed；设置页本次 BUS 设0保存不得因为 geometry 缺失、不新鲜或不一致而失败。
- BUS 请求试图写 `home_offset`、旧 `旧浮点零位字段/旧倒数比例字段`，或绕过 canonical native/microkernel gate 直接执行 hardware preset、`0x607C`、`0x1010`、drive rebase。
- BUS 设0后没有按新 0 位和 UI 限位距离同次计算并写入硬盘 raw 限位，或 `MIN_LIMIT/MAX_LIMIT`、`soft_minus/soft_plus` 区间非法、方向错误、距离被扩大/缩小。
- Pulse 缺 `home_offset`、非 finite，或试图把回零方向塞进普通设置页 `设零` 键盘路径。
- Pulse `home_offset` 超出当前有效 runtime/native 软限位区间，或软限位 owner/readback 不可信导致无法证明该 offset 安全。
- BUS Settings Drive 私域持久文件写入、fsync、rename、读回失败。
- Pulse runtime INI/native owner 写入、生成或读回失败。

建议错误码维持现有命名族：`SETTINGS_ZERO_*`、`SETTINGS_COUNT_DOMAIN_ZERO_*`、`SETTINGS_PULSE_*`。

## 10. 本地门禁

代码修改后至少覆盖：

- `python -m py_compile` touched Python。
- `test_command_broker_protocol.py`
- `test_product_command_settings_read_direct_uds.py`
- `test_settings_axis_zero_dual_mode.py`
- 与 drive-only、settings apply、profile/readback、禁止 SHM/display epoch 参与设0准入相关的 focused 测试。
- 静态检查证明普通设0路径没有绕过 canonical native/microkernel gate 的 SDO preset、旧 FIFO/response JSON、Pulse `home_offset` 写入 `settings_runtime.json` 或 `motion_driver.conf`。
- 文档/规则改动按 `AGENTS.md` 跑 doc single-source audit、`git diff --check` 和 preflight。

## 11. 板端闭环


| 模式 | 板端关闭条件 |
| --- | --- |
| BUS/EtherCAT | 完整构建产物上板；active mode 为 BUS；设置页点击目标轴 `设0`；profile 具备 required read commands；采集 UDS response、`settings_runtime.json`、SHA256、runtime INI、native readback 和必要 SHM 显示截图；同一次设0动作后硬盘 raw 限位已按 `raw_zero_position + UI限位距离` 写入；A/C RTCP/G53 geometry 只在可用时作为诊断证据，不作为设0通过条件；最后统一保存重启后 LinuxCNC/HAL/EtherCAT live raw 限位装载一致；UI 显示值由真实 raw 机械坐标经允许的 SHM 坐标/UI 等效投影计算得到并显示 `0.000`；UI 限位距离值保持为相对 0 位的物理距离，不被 raw 限位坐标污染；安全小增量运动证明控制空间、UI 显示和 count-domain/native MCS 改变量一致 |
| Pulse/StepDir | active/target mode 为 Pulse；设置页轴参数表点击当前行当前轴 `设零` 后弹出键盘；键盘输入坐标值作为当前轴 0 位；轴参数表 `回零方向` 列可下拉选择回零方向并写 `home_direction` owner；后端证明 `home_offset` 在当前有效 `MIN_LIMIT/MAX_LIMIT` 或对应 soft limit owner 区间内；越界输入返回 `SETTINGS_PULSE_HOME_OFFSET_OUT_OF_LIMITS`；apply/restart 后 runtime INI 目标 joint 的 `HOME_OFFSET` 和 `home_direction` 字段正确；LinuxCNC homing 后坐标建立符合该 0 位；普通设置页轴参数表 `设零` 不提交回零方向；无 EtherCAT SDO read/write |

涉及真实运动、小增量 MDI/G-code、Home/Jog/Start/E-stop 或运动 gate 放行时，还必须按 `AGENTS.md` 完成 `nc/cc.ngc` 黄金运动闭环。

## 12. 关闭条件

关闭前必须说明：

- 当前模式、请求 payload、响应码和 applied/fail-closed 状态；BUS 不得以 pending 成功关闭。
- BUS 的驱动读回来源、profile hash、zero count-domain 证据、active `SCALE`、LinuxCNC/HAL/EtherCAT raw 读回、UI 限位距离、硬盘 raw 限位写入证明；最后统一保存重启后再补 live raw 限位装载证明和 SHM/UI 等效归零证明。
- Pulse 的当前轴 0 位坐标值、`HOME_OFFSET` 写入、`回零方向` 下拉选择写入、重启和 homing 证明。
- 本地测试、板端截图/日志/状态证据和最终安全态。
- 未闭合事项只写真实缺口，不保留过程日志。
