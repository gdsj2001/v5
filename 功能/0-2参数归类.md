# 参数归类

## AI 阅读入口

<!-- AI_FAST_READ_BEGIN -->
owner_reqs: []
read_when: [设置页参数归类, G53 参数, 轴参数表, 字段读写 owner, 软件盘参数提交]
truth: [本文是字段落点视图；参数真值由索引登记的 native/drive/parameter owner 与 readback 裁决；无有效变更返回主页，有效变更后保存并重启]
forbidden: [UI 控件值当真源, SHM 当参数 owner, drive-only 反向拥有运动语义, 多处双写]
readback: [字段持久 owner 回读, runtime/native actual, drive provider actual, fresh generation]
impact: [设置页 UI, runtime INI, 参数表, drive profile, SHM 白名单, 保存并重启]
acceptance: [逐字段 owner/write/readback 一致；无变更本地返回不重启；有效变更后按钮单向切换并由重启后 native/drive 回读证明生效]
detail_sections: [归类原则, 微内核参数, 自建参数, 驱动映射表参数, 禁止项]
<!-- AI_FAST_READ_END -->

- 本文是设置页参数分类和字段落点视图；跨功能真源先回到 `功能/需求真源索引.md` 的对应 `REQ-*` owner。
- 参数进入微内核/native、runtime INI、LinuxCNC `PARAMETER_FILE`、tool、SHM 显示投影或 drive-only 域的裁决，以 `REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-NATIVE-OWNER-FIRST`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY` 和 `REQ-SHM-DISPLAY-PROJECTION` 为准。
- BUS/Pulse、RTCP/G53、比例链和软件盘首帧策略只在本文记录本页面怎样消费；若与对应 owner 冲突，先改 owner，再同步本文。

引用需求真源：`REQ-DOC-SINGLE-SOURCE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-NATIVE-OWNER-FIRST`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-SHM-DISPLAY-PROJECTION`、`REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION`、`REQ-UI-FIRST-FRAME-CACHE`。

## 归类原则

设置页参数只按三类 owner 归类：

1. 微内核参数：LinuxCNC runtime INI、HAL/native、kinematics/native、WCS/native、已登记 native memory/API。凡是微内核已有或应该下沉到微内核的参数，必须使用微内核/native readback，不得进自建参数表。
2. 自建参数：微内核参数和驱动映射表参数都没有承载的设置页参数。自建参数必须开机进入 RAM，UI 自动读取，不得显示 `NAT`、空占位、UI 默认值、旧 JSON、`/run` result 或 SHM 投影。
3. 驱动映射表参数：驱动参数映射表/drive provider，只服务驱动私域对象字典、SDO/PDO、驱动 profile、驱动读写证据和授权下载链路。驱动映射表参数不能反向成为 LinuxCNC 运动参数、坐标基、RTCP/G53、WCS/G92、刀补、模态或安全真源。

读取来源和写入目标必须分开记录：读取来源决定 UI 初始显示和默认值来源；写入目标必须落到对应硬盘持久 owner。双写不得形成双真值；对 runtime/native 类字段，单字段确认只允许证明持久 owner 写入并从同 owner/resident 表回读一致，运行态生效必须等受控重载/运行态写入后，再由微内核/native 或 drive provider readback 一致确认。`写入状态` 只表达驱动写入/readback 动作结果，不得作为普通参数提交成功状态。

UI 中任何可修改参数控件发生改变时，UI 只能提交字段保存请求给唯一 settings/native apply owner；由该 owner 串行写入字段对应硬盘/native 持久 owner，并从同一 owner/readback 链路读回后把结构化结果返回 UI 刷新单元格。UI 进程不得直接调用参数表写盘函数、runtime INI 写盘函数或用 LVGL 控件文本/RAM 临时变量/屏幕显示冒充保存成功。只要 owner 写入或 readback 不一致，就必须保持/刷新为 owner 读回值；普通参数提交不得改写 `写入状态`。

字段已经写入唯一 owner 并从同一 owner/readback 链路回读一致后，该字段不再属于“修改未保存”；UI 只能显示已保存、待重启生效或等价 restart pending。软件盘尚未确认的编辑态只属于当前控件输入态，不是稳定参数真源；不得把编辑态、dirty 标志或 restart pending 扩展为 Start/Resume 前置 gate。

设置页右上角是同一按钮的两态合同：进入设置页、本次会话尚无有效参数变更且无 restart pending 时显示 `返回主页`，只执行本地页面导航，不发 settings action、不触发 restart；任意字段在软键盘 `OK` 或下拉提交后写入自己的唯一 owner 并完成源位置 readback，或设置动作成功回读 `restart_required/restart_deferred` 后，立即单向切换为 `保存并重启`。点击 `保存并重启` 时只允许把这些已提交字段统一带入开发板断电重启等价的 clean restart handoff。不得把未写入硬盘参数表的 UI 临时值当成已保存参数；不得把 `保存并重启` 解释为运动准入、安全互锁或 Start/Resume 拦截，也不得实现成只重启 UI/Broker/State Publisher 的局部服务重载。

设置页参数 UI 的视觉布局必须直接继承 v3 设置页源码坐标，不得另造一套布局。v3 基准坐标为：顶部三块从 `y=42` 开始，左侧运动模型面板 `16,42,220,176`，G53 面板 `247,42,449,176`，右上机械坐标外框 `696,42,312,176`；G53 标题 `264,55`，G53 表头 `X/Y/Z=395/479/563,y=86`，原 v3 行为 `A中心/C中心/对刀仪/5方向监测仪`，单元格列锚点固定为 `366/450/534`；驱动动作按钮行为 `x=410/494/.../914,y=236`；轴参数大面板为 `21,225,982,343`，轴表标题 `y=230`，横向滚动区 `95,274,908,300`，轴名行从 `y=318` 起按 31px 行距排列。轴参数表必须在红框所示左标题区和右端保留图形化左右翻页按钮，用于横向翻页查看轴参数列；按钮只控制现有横向滚动区按页左/右滚动，不改变轴名固定列、列锚点、字段 owner、保存/readback 语义，也不得用第二套表格或隐藏列替代现有横向滚动区。右上机械坐标小面板必须按 v3 `create_settings_machine_coordinate_widget(page, 696, 42)`：外层保留 `696,42,312,176` 背景，内部坐标面板只显示 X/Y/Z 加 fresh Active Model descriptor 对应的两根旋转轴和 `MCS`，AC 显示 A/C、BC 显示 B/C，后续模型按 descriptor 显示自己的轴组；未知模型不得回退 A/C。数值区保持 v3 固定宽度/数码字视觉；龙门、刀库是下方轴参数表第 7/8 轴，不得塞进右上机械坐标小面板形成自造 8 行普通 label 表。新增 `B中心` 行必须插在 `A中心` 与 `C中心` 中间，但不得因此把驱动动作按钮行、轴参数大面板、轴表标题、横向滚动区、轴名行或轴参数行整体改成另一套布局；为容纳第 5 行，只允许在 G53 小表内部做最小竖向压缩，保持 v3 的列锚点、颜色、表头、单元格风格和视觉层级。轴参数标题文案沿用 v3 `轴参数(总线)`，不得把当前新增轴数写成标题视觉元素。轴参数表新增 `方向` 列时，只允许压缩 v3 原 `轴模式`/`从站` 段内部宽度来插入，`目标精度` 及其右侧所有列必须继续使用 v3 原始 x 坐标锚点（`272/390/472/...`），不得因为新增列把后续字段整体右移。

轴参数表控件类型固定：`轴模式`、`方向`、`从站`、`回零次序`、`回零方向`、`bit` 使用下拉；`分子`、`分母`、`写入状态` 为自动计算/动作结果只读显示；除此之外的轴参数单元格都必须点击弹出软件盘输入。所有下拉列的触发命中区必须覆盖整个单元格，不得只在三角箭头或文字局部触发。软件盘必须保持 v3 设置页同款数字键盘布局和视觉风格，用 LVGL 面板/按键/标签自绘实现，不得用 LVGL 默认 `lv_keyboard` 成品键盘替代。软件盘打开/返回必须遵守 `REQ-UI-FIRST-FRAME-CACHE`：打开前必须通过公共首帧 guard/helper（当前源码为 `V5UiFirstFrameGuard` 或等价 owner 入口）保留当前设置页帧，再创建全屏不透明遮罩快速盖住后部设置页内容，键盘区域作为独立命中区拦截内部点击；点击软件盘外的遮罩区域必须立即退出键盘。退出或取消时必须恢复打开前保留帧，不触发后部设置页整页重绘；确认后立即写入该字段对应硬盘参数表，并按 owner/readback 回读，只刷新发生变更的参数单元格。

轴参数表数字显示和输入只允许 `目标精度` 使用小数；除 `目标精度` 外，所有数字参数在表格中必须显示为整数，软件盘提交值也必须是整数。读取 owner 得到的历史小数、LinuxCNC 秒单位换算值或比例链重算值，只能在设置页显示层按机械输出侧整数归一化；其它数字字段带小数点、分数、乘号或非整数格式时必须拒绝提交。

G53 机床坐标目标位置表中，只有图示红线 owner 格 `A中心 X`、`B中心 Y`、`C中心 Z` 保持对应 WCS/native owner 只读，不从设置页软件盘写入。除此之外的 G53 区单元格，包括 `A中心 Y/Z`、`B中心 X/Z`、`C中心 X/Y`、`对刀仪 X/Y/Z`、`5方向监测仪 X/Y/Z`，点击必须弹出同一套 v3 样式 LVGL 自绘软件盘输入；确认后按字段归属写入对应硬盘参数表并回读刷新。

`A中心 Y/Z`、`B中心 X/Z`、`C中心 X/Y` 属于 G53 native geometry/runtime INI readback。设置页软件盘确认后只能向 settings/native apply owner 发保存请求，由该 owner 写入 active runtime INI `[RTCP]` 的唯一 owner，并从同一 `[RTCP]` 字段源位置回读一致后返回 UI 清 dirty、关闭软件盘并刷新当前单元格；UI 进程不得直接写 runtime INI，不得再写 `microkernel_parameter_table.tsv`、`settings_runtime.json`、自建参数表或任何 V3 shadow owner。该刷新只表示持久 owner 已写入/待受控重载，不表示当前 native actual 已改变；受控重载/运行态写入完成后，最终 actual 仍必须从微内核/native 或 runtime INI/native readback 读回一致。加载设置页时不得把旧 UI 默认值、SHM、`/run` result 或旧 cache 当成 G53 几何真源；当前运行态 actual 以 native/readback 为准。`对刀仪` 和 `5方向监测仪` 仍按自建参数表 owner 读写刷新。

## 微内核参数

| 设置页位置 | UI 参数 | 微内核来源 | 读取要求 |
| --- | --- | --- | --- |
| 顶部 | 运动模型 | kinematics/native 或 runtime INI `[RTCP]` | 设置页“模型选择”不是 UI 私有文案；这里选择什么，就必须由 settings/native apply owner 提交并写入微内核/native `motion_model` 参数，源位置回读一致后才允许显示为新模型。active model readback 同时决定微内核运动学和主页面 UI 轨迹投影消费哪组 G53 几何：AC/`XYZAC_TRT` 使用 A/C 的 G53 设置值，BC/`XYZBC_TRT` 使用 B/C 的 G53 设置值。主页面 UI 的黄色轨迹、对应轴线、当前加工坐标系投影和相关几何显示旋转方式必须消费同一个微内核/native active model readback，不能硬编码为 AC，也不能直接信设置页下拉文本、本地缓存或自建参数表。UI 不实现、不补偿、不模拟运动学，也不得把模型选择当作本地绘图开关。 |
| G53 机床坐标目标位置表 | A 中心 X | 当前模态加工坐标系 X 偏移值，WCS/native owner readback | 不是固定 G53 几何常量；不得写入自建参数。 |
| G53 机床坐标目标位置表 | A 中心 Y | `G53_A_Y`，native geometry/runtime INI readback | 从微内核/native 读取；不得用 UI 默认值补。 |
| G53 机床坐标目标位置表 | A 中心 Z | `G53_A_Z`，native geometry/runtime INI readback | 从微内核/native 读取；不得用 UI 默认值补。 |
| G53 机床坐标目标位置表 | B 中心 X | `G53_B_X` 或等价 G53/native geometry readback | B 中心位于 A/C 中间，X 是 G53 几何坐标；不得写入自建参数。 |
| G53 机床坐标目标位置表 | B 中心 Y | 当前模态加工坐标系 Y 偏移值，WCS/native owner readback | B 中心位于 A/C 中间，Y 不是固定 G53 几何常量；不得写入自建参数。 |
| G53 机床坐标目标位置表 | B 中心 Z | `G53_B_Z` 或等价 G53/native geometry readback | B 中心位于 A/C 中间，Z 是 G53 几何坐标；不得写入自建参数。 |
| G53 机床坐标目标位置表 | C 中心 X | `G53_C_X`，native geometry/runtime INI readback | 从微内核/native 读取；不得用 UI 默认值补。 |
| G53 机床坐标目标位置表 | C 中心 Y | `G53_C_Y`，native geometry/runtime INI readback | 从微内核/native 读取；不得用 UI 默认值补。 |
| G53 机床坐标目标位置表 | C 中心 Z | 当前模态加工坐标系 Z 偏移值，WCS/native owner readback | 不是固定 G53 几何常量；不得写入自建参数。 |
| 右上机械坐标小组件 | X/Y/Z + Active Model 两根旋转轴 MCS | native 运行态坐标/显示投影链路 | 右上区域必须保持 v3 设置页 `create_settings_machine_coordinate_widget(page, 696, 42)` 的五行界面；前 3 行固定 X/Y/Z，后 2 行只能由 fresh native active model descriptor 决定：AC 显示 A/C，BC 显示 B/C，未知模型显示 unavailable 且不得回退 A/C。龙门、刀库不得塞进右上角自造 8 行坐标表，只在下方轴参数表中显示。 |
| 轴参数表 | 轴模式 | runtime INI `[AXIS_*]` / native axis readback | 下拉选择 `直线`、`旋转`、`虚拟`；龙门、刀库作为第 7、8 轴，有 native owner 时同样走 native，不得灰色禁用。 |
| 轴参数表 | 方向 | runtime INI `[AXIS_*]` / native axis readback | 放在 `轴模式` 右侧，以下拉选择 `cw`、`ccw`；归微内核参数，不进自建参数或驱动映射表。 |
| 轴参数表 | 目标精度 | runtime INI/native `SCALE` 相关 readback | 不能用 UI 默认值补。 |
| 轴参数表 | 螺距/每转角度 | runtime INI/native 比例链 readback | 已有微内核/native owner；影响 `SCALE` 和电子齿轮计算，必须从 runtime/native 读取并写回同 owner。 |
| 轴参数表 | 电机 | native/runtime `motor_rev` readback | 已有微内核/native owner；影响比例链，必须从 runtime/native 读取并写回同 owner。 |
| 轴参数表 | 负载 | native/runtime `load_rev` readback | 已有微内核/native owner；影响比例链，必须从 runtime/native 读取并写回同 owner。 |
| 轴参数表 | 回零次序 | LinuxCNC homing/native owner | 从微内核/native 读取。 |
| 轴参数表 | 回零方向 | LinuxCNC homing/native owner | 从微内核/native 读取。 |
| 轴参数表 | -软限位 | runtime INI/native limit owner | 从微内核/native 读取。 |
| 轴参数表 | 零位 | Pulse 为 `HOME_OFFSET`/runtime owner | BUS 设零动作仍按对应 native/drive 证据闭环。 |
| 轴参数表 | +软限位 | runtime INI/native limit owner | 从微内核/native 读取。 |
| 轴参数表 | 速度 | runtime INI/native motion owner | 从微内核/native 读取。 |
| 轴参数表 | 加速 | runtime INI/native motion owner | 从微内核/native 读取。 |
| 轴参数表 | 反向间隙 | runtime INI/native motion owner | 从微内核/native 读取；不得进自建参数。 |

运动模型提交必须是一个完整的 settings/native owner 事务，不能只改 `[RTCP]`、`[KINS]` 或 `[TRAJ]` 文本。descriptor 选中的每个轴必须把对应 `[AXIS_*]` 参数装载到该 descriptor 的实际 `JOINT_<status_slot>`，并对速度、加速度、软限位、比例链/SCALE、回零参数和反向间隙做源位置回读；AC 与 BC 共用第一旋转物理槽 3 时，AC→BC 必须把该槽当前真实从站绑定从 A 行迁移到 B 行并把 A 行置为 `NAT`，BC→AC 则反向迁移，物理从站编号不得凭空改变。事务返回成功前必须同时证明 `motion_model`、KINS/TRAJ、active axis→joint 参数和 resident self 轴→从站绑定回读一致；任一项失败不得发布新模型成功。owner 返回成功后，设置页轴参数表必须立即以同一事务 readback 刷新 A/B 行的 `从站` 列及对应禁用态，不能保留切换前的 UI 值；AC 显示 A=实际从站、B=`NAT`，BC 显示 A=`NAT`、B=同一实际从站。clean restart 后，微内核/native active model、HAL kinematics、设置页轴参数从站列、设置页右上坐标、主页面坐标/轨迹和驱动动作轴映射都只能消费这一组回读。

轴参数表的速度、加速度和软限位按机械输出侧记录和显示，不按电机侧 RPM 记录。速度单位为 X/Y/Z 直线轴 `mm/min`、A/C 旋转轴 `deg/min`；runtime INI `MAX_VELOCITY` 按 LinuxCNC 秒单位保存，设置页写入时将每分钟单位除以 60，读取显示时乘以 60。换算必须考虑螺距/每转角度、`motor_rev/load_rev` 和减速比；默认值为 X/Y/Z `速度=10000`、`加速=500`、软限位 `-500/+500`，A/C `速度=50000`、`加速=2000`，旋转轴软限位默认沿用 owner 的 no-soft-limit 语义。

## 自建参数

| 设置页位置 | UI 参数 | 自建参数用途 | 读取要求 |
| --- | --- | --- | --- |
| 顶部 | 脉冲/总线 | 当前机型设置项 | 自建参数表必须保存默认值 `总线`，开机进入 RAM 后由 UI 从自建参数表 owner/readback 自动读取；不得用 UI 默认文案、硬件探测或驱动映射表补值。 |
| G53 机床坐标目标位置表 | 对刀仪 X/Y/Z | 设置页测量/标定显示参数 | 微内核和驱动映射表没有该设置页参数，放入自建参数并由 UI 读取。 |
| G53 机床坐标目标位置表 | 5 方向检测仪 X/Y/Z | 设置页测量/标定显示参数 | 微内核和驱动映射表没有该设置页参数，放入自建参数并由 UI 读取。 |
| 轴参数表 | 从站 | 设置页轴从站编号显示参数 | 读取来自硬盘自建参数表；`扫描从站` 扫到的从站候选必须写入 `board/config/settings/self_parameter_table.tsv` 的 `SETTINGS/slave_options` 硬盘字段，并作为该列真实从站候选。`从站` 下拉必须额外提供固定 `NAT` 选项，表示该轴显式不绑定物理从站；选择 `NAT` 后写入自建参数表的值为字面量 `NAT`。真实候选显示必须是 `轴号 驱动名型号`，例如 `0 SV630_1Axis_03713`；用户选择真实从站后写入自建参数表的值仍只能是纯轴号，不得把显示用驱动名写入轴绑定字段。每个轴行打开 `从站` 下拉时必须重读 resident 候选列表，并排除已被其他轴当前选定的真实从站编号；当前轴自己的真实从站仍保留，`NAT` 不参与唯一性排除且始终可选。默认 X/Y/Z/A/B/C/龙门/刀库从站列为 `0-7`。扫描只刷新候选列表，不自动改写当前轴-从站绑定；用户改变从站后写入自建参数表并从 resident self owner 回读一致，受控重载后由驱动/运行 owner 消费该绑定；不得恢复第二套运行表。 |
从站选择改变并经硬盘自建参数表/resident self owner 回读一致后，设置页当前会话中的轴到从站关系必须立即按该 resident 绑定刷新，后续设置页动作请求只能使用该行当前 `从站` 值；最终 LinuxCNC/HAL/EtherCAT/native live 轴绑定仍通过 `保存并重启` canonical clean restart 后由运行 owner 消费并 readback 证明。若某行 `从站` 选择为 `NAT`，该轴显式不绑定物理从站；轴参数表该行除 `从站` 列外全部显示为灰色禁用态，不允许打开下拉、软件盘、设0/设零或提交参数改动，`从站` 列保持可用以便重新选择真实从站。

| 轴参数表 | 分子 | 驱动实际读回只读显示 | 来源只能是 `读取驱动`、`复位驱动`、`设置驱动` 等 drive provider 对 `drive.read_egear` 的真实 SDO readback；电子齿轮证据归属物理从站，硬盘 drive-only 参数表必须保存可按 `SLAVE_<从站号>` 回读的快照，UI 显示该列时必须先按本行当前 `从站` 值读取对应从站证据，再刷新到当前轴行。允许同步轴名行作为兼容显示快照，但轴名行不得覆盖当前从站证据。`从站=NAT` 或缺少该从站可信读回时显示 unavailable/`--`，不得沿用该轴旧从站、旧 `/run` result、UI 默认值或自建参数表旧值补数。 |
| 轴参数表 | 分母 | 驱动实际读回只读显示 | 同 `分子`，只读显示当前行所选物理从站的电子齿轮分母；来源只能是 drive provider readback 或硬盘 drive-only 参数表中的 `SLAVE_<从站号>` readback 快照，不能用逻辑轴名旧值冒充新从站读回。 |
| 轴参数表 | 写入状态 | 驱动实际动作状态只读显示 | 来源只能是同一 drive action 的写入/读回结论：设置驱动必须在电子齿轮和模式写入、驱动 activation restart、post-reset 必读 SDO readback 全部一致后才显示已写入；读取驱动只显示读回实际，复位后显示复位失效或待重新设置，失败显示对应失败状态。写入状态归属物理从站，UI 必须按本行当前 `从站` 的 `SLAVE_<从站号>` drive-only 快照显示；从站改变后立刻刷新本行 `分子`、`分母`、`写入状态`，新从站无可信证据时显示 unavailable/`--`，不得继续显示该轴原从站状态。 |

## 驱动映射表参数

| 设置页位置 | 参数/动作 | 驱动映射表来源 | 边界 |
| --- | --- | --- | --- |
| 设置驱动按钮/下载授权 | 驱动 profile | drive provider / 驱动参数映射表 | 只服务驱动私域选择、下载和证据链；不是轴运动参数 owner。 |
| 设置驱动按钮/下载授权 | SDO/PDO 对象字典 | 驱动参数映射表 | 只用于真实驱动读写；不得反向成为 UI 稳定显示参数真源。 |
| 设置驱动按钮/下载授权 | 授权下载参数 | drive provider 授权/下载链路 | 只服务驱动写入流程；不得接管微内核参数或自建参数。 |
| 设置驱动按钮/下载授权 | 驱动读写证据 | drive provider readback/evidence | 只能作为设置驱动动作闭环证据；不能替代 LinuxCNC/native readback。 |
| 轴参数表 | bit | 驱动参数表 / 驱动映射表 `encoder_bits` | 默认读取硬盘驱动参数表；允许人工更改，人工值仍归驱动参数表/驱动映射表参数并优先于默认值。用户改变 bit 后必须写入硬盘驱动参数表并从 resident drive owner/drive provider readback 一致；受控重载后由 drive/runtime owner 消费该值。驱动表缺失 `encoder_bits` 时必须显示 unavailable/degraded，不得用 `NAT`、默认值或旧值伪造；不得进自建参数，也不得恢复 `microkernel_parameter_table.tsv` 第二套运行表。 |
| 轴参数表驱动动作 | 分子/分母/写入状态实际显示 | 驱动参数映射表和 drive provider readback | 驱动映射表记录真实驱动读写证据；读回后的分子/分母和写入状态必须以物理从站为 owner 同步保存到硬盘 drive-only 参数表的 `SLAVE_<从站号>` 行，允许同时写当前轴名行作为显示快照。设置页 UI 显示三列时以当前 `从站` 的 `SLAVE_<从站号>` 证据为准，因此用户改变 `从站` 后三列必须跟随新从站刷新；自建参数表、UI 默认值、轴名旧值和旧 result 文件不得成为该三列的显示 owner。 |

## 从站/bit 受控重启生效机制

从站和 bit 的物理生效路径固定为“字段 owner 保存 -> 保存并重启 -> 开发板断电重启等价 clean restart handoff -> 开机链路重新入 RAM -> 物理总线/native readback”。从站改变只写自建参数表并由 resident self owner 回读；bit 改变只写驱动参数表/驱动映射表并由 resident drive owner 或 drive provider 回读。二者在字段保存阶段都不得直接改写微内核运行态内存、HAL 映射、EtherCAT master 或轴到驱动的 live 绑定。

点击 `保存并重启` 后，由 `v5-settings-actiond` 或等价 canonical clean restart owner 挂起交互并拉起系统级干净重启流程。该流程必须达到开发板断电后重新上电的同等干净度：停止 UI、Broker、Action Dispatcher、State Publisher、LinuxCNC/微内核运行服务、HAL/EtherCAT/drive provider 等运行闭包，清理或重建 `/run` 运行态、UDS socket、SHM 显示投影、native 状态块、resident snapshot、命令队列和进程内 cache，再按开机链路从唯一持久 owner 重新装载到 RAM。可使用受控脚本，例如 `re-v3-8ax-backend.sh` 或新的 clean restart 脚本，但脚本职责不能只是 restart 若干服务；如果软件 clean restart 不能证明与开发板断电重启等价，必须走板端系统级 reboot 或断电重启等价路径。

clean restart 期间必须让相关驱动进入 Disabled / Safe-Op 或等价安全去使能状态，再重新初始化 HAL、EtherCAT 主站、SDO/PDO 映射、轴到从站绑定、encoder bit 和轴当量换算。重启后的 UI/Broker/Publisher/provider 必须消费新开机链路装载出的 resident owner/readback；重启完成前不得宣称运行态已生效。

clean restart 成功证明必须来自真实 owner readback：硬盘持久 owner 与新装载 resident snapshot 一致，EtherCAT/驱动物理总线读回的从站 evidence、SDO/PDO/encoder bit readback 和 native/runtime 轴绑定 generation 一致。只要任一从站握手、SDO 写入、PDO 映射、驱动 readback、native/runtime generation、resident snapshot 签名或清理后的运行态重建不一致，必须终止重启完成声明并 fail-closed，保持去使能并报告结构化错误；严禁带半更新参数、脏绑定、旧 bit、旧 `/run` 快照、旧 SHM/native 状态块或旧进程 cache 上线。

动态 Native Gate 参数注入只作为未来优化保留。启用前必须由底层 native owner 提供 P0 原子事务：微内核确认运动完全静止且处于安全窗口，驱动安全去使能或可原子重映射，HAL/EtherCAT/native runtime 一次性提交并可失败回滚，随后由物理总线和 native memory readback 证明一致。在该完整事务存在前，UI/Broker/设置页不得直接修改微内核运行内存来让从站或 bit live 生效。

## 禁止项

- `轴模式`、`方向`、`从站`、`回零次序`、`回零方向`、`bit` 必须渲染为实际下拉控件；其中 `从站` 下拉候选只能是固定 `NAT` 加 `扫描从站` 写入硬盘自建参数表 `SETTINGS/slave_options` 的未占用真实候选。真实候选显示为 `轴号 驱动名型号`，提交落盘值仍为纯轴号；`NAT` 提交落盘值为字面量 `NAT`，只表示该轴显式不绑定物理从站，不得作为缺失候选或旧值 fallback。
- 除下拉列以及自动计算/只读的 `分子`、`分母`、`写入状态` 外，轴参数表其他参数不得做成静态文本；点击必须弹出 v3 同款 LVGL 自绘数字软件盘，并按硬盘参数表写入/回读链路保存。
- 不得把“读一个地方、写两个地方”误写成双真值；`从站` 改变后写硬盘自建参数表并由 resident self owner 回读，`bit` 改变后写硬盘驱动参数表并由 resident drive owner/drive provider 回读。二者都不得恢复 `microkernel_parameter_table.tsv`，最终运行态生效以 clean restart 后的真实 owner readback 与硬盘持久 owner 一致为准。
- 比例链字段、电子齿轮、runtime/native `SCALE` 和 drive provider readback 的一致性审计只能由 settings_apply、drive provider 或已登记 native owner 执行，容差和单位必须来自 owner；不得在 UI/Broker 中写固定阈值或自造当量真值，不得恢复上层 `motion_stillness_gate`、`velocity == 0` 或 `Interpreter == IDLE` 私有停稳判据。
- 右上角必须按本次设置会话状态切换：无有效参数变更/restart pending 时显示并执行 `返回主页` 本地导航；字段 owner 写入/readback 或动作 owner 的 restart pending 一旦成立，同一按钮必须单向切换并执行 `保存并重启`。`保存并重启` 不能做成普通返回、只切主页面、只改按钮文字或只 restart 若干 daemon；它必须对应参数落盘确认后的开发板断电重启等价 clean restart 语义。
- 不得把微内核/native 已有或应该拥有的参数放进自建参数。
- 不得为了消除 `NAT` 把 UI 默认值、旧 `settings_runtime.json`、`config_cache`、SHM、`/run` result 或驱动映射表中不存在的伪字段当真值。
- 不得把驱动参数映射表扩展成运动参数、G53/RTCP、WCS/G92、刀补、模态或安全 owner。
- 龙门和刀库是第 7、8 轴，不是占位行；有 owner 的字段必须和其他轴一样显示，不得灰色禁用。
