# 共享内存字段清单与最终边界

引用需求真源：`REQ-SHM-DISPLAY-PROJECTION`、`REQ-REMOTE-DISPLAY-RELAY-DECOUPLING`、`REQ-UI-RUNTIME-REFRESH-RATE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-NATIVE-OWNER-FIRST`、`REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-GCODE-RUN-HOT-PATH`、`REQ-RTCP-NATIVE-STATUS-SOURCE`、`REQ-NO-PERIODIC-DISK-WRITES`。

## AI 阅读入口

- 本文定位：`/dev/shm/v3_status_shm` 的字段清单、允许内容、30Hz 坐标/轨迹显示投影同步边界和禁止内容。
- 先读 `REQ-SHM-DISPLAY-PROJECTION`。需要判断字段 owner 时，再读 `功能/0-1开机参数入内存.md`、`系统代码架构硬边界守则.md`、`功能/微内核.md` 和相关功能 owner。
- SHM 只是 UI 坐标值、UI 轨迹变化、转速、进给速度和主轴/进给倍率显示投影，不是运行态真源、保存证明、动作结果、安全证明、native gate readback、参数 owner、诊断总线或兜底通道。当前模态文字必须来自微内核/native modal/tool 状态块或等价原生 API；历史 `runtime_modal_text` 和 `V5_STATUS_VALID_MODAL` 必须从 SHM ABI 删除，不得作为 padding、诊断文本、兼容字段或 fallback 保留。
- 本文消费 `功能/0-1开机参数入内存.md` 的最高启动内存架构：LinuxCNC/微内核、UI 和产品自写代码运行闭包开机全量入 RAM；SHM 发布端只能从微内核/native 常驻内存/API 或登记 native memory block 采样，不能运行期读硬盘、JSON、result、脚本快照或目录扫描来补帧。

## P0 唯一允许的 SHM 内容

/dev/shm/v3_status_shm 只允许承载 UI 显示投影。微内核/native 到 SHM 的同步目标为 30Hz（约 33ms）坐标/轨迹动态显示；实现可用不低于 30Hz 的 30ms tick，但不得把全 UI 都提升到 30Hz，也不得用周期性整屏刷新或整页重绘消费 SHM。普通按钮轮询、急停按键显示和其它普通 UI 状态刷新按 `REQ-UI-RUNTIME-REFRESH-RATE` 分层处理；UI 必须按坐标、轨迹动态图层、按钮、急停、诊断等 dirty 对象/dirty 区域分块按需刷新。在保障状态与数据保真度（数据不说谎）的前提下，UI 允许 100ms~300ms 之间的短暂视觉延迟（Stale 过渡），这属于合理的人眼感知“视觉大概”范围，无需为此过度消耗 CPU；同步进程必须属于开机已加载的产品自写运行闭包，从微内核/native 常驻内存/API 或登记 native memory block 采样后单向发布，不得在 steady-state 读取硬盘文件、扫描脚本目录、动态导入模块、执行一次性 helper 或读取 `/run` result 补帧。

SHM 读端必须采用有限重试的 SeqLock/CRC 校验。UI reader 在检测到 seq 分裂、CRC 不一致、size/version 不匹配或帧复制失败时，只允许在有限次数内重读；达到上限仍失败时必须返回 stale/degraded/unavailable 显示状态并退出读取循环，不能无限自旋卡住 UI 主线程。短暂使用上一帧只允许作为明确标记的视觉 stale 过渡，最多用于坐标、轨迹、转速、进给速度和倍率显示；不得作为动作证明、保存证明、控制准入、参数真源、安全证明、Work Zero/WCS/RTCP/G92 证明或 native gate readback。

UI 启动首屏不同于运行期 stale 过渡：`v5-ui-relay` 或等价 supervisor 在清屏和显示 UI 前必须等待 `/dev/shm/v3_status_shm` 已存在、SeqLock/CRC/size/version 校验通过、`status_epoch` fresh，且坐标/速度/倍率显示白名单至少有一组 dynamic valid mask。未达到该条件时不得先显示空坐标界面；只能保持 UI 未出屏并 fail-closed 记录启动阻塞。运行期短暂 stale 只用于已经有 last-good 帧后的视觉过渡。

后台 Action Dispatcher、Broker 服务、提权 gate、native gate wrapper 和参数/动作 owner 的源程序不得直接或间接读取 `/dev/shm/v3_status_shm` 或其历史 typed SHM 结构；不得 include、mmap、包装 helper、读取 UI stale frame，或通过 UI status view、缓存对象、Broker result 把 SHM 投影转成动作输入。SHM 只允许前台 UI 显示 reader 单向消费，且消费结果只进入显示模型。

凡是需要动作结果证明或安全/参数同源事实的路径，必须通过 UDS、登记 native memory block 或等价强类型 native readback 直连微内核/native owner。例如 Work Zero、WCS 写入、G92/RTCP 切换、坐标设零成功确认和参数运行态生效证明，命令下发后必须通过 `EMC_STAT_GET` 或等价强类型查询从微内核/native 内存/API 回读 offsets、modal、state、generation 和 readback token；不得用 SHM fresh/stale 帧、UI 显示值、`status_epoch`、`/run` result 或旧缓存补证明。

硬实时安全主权属于 LinuxCNC realtime、HAL、EtherCAT 主站、驱动和微内核 native owner。上层 UDS readback 与 UI 显示只作为离散请求反馈和状态展示，不作为实时安全互锁依据；使能、限位、插补、急停、servo 周期和硬件安全互锁必须在底层周期控制内独立闭合并 fail-closed。

State Publisher 和 WCS position publisher 只承担显示投影发布，不是硬实时控制 owner。它们不得默认使用 `SCHED_FIFO` 或主动绑定/抢占 CPU0；应通过常驻内存、限时采样、poll/reconnect、degraded/unavailable/stale 发布和 UI 读端有限重试来处理显示抖动。

必须写入 SHM 的 UI 显示字段只有：编码器反馈机械坐标、运动学理论坐标、转速、进给速度、主轴倍率和进给倍率。当前模态文字不属于 SHM 白名单，必须直连微内核/native modal/tool 状态块或等价原生 API。字段如下：

| 字段/字段族 | 内容 | UI 用途 | 真源 owner |
| --- | --- | --- | --- |
| SHM envelope | `magic`、`version`、`header_size`、`total_size`、`payload_size`、`flags`、`seq`、CRC、dual-page header、page seq/CRC/generation | 只用于 seqlock、版本、长度和帧完整性校验 | SHM ABI |
| `status_epoch` | 当前发布帧 epoch | 只表示这一帧显示投影的新鲜度 | State Publisher 发布帧 |
| `typed_valid_mask` / `typed_flags` | 只允许表达下表显示字段的有效性和帧退化状态 | UI 判断显示项是否可画；不得作为动作准入 | SHM ABI |
| `mcs[5]` | 编码器反馈/关节 actual 收敛后的机械坐标 | UI 坐标值、UI 轨迹当前机械点 | LinuxCNC/HAL/EtherCAT/native 反馈内存 |
| `cmd_mcs[5]` | 运动学/插补器 commanded/theory 收敛后的理论机械坐标；开机初始命令位置和下使能期间的命令起点必须由编码器/驱动 actual 位置经 LinuxCNC/HAL/native 同步计算得到，不能停留在旧命令值或默认 0 | UI 轨迹理论点、跟随误差显示 | LinuxCNC/HAL/native commanded/theory 内存，BUS/Pulse 下使能和开机重基准必须使用同轴 actual feedback 形成的 joint commanded/output |
| `spindle_speed_rpm` | 主轴转速显示值 | UI 转速显示，必须进 SHM | LinuxCNC/HAL runtime actual |
| `linear_velocity_mm_per_min` | 线速度显示值 | UI 进给速度显示，必须进 SHM | LinuxCNC/HAL runtime actual |
| `feedrate_override` | 进给倍率显示值 | UI 进给倍率显示，必须进 SHM | LinuxCNC/HAL runtime actual |
| `spindle_override` | 主轴倍率显示值 | UI 主轴倍率显示，必须进 SHM | LinuxCNC/HAL runtime actual |

这些字段只能从微内核/native 常驻内存/API 或登记 native memory block 采样后发布。缺 fresh 投影时 UI 只能显示 degraded/unavailable；不得用硬盘 JSON、`/run` result、旧 SHM 帧、默认值、Broker 结果或 UI 缓存补成有效。`cmd_mcs` 不得从未重基准的 `stat.position`、默认 0、旧命令缓存或 UI 本地种子值发布；当 LinuxCNC/HAL/native 已提供 joint commanded/output 且该值已在开机或下使能状态跟随 actual feedback 时，State Publisher 必须优先发布该值。
State Publisher/WCS position publisher 必须以常驻进程进入 poll/reconnect 循环；不得在启动后用 `linuxcnc.stat()`、坐标 ready、WCS ready、CMD_MCS ready 或其它 preflight 稳定探测做无限等待而不发布。LinuxCNC/HAL/native 暂不可用时只允许发布 invalid/degraded 帧并继续重连；一旦 native actual 可读，必须立即写入 fresh position block 和 SHM 坐标投影。

## P0 远程投屏解耦边界

远程投屏属于 UI 显示投影的网络出口，不是 UI 主循环的职责。UI 进程不得监听 `18080` 或任何 TCP 远程投屏端口，不得解析 HTTP/WebSocket，不得在 UI 主线程里执行 `accept`、`recv`、网络发送、全屏网络帧拷贝或 peer allow-list 判断。远程连接数量、网络缓冲区就绪状态、上位机重连频率和 WebSocket 协议开销不得影响 UI 主循环调度；UI 主循环底部必须保留 10ms sleep/yield 保底，防止远程投屏或其它非运动辅助路径把 CPU1 拖入 busy loop。

UI 进程只允许在 `/run/8ax_v5_product_ui/` 创建远程显示本地 IPC：`remote_framebuffer.bgra` 是 BGRA32 mmap 帧缓冲，大小为当前 UI 宽度乘高度乘 4；`remote_dirty` 是 FIFO dirty rect 通知。LVGL `flush` 和页面 cache blit 只把 dirty 区域像素写入 mmap，并向 FIFO 写入一行 `frame_id base_frame_id x y w h` 文本通知；该通知只表达显示投影帧序和 dirty 区域，不承载控制、动作证明、状态真源、诊断 JSON 或 fallback 数据。没有 relay reader 时，UI 只能非阻塞丢弃通知或等待下一次 dirty，不得阻塞 UI 主循环。

独立 Python relay 是唯一允许监听 `18080` 的远程显示/输入网络进程。它读取 `remote_dirty`，从 `remote_framebuffer.bgra` 切出 dirty rect，对外提供 `/remote/info`、`/remote/frame/full`、`WS /remote/stream` 和 `WS /remote/input`；bind 地址和 peer CIDR allow-list 仍按 `REQ-SERVICE-RUNTIME-SANDBOX` 执行。为降低 CPU1 和网络占用，relay 必须把不超过 33ms 窗口内连续的 dirty 通知合并为一个 coalesced stream frame 发送；该 frame 优先保留多个原始 dirty rect，避免远隔小区域被单个外接矩形放大，只有 rect 数异常多或超出协议/CPU 边界时才允许退化为 bounded union rect。合并后的 stream frame 以客户端已持有帧为 `base_frame_id`，以窗口内最新 UI 投影帧为 `frame_id`，不得因为合并导致 full-frame repair。远程输入经 relay 解析 WebSocket 后只能写入本地输入 FIFO，由 UI 在自己的 LVGL 输入驱动里消费；不得把 TCP fd、HTTP request 或 WebSocket parser 传回 UI 进程。

Windows 端远程画面是产品常驻观察/操作链路，默认必须保持可用并允许长连接一直存在。CPU 占用整改不得把断开 Windows 远程端、停止 `v5-ui-relay`、关闭 `/remote/stream`、降级为手动刷新或删除远程输入作为解决方案；只能通过 UI dirty 区域收敛、relay mmap/零拷贝或低拷贝、33ms 合并窗口、大 dirty rect 限频、异常客户端 fail-closed 清理、publisher 降频/事件化和 CPU affinity 重新分配来降低负载。验收必须在 Windows 远程画面保持连接、远程输入可用、`/remote/info` 和 `WS /remote/stream` 正常的状态下完成；full-frame 只允许用于 initial/repair，稳态不得靠高频 full-frame polling 维持画面。

CPU 归因必须区分“远程长连接存在”和“远程显示有实际负载”：`stream_sessions > 0` 只证明远程端打开，不是高 CPU 根因；只有在同一采样窗口内 `dirty_events`、`dirty_rect_frames`、`dirty_payload_bytes`、`full_frame_requests`、`stream_repair_full_frames` 或异常 polling 计数持续增长，才能把远程显示链路列为当前 CPU 负载来源。后续远程显示代码必须保留这些 diagnostics counter，并用同一时间窗口的 `/proc/stat`、线程 CPU ticks 和 relay metrics 增量一起判断；不得用单次截图、单次连接状态、`ps %CPU` 长周期均值或 SSH 诊断扰动替代因果证据。

## P0 禁止进入 SHM 的内容

下列内容必须直连微内核/native owner 或登记 native memory block，不得写入 `/dev/shm/v3_status_shm`：

| 禁止字段/语义 | 正确 owner | 处理口径 |
| --- | --- | --- |
| WCS/G54-G59.3、`wcs_pos`、`wcs_offset`、`wcs_offsets`、`active_wcs_index` | LinuxCNC `PARAMETER_FILE`、interpreter/native readback | UI/刀路直连读取；不得用 SHM 证明 Work Zero、WCS 切换或坐标基 |
| G92 offset/actual | LinuxCNC interpreter/native G92 actual | 只可按登记 native policy 显示/写入；不得同步到 SHM |
| RTCP/switchkins actual、RTCP/G53/A-C 几何 | HAL/native component、switchkins/native owner/gate、runtime INI `[RTCP]` | `rtcp_active`、RTCP 几何和 `tool_rtcp_geometry` 不进 SHM；已删除 `re_hal_select_helper status-daemon` 和 `/dev/shm/v3_rtcp_status.json` |
| tool/TLO/刀补/当前刀号 | LinuxCNC `tool.tbl`、`stat.tool_offset`、native tool owner | UI 直连显示；不得用 SHM tool/TLO 字段作刀补真源 |
| 急停、使能、安全互锁、限位、all_homed/homed | LinuxCNC/微内核/HAL/EtherCAT realtime/native safety owner | 上层只请求/显示同源回读；不得用 SHM 判定安全、取消急停成功或运动准入 |
| active driver mode、BUS/Pulse proof | mode-switch/backend/微内核 native active-mode readback | 不得用 SHM 或 UI 下拉判断 active mode |
| motion stillness gate、速度门禁容差 | 禁止的 V3 上层门禁 | 不得把门禁参数或判定结果投影到 SHM；停稳和速度安全只属于微内核/native 内部 owner，上层不消费 SHM/JSON 证明 |
| rotary count-domain、unwrap、range guard、wcheckpoint/rebase | 旋转轴 native/window/checkpoint owner | `logical_abs_counts64`、`wcheckpoint_base_counts64`、`runtime_window_counts` 等不进 SHM |
| segment boundary / program boundary proof | 微内核/native segment boundary readback | 不得用 SHM 作为段边界 proof 或 Start 准入 |
| current line / motion line / MDI run line / MDI command | LinuxCNC interpreter/native actual | 可按需直连显示；不得作为 SHM 准入或运行证明 |
| system metrics、CPU usage、PS IP、heartbeat、ready、freshness | OS/UDS/native health 或 supervisor 启动诊断 | 不得用 SHM 表达运行心跳、ready 或性能诊断 |
| settings projection、config cache、保存成功、动作结果、Broker result、diagnostic JSON | 对应 owner/source readback 或动作响应 | 不得作为 SHM payload 或字段；保存成功必须源位置回读 |
| 通用 JSON payload | 无产品 owner | SHM frame 内不得保留通用 JSON payload；typed frame 只保留允许显示投影 |

## P0 禁止保留的 ABI 字段

ABI mask 只能表达产品允许的 UI 显示投影字段。下列字段不得作为产品白名单、兼容字段、诊断字段或 fallback 字段保留；触碰 SHM 打包、reader、layout、测试或 UI state apply 时，必须删除对应 SHM 投影和 ABI 位。

| 禁止 ABI 字段/位 | 状态 | 正确 owner |
| --- | --- | --- |
| `TYPED_VALID_CORE_STATE`、`machine_state`、`program_state`、`task_state`、`interp_state`、`exec_state` | 禁止 | 运行态/解释器状态直连 native provider |
| `TYPED_VALID_ENABLE_STATE`、`TYPED_FLAG_ESTOP`、`TYPED_FLAG_ENABLED`、`TYPED_FLAG_ALL_HOMED`、`homed_mask` | 禁止 | 安全/使能/Home 状态直连 safety/native owner |
| `TYPED_VALID_ACTIVE_WCS`、`TYPED_VALID_WCS_OFFSETS`、`wcs_*` | 禁止 | WCS 直连 `PARAMETER_FILE`/interpreter/native |
| `TYPED_VALID_TOOL_RTCP_OVERLAY`、tool/TLO/holder/tip/RTCP geometry flags | 禁止 | tool/TLO/RTCP/G53 直连 native owner；只允许坐标点本身作为 UI 轨迹显示投影时保留到允许坐标族 |
| `TYPED_VALID_PSIP`、`ps_eth0_ip` | 禁止 | 网络状态走 UI/provider 或诊断，不进 SHM |
| `TYPED_VALID_ROTARY_COUNT_DOMAIN`、`TYPED_VALID_ROTARY_RANGE_GUARD`、`TYPED_VALID_ROTARY_WCHECKPOINT` | 禁止 | 旋转轴 native/window/checkpoint 直连 readback |
| `TYPED_VALID_G92_OFFSET` | 禁止 | G92 actual 直连 interpreter/native |
| `TYPED_VALID_MOTION_DRIVER`、`active_driver_mode_code` | 禁止 | active mode 直连 mode-switch/native readback |
| `TYPED_VALID_SYSTEM_METRICS`、CPU usage flags | 禁止 | 系统指标走诊断/OS，不进 SHM |
| `TYPED_VALID_SEGMENT_BOUNDARY` | 禁止 | segment boundary 直连 native readback |
| `TYPED_VALID_MOTION_STILLNESS_GATE`、`motion_stillness_gate_packed` | 禁止 | 直接删除；不得改名保留为诊断、兼容字段或上层速度门禁 |
| `V5_STATUS_VALID_MODAL`、`runtime_modal_text` | 禁止 | 当前模态文字直连微内核/native modal/tool 状态块或等价原生 API |
| JSON payload flag/payload | 禁止 | 删除通用 JSON payload 发布和读取 |

## P1 验收证据

- 文档证据：`功能/需求真源索引.md` 中 `REQ-SHM-DISPLAY-PROJECTION` 指向本文，非 owner 文档只引用本文或对应 REQ，不再另写一份字段白名单。
- 本地源码证据：`v3_state_shm_layout.py`、`v3_state_shm_typed.py`、C header、C SHM reader、UI state apply 中只保留本文 P0 允许字段；退役字段不再设置 valid mask/flag，不再打包，不再被 UI 读取。
- 扫描证据：产品路径不存在 `v3_state_shm_writer.py --input-json`、RTCP SHM 状态块、settings projection/config cache SHM payload、status/ready/perf JSON 周期发布。
- 读端证据：C SHM reader 对 seq/CRC/size/version 错误必须有限重试并返回 stale/degraded/unavailable；测试或静态检查必须证明不会无限自旋，且 stale 帧不被控制、保存、参数、WCS/G92/RTCP、native gate 或安全路径消费。
- 板端证据：UI 坐标、轨迹动态点、转速、进给速度和主轴/进给倍率显示来自 fresh 30Hz SHM 投影；模态文字、WCS、RTCP、tool、安全、Home、active mode、stillness、rotary/wcheckpoint、segment boundary 等显示或动作证明来自微内核/native 直连 readback。
- 重启恢复证据：继电器/COM 重启后，position publisher 与 State Publisher 必须自动恢复 `MCS|CMD_MCS` fresh 投影；单个 degraded/无动态帧不得让 UI 机械坐标整列闪成空值，超过 freshness 窗口时才显示 unavailable。
