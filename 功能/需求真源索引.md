# 需求真源索引

本文解决 `功能/` 文档重复描述导致需求变化时多处修改的问题。原则是：一个需求只允许一个 owner 文档；非 owner 文档只引用 `REQ-*` 编号和描述本功能如何消费该需求，不再复制可变政策全文。

`功能/需求真源索引.md` 和本文登记的唯一 owner 文档是项目本地最高产品需求。产品功能 owner 原则上位于 `功能/`；native/服务边界等跨功能架构规则只有在本表明确登记时，才允许由项目根目录架构文档担任唯一 owner。`AGENTS.md`、未登记的架构规则、`待做工作/`、测试、代码注释、旧聊天和旧实现只能作为流程、实现或记录输入；冲突时先把最新用户反馈落到本文或对应 owner，再同步其它文本和实现。

## AI 阅读入口

AI 接手 `功能/` 文档时只按下面的最短路径读，不要把多个文档的重复规则拼成第三套口径。

1. 先用 `P0 找规则速查` 定位问题域。
2. 再用 `P0 当前需求 owner 表` 找到唯一 `REQ-*` 和唯一 owner 文档。
3. 只打开这个 owner 文档；其它 `功能/` 文档只看本地字段、按钮、页面、路径、验收细节。
4. 如果最新用户反馈改变需求，先改本文或对应 owner 文档，再改代码、测试、待办或其它引用文档。

### AI 冲突裁决

| 冲突来源 | 裁决方式 |
| --- | --- |
| 两个 `功能/` 文档写了同一易变规则 | 以本文登记的 `REQ-*` owner 为准；非 owner 重复文字视为引用或待同步债务。 |
| `功能/` owner 与 AGENTS、架构、待办、测试、旧聊天、代码注释冲突 | 以本文和对应 owner 为产品真源；流程文件只提供工作顺序和门禁。 |
| 最新用户反馈与现有 owner 冲突 | 最新反馈是需求变更输入；先落到本文或 owner，再实施。 |
| 旧路径、fallback、兼容、临时、默认值、兜底、镜像路径、AI 自造分支仍存在 | 当前 owner 没明确要求就先物理删除；若仍被真实路径依赖，先迁移到 canonical owner，再同 slice 删除旧路。 |

### 文档类型

| 类型 | 允许内容 | 不允许内容 |
| --- | --- | --- |
| 本文索引 | `REQ-*` 路由、owner、冲突裁决、目录地图、跨功能快变规则 | 具体实现日志、命令输出、板端证据、进度流水 |
| owner 文档 | settled requirement、owner 边界、字段/按钮/协议映射、验收条件；通常位于 `功能/`，仅本表明确登记的跨功能架构 owner 可位于项目根目录 | 任务进度、旧聊天摘录、重复其它 owner 的可变全文 |
| 非 owner 功能文档 | 本页面/本功能的局部实现细节、消费的 `REQ-*`、本地验收边界 | 自行改写参数/native/SHM/运动/安全等跨功能规则 |
| `待做工作/` | 活动任务输入、未闭合工作卡、人工可读问题描述 | 代替 `功能/` 成为最终产品真源 |

HMI/UI 定位为“人眼视觉辅助与状态保真（数据不欺骗）”。电机实际运动精度和安全是绝对的 P0，UI 刷新率按 `REQ-UI-RUNTIME-REFRESH-RATE` 分层：坐标/轨迹动态层 30Hz，普通按钮轮询 10Hz，急停按键显示/状态刷新 10Hz，其它普通 UI 状态和诊断显示 5Hz；运行期禁止周期性整屏刷新，必须按 dirty 对象、dirty 单元格、dirty 区域或 LVGL invalidated rect 分块按需刷新，避免过度消耗 CPU 影响实时性。文档中 `5px` 投影误差或 `1.0mm` 空间误差仅用于开发阶段的公式正确性校验，不作为运行时重绘开销约束。

## 功能目录地图

| 文档 | 主要用途 | 先看 REQ/owner |
| --- | --- | --- |
| `功能/需求真源索引.md` | 全目录入口、REQ owner 表、重复规则仲裁、全页面首帧缓存/恢复规则、UI 运行期刷新频率分层 | `REQ-DOC-SINGLE-SOURCE`、`REQ-UI-FIRST-FRAME-CACHE`、`REQ-UI-RUNTIME-REFRESH-RATE` |
| `功能/0-1开机参数入内存.md` | 开机全量入 RAM、LinuxCNC/微内核运行闭包、UI/自写代码运行闭包、参数 owner 映射、`settings_runtime.json`、比例链、V3 上层速度门禁退役、G-code 运行热路径 | `REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION`、`REQ-MOTION-STILLNESS-TOLERANCE`、`REQ-GCODE-RUN-HOT-PATH` |
| `功能/0-2参数归类.md` | 设置页参数三类 owner、顶部参数、G53 表、轴参数表字段归属、软件盘首帧返回 | `REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-NATIVE-OWNER-FIRST`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION`、`REQ-SHM-DISPLAY-PROJECTION`、`REQ-UI-FIRST-FRAME-CACHE` |
| `功能/0-4共享内存.md` | `/dev/shm/v3_status_shm` 字段清单、允许显示投影、30Hz 坐标/轨迹投影、远程投屏 mmap/FIFO 解耦和禁止边界 | `REQ-SHM-DISPLAY-PROJECTION`、`REQ-UI-RUNTIME-REFRESH-RATE`、`REQ-REMOTE-DISPLAY-RELAY-DECOUPLING` |
| `功能/2-1总线脉冲区别.md` | BUS/EtherCAT 与 Pulse/StepDir 差异、模式切换、BUS 主页面真实回零、Pulse 设零/回零和驱动动作能力边界 | `REQ-BUS-PULSE-MODE-DIFFERENCE` |
| `功能/微内核.md` | 微内核前瞻、小线段连续进给、RTCP native 状态源、RTCP ON/OFF 下 X/Y/Z 补偿归属、源码可重建和无周期落盘 | `REQ-MICROKERNEL-MOTION-LOOKAHEAD`、`REQ-MICROKERNEL-SOURCE-REBUILD`、`REQ-RTCP-NATIVE-STATUS-SOURCE`、`REQ-NO-PERIODIC-DISK-WRITES` |
| `功能/3-4UI刀路绘图顺序与状态稳定方案.md` | 刀路显示、同帧 actual、RTCP/G53 UI 消费、30Hz 坐标/轨迹动态层 | `REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-UI-RUNTIME-REFRESH-RATE` |
| `功能/3-1主页面按钮功能.md` | 主页面按钮、Start 分支、急停/取消急停、按钮 10Hz 轮询和主页面状态显示 | `REQ-START-RUN-HOT-PATH`、`REQ-MAIN-ESTOP-LATENCY`、`REQ-UI-RUNTIME-REFRESH-RATE` |
| `功能/3-3旋转轴等效角目标与解决方案.md` | A/C 等效角、count-domain、unwrap、rebase、连续旋转窗口 | `REQ-ROTARY-UNWRAP-ABSOLUTE-PROOF`、`REQ-ROTARY-REBASE-NATIVE-GATE` |
| `功能/1-1加工坐标系偏移值保存与读取实现.md` | WCS/G54-G59.3、Work Zero、WCS 切换 | `REQ-WCS-NATIVE-OWNER` |
| `功能/2-5轴设0软件偏置方案.md` | 设置页单轴设零/零位请求契约，消费 BUS/Pulse、比例链、SHM 显示隔离和 clean restart owner | `REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-SHM-DISPLAY-PROJECTION`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION`、`REQ-G92-NATIVE-RUNTIME`、`REQ-WCS-NATIVE-OWNER`、`REQ-ROTARY-REBASE-NATIVE-GATE` |
| `功能/3-2回零按钮开机强制回零与双模式真实回零方案.md` | 主页面回零/Home、开机回零前置门禁与归零/Work Zero 分界 | `REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-WCS-NATIVE-OWNER`、`REQ-POWER-ON-HOME-PRECONDITION` |
| `功能/2-2设置页总线模式设置项作用说明.md` | BUS 设置页字段、字段 owner、保存/重启边界 | `REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE` |
| `功能/2-3设置页每个按钮作用和目的.md` | 设置页按钮现状和按钮到后台动作映射 | 只作现状入口，需求变更回到对应 `REQ-*` owner |
| `功能/2-4设置页设置驱动按钮作用和目的.md` | BUS 设置驱动动作、驱动读写和三点校准 | `REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION` |
| `功能/1驱动命令映射表命令需求.md` | 驱动 profile schema、标准 `drive.*` 命令和适配边界 | `REQ-DRIVE-PROFILE-AUTH-CHAIN` 本身归 VPS 文档；本文件管映射表 schema 和命令接口 |
| `功能/跳窗提示方案.md` | 后台命令结果弹窗、成功提示和确认继续 | `REQ-UI-POPUP-RESULT-POLICY` |
| `功能/微内核错误提示完整映射表.md` | 微内核原生错误完整清单、中文映射、UI 显示和重复自建错误清理 | `REQ-NATIVE-OPERATOR-ERROR-MAP` |
| `功能/VPS登录与项目对接说明.md` | VPS 登录、授权、private/public 下载、驱动 profile、OTA package 身份链 | `REQ-DRIVE-PROFILE-AUTH-CHAIN` |
| `功能/自动闭环测试方式.md` | 自动闭环、operator path、按 active model 选择 `cc-ac.ngc`/`cc-bc.ngc` golden motion、默认跑 `待做工作/跑cc.md` 执行卡、板端程序修改构建部署上板闭环、证据状态口径 | `REQ-AUTO-STATE-DRIVEN`、`REQ-BOARD-PROGRAM-FULL-BUILD-CLOSURE` |
| `功能/0-3native_helper白名单与验收说明.md` | native helper 白名单、允许操作和验收 | 架构 owner 与对应 `REQ-*` |
| `功能/-触摸校准与输入链路说明.md` | 触摸校准、输入链路、现场校准验收 | `REQ-TOUCH-CALIBRATION-INPUT` |

## P0 找规则速查

先用本表找到 owner，再读 owner 正文；不要把多个非 owner 文档里的重复规则拼成新口径。

| 要判断的事 | 先看 | 再看 | 不要做 |
| --- | --- | --- | --- |
| 文档、规则或需求是否重复 | `REQ-DOC-SINGLE-SOURCE` | 本文 P0 owner 表 | 不要新建平行规则文档。 |
| 项目文件、临时输出、证据和工具应放哪里 | `REQ-FILE-CLASSIFIED-PLACEMENT` | 本文 P0 owner 表 | 不要把 JSON、截图、日志、scratch、证据、工具或源码散落到无 owner 的目录；临时/运行输出必须进入 `repo_ignored/temp/` 或更具体的 ignored 分类目录。 |
| `功能/` owner 与 AGENTS/架构/待办/测试冲突 | `REQ-FEATURE-DOCS-SUPREMACY` | 本文 P0 owner 表和对应功能 owner | 不要让流程文件、旧记录、旧测试或旧聊天覆盖当前功能 owner。 |
| 弃用代码、旧入口、fallback、AI 自造分支是否保留 | `REQ-DELETE-FIRST-RETIREMENT` | 本文 P0 owner 表和对应功能 owner | 不要先包兼容、改名保留、环境变量藏后门或用报错提示替代删除。 |
| Windows 一键先推送 git 再备份 NAS 的项目工具 | `REQ-V5-GIT-NAS-BACKUP-TOOL` | 本文 P0 owner 表 | 工具唯一职责是先把当前 Windows v5 源真相提交并推送到 `origin/main`，git 成功后再镜像备份到 `\\sjnas\备份\v5`；不得再使用“VM 到 Windows 同步”命名或行为，不得内嵌 NAS 明文密码，不得在 NAS 阶段做无边界递归预清理导致窗口长时间无输出。 |
| 页面切换、全屏弹窗/软件盘返回是否可以等待整页重绘 | `REQ-UI-FIRST-FRAME-CACHE` | 本文 P0 owner 表 | 不要让页面切换、弹窗关闭或软件盘返回以 LVGL 整页重绘作为首个可见帧；必须先显示缓存/保留帧，再只刷新变更区域。 |
| UI 运行期刷新频率、按钮轮询频率和非运动状态刷新频率 | `REQ-UI-RUNTIME-REFRESH-RATE` | 本文 P0 owner 表 | 不要把全 UI 都拉到 30Hz，也不要周期性整屏刷新；坐标/轨迹动态层 30Hz，普通按钮轮询 10Hz，急停按键显示/状态刷新 10Hz，其它普通 UI 状态和诊断显示 5Hz，刷新必须按 dirty 对象/区域分块按需执行。 |
| 远程投屏是否允许 UI 进程监听 18080 或解析 HTTP/WebSocket | `REQ-REMOTE-DISPLAY-RELAY-DECOUPLING` | `功能/0-4共享内存.md` | UI 进程不得拥有 TCP 监听、HTTP/WebSocket 解析、网络发送或整屏网络拷贝；UI 只写 `/run/8ax_v5_product_ui/remote_framebuffer.bgra` mmap 并通过 `/run/8ax_v5_product_ui/remote_dirty` FIFO 发 dirty rect 通知，18080 网络 relay 必须由独立进程承担。 |
| 参数表保存、status_epoch、运行态真值 | `REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE` | `功能/0-1开机参数入内存.md` | 不要用 UI dirty、JSON、默认值、旧快照、SHM 投影或硬盘热路径读取冒充真值；`status_epoch` 只服务 SHM 显示投影 freshness 和诊断，不参与保存准入、控制动作或运动 gate；LinuxCNC/微内核运行闭包、UI 运行闭包、产品自写运行代码闭包和参数 owner 映射必须开机全量读入 RAM，运行期 UI/Broker/Actions/provider 不得临时读盘、懒导入、扫描目录或用 wrapper 兜底；`保存并重启` 必须执行开发板断电重启等价 clean restart，不得降级为只返回主页面、只刷新 provider 或只 restart 若干 daemon；微内核/native 已有的参数和数据必须优先直接使用微内核/native 常驻内存/API，驱动参数映射表已有的驱动私域参数必须使用驱动 owner；只有微内核和驱动参数映射表都没有承载的设置页轴参数才进入自建参数表，并由 UI 自动读取；`设置驱动`/驱动参数表是 drive-only 例外，允许项目自有 schema，但不能反向成为运动、坐标、WCS/G92、RTCP、模态或安全真源。 |
| SHM 字段清单和禁止边界 | `REQ-SHM-DISPLAY-PROJECTION` | `功能/0-4共享内存.md` | 不要把 ABI known mask 当产品白名单；新增/保留字段必须命中共享内存文档的允许显示投影表，否则直连微内核/native 并删除 SHM 投影。 |
| native/LinuxCNC/HAL/EtherCAT 边界 | `REQ-NATIVE-OWNER-FIRST`、`REQ-LINUXCNC-COMMAND-GATE` | `系统代码架构硬边界守则.md` | 不要在 product Python/UI/Broker 里新增直接 native 第二路径。 |
| 板端常驻服务 root 权限、Capability 和远程 relay 暴露 | `REQ-SERVICE-RUNTIME-SANDBOX` | `系统代码架构硬边界守则.md` | 不要让 UI、State Publisher、Command Gate、settings actiond 等常驻服务无审计地长期 root 运行；不要让 18080 remote relay 裸监听全网且无 peer allow-list。 |
| `settings_runtime.json` 能保存什么 | `REQ-SETTINGS-RUNTIME-DRIVE-ONLY` | `功能/0-1开机参数入内存.md` | 不要把 WCS、G92、RTCP、tool、modal 或 status 帧塞回 drive-only 文件。 |
| BUS/Pulse 模式差异、模式切换和驱动动作能力边界 | `REQ-BUS-PULSE-MODE-DIFFERENCE` | `功能/2-1总线脉冲区别.md` | 不要按旧待办、UI 文案、`settings_runtime.json`、`motion_driver.conf` 单点读回或默认值判断 active mode；BUS/Pulse 模式 Home 差异只作用于主页面当前选择为 `机械全轴` 的正常 Home 分支，BUS 必须每次触发真实 Home 运动和到位证明，不能只把坐标变 0 或写 homed 标记；MCS/WCS 单轴选择后的 `回零` 分别是真实回机械 0 / 回当前加工 0 定位，不改变 homed、不得改显示或 offset 冒充运动；不要把静态禁用/预检拦截组合作为默认闭合方式。 |
| 微内核速度前瞻、预读和小线段连续进给 | `REQ-MICROKERNEL-MOTION-LOOKAHEAD` | `功能/微内核.md` | 不要在 UI/Broker/product action 里做第二套轨迹规划、逐段停顿或小线段节拍门禁。 |
| RTCP/switchkins native 状态源、两条直驱微内核输入、X/Y/Z 补偿归属和 State Publisher 无 fork 回读 | `REQ-RTCP-NATIVE-STATUS-SOURCE` | `功能/微内核.md` | RTCP OFF 下 active model 旋转轴运动不自动补偿 X/Y/Z，RTCP ON 下 active model 旋转轴运动由微内核/LinuxCNC/switchkins 自动补偿 X/Y/Z；UI 只显示 native actual，不计算或证明补偿。RTCP 允许两条互不代发、互不预置的直驱微内核链路：UI 按钮链路通过 `native_rtcp_control` latch，G-code 程序链路通过程序内 `M64/M65` 已登记输出；两条链路最后由 HAL/native owner 合成为同一个 switchkins actual。不要让 State Publisher steady-state fork helper/halcmd、读硬盘文件或通过 `RTCP_STATUS_HELPER_FALLBACK`/环境变量重新打开短生命周期 helper 查询 native actual；能下沉的状态必须用 HAL pin/native component、微内核/native 常驻内存/API 或登记 native memory block，RTCP/switchkins actual 不得同步到 SHM。 |
| 周期性落盘、心跳文件和临时停用开关 | `REQ-NO-PERIODIC-DISK-WRITES` | `功能/微内核.md` | 不要用 JSON 文件当心跳、ready、性能或状态；不要保留默认关闭、环境变量再开启的周期性落盘后门；ready/heartbeat 必须读微内核/native 内存事实或 UDS health，不得进入 SHM；一次性 supervisor ready 文件只能作启动诊断。 |
| 比例链、`SCALE`、电子齿轮和历史污染 | `REQ-SCALE-CHAIN-HISTORY-POLLUTION` | `功能/0-1开机参数入内存.md` | 不要让历史 JSON/cache 重新参与比例、运动准入或驱动写入。 |
| 触摸校准和输入链路 | `REQ-TOUCH-CALIBRATION-INPUT` | `功能/-触摸校准与输入链路说明.md` | 不要用模拟点击替代真实手指校准；不要把旧 UI 路径镜像当产品 owner 或 fallback。 |
| 板端程序修改能否只用本地/局部测试关闭 | `REQ-BOARD-PROGRAM-FULL-BUILD-CLOSURE` | `功能/自动闭环测试方式.md` | 不要用 touched build、局部 smoke、VM 构建、board_deployed 或 direct UDS 代替当前部署产物目标集合构建、板端部署和原始 operator/control 路径闭环；没有板端证据只能报 `source_only`、`local_verified_only` 或 `blocked`。 |
| 自动闭环、operator path、motion golden loop | `REQ-AUTO-STATE-DRIVEN` | `功能/自动闭环测试方式.md` | 不要用 direct UDS、dry-run、matrix pass、board_deployed 或旧 result.json 冒充真实 UI/operator/motion 证据。 |
| G92、WCS、RTCP/G53、旋转轴 unwrap/rebase | 对应 `REQ-*` 行 | 对应 owner 文档 | 不要按旧聊天或非 owner 摘录恢复旧政策。 |
| 大文件拆分和 owner 下沉 | `AGENTS.md` 的 `SELF_WRITTEN_CODE_SIZE` | 对应 `功能/` owner 与当前源码目录 | 当前没有 `待做工作/拆分.md`；500 行只约束 v5 自写业务/控制流代码，参数文件/参数表等单一真源属于原子级不可拆分例外，微内核/native 文件不要为行数规则机械拆改。 |
| 当前未闭合问题 | `待做工作/遗留.md` | 对应作业指导书或 owner 文档 | 不要把已关闭问题保留成完成记录。 |

## P0 文档单一真源规则

- 新增或改变跨功能需求时，先在本文新增或修改一条 `REQ-*` 记录，并指定唯一 owner 文档。
- 需求正文只在 owner 文档维护；其他文档只能写“引用需求：REQ-...”，不得复制“永远禁止/一律允许/默认兜底/保存成功”等可变硬规则。
- 如果非 owner 文档、旧注释、待办、旧聊天、旧测试或 `AGENTS.md`/架构规则与本文索引指向的 owner 文档冲突，以当前用户最新反馈落到的本文和该 REQ owner 文档为准；冲突文本视为待同步债务，不要求为每次需求变化同步修改所有引用文档。
- 改实现时仍要读对应功能文档；但判断需求是否变更、是否允许、是否 fail-closed，先看本文对应 `REQ-*` 和 owner 文档。
- 新功能文档必须在开头列出“引用需求真源”，至少包含和本功能相关的 `REQ-*`。

## P0 当前需求 owner 表

| REQ ID | 需求范围 | 唯一 owner 文档 | 需求摘要 |
| --- | --- | --- | --- |
| REQ-DOC-SINGLE-SOURCE | 功能文档去重复、需求变更入口 | `功能/需求真源索引.md` | 一个需求一个 owner；其他文档引用 ID，不复制可变硬规则。 |
| REQ-FEATURE-DOCS-SUPREMACY | `功能/` owner 文档相对规则、架构、待办、测试、代码注释和旧聊天的最高产品需求地位 | `功能/需求真源索引.md` | `功能/需求真源索引.md` 和本文登记的唯一 `功能/` owner 是项目本地产品真源；最新用户反馈是需求变更输入，先落到本文或 owner，再同步 AGENTS、架构、待办、测试和实现。 |
| REQ-FILE-CLASSIFIED-PLACEMENT | 项目文件分类放置、唯一源码位置、临时输出和证据目录边界 | `功能/需求真源索引.md` | V5 可维护源码只能存在于 Windows `D:\v5` 这一棵 Git 工作树，并按互斥 owner 放置：`linux/kernel/` 是完整 Xilinx Linux kernel owner，`linux/realtime/` 是 PREEMPT_RT/Yocto realtime metadata owner，`linuxcnc/` 是完整 LinuxCNC 与 V5 native owner，`board/petalinux/` 是完整 PetaLinux project-spec owner，`board/linuxcnc/` 只保留 HAL/INI/NC/runtime/component/Yocto 集成，`board/app/`、`board/services/`、`board/tools/` 分别承载 LVGL、常驻服务和项目工具，`vivado_hw_project/` 是硬件工程 owner。VM 不得保存、同步、解压或生成第二棵源码树；板端文件只能是构建/部署产物。当前详细位置以本文“当前 canonical 文件位置表”为准。 |
| REQ-DELETE-FIRST-RETIREMENT | 弃用代码、旧入口、fallback、兼容分支、AI 自造分支和退役路径的删除优先级 | `功能/需求真源索引.md` | 当前 `功能/` owner 没有明确要求的旧路先物理删除，用 owner 真源对应编译/检查暴露真实依赖，再补回唯一 canonical owner 路径；不得通过改名保留、环境变量、测试入口、报错提示、兼容 wrapper 或 disabled branch 留退路；退役的 VM 源码同步目录、源码 manifest、源码归档输入、板端部署目录和打包清单中的 `.bak`、`.before`、`old`、`backup` 等残留同样必须删除，不得作为构建输入、现场 rollback 或 shadow 路径保留。 |
| REQ-V5-GIT-NAS-BACKUP-TOOL | Windows 一键先推送 git 再备份 NAS 的项目工具 | `功能/需求真源索引.md` | Git 根固定为 Windows `PROJECT_ROOT`，范围必须覆盖下方“V5 Git 范围”登记的完整 canonical owner，尤其是 `linux/`、`linuxcnc/`、`board/`、`board/petalinux/` 和 `vivado_hw_project/`；不能因源码目录名为 `build`、`bin` 或被导入上游 `.gitignore` 命中而漏跟踪。Canonical 工具源码必须在 `board/tools/` 下维护，面向人工双击的桌面 exe 只能由该源码生成；运行职责是先校验范围、提交并推送到 `origin/main`，git 成功后再把同一份源真相镜像备份到 NAS `\\sjnas\备份\v5`。工具不得从 VM 反向同步源码、不得用 `sync_vm_v5_to_windows` 等误导命名、不得内嵌 NAS 明文密码；NAS 凭据使用 Windows Credential Manager 或运行环境提供的凭据。NAS 阶段必须做有超时的可达性检查，再执行 `robocopy /MIR`，且 Git 与 NAS 必须采用同一源码范围；不得在正式镜像前对整个 NAS 目标树执行无边界递归 `attrib`、预扫描或预清理导致窗口长时间无输出。 |
| REQ-UI-FIRST-FRAME-CACHE | UI 页面切换、全屏弹窗/软件盘打开和返回的首帧缓存/恢复策略 | `功能/需求真源索引.md` | 所有页面切换、新页面首帧、全屏弹窗关闭和软件盘返回都必须使用打开前保留帧或目标页 resident cache 作为第一个可见帧；当前和后续覆盖层必须统一调用公共首帧 guard/helper（当前源码为 `V5UiFirstFrameGuard` 或等价 owner 入口）完成 capture、restore、restore 后局部 dirty 刷新，不得在各弹窗/键盘里复制私有 cache/blit 分支。后续 LVGL 正常刷新只能跟随缓存帧之后，并且只刷新变更区域、变更单元格或明确 dirty 的页面。新增页面必须在启动或 canonical reload 时建立显式 cache slot 并走 `cache_blit` 首帧路径，不得让用户等待整页重绘、懒加载、运行期扫描或空白帧。 |
| REQ-UI-RUNTIME-REFRESH-RATE | UI 运行期刷新频率分层、按钮轮询、急停显示刷新和局部刷新边界 | `功能/需求真源索引.md` | UI 坐标值、UI 轨迹动态点、红/绿点、刀柄/刀尖动态线、转速、进给速度和主轴/进给倍率显示按 30Hz 目标刷新；普通按钮状态轮询按 10Hz；急停按键的显示状态和微内核/native 急停/使能回读刷新按 10Hz，但急停按下事件必须 edge 直通 native safety owner，不等待下一次轮询；其它普通 UI 状态、诊断、非运动文字和后台信息显示按 5Hz 即可。运行期 UI 刷新禁止周期性整屏刷新、全页面重建或用 full-frame 刷新掩盖 dirty 管理缺失；必须由对象变化、单元格变化、图层变化、触摸/视角变化或明确 dirty 区域驱动 LVGL invalidated rect/局部 flush。UI 主循环可以保留 10ms sleep/yield 保底，但 LVGL render/handler 不得固定 100Hz 空跑；只有分层刷新到期、输入事件或明确 dirty 操作才进入 LVGL handler。运行期状态标签、坐标文本和诊断文本必须先比较再写，文本不变不得触发 dirty。WinRemote 正式 relay 显示路径必须以 WebSocket dirty-rect stream 承载 30Hz 目标展示节奏，HTTP full-frame 只允许用于初始化和明确 repair，不得用 HTTP 全屏轮询、直接 fb0、SSH/FIFO/文件旁路或周期性整屏刷新替代 dirty-rect 流；客户端可按 30Hz 节流状态/指标记录，但不得丢弃或重排 dirty-rect 帧。为降低 CPU1 占用，relay 必须在不超过 33ms 的窗口内把连续 LVGL dirty 通知合并成一个 coalesced dirty-rect frame；该 frame 的 `base_frame_id` 指向客户端已持有帧，`frame_id` 可跳到合并窗口内最新 UI 投影帧，客户端必须把这种 coalesced frame 当作一次原子局部更新接受，不得因此触发 full-frame repair。WinRemote 右上角连接徽标只显示稳定会话状态：健康 dirty-rect stream 保持 live，dirty repair/full-frame repair 不得切 recovering，断流或启动失败后保持稳定 error，重连尝试次数只写底部状态/evidence，不得在 error/connecting/reconnecting 之间反复跳动。WinRemote 底部状态条必须用固定槽位显示 frame、fps、relay、dirty、full/dirty/repair/reject、source、remote pointer 和 pointer 细节，数值变化、计数位数变化、source 截断或 pointer 文案变化不得重新分配整行宽度、挤动相邻字段或让红框状态区域左右跳动。全屏缓存/首帧恢复只服务页面切换、弹窗/键盘返回和启动首帧，不得成为稳定运行循环的刷新方式。刷新节流只影响显示重绘和普通轮询，不得降低 State Publisher/native 采样、SHM freshness、命令下发 freshness、动作证明、安全/运动门禁或急停直通链路。 |

`REQ-UI-RUNTIME-REFRESH-RATE` 的主页面坐标点选硬规则：点选 MCS/WCS 单轴、改选另一单轴、点击 `机械全轴` 取消单轴选择，或 3 秒空闲计时自动取消选择时，必须在同一个 UI 事件事务内重绘受影响坐标数码字并立即把对应数值单元格加入 LVGL invalidated rect/remote dirty rect；不得先清空该数值区域、不得等待下一次 30Hz 状态帧、坐标值实际变化、页面重建或取消选择后才恢复显示，也不得用整屏刷新兜底。仅选中色变化时必须保留同一坐标文本和同一 owner 真值，局部重绘不得改写坐标数据或制造第二套显示缓存。
| REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE | 开机全量入 RAM、参数表内存闭环、轻量保存、dirty 清除边界 | `功能/0-1开机参数入内存.md` | LinuxCNC/微内核运行闭包、UI 运行闭包、产品自写运行代码闭包和参数 owner 映射必须开机全量读入 RAM；运行期只允许设置保存、程序文件选择/打开并生成运行内存副本、事件触发且限频的日志/诊断、受控重启或重载触发硬盘/闪存，且日志/诊断不得进入 Start/Resume、State Publisher steady-state、UI 稳定显示或正常空闲循环的周期落盘路径。SHM `status_epoch` 只用于显示投影 freshness 和诊断，不参与保存准入、控制动作或运动 gate。使用 `mlockall` 的常驻进程必须有资源预算和可回收资源域，非实时受控边界可执行 `malloc_trim(0)` 或等价 trim，但验收以板端 `VmRSS`/`VmLck`/`Locked` 水位稳定回落为准。字段保存可在唯一 owner 写入并源位置回读后显示已保存/待重启；设置页右上角固定为 `保存并重启`，不得保留无变更时的普通 `返回主页面` 两态分支；点击后必须执行开发板断电重启等价 clean restart，停止并重建 UI/Broker/Action Dispatcher/State Publisher/LinuxCNC/微内核/HAL/EtherCAT/drive provider 运行闭包，清理旧 `/run`、socket、SHM/native 状态块、resident snapshot、cache 和命令队列后按开机链路重新入 RAM，不能只 restart 若干服务。微内核/native 已有的参数、状态和运行数据必须优先用微内核/native 常驻内存/API 或登记 native memory block，驱动参数映射表已有的驱动私域参数必须使用驱动 owner；只有微内核和驱动参数映射表都没有承载的设置页轴参数才进入自建参数表，并由 UI 自动读取。`设置驱动`/驱动参数表是明确 drive-only 例外，允许项目自有 schema 保存驱动 profile、SDO/PDO、encoder raw/multiturn、电子齿轮、count-domain 校准和驱动读回证据，但不得反向成为 LinuxCNC/微内核运动参数、坐标基、WCS/G92、RTCP actual、模态或安全真源。 |
| REQ-SHM-DISPLAY-PROJECTION | `/dev/shm/v3_status_shm` 字段清单、允许显示投影、UI 启动门槛和禁止边界 | `功能/0-4共享内存.md` | SHM 只允许 UI 显示投影：编码器反馈机械坐标、运动学理论坐标、`spindle_speed_rpm`、`linear_velocity_mm_per_min`、`feedrate_override`、`spindle_override`，以及必要 frame envelope/epoch/CRC/valid bits；同步目标为稳定 30Hz 坐标/轨迹动态显示投影，100-300ms 只能是已经取得 last-good 帧后的显式 stale/degraded 过渡，不能作为正常刷新节奏。发布进程必须属于开机已加载的产品自写运行闭包，并从微内核/native 常驻内存采样维护，不得运行期读硬盘、JSON/result、脚本快照或目录扫描补帧。`v5-ui-relay` 或等价 supervisor 必须在首个可见 UI 帧前确认 SHM 存在且 SeqLock/CRC/size/version、fresh epoch 和 dynamic valid mask 均有效；未满足时 UI 不得接管/清空 framebuffer，也不得先显示空骨架、默认坐标或 degraded 页面。当前模态、当前刀号和对应刀长必须走微内核/native modal/tool 状态块或等价原生 API；SHM ABI 中历史 `runtime_modal_text` 不得作为主 UI 真源。后台 Action Dispatcher、Broker、提权 gate、native gate wrapper 和 owner 响应路径不得读取或包装消费 SHM；动作证明、安全证明、保存证明、Work Zero/WCS/G92/RTCP readback 必须通过 UDS 或登记 native readback 直连微内核/native owner。WCS、G92、RTCP、tool/TLO、安全、active mode、stillness、rotary/wcheckpoint、segment boundary、system metrics、settings projection、动作结果、诊断和通用 JSON payload 均为禁止项。 |
| REQ-REMOTE-DISPLAY-RELAY-DECOUPLING | 远程投屏进程边界、UI framebuffer mmap/FIFO dirty rect 通知和 18080 relay 解耦 | `功能/0-4共享内存.md` | UI 进程不得监听 TCP 端口、解析 HTTP/WebSocket、执行网络发送或因远程连接承担全屏帧网络拷贝；UI 只在启动时创建并 mmap `/run/8ax_v5_product_ui/remote_framebuffer.bgra`，在 LVGL flush/cache blit 时按 dirty rect 写 BGRA 像素，并向 `/run/8ax_v5_product_ui/remote_dirty` FIFO 写入 `frame_id base_frame_id x y w h` 通知。独立 Python relay 才能监听 18080，按 `REQ-SERVICE-RUNTIME-SANDBOX` 做 bind/peer CIDR allow-list，提供 `/remote/info`、`/remote/frame/full`、`WS /remote/stream` 和 `WS /remote/input`；relay 必须把 33ms 窗口内的连续 dirty 通知合并后发送，避免正常多 rect 刷新被误判为丢帧并反复触发 full-frame repair；远程输入经本地 FIFO 注入 UI，UI 主循环底部必须有 10ms sleep/yield 保底，不能被远程网络就绪状态拖入 busy loop。 |
| REQ-NATIVE-OWNER-FIRST | LinuxCNC/微内核/HAL/EtherCAT/native owner 优先 | `系统代码架构硬边界守则.md` | native 可拥有的语义必须以 native owner 为主真源；除微内核/native 硬安全和 VPS/授权身份链外，V3 上层不再自写准入门禁，只做请求、路由、投影、诊断和最小 adapter。 |
| REQ-SERVICE-RUNTIME-SANDBOX | 板端常驻服务权限边界、Capability allow-list 和 remote relay 端口暴露 | `系统代码架构硬边界守则.md` | UI、State Publisher、Command Gate、settings actiond、touch diagnostics 等常驻服务必须有可审计的设备/路径/端口/Capability allow-list；默认不得把 18080 remote relay 裸露给全网，必须至少有绑定地址和 peer CIDR allow-list。root 运行在未完成设备权限切换前只能作为审计告警和分阶段整改状态，不得视为最终安全闭环；切换非 root 必须逐服务验证 framebuffer、input、SHM、UDS、LinuxCNC/HAL/EtherCAT 访问和 clean restart。 |
| REQ-SETTINGS-RUNTIME-DRIVE-ONLY | `settings_runtime.json` 收敛边界 | `功能/0-1开机参数入内存.md` | 只允许 Settings Drive 私域和深度清洗后的驱动/count-domain 证据；不得夹带 WCS/G92/TLO/tool/RTCP/modal/status/runtime INI 真源。 |
| REQ-BUS-PULSE-MODE-DIFFERENCE | BUS/Pulse 模式差异、模式切换闭环、BUS 主页面真实回零、Pulse 轴参数设零/回零、驱动动作能力边界 | `功能/2-1总线脉冲区别.md` | `motion_driver.conf` 只作为 requested/持久 owner；active mode 必须由 mode-switch/backend/微内核/native 常驻内存链路在 requested mode 等于 SelectLink 选中 INI、LinuxCNC fresh/running、期望侧 RT/HAL 已加载、非期望侧 inert/disabled 四项同时成立后直接回读；不得写 SHM。主页面选择 `机械全轴` 后按 `回零` 才进入 BUS/Pulse 正常 Home：BUS 每次必须触发 native/EtherCAT Home owner 的真实回零运动循环和本次到位/readback token，不得以坐标清零、MCS/SHM 显示归零、`zero_model` 写入、单独 `Set Home` 标记、Work Zero、G92 或 drive preset 冒充完成；MCS 单轴和 WCS 单轴选择后的 `回零` 分别由 native positioning owner 真实移动到机械坐标 0 和当前 active 加工坐标 0，不改变 homed、不得回退全轴 Home。Pulse 轴参数表 `设零` 键盘只写当前轴 0 位 `HOME_OFFSET`，`回零方向` 列独立下拉；设置页驱动动作的修复方向是打通真实 action/profile/readback 路径，不再以静态禁用/预检拦截组合作为默认闭合方式；只有硬安全、身份授权、资源确实不存在或目标模式真实不适用时才结构化失败。 |
| REQ-SCALE-CHAIN-HISTORY-POLLUTION | 比例链、SCALE、电子齿轮和历史旁路污染修复 | `功能/0-1开机参数入内存.md` | `pitch/precision/ratio` 等影响比例链的字段必须同事务写 runtime INI `SCALE` 并源位置回读；旧 drive-only/旧 JSON 不能参与比例、电子齿轮或运动准入；旧旁路不得被编译、打包、部署、import、exec、Broker 调用、UI 读取或参数计算引用。 |
| REQ-DRIVE-PROFILE-AUTH-CHAIN | 统一板端软件、授权功能包、VPS gated download、驱动 Profile 和 OTA package 身份链 | `功能/VPS登录与项目对接说明.md` | 板端保持一套主线；功能开放、private profile、OTA package 和服务器下载走授权/DNA/challenge/签名/hash/运行时身份链。OTA 包选择必须先解析当前 DNA private 区；有当前 DNA private OTA 包记录时不得下载 public OTA 包，private 包不存在并记录 reason 后才可选择 public，private 包存在但验签/hash/版本/授权失败必须 fail-closed。驱动映射表 schema、标准命令和 profile 适配边界仍引用 `功能/1驱动命令映射表命令需求.md`。 |
| REQ-TOUCH-CALIBRATION-INPUT | 触摸校准、真实手指输入链路、校准文件 owner 和旧路径退役边界 | `功能/-触摸校准与输入链路说明.md` | 产品 UI 只以 `safe_ui` 校准文件为主 owner；旧 `/opt/8ax/ui/re_touch_calibration.json` 不得作为读取 fallback、写入镜像或验收对象。 |
| REQ-LINUXCNC-COMMAND-GATE | LinuxCNC command API 边界 | `系统代码架构硬边界守则.md` | 只有 State Publisher 可 import linuxcnc 采样状态；控制动作必须走已登记 native/linuxcncrsh gate，product Python 不得直接拥有 `linuxcnc.command()`。linuxcncrsh 客户端是 TCP 文本命令 + 串行 request/response 模型，适合运行、暂停、回零、MDI、Work Zero、设零、模式切换、低频控制和诊断读取；native gate 初始化时必须建立单条常驻 TCP 连接并复用，单 writer/FIFO 串行确认，不得按命令短连接或多 socket 连接池并发抢发。进给/主轴倍率等操作员非运动连续 UI 输入可按 100ms 合流只发最新值，但这不是伺服周期实时 override 流；急停、Start/Pause/Abort/Home/Jog 边沿、MDI、Work Zero、设零、驱动使能、模式切换、WCS/G92 等一次性/安全/运动命令不得 debounce。RTCP 按 `REQ-RTCP-NATIVE-STATUS-SOURCE` 使用 UI `native_rtcp_control` latch 和 G-code 程序 `M64/M65` 已登记输出两条直驱微内核链路，不归并为 linuxcncrsh 普通文本命令，也不得 debounce。高频轨迹流、实时插补、脉冲控制、伺服闭环、硬实时安全互锁、实时 override 流和低抖动控制必须下沉到 LinuxCNC realtime/HAL/EtherCAT/驱动/FPGA/微内核 native owner。 |
| REQ-G92-NATIVE-RUNTIME | G92 使用政策 | `功能/需求真源索引.md` | G92 不再一刀切禁止；若采用，必须登记为 LinuxCNC 原生运行态/显式坐标策略，经登记 native gate 写入并由微内核/native 内存或 LinuxCNC 原生 API 直接回读证明；不得同步到 SHM。V3 私有坐标补丁、未登记 G92 或绕过 Rebase 闭环仍禁止。 |
| REQ-WCS-NATIVE-OWNER | WCS/G54-G59.3/Work Zero | `功能/1-1加工坐标系偏移值保存与读取实现.md` | WCS 持久化真源是 LinuxCNC `PARAMETER_FILE`，运行真源是解释器/`stat.g5x_*` 或微内核/native 内存事实；Work Zero 走登记 gate 写入并由原生内存/API 直接回读证明；不得同步到 SHM。 |
| REQ-POWER-ON-HOME-PRECONDITION | 开机全轴回零前置条件、动作分类、未回零提示窗和 native 二次门禁 | `功能/3-2回零按钮开机强制回零与双模式真实回零方案.md` | 开机或 clean restart 后，只有 fresh native `all_homed=true` 才允许启动 AUTO/MDI、继续/恢复、首点、增量/连续点动、MCS/WCS 单轴回零和归零/Work Zero；`机械全轴` Home、暂停、Jog Stop、急停/取消急停、程序打开/编辑、选择/视图/步距/倍率、RTCP、G92 清除、页面导航和设置页非运动动作不受该前置条件阻断。UI 必须在下发前直读 fresh native `all_homed`，false 或 unknown 时不发送动作并显示三段式提示窗；command gate 必须以 LinuxCNC/native actual 再校验，禁止绕过 UI。坐标为 0、旧 homed、旧结果、SHM、UI 缓存或设置文件都不能替代本次开机回零。 |
| REQ-ROTARY-UNWRAP-ABSOLUTE-PROOF | A/C unwrap checkpoint、掉电恢复和 fail-closed | `功能/3-3旋转轴等效角目标与解决方案.md` | 没有 direct64 或可信 split absolute basis 时，checkpoint-only 不能无条件恢复 unwrap；bounded-window proof 完整成立时，checkpoint candidate 可作为受控恢复来源，否则必须发布 invalid/fail-closed。`logical_abs_counts64`、`wcheckpoint_base_counts64`、`runtime_window_counts`、`generation`、`invalid_reason` 必须是微内核/native 常驻内存字段并由直接内存/API 回读证明，不得同步到 SHM，也不得从硬盘 checkpoint、`/run` result、旧快照或默认值兜底。 |
| REQ-ROTARY-REBASE-NATIVE-GATE | 旋转轴 Rebase、position preset、G92 边界 | `功能/3-3旋转轴等效角目标与解决方案.md` | drive_rebase 与 position_preset 分离；Rebase 必须走受控 native gate/同周期证明，G92 只能按 REQ-G92-NATIVE-RUNTIME 采用。wcheckpoint/rebase 运行窗口切换必须由微内核/native gate 更新常驻 `logical_abs_counts64`、`wcheckpoint_base_counts64`、`runtime_window_counts`、`generation` 和 `invalid_reason`，再由微内核/native 内存直接回读证明一致；不得同步到 SHM。 |
| REQ-RTCP-G53-NATIVE-ACTUAL | RTCP/G53/kinematics actual | `功能/3-4UI刀路绘图顺序与状态稳定方案.md` | 硬规则：`G53_A_Y/G53_A_Z/G53_B_X/G53_B_Z/G53_C_X/G53_C_Y` 必须在开机/受控 clean restart 时由微内核/native geometry owner 装载进 native 参数内存块，runtime INI 只作装载输入；设置页“运动模型”选择什么，就必须提交到微内核/native `motion_model` owner，微内核运动学、UI 黄色轨迹、旋转轴线和当前加工坐标系 RTCP ON 投影必须消费同一个 fresh active model readback；模型选择参数是实际运动学几何组的唯一选择依据，AC/`XYZAC_TRT` 用 A/C G53 设置值，BC/`XYZBC_TRT` 用 B/C G53 设置值；LinuxCNC/HAL kinematics 必须消费设置页保存并经 native geometry owner 回读、再由 active model 选中的 G53 几何作为实际旋转中心，不得使用旧 `ROT_POINT/OFFSET`、UI 组合显示值、SHM、设置页文本或默认值作为第二套运动学几何真源；UI 黄色轨迹和模型几何显示旋转方式必须消费微内核/native active model readback，当前 active model 为 AC 时，刀路 A/C 图形中心和底部 A/C G53 状态行必须使用同一组组合值：`A.X` 取当前 active WCS 的 X 偏置、`C.Z` 取当前 active WCS 的 Z 偏置，其它 A/C 几何分量来自 native G53 geometry readback；BC 模型下同理使用 B/C 的 G53 设置值和对应 model projector；RTCP OFF 下当前加工坐标系显示投影不随当前模型旋转轴旋转，RTCP ON 且 native RTCP actual、active model 和 active geometry fresh 有效时，当前加工坐标系原点和三轴显示投影必须使用同一套 active model 姿态跟随旋转轴旋转，但 active WCS offset 数值本身不得被 UI 改写；开机读 30Hz SHM 坐标、转速、进给速度和主轴/进给倍率显示投影，并直连必要微内核/native 状态建 WCS/MCS/A/B/C 轴线、当前模态、当前刀号、对应刀长和刀长下方 RTCP 模态行并 fit；打开 G-code 建 `2px` 黄色细线，黄色轨迹载入后必须把黄色刀路、WCS/MCS、active model 几何、红点、绿点、刀柄线和标签锚点等全部当前可见元素纳入同一个 screen fit；XY/XZ/YZ/3D 视角切换必须取消手势旋转并恢复对应默认视角；之后只机械坐标值、native 模态/tool 状态和速度/倍率显示投影驱动 UI 动态层，不得重建。UI 可以用 SHM 消费编码器反馈机械坐标、运动学理论坐标、`spindle_speed_rpm`、`linear_velocity_mm_per_min`、`feedrate_override`、`spindle_override`；当前模态、当前刀号、对应刀长、RTCP 模态行、active model/G53 几何、WCS、TLO、动作证明和门禁必须直连微内核/native。产品动作不得用 SHM modal、速度、倍率、request、dirty、设置文件、非同源 helper/cache 或旧快照拼 active。 |
| REQ-START-RUN-HOT-PATH | G-code Start 热路径、Program Open/Load 分工和产品层自写门禁删除边界 | `功能/3-1主页面按钮功能.md` | Start/Pause 按钮到 native 下发之间只保留 `REQ-POWER-ON-HOME-PRECONDITION` 定义的 fresh native `all_homed` 前置条件，不得再设置其它 V3 自写准入门禁，也不得再经过 Broker/Python product action 中间层；UI C 端只做暂停/非暂停分支、开机回零前置判定和 native command gate 直达下发。Program Open/Load 负责读取程序文件并生成原始程序身份/runtime metadata；非暂停程序启动只允许把操作员打开的原始 G-code 路径投喂给 LinuxCNC/微内核，一次启动事务里最多一次 `Set Open`，不得先 `program_open` 后再用带 `Set Open` 的 Start 重复打开同一文件。Start/Resume 热路径不得再读程序文件、计算 SHA、重新生成 runtime 文件、扫描脚本、读配置或等待其它 proof。LinuxCNC/微内核/HAL/EtherCAT 硬安全与 VPS/授权身份链仍按各自 owner 执行。 |
| REQ-MAIN-ESTOP-LATENCY | 主页面急停按下 0.1s 生效与取消急停 1s 恢复 | `功能/3-1主页面按钮功能.md` | 急停执行 owner 必须下沉到 LinuxCNC/微内核/HAL/EtherCAT realtime/native safety 层；UI/remote input/Broker 收到 `estop_force` 后不得做任何 V3 产品门禁、预检、身份/DNA/状态 epoch/backend-ready/command_busy 判断或后台清理前置动作，按键/触摸/远程输入只产生 edge event，记录不得挡在急停直通前，必须直通触发微内核 realtime emergency latch/gate，0.1s 内取得微内核原生急停和 native 内存停止证明。`estop_force` 不得发送或降级成 linuxcncrsh `Set Machine Off`、`Set EStop On`、MDI、HAL helper 或其它普通文本/用户态命令；UI 只读 native safety actual 改变显示。上层急停状态直接读取微内核/native 内存急停状态，不同步到 SHM，不维护第二套上层急停状态，确认生效后 UI 必须切换为取消急停入口，并可同步清 UI 侧 pending 流程和队列，但不得清 G-code 缓存和页面显示内容。取消急停从恢复入口或 Broker `estop_or_reset` urgent 收到开始计时，只允许最小链路：直通微内核 reset/cancel-estop、读取微内核/native 内存急停状态、确认底层 latch 清除、发起上使能并读取 Machine Enable actual；只有 `estop=false && enabled=true` 时右下按钮才能恢复为“急停”。解释器 idle、底层 motion-ready、BUS/EtherCAT 5 轴 Operation Enabled、急停源/硬故障、队列/缓存状态只作为 reset 后顶部状态/问题显示和后续运动准入，不参与取消急停按钮成功/失败判定，且 V3 不得用 SHM 速度、JSON 或私有容差自造 motion-ready。 |
| REQ-MOTION-STILLNESS-TOLERANCE | V3 上层速度门禁退役、运动安全判断归微内核/native owner | `功能/0-1开机参数入内存.md` | 运动直接由 LinuxCNC/微内核/HAL/EtherCAT 控制；停稳、速度安全、busy/idle、队列状态和运动准入属于微内核/native 内部 owner。V3/UI/Broker/product action 不得维护 `motion_stillness_gate` 参数组、私有速度阈值、SHM/JSON 停稳证明或上层预检门禁；只发请求、显示底层状态/错误码，必要诊断也必须直连原生状态。 |
| REQ-GCODE-RUN-HOT-PATH | G-code 运行期热路径、微内核 CPU 独占/扩展、30Hz 坐标/轨迹显示和非必要校验退场 | `功能/0-1开机参数入内存.md` | 按下 Start 前必须完成必要核查和程序文件打开；打开后只建立指向原始文件的只读 resident 运行身份、必要内存索引和显示模型，不生成执行用运行副本。Start/Resume 投喂给微内核的 `Set Open` 路径、`runtime_program_path`、resident `source_path` 和 `source_sha256` 必须对应操作员打开的原始 G-code 文件，不得换成过滤后的临时文件、sidecar、运行副本、等效角改写文件或 UI 预览解析产物；Start/Resume 热路径不得再读程序文件、脚本、配置或 UI 资源。按下 Start 后，除急停/硬安全/fail-closed actual 外，最高产品目的就是让 G-code 顺畅运行。运行中 LinuxCNC/HAL/EtherCAT realtime/servo owner 默认独占或受硬隔离保护 CPU0；若 CPU0 在真实运动负载下接近满负荷，微内核/native 可扩展使用 CPU1，但 CPU1 只能作为同一微内核运行域的连续算力，不得形成第二套状态 owner、第二套运动判断或割裂运行链路；普通 UI/Broker/Publisher/诊断进程必须避让。SHM 只保留 30Hz 坐标/轨迹、转速、进给速度和倍率显示投影；当前模态文字直连微内核/native modal/tool 状态块或等价原生 API；同步进程必须常驻内存并从微内核/native 内存采样，其它状态全部直连微内核/native，非必要校验、慢投影重建、文件 I/O、诊断写入和授权/参数 proof 刷新必须前置、缓存、降频或取消。 |
| REQ-MICROKERNEL-MOTION-LOOKAHEAD | 微内核速度前瞻、100 行预读和小线段连续进给 | `功能/微内核.md` | 轨迹连续性由 LinuxCNC/微内核 trajectory planner 原生能力负责；运行前必须启用等价 100 行预读/前瞻和连续路径模式，密集小线段不得导致 V3/UI/Broker 逐段停顿、频繁加减速或二次规划。 |
| REQ-MICROKERNEL-SOURCE-REBUILD | Linux/PREEMPT_RT、LinuxCNC 与 PetaLinux 的 Windows 唯一完整源码、固定版本、native 构建和 SD 卡重建 | `功能/微内核.md` | 当前唯一 owner 分别是 `linux/kernel/`、`linux/realtime/`、`linuxcnc/` 和 `board/petalinux/`，每棵树根部都有对应 `v5_*_source_identity.json`；PetaLinux/LinuxCNC 构建与验证工具分别在 `board/tools/petalinux/`、`board/tools/linuxcnc/`，LinuxCNC image recipe 在 `board/linuxcnc/yocto/`。`board/linuxcnc/` 不得承载第二套 LinuxCNC 源码或活动 patch stack。VM 只从 Windows 只读挂载构建并把输出写到挂载外；SD 卡丢失后必须能从这些 Windows owner 和 `vivado_hw_project/` 完整重建并通过 identity 与 artifact hash 证明一致。 |
| REQ-RTCP-NATIVE-STATUS-SOURCE | RTCP/switchkins native 状态源、持久启动默认、RTCP 双链路直通微内核控制、RTCP ON/OFF 下 X/Y/Z 补偿归属、HAL pin/native component、native memory block 和 State Publisher 无 fork/读盘回读 | `功能/微内核.md` | RTCP 运动学补偿只属于微内核/LinuxCNC/HAL/switchkins native 运行域：RTCP OFF 下 active model 旋转轴运动不得由系统自动补偿 X/Y/Z，RTCP ON 下 active model 旋转轴运动必须由 native 运动学自动计算并执行 X/Y/Z 补偿；UI/State Publisher/Broker/product action/刀路显示不得计算、注入、抑制或伪造补偿。RTCP 开机初值由 runtime INI `[RTCP].DEFAULT_ACTIVE` 唯一持久 owner 在 HAL/native 初始化时装载一次；启动、Machine On、Home 和程序打开不得再命令式强制 OFF。微内核或 switchkins owner 直接提供 `rtcp_active`/`switchkins_type` 状态 pin、native component、微内核/native 常驻内存/API 或登记 native memory block。RTCP 控制有两条互不代发、互不预置的直驱微内核输入：C UI 按钮链路只通过 `native_rtcp_control` 递交 ON/OFF 到微内核控制 latch；G-code 程序链路只通过解释器执行程序内 `M64/M65` 已登记输出递交 ON/OFF。两条链路都不把请求值写成 actual，最终由 native owner 合成为同一个 HAL/switchkins actual，并以 fresh native actual 驱动 C UI RTCP 按钮目标、ON 高亮状态和主页面刀长下方 RTCP 模态行。UI 按钮不得把点击转义、转换或降级为 G-code 程序输出、历史 M-code 文件、MDI 文本、linuxcncrsh 普通文本命令、`halcmd` 子进程或 Python product action；G-code runner 不得在 Start 前通过 UI latch、MDI、linuxcncrsh 普通文本、临时 sidecar 或 Python product action 替程序外部预置 RTCP；Home 不得发送 `M65 P0`、清 UI latch 或要求 RTCP actual OFF，必须在 native joint/Home 域完成真实回零；历史 M128/M129 M-code 文件、部署项、测试断言和推导函数必须删除。RTCP/switchkins actual 不得同步到 `/dev/shm/v3_status_shm`，State Publisher 不得把 SHM/status block 当主真源，不得 steady-state `subprocess.run(helper status)`、`halcmd`、短生命周期 helper、硬盘结果文件查询 actual，也不得保留 `RTCP_STATUS_HELPER_FALLBACK` 或等价环境变量重新打开查询。 |
| REQ-NO-PERIODIC-DISK-WRITES | 正常运行期禁止周期性落盘、心跳文件和临时停用开关 | `功能/微内核.md` | 设置保存是唯一允许为了持久配置而写盘的运行期主路径；程序文件选择/打开只允许生成运行内存副本，Start/Resume 热路径不得再读程序文件。UI/State Publisher 不得靠 JSON 写盘表达心跳、ready、性能或状态，也不得用硬盘文件作为 steady-state actual/fallback；心跳/ready 必须读微内核/native 内存事实或 UDS health，不得同步到 SHM；一次性 supervisor ready 文件只能作启动诊断，SHM frame 内不得保留通用 JSON payload，已判定无用的周期落盘必须删除。 |
| REQ-UI-POPUP-RESULT-POLICY | 后台命令结果弹窗、开机回零前置提示、进程取消、成功提示、确认继续和急停遮挡边界 | `功能/跳窗提示方案.md` | 失败、阻塞、确认继续和需要操作员处理的结果才用全屏三行弹窗；主页面普通运动失败只写顶部提示，但 `REQ-POWER-ON-HOME-PRECONDITION` 的 fresh native `all_homed=false/unknown` 是唯一明确例外，必须显示统一三段式弹窗且不发送原动作。该前置提示在显示时已经是本地阻塞终态，`关闭` 只隐藏提示且不得执行、排队或重放原动作。所有微内核原生错误及其内部 adapter 别名的可见文案统一服从 `REQ-NATIVE-OPERATOR-ERROR-MAP`，不得显示内部别名码或再造重复原因。设置页驱动动作、授权/服务器下载、单轴设0等需要操作员确认的成功结果必须保留同一弹窗，其它普通成功只关闭进度窗并在顶部显示约 2 秒简短文字。进度弹窗存在可点击 `关闭` 时，`关闭` 的唯一语义是向 action owner 发送本次 run_id 的正式取消请求并终止该后台进程/进程组，收到 cancelled 终态后再隐藏和释放 action lock；不得只隐藏弹窗让进程继续。动作一旦越过不可逆提交点，必须移除/禁用 `关闭`，不能展示一个无法取消的关闭按钮。 |
| REQ-NATIVE-OPERATOR-ERROR-MAP | 微内核原生错误完整清单、通俗中文映射、唯一 UI 显示入口和重复自建错误清理 | `功能/微内核错误提示完整映射表.md` | 以当前板端微内核 2.9.7 v5 补丁源码的真实错误发射点为版本化清单；当前基线必须完整覆盖 197 个解释器固定错误和 358 个任务层、实时运动层及动态解释器格式错误，共 555 项。State Publisher 是唯一原生错误通道读取 owner，事件到达后写独立 resident native memory block；UI 只读该块并按完整映射显示中文标题、原因和下一步，不显示底层控制框架名称、原始英文、原生格式串、内部进程名或 adapter 别名码。原生已有等价错误时，删除 UI/Broker 自造的重复可见文案并把必要内部 gate 结果别名映射到同一原生项；只有没有原生等价的 UI 输入错误、owner 通道错误和资源缺失错误可以保留。未知新错误只显示通俗中文未知错误和不透明指纹，原始内容仅进入工程日志。微内核源码版本变化时必须重新提取并使清单、映射和覆盖测试同时达到 100%，缺一项不得部署。 |
| REQ-BOARD-PROGRAM-FULL-BUILD-CLOSURE | 板端程序修改的构建、部署和上板闭环关闭口径 | `功能/自动闭环测试方式.md` | 任何会进入板端产物或影响板端运行的 `linux/`、`linuxcnc/` 或 `board/` 程序、配置、脚本、服务、recipe、部署项修改，都必须由 VM 直接只读挂载当前 Windows 唯一真源，在挂载外 canonical build 目录构建本次会进入部署产物的目标集合，部署到开发板并走原始 UI/operator/control 路径闭环；局部构建、VM build、smoke、dry-run、board_deployed 和 direct UDS 只能作为前置证据，不能声明功能完成或上板通过。 |
| REQ-AUTO-STATE-DRIVEN | 自动闭环、真实 operator path、远程输入/relay、按 active model 选择 `cc-ac.ngc`/`cc-bc.ngc` golden motion 和证据状态口径 | `功能/自动闭环测试方式.md` | 自动化必须证明真实 UI/operator/motion 路径；direct UDS、后端 runner、linuxcncrsh 直跑、dry-run、matrix pass、board_deployed 和旧 result.json 不能声明功能通过。当前测试台架电机为空载且未连接机械结构，默认允许直接执行真实 Home/Jog/MDI/Start/G-code 运动；AC/`XYZAC_TRT` 只运行原始 `cc-ac.ngc`，BC/`XYZBC_TRT` 只运行原始 `cc-bc.ngc`，active model 未知、回读不新鲜或程序不匹配必须 fail-closed；不得改写轴字或回退到旧 `cc.ngc`。黄金测试唯一入口是主页面点击 `打开程序`，程序页第一次点击模型匹配行完成选中、第二次点击同一行完成打开，回主页面核对程序 identity/hash 后，再通过 UI 完成回零、启动、1 秒急停、取消急停和再次回零；任何后端直接 `Set Open`/`Set Run` 都不是有效黄金测试。 |

## REQ-V5-GIT-NAS-BACKUP-TOOL：V5 Git 范围

`GIT_ROOT=PROJECT_ROOT`。Git 和 NAS 镜像都必须从当前 Windows `D:\v5` 根解析，禁止把 VM、板端、NAS、另一个 checkout、子目录或人工挑选文件列表当作提交根。

### 必须纳入

- 根规则和 Git 配置：`AGENTS.md`、`.gitignore`、`.gitattributes`。
- settled requirement 与架构文档：`功能/`、`系统代码架构硬边界守则.md`、`项目软硬件架构和后期修改指导说明.md`，以及当前受 Git 管理的其它项目文档。
- 完整重建源码 owner：`linux/kernel/`、`linux/realtime/`、`linuxcnc/`、`board/petalinux/`、`board/linuxcnc/`、`board/app/`、`board/services/`、`board/tools/`、`vivado_hw_project/`。
- 当前 Windows 客户端、驱动映射、G-code/配置和其它已登记项目源码 owner；新增 canonical owner 默认进入同一根范围，不能依赖人工扩白名单才被备份。
- `linux/`、`linuxcnc/` 这类导入的完整上游源码必须按当前 Windows owner 的完整文件快照纳入。`linux/kernel/tools/build/`、`linux/realtime/patches/build/`、`linuxcnc/bin/`、`linuxcnc/tcl/bin/` 是真实源码目录，不能被项目级 `build/`、`**/bin/`、`**/obj/` 通配排除；导入树自身 `.gitignore` 会隐藏但上游原本跟踪的文件时，canonical 工具必须显式强制纳入。

### 必须排除

- `.git/`、`repo_ignored/`、Python/Node/IDE 缓存、CMake generated files、明确的 .NET `bin/obj`、Vivado `.Xil/*.cache/*.gen/*.hw/*.runs` 和其它已分类生成目录。
- 压缩包、安装包、发布制品、截图、测试 proof/evidence、日志、临时文件和构建产物；这些内容进入各自恢复/发布/evidence owner，不得冒充源码提交。
- VM/板端/NAS 副本、旧同步目录和源码 staging；Git 远端只是恢复点，不是第二棵活动源码。

### 提交前强制校验

- 一键工具必须先普通 stage 当前 `PROJECT_ROOT` 全部变化，再显式纳入 `linux/`、`linuxcnc/` 的完整 owner 快照，随后清除仅限明确生成物的排除项。
- 提交前必须验证当前 `board/tools/v5_git_nas_backup/v5_git_scope.ps1` 登记的全部哨兵已进入 Git index，至少覆盖四份 source identity、Linux/LinuxCNC 被通配误伤的源码、PetaLinux config/XSA/bit/recipe、`board/tools/petalinux/` verifier/overlay 和 `vivado_hw_project/vivado_hw_project.xpr`；文档不得另写一份落后的固定清单。
- 任一必含 owner/哨兵不存在、被忽略、未进入 index，或 Git 与 NAS 排除范围不一致时，必须在 commit/push 前 fail closed；不能以“目录存在”“NAS 有一份”或“以后补推”通过。

## REQ-FILE-CLASSIFIED-PLACEMENT

### 当前 canonical 文件位置表

| 范围 | 当前唯一位置 |
| --- | --- |
| Xilinx Linux kernel 完整源码与 identity | `linux/kernel/`、`linux/kernel/v5_linux_source_identity.json` |
| PREEMPT_RT/Yocto realtime metadata 与 identity | `linux/realtime/`、`linux/realtime/v5_realtime_source_identity.json` |
| LinuxCNC 2.9.7 完整源码、V5 native 修改与 identity | `linuxcnc/`、`linuxcnc/v5_linuxcnc_source_identity.json` |
| LinuxCNC 板端集成 | `board/linuxcnc/components/`、`board/linuxcnc/hal/`、`board/linuxcnc/ini/`、`board/linuxcnc/nc/`、`board/linuxcnc/runtime/`、`board/linuxcnc/yocto/` |
| PetaLinux project-spec、硬件输入、recipes 与 identity | `board/petalinux/`、`board/petalinux/v5_petalinux_source_identity.json` |
| PetaLinux/LinuxCNC 构建与 source identity 校验工具 | `board/tools/petalinux/`、`board/tools/linuxcnc/` |
| LVGL 页面与输入 | `board/app/`；设置页拆分在 `board/app/src/v5_settings_page*.c`、`v5_settings_axis_table*.c`、`v5_settings_action_ipc.*`，主页面拆分在 `v5_main_page*.c`、`v5_toolpath_display.*` |
| Command Gate/native control | `board/services/command_gate/` |
| 驱动设置服务 | `board/services/drive_profile/`；入口、合同、runtime、查询、SDO、结果和持久层分别由 `v5_settings_action*`、`v5_drive_*` 当前模块承担 |
| G-code resident/preview | `board/services/program_runtime/v5_program_runtime.*`、`board/services/program_runtime/v5_program_preview.*` |
| 状态发布 | `board/services/state_publisher/`；machine projection、WCS codec/publisher、SHM mmap/writer 是独立 owner |
| 远程显示/输入 | UI 本地端 `board/app/src/v5_lvgl_remote_*`；网络端 `board/services/ui/v5_remote_ui_*` |
| 部署、板端验收与触摸校准工具 | `board/tools/deploy/`、`board/tools/v5_touch_calibration/` |
| Vivado 硬件工程 | `vivado_hw_project/` |

- 新建、移动或生成任何项目文件前，必须先判断文件类别和 owner，再落到对应目录；找不到目录时先更新本规则或对应 owner，不得先把文件放到就近目录。
- `linux/kernel/` 只承载 kernel 完整源码，`linux/realtime/` 只承载 realtime metadata，`linuxcnc/` 只承载完整 LinuxCNC/V5 native，`board/petalinux/` 只承载当前 PetaLinux 工程。四处必须保留各自根部 identity，不得再以活动 patch tree、解压副本、generated source 或 VM checkout 出现在其它目录。
- `board/linuxcnc/` 只承载 LinuxCNC 板端集成输入；完整 PetaLinux project-spec 不在该目录，而在 `board/petalinux/`。`board/linuxcnc/yocto/linuxcnc-prebuilt.bb` 只负责 LinuxCNC image recipe，不是 PetaLinux 工程根。
- `功能/` 只放 settled requirement owner 文档；`待做工作/` 只放工作卡、遗留、改进记录和人工可读进度，不得承载工具运行 JSON、一次性状态、缓存、日志或临时输出。
- 可复用项目工具真源放 `D:\v5\board\tools\`；Windows 任务一次性 scratch、运行状态、诊断 JSON、临时日志、缓存、编译/部署过程备份和中间产物放 `repo_ignored/temp/` 或 `repo_ignored/<work-id>/...`，不得进入业务文档目录、源码目录或根目录。项目顶层 `bak/` 只允许全量编译/部署前保存从板端持久参数 owner 只读导出的非源码值快照；源码、二进制、打包产物、日志和普通过程备份禁止进入 `bak/`。
- 人工需要查看的截图证据放 `截图/` 或 owner 明确指定的证据目录；VM 的 CMake/Ninja 状态、generated files、中间物和产物只能放 `VM_BUILD_ROOT`，板端文件只能是部署产物或运行状态，两者都不得作为源码真源或反向覆盖 Windows 跟踪源码。`D:\v3` 只允许作只读历史参考，不得作为 v5 live source。
- 临时文件不靠 `.gitignore` 在原地隐藏来闭合；发现散落临时文件时，必须移动到 ignored 临时分类目录或删除，并修正生成它的工具默认输出位置。

## P1 引用写法

非 owner 文档只保留这种短引用：

```text
引用需求真源：REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE, REQ-NATIVE-OWNER-FIRST
```

跨功能通用规则不要在非 owner 文档复制全文。需要引用开机全量入 RAM、运行期热路径不得读盘、参数 owner 优先等通用规则时，统一使用：

```text
启动内存/热路径通用规则：见 `REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE` / `功能/0-1开机参数入内存.md`，本文只保留本功能特有边界。
```

如果需要解释本功能如何使用该需求，只写本功能特有的 owner、字段、路径和验收证据；不要复制跨功能总规则全文。

## P1 文档一致性

仓库自写审计/预检门禁已删除。文档修改仍以本文索引和单一 owner 为准；需要检查时使用人工 diff review、`git diff --check` 或对应 owner 的专门测试，不再要求运行仓库自写审计门禁。
