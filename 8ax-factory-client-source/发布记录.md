# 8AX Factory Client 发布记录

## 发布要求

- 每次修改 Factory Client 的源码、项目配置、资源或发布行为后，必须生成新的 Release exe。
- 每次发布必须更新 `ClientInfo.Version`、`ClientInfo.Build` 和 `.csproj` 版本号。
- 每次发布必须记录输出路径、exe SHA256、变更范围和验证结果。

## 2026-07-03 0.1.12

- 构建号：`2026.07.03.005`
- 输出路径：`artifacts/8ax.FactoryClient-0.1.12-20260703005-win-x64/8ax.FactoryClient.exe`
- exe SHA256：`A1055BE3F2D4B5FA4CF2FF811C7CA5ACBDD467C4300358E603C77C2727689AD9`
- 变更范围：设备 DNA 表读取 VPS `authorizationStatus`，未人工确认授权/未存在 `device_authorization.json` 时状态显示“待人工授权”并标红；已存在授权文件时显示“已授权”。删除设备 DNA 登记成功后，VPS 同步删除该设备 id-dna private 目录里的 `device_authorization.json`，并返回 `authorizationDeleted` 供 Factory Client 日志记录。
- 发布命令：`dotnet publish 8ax-factory-client-source\8ax.FactoryClient\8ax.FactoryClient.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -o 8ax-factory-client-source\artifacts\8ax.FactoryClient-0.1.12-20260703005-win-x64 --nologo`
- 验证结果：`python -m py_compile repo_ignored/implement_public_key_dna_register/gateway.py` 通过；`dotnet build 8ax-factory-client-source/8ax.FactoryClient/8ax.FactoryClient.csproj -c Release --nologo` 通过，0 warning/0 error；`dotnet publish` 已生成 Release win-x64 自包含单文件 exe；VPS `8ax-auth-gateway` 已备份、部署并重启，`/healthz` 返回 `status=ok`。

## 2026-07-03 0.1.12

- 构建号：`2026.07.03.004`
- 输出路径：`artifacts/8ax.FactoryClient-0.1.12-win-x64/8ax.FactoryClient.exe`
- exe SHA256：`CDE97310D8328CE5E88653AFA52DF1D9AD3393FEA0E3B5CD6E47B9E1FE6CBA21`
- 变更范围：VPS private 目录统一改为只使用 6 位 `VPS分发ID`；`PL DNA hash` 仅作为 VPS 侧设备绑定校验输入，不进入目录名、URL、manifest、`targetRel/private_folder` 或 Factory Client 可见发布摘要。OTA 发布结果新增 `vps_distribution_id`、`dna_binding`、`private_folder` 摘要字段，均不显示 hash。
- 发布命令：`dotnet publish 8ax-factory-client-source\8ax.FactoryClient\8ax.FactoryClient.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -o 8ax-factory-client-source\artifacts\8ax.FactoryClient-0.1.12-win-x64 --nologo`
- 验证结果：`python -m unittest discover -s 8ax-factory-client-source\vps-admin-api\tests -p "test_*.py"` 通过；`python -m py_compile` 通过；`dotnet build` 通过；`dotnet publish` 已生成 Release win-x64 自包含单文件 exe；VPS `8ax-auth-gateway.service` 和 `8ax-ota-admin-api.service` 均 active；VPS private OTA 发布探针返回 `targetRel=private/359764/...`、`private_folder=359764`、`dna_binding=server_verified`，hash 暴露扫描为 none，错误绑定返回 403；`python tools/v3_doc_single_source_audit.py --repo-root . --json-out repo_ignored/doc_single_source/audit.json` 通过，`errors=0 warnings=0`。

## 2026-07-03 0.1.11

- 构建号：`2026.07.03.003`
- 输出路径：`artifacts/8ax.FactoryClient-0.1.11-win-x64/8ax.FactoryClient.exe`
- exe SHA256：`258317EC01C1B7F6A36D20A5F5A9BCB31740069EF68479F94E9FC083BEADA4E4`
- 变更范围：新增 `驱动发布` 页，支持 public/private 发布 `driver_profile_map.json`；private 发布必须填写 6 位 `VPS分发ID` 和 64 位 `PL DNA hash`，VPS private 目录统一为 6 位 ID，DNA hash 只用于 VPS 侧校验且不进入路径；OTA private 发布同步增加私有 ID；VPS admin API 新增 `POST /api/v1/admin/drive-profiles`。
- 发布命令：`dotnet publish 8ax-factory-client-source\8ax.FactoryClient\8ax.FactoryClient.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -o 8ax-factory-client-source\artifacts\8ax.FactoryClient-0.1.11-win-x64 --nologo`
- 验证结果：`dotnet build` 通过；`dotnet publish` 已生成 Release win-x64 自包含单文件 exe；文件版本为 `0.1.11.0`；VPS admin API 和旧 drive profile 上传脚本相关 pytest 通过。

## 2026-07-03 0.1.10

- 构建号：`2026.07.03.002`
- 输出路径：`artifacts/8ax.FactoryClient-0.1.10-win-x64/8ax.FactoryClient.exe`
- exe SHA256：`1921066D075E8F3C7031F9A47B769EBE7B0E68F607036BAC70F32D38C7380279`
- 变更范围：设备 DNA 表将 `VPS分发ID` 移到第一列，便于人工优先核对 6 位显示 ID；保留设备内部 `设备ID` 后续列和接口使用语义。
- 发布命令：`dotnet publish 8ax-factory-client-source\8ax.FactoryClient\8ax.FactoryClient.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -o 8ax-factory-client-source\artifacts\8ax.FactoryClient-0.1.10-win-x64 --nologo`
- 验证结果：`dotnet build` 通过；`dotnet publish` 已生成 Release win-x64 自包含单文件 exe；文件版本为 `0.1.10.0`。

## 2026-07-03 0.1.9

- 构建号：`2026.07.03.001`
- 输出路径：`artifacts/8ax.FactoryClient-0.1.9-win-x64/8ax.FactoryClient.exe`
- exe SHA256：`0EB9BA6E08280F834110B542F8623AF369180AC6155B339DFBE066E704B5BF76`
- 变更范围：新增 OTA 发布入口与 VPS OTA 管理接口；主窗口默认客户区调整为 `1600x1200` 并保持 PerMonitorV2/DPI aware；设备 DNA 表新增 6 位 `VPS分发ID` 显示规则。
- 发布命令：`dotnet publish 8ax-factory-client-source\8ax.FactoryClient\8ax.FactoryClient.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -o 8ax-factory-client-source\artifacts\8ax.FactoryClient-0.1.9-win-x64 --nologo`
- 验证结果：`dotnet build` 通过；`dotnet publish` 已生成 Release win-x64 自包含单文件 exe；文件版本为 `0.1.9.0`；`v3_doc_single_source_audit` 为 `errors=0 warnings=0`；`git diff --check` 通过。

## 2026-07-03 0.1.10

- 构建号：`2026.07.03.002`
- 输出路径：`artifacts/8ax.FactoryClient-0.1.10-win-x64/8ax.FactoryClient.exe`
- exe SHA256：`292F427506171FAAE80E00981FB131D3F6B1B1D0FC702D992590D08623EE5B16`
- 变更范围：VPS 端 `8ax-auth-gateway` 返回 `vpsDistributionId`；VPS 端按 PL DNA 固定算法生成 6 位 ID，和已有 ID 对比去重后保存，已保存 ID 后续不再改变；Factory Client 保持只读 VPS 返回字段，不在客户端本地生成最终 ID。
- 发布命令：`dotnet publish 8ax-factory-client-source\8ax.FactoryClient\8ax.FactoryClient.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -o 8ax-factory-client-source\artifacts\8ax.FactoryClient-0.1.10-win-x64 --nologo`
- 验证结果：VPS `/healthz` 返回版本 `2026-07-03-001`；VPS `/api/v1/admin/devices` 返回 `vpsDistributionId=359764`；完整 PL DNA `0x0004D52826A3985C` 按 VPS 算法复算为 `359764`；`dotnet publish` 已生成 Release win-x64 自包含单文件 exe，文件版本为 `0.1.10.0`。Debug 构建未覆盖正在运行的 `bin/Debug/net9.0-windows/8ax.FactoryClient.exe`，该文件被进程 `31012` 锁定。
