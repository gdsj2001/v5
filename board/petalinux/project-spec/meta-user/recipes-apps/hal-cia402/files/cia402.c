/* Autogenerated by /usr/bin/halcompile on Fri May 15 14:37:10 2026 -- do not edit */
#include "rtapi.h"
#ifdef RTAPI
#include "rtapi_app.h"
#endif
#include "rtapi_string.h"
#include "rtapi_errno.h"
#include "hal.h"
#include "rtapi_math64.h"

static int comp_id;

#ifdef MODULE_INFO
MODULE_INFO(linuxcnc, "component:cia402:HAL CiA402 Drive interface layer");
MODULE_INFO(linuxcnc, "author:Dominik Braun");
MODULE_INFO(linuxcnc, "license:GPL");
MODULE_INFO(linuxcnc, "descr:\nHAL Interface for CiA402 Devices,\n\nthis component acts as a glue layer between hardware to Hal modules like Ethercat, CAN-Bus or others.\n\nIt translates raw IO Data from the PDOs to the common linuxcnc Hal pin structure and has build in logic\n\n \nfor the CiA402 State Control, feedback handling, external homing and build in scaling functions.\n\n\nIt delivers two functions: read_all and write_all.\n\nThe concept of integration in the correspondending task should be as following: \n\n\n\n\n###################      ###########       #########     ############      #####################\n\n#HARDWARE INPUT   #      # CiA402  #       #Motion #     #  CiA402  #      #  Hardware Output  #\n\n#  like           #-->>--#read_all #-->>>--#Pids   #-->--#write_all #-->>--#        like       #\n\n#Ethercat read_all#      #  etc.   #       #       #     #          #      # Ethercat write_all#\n\n###################      ###########       #########     ############      #####################\n\n\n\nHal Example:\n\n\n#########\n\n# Setup\n\n#########\n\n\n\nloadrt [KINS]KINEMATICS\n\nloadrt [EMCMOT]EMCMOT servo_period_nsec=[EMCMOT]SERVO_PERIOD num_joints=[KINS]JOINTS\n\n(loadusr -W lcec_conf ethercat-conf.xml)\n\nloadrt lcec\n\nloadrt cia402 count=3\n\nloadrt pid names=x-pid,y-pid,z-pid\n\n\n##########################\n\n# Functions servo-thread\n\n##########################\n\naddf lcec.read-all servo-thread\n\naddf cia402.0.read-all servo-thread\n\naddf cia402.1.read-all servo-thread\n\naddf cia402.2.read-all servo-thread\n\n\naddf motion/ PIDs / PCL / etc .\n\n\naddf cia402.0.write-all servo-thread\n\naddf cia402.1.write-all servo-thread\n\naddf cia402.2.write-all servo-thread\n\naddf lcec.write-all servo-thread\n\n#########################################\n\n#nets .....\n\n\nBy default the component is set to CSP Mode,\n\nCSV Mode could be selected with an:\n \nsetp cia402.0.csp-mode 0  in hal.\n\nMode changing in runtime is currently not supported, to avoid\n\nunwanted behaviour.\n \n\nFor using the servo drives internal homing procedure configure your\n\njoint homing to  Home on Index Pulse only and connect the components\n\nhome Input to the index-enable Pin:\n\n\nHOME_SEARCH_VEL = 0.0\n\nHOME_LATCH_VEL = 0.2  (Any value but zero, the homing speed is predetermined by the drives configured speed\n\nHOME_USE_INDEX = TRUE\n\n\nIf you are using PIDs, don't forget to connect the PIDs index-enable pin.\n\n\n\nEven though this component exports many pins, you can choose which functions you want to use:\n\n\nIf you would like to use the CiA State Machine, connect Statusword and Controlword.\n\nFor single use of the scaling function connect only the fb and cmd pins from position or velocity.\n\nIf no Drives homing is needed, let the Pins unconnected.");
MODULE_INFO(linuxcnc, "pin:statusword:u32:0:in:Drives CiA402 Statusword, index 0x6041:None:None");
MODULE_INFO(linuxcnc, "pin:opmode_display:s32:0:in:Drives Modes of Operation feedback register, index 0x6061:None:None");
MODULE_INFO(linuxcnc, "pin:drv_actual_position:s32:0:in:Drives actual Position, index 0x6064:None:None");
MODULE_INFO(linuxcnc, "pin:drv_actual_velocity:s32:0:in:Drives actual Velocity, index 0x606C:None:None");
MODULE_INFO(linuxcnc, "pin:controlword:u32:0:out:Drives CIA402 Controlword, index 0x6040:None:None");
MODULE_INFO(linuxcnc, "pin:opmode:s32:0:out:Drives Modes of Operation Input, index 0x6060:None:None");
MODULE_INFO(linuxcnc, "pin:drv_target_position:s32:0:out:Position command to the drive, index 0x607A:None:None");
MODULE_INFO(linuxcnc, "pin:drv_target_velocity:s32:0:out:Velocity command to the drive, index 0x60FF:None:None");
MODULE_INFO(linuxcnc, "pin:enable:bit:0:in:true enables the Drive:None:None");
MODULE_INFO(linuxcnc, "pin:pos_cmd:float:0:in:target Position, from Motion or PID:None:None");
MODULE_INFO(linuxcnc, "pin:velocity_cmd:float:0:in:target Velocity, from Motion or PID:None:None");
MODULE_INFO(linuxcnc, "pin:pos_fb:float:0:out:Position feedback, scaled:None:None");
MODULE_INFO(linuxcnc, "pin:velocity_fb:float:0:out:Velocity feedback scaled:None:None");
MODULE_INFO(linuxcnc, "pin:drv_fault:bit:0:out:true indicates an Drive Error:None:None");
MODULE_INFO(linuxcnc, "pin:home:bit:0:io:true starts the Drives internal home procedure, connect this to joint.x.index-enable pin:None:None");
MODULE_INFO(linuxcnc, "pin:stat_homed:bit:0:out:true indicates that the Drive is internaly homed:None:None");
MODULE_INFO(linuxcnc, "pin:stat_homing:bit:0:out:true indicates that the Drives homing procedure is running:None:None");
MODULE_INFO(linuxcnc, "pin:stat_switchon_ready:bit:0:out:Drive in CiA State: ready to switch on:None:None");
MODULE_INFO(linuxcnc, "pin:stat_switched_on:bit:0:out:Drive in CiA State: switched on:None:None");
MODULE_INFO(linuxcnc, "pin:stat_op_enabled:bit:0:out:Drive in CiA State: Operation enabled:None:None");
MODULE_INFO(linuxcnc, "pin:stat_voltage_enabled:bit:0:out:Drive in CiA State: switched on, Voltage enabled:None:None");
MODULE_INFO(linuxcnc, "pin:stat_fault:bit:0:out:Drive hast the fault bit set:None:None");
MODULE_INFO(linuxcnc, "pin:stat_quick_stop:bit:0:out:Drive in State: Quick Stop:None:None");
MODULE_INFO(linuxcnc, "pin:stat_switchon_disabled:bit:0:out:Drive in CiA State: Switch on disabled:None:None");
MODULE_INFO(linuxcnc, "pin:stat_warning:bit:0:out:Drive has the warning bit set:None:None");
MODULE_INFO(linuxcnc, "pin:stat_remote:bit:0:out:Drive in State remote / bus operation:None:None");
MODULE_INFO(linuxcnc, "pin:stat_target_reached:bit:0:out:Drive has reached target position / velocity:None:None");
MODULE_INFO(linuxcnc, "pin:opmode_no_mode:bit:0:out:Drive has no operation Mode set:None:None");
MODULE_INFO(linuxcnc, "pin:opmode_homing:bit:0:out:Drive in Mode Homing:None:None");
MODULE_INFO(linuxcnc, "pin:opmode_cyclic_position:bit:0:out:Drive in Mode Cyclic synchronus Position:None:None");
MODULE_INFO(linuxcnc, "pin:opmode_cyclic_velocity:bit:0:out:Drive in Mode Cyclic synchronus Velocity:None:None");
MODULE_INFO(linuxcnc, "pin:fault_reset:bit:0:in:true, sends fault reset command to the Drive:None:None");
MODULE_INFO(linuxcnc, "param:pos_scale:float:0:rw:increments per machine unit:None:None");
MODULE_INFO(linuxcnc, "param:velo_scale:float:0:rw:velocity in machine units per 1 Motor revolution:None:None");
MODULE_INFO(linuxcnc, "param:auto_fault_reset:bit:0:rw:true resets an actual Drive Fault automatically at the next enable Signal:None:None");
MODULE_INFO(linuxcnc, "param:csp_mode:bit:0:rw:true= CS Position Mode, false= CS Velocity Mode, is only recognized at Linuxcnc Startup, default true:None:None");
MODULE_INFO(linuxcnc, "funct:read_all:1:");
MODULE_INFO(linuxcnc, "funct:write_all:1:");
MODULE_LICENSE("GPL");
#endif // MODULE_INFO


struct __comp_state {
    struct __comp_state *_next;
    hal_u32_t *statusword;
    hal_s32_t *opmode_display;
    hal_s32_t *drv_actual_position;
    hal_s32_t *drv_actual_velocity;
    hal_u32_t *controlword;
    hal_s32_t *opmode;
    hal_s32_t *drv_target_position;
    hal_s32_t *drv_target_velocity;
    hal_bit_t *enable;
    hal_float_t *pos_cmd;
    hal_float_t *velocity_cmd;
    hal_float_t *pos_fb;
    hal_float_t *velocity_fb;
    hal_bit_t *drv_fault;
    hal_bit_t *home;
    hal_bit_t *stat_homed;
    hal_bit_t *stat_homing;
    hal_bit_t *stat_switchon_ready;
    hal_bit_t *stat_switched_on;
    hal_bit_t *stat_op_enabled;
    hal_bit_t *stat_voltage_enabled;
    hal_bit_t *stat_fault;
    hal_bit_t *stat_quick_stop;
    hal_bit_t *stat_switchon_disabled;
    hal_bit_t *stat_warning;
    hal_bit_t *stat_remote;
    hal_bit_t *stat_target_reached;
    hal_bit_t *opmode_no_mode;
    hal_bit_t *opmode_homing;
    hal_bit_t *opmode_cyclic_position;
    hal_bit_t *opmode_cyclic_velocity;
    hal_bit_t *fault_reset;
    hal_float_t pos_scale;
    hal_float_t velo_scale;
    hal_bit_t auto_fault_reset;
    hal_bit_t csp_mode;
    float pos_scale_old;

    float velo_scale_old;

    bool enable_old;

    bool stat_homed_old;

    bool stat_fault_old;

    double pos_scale_rcpt;

    double velo_scale_rcpt;

    bool pos_mode;

    bool init_pos_mode;

    long auto_fault_reset_delay;

};
struct __comp_state *__comp_first_inst=0, *__comp_last_inst=0;

static void read_all(struct __comp_state *__comp_inst, long period);
static void write_all(struct __comp_state *__comp_inst, long period);
static int __comp_get_data_size(void);
static int extra_setup(struct __comp_state *__comp_inst, char *prefix, long extra_arg);
#undef TRUE
#define TRUE (1)
#undef FALSE
#define FALSE (0)
#undef true
#define true (1)
#undef false
#define false (0)

static int export(char *prefix, long extra_arg) {
    char buf[HAL_NAME_LEN + 1];
    int r = 0;
    int sz = sizeof(struct __comp_state) + __comp_get_data_size();
    struct __comp_state *inst = hal_malloc(sz);
    memset(inst, 0, sz);
    r = extra_setup(inst, prefix, extra_arg);
    if(r != 0) return r;
    r = hal_pin_u32_newf(HAL_IN, &(inst->statusword), comp_id,
        "%s.statusword", prefix);
    if(r != 0) return r;
    r = hal_pin_s32_newf(HAL_IN, &(inst->opmode_display), comp_id,
        "%s.opmode-display", prefix);
    if(r != 0) return r;
    r = hal_pin_s32_newf(HAL_IN, &(inst->drv_actual_position), comp_id,
        "%s.drv-actual-position", prefix);
    if(r != 0) return r;
    r = hal_pin_s32_newf(HAL_IN, &(inst->drv_actual_velocity), comp_id,
        "%s.drv-actual-velocity", prefix);
    if(r != 0) return r;
    r = hal_pin_u32_newf(HAL_OUT, &(inst->controlword), comp_id,
        "%s.controlword", prefix);
    if(r != 0) return r;
    r = hal_pin_s32_newf(HAL_OUT, &(inst->opmode), comp_id,
        "%s.opmode", prefix);
    if(r != 0) return r;
    r = hal_pin_s32_newf(HAL_OUT, &(inst->drv_target_position), comp_id,
        "%s.drv-target-position", prefix);
    if(r != 0) return r;
    r = hal_pin_s32_newf(HAL_OUT, &(inst->drv_target_velocity), comp_id,
        "%s.drv-target-velocity", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_IN, &(inst->enable), comp_id,
        "%s.enable", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_IN, &(inst->pos_cmd), comp_id,
        "%s.pos-cmd", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_IN, &(inst->velocity_cmd), comp_id,
        "%s.velocity-cmd", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_OUT, &(inst->pos_fb), comp_id,
        "%s.pos-fb", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_OUT, &(inst->velocity_fb), comp_id,
        "%s.velocity-fb", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->drv_fault), comp_id,
        "%s.drv-fault", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_IO, &(inst->home), comp_id,
        "%s.home", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_homed), comp_id,
        "%s.stat-homed", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_homing), comp_id,
        "%s.stat-homing", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_switchon_ready), comp_id,
        "%s.stat-switchon-ready", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_switched_on), comp_id,
        "%s.stat-switched-on", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_op_enabled), comp_id,
        "%s.stat-op-enabled", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_voltage_enabled), comp_id,
        "%s.stat-voltage-enabled", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_fault), comp_id,
        "%s.stat-fault", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_quick_stop), comp_id,
        "%s.stat-quick-stop", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_switchon_disabled), comp_id,
        "%s.stat-switchon-disabled", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_warning), comp_id,
        "%s.stat-warning", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_remote), comp_id,
        "%s.stat-remote", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->stat_target_reached), comp_id,
        "%s.stat-target-reached", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->opmode_no_mode), comp_id,
        "%s.opmode-no-mode", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->opmode_homing), comp_id,
        "%s.opmode-homing", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->opmode_cyclic_position), comp_id,
        "%s.opmode-cyclic-position", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->opmode_cyclic_velocity), comp_id,
        "%s.opmode-cyclic-velocity", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_IN, &(inst->fault_reset), comp_id,
        "%s.fault-reset", prefix);
    if(r != 0) return r;
    r = hal_param_float_newf(HAL_RW, &(inst->pos_scale), comp_id,
        "%s.pos-scale", prefix);
    if(r != 0) return r;
    r = hal_param_float_newf(HAL_RW, &(inst->velo_scale), comp_id,
        "%s.velo-scale", prefix);
    if(r != 0) return r;
    r = hal_param_bit_newf(HAL_RW, &(inst->auto_fault_reset), comp_id,
        "%s.auto-fault-reset", prefix);
    if(r != 0) return r;
    r = hal_param_bit_newf(HAL_RW, &(inst->csp_mode), comp_id,
        "%s.csp-mode", prefix);
    if(r != 0) return r;
    inst->init_pos_mode = 0.0;
    rtapi_snprintf(buf, sizeof(buf), "%s.read-all", prefix);
    r = hal_export_funct(buf, (void(*)(void *inst, long))read_all, inst, 1, 0, comp_id);
    if(r != 0) return r;
    rtapi_snprintf(buf, sizeof(buf), "%s.write-all", prefix);
    r = hal_export_funct(buf, (void(*)(void *inst, long))write_all, inst, 1, 0, comp_id);
    if(r != 0) return r;
    if(__comp_last_inst) __comp_last_inst->_next = inst;
    __comp_last_inst = inst;
    if(!__comp_first_inst) __comp_first_inst = inst;
    return 0;
}
static int default_count=1, count=0;
RTAPI_MP_INT(count, "number of cia402");
char *names = ""; // comma separated names
RTAPI_MP_STRING(names, "names of cia402");
int rtapi_app_main(void) {
    int r = 0;
    int i;
    comp_id = hal_init("cia402");
    if(comp_id < 0) return comp_id;
    if(count && names[0]) {
        rtapi_print_msg(RTAPI_MSG_ERR,"count= and names= are mutually exclusive\n");
        return -EINVAL;
    }
    if(!count && !names[0]) count = default_count;
    if(count) {
        for(i=0; i<count; i++) {
            char buf[HAL_NAME_LEN + 1];
            rtapi_snprintf(buf, sizeof(buf), "cia402.%d", i);
            r = export(buf, i);
            if(r != 0) break;
       }
    } else {
        size_t i, j;
        int idx;
        char buf[HAL_NAME_LEN+1];
        const size_t length = strlen(names);
        for (i = j = idx = 0; i <= length; i++) {
            const char c = buf[j] = names[i];
            if ((c == ',') || (c == '\0')) {
                buf[j] = '\0';
                r = export(buf, idx);
                if(r != 0) {break;}
                idx++;
                j = 0;
            } else {
                if (++j == (sizeof(buf) / sizeof(buf[0]))) {
                    buf[j - 1] = '\0';
                    rtapi_print_msg(RTAPI_MSG_ERR,"names: \"%s\" too long\n", buf);
                    r = -EINVAL;
                    break;
                }
            }
        }
    }
    if(r) {
        hal_exit(comp_id);
    } else {
        hal_ready(comp_id);
    }
    return r;
}

void rtapi_app_exit(void) {
    hal_exit(comp_id);
}

#undef FUNCTION
#define FUNCTION(name) static void name(struct __comp_state *__comp_inst, long period)
#undef EXTRA_SETUP
#define EXTRA_SETUP() static int extra_setup(struct __comp_state *__comp_inst, char *prefix, long extra_arg)
#undef EXTRA_CLEANUP
#define EXTRA_CLEANUP() static void extra_cleanup(void)
#undef fperiod
#define fperiod (period * 1e-9)
#undef statusword
#define statusword (0+*__comp_inst->statusword)
#undef opmode_display
#define opmode_display (0+*__comp_inst->opmode_display)
#undef drv_actual_position
#define drv_actual_position (0+*__comp_inst->drv_actual_position)
#undef drv_actual_velocity
#define drv_actual_velocity (0+*__comp_inst->drv_actual_velocity)
#undef controlword
#define controlword (*__comp_inst->controlword)
#undef opmode
#define opmode (*__comp_inst->opmode)
#undef drv_target_position
#define drv_target_position (*__comp_inst->drv_target_position)
#undef drv_target_velocity
#define drv_target_velocity (*__comp_inst->drv_target_velocity)
#undef enable
#define enable (0+*__comp_inst->enable)
#undef pos_cmd
#define pos_cmd (0+*__comp_inst->pos_cmd)
#undef velocity_cmd
#define velocity_cmd (0+*__comp_inst->velocity_cmd)
#undef pos_fb
#define pos_fb (*__comp_inst->pos_fb)
#undef velocity_fb
#define velocity_fb (*__comp_inst->velocity_fb)
#undef drv_fault
#define drv_fault (*__comp_inst->drv_fault)
#undef home
#define home (*__comp_inst->home)
#undef stat_homed
#define stat_homed (*__comp_inst->stat_homed)
#undef stat_homing
#define stat_homing (*__comp_inst->stat_homing)
#undef stat_switchon_ready
#define stat_switchon_ready (*__comp_inst->stat_switchon_ready)
#undef stat_switched_on
#define stat_switched_on (*__comp_inst->stat_switched_on)
#undef stat_op_enabled
#define stat_op_enabled (*__comp_inst->stat_op_enabled)
#undef stat_voltage_enabled
#define stat_voltage_enabled (*__comp_inst->stat_voltage_enabled)
#undef stat_fault
#define stat_fault (*__comp_inst->stat_fault)
#undef stat_quick_stop
#define stat_quick_stop (*__comp_inst->stat_quick_stop)
#undef stat_switchon_disabled
#define stat_switchon_disabled (*__comp_inst->stat_switchon_disabled)
#undef stat_warning
#define stat_warning (*__comp_inst->stat_warning)
#undef stat_remote
#define stat_remote (*__comp_inst->stat_remote)
#undef stat_target_reached
#define stat_target_reached (*__comp_inst->stat_target_reached)
#undef opmode_no_mode
#define opmode_no_mode (*__comp_inst->opmode_no_mode)
#undef opmode_homing
#define opmode_homing (*__comp_inst->opmode_homing)
#undef opmode_cyclic_position
#define opmode_cyclic_position (*__comp_inst->opmode_cyclic_position)
#undef opmode_cyclic_velocity
#define opmode_cyclic_velocity (*__comp_inst->opmode_cyclic_velocity)
#undef fault_reset
#define fault_reset (0+*__comp_inst->fault_reset)
#undef pos_scale
#define pos_scale (__comp_inst->pos_scale)
#undef velo_scale
#define velo_scale (__comp_inst->velo_scale)
#undef auto_fault_reset
#define auto_fault_reset (__comp_inst->auto_fault_reset)
#undef csp_mode
#define csp_mode (__comp_inst->csp_mode)
#undef pos_scale_old
#define pos_scale_old (__comp_inst->pos_scale_old)
#undef velo_scale_old
#define velo_scale_old (__comp_inst->velo_scale_old)
#undef enable_old
#define enable_old (__comp_inst->enable_old)
#undef stat_homed_old
#define stat_homed_old (__comp_inst->stat_homed_old)
#undef stat_fault_old
#define stat_fault_old (__comp_inst->stat_fault_old)
#undef pos_scale_rcpt
#define pos_scale_rcpt (__comp_inst->pos_scale_rcpt)
#undef velo_scale_rcpt
#define velo_scale_rcpt (__comp_inst->velo_scale_rcpt)
#undef pos_mode
#define pos_mode (__comp_inst->pos_mode)
#undef init_pos_mode
#define init_pos_mode (__comp_inst->init_pos_mode)
#undef auto_fault_reset_delay
#define auto_fault_reset_delay (__comp_inst->auto_fault_reset_delay)


#line 162 "cia402.comp"

//constants
#define FAULT_AUTORESET_DELAY_NS 100000000LL
#define OPMODE_CYCLIC_POSITION 8
#define OPMODE_CYCLIC_VELOCITY 9
#define OPMODE_HOMING 6
#define OPMODE_NONE 0

EXTRA_SETUP ()  {
  // initialize variables
  pos_scale = 1.0;
  pos_scale_old = pos_scale + 1.0;
  pos_scale_rcpt = 1.0;
  velo_scale = 1.0;
  velo_scale_old = velo_scale + 1.0;
  velo_scale_rcpt = 1.0;
  auto_fault_reset = 1;
  csp_mode = 1; 
  return 0;
}

void check_scales(hal_float_t *scale,float *scale_old, double *scale_rcpt) {
  // check for change in scale value
  if (*scale != *scale_old) {
    // scale value has changed, test and update it
    if ((*scale < 1e-20) && (*scale > -1e-20)) {
      // value too small, divide by zero is a bad thing
      *scale = 1.0;
    }
    // save new scale to detect future changes
    *scale_old = *scale;
    // we actually want the reciprocal
    *scale_rcpt = 1.0 / *scale;
  }
}
  
FUNCTION(read_all) {

  // check for change in scale value
  check_scales(&pos_scale, &pos_scale_old, &pos_scale_rcpt);
  check_scales(&velo_scale, &velo_scale_old, &velo_scale_rcpt);

  // read position feedback
  pos_fb = ((double)drv_actual_position) * pos_scale_rcpt;

  // read velocity feedback
  velocity_fb = ((double)drv_actual_velocity) * velo_scale_rcpt;

  // read Modes of Operation
  opmode_no_mode = (opmode_display == OPMODE_NONE);
  opmode_homing = (opmode_display == OPMODE_HOMING);
  opmode_cyclic_velocity = (opmode_display == OPMODE_CYCLIC_VELOCITY);
  opmode_cyclic_position = (opmode_display == OPMODE_CYCLIC_POSITION);
 
  // read status
  stat_switchon_ready    = (statusword >> 0) & 1;
  stat_switched_on       = (statusword >> 1) & 1;
  stat_op_enabled        = (statusword >> 2) & 1;
  stat_fault             = (statusword >> 3) & 1;
  stat_voltage_enabled   = (statusword >> 4) & 1;
  stat_quick_stop        = (statusword >> 5) & 1;
  stat_switchon_disabled = (statusword >> 6) & 1;
  stat_warning           = (statusword >> 7) & 1;
  stat_remote            = (statusword >> 9) & 1;

  if (opmode_cyclic_position || opmode_cyclic_velocity) {
    stat_target_reached = (statusword >> 10) & 1;
    } else {
      stat_target_reached = 0;
    }

  //home states  
  if (opmode_homing) {
    stat_homed    = ((statusword >> 10) & 1) && ((statusword >> 12) & 1);
    stat_homing   = !stat_homed && !((statusword >> 10) & 1);
  }

  // update fault output
  if (auto_fault_reset_delay > 0) {
    auto_fault_reset_delay -= period;
    drv_fault = 0;
  } else {
    drv_fault = stat_fault && enable;
  }
}

FUNCTION(write_all) {
  int enable_edge;

  //init opmode
  if (!init_pos_mode) {
    pos_mode = csp_mode;
    init_pos_mode = 1;
  }

  // detect enable edge
  enable_edge = enable && !enable_old;
  enable_old = enable;

  // write control register
  controlword = (1 << 2); // quick stop is not supported
  if (stat_fault) {
    home = 0; 
    if (fault_reset) {
      controlword |= (1 << 7); // fault reset
    }
    if (auto_fault_reset && enable_edge) {
      auto_fault_reset_delay = FAULT_AUTORESET_DELAY_NS;
      controlword |= (1 << 7); // fault reset
    }
  } else {
    if (enable) {
      controlword |= (1 << 1); // enable voltage
      if (stat_switchon_ready) {
        controlword |= (1 << 0); // switch on
        if (stat_switched_on) {
          controlword |= (1 << 3); // enable op
        }
      }
    }
  }

  // write position command
  drv_target_position = (int32_t) (pos_cmd * pos_scale);
  // write velocity command
  drv_target_velocity = (int32_t) (velocity_cmd * velo_scale);

  // reset home command   
  if (home && (stat_homed && !stat_homed_old) && opmode_homing) {
    home = 0;
  }
  stat_homed_old = stat_homed;  

  // OP Mode 
  // set to position mode 
  if (stat_voltage_enabled && !home ) {
    opmode = OPMODE_CYCLIC_POSITION;
  }
  // set velo mode
  if (stat_voltage_enabled && !pos_mode && !home) {
    opmode = OPMODE_CYCLIC_VELOCITY;
  }
  // mode Home and start homing
  if (home) {
    opmode = OPMODE_HOMING;
    controlword |= (home << 4);  
  }
}


static int __comp_get_data_size(void) { return 0; }
