# 改进重建作业指导书
如果上下文压缩了，先重读 `AGENTS.md`，再读 `功能/需求真源索引.md` 和本文档当前缺口。
## 1. 作业目的

按 `功能/` owner 文档重建一条干净主干，不再把 `D:/v3` 作为继续修补的源码主线。源码放置遵守“哪边编译、编辑、验证方便就放哪边”的总原则，不人为规定所有源码必须在 Windows 或 VM 单侧闭包；但同一个源码域只能有一份真源，严禁 Windows/VM 双边镜像、互相同步副本或保留备用副本。当前分工是：Linux/native/LVGL/运行服务源码和项目工具真源放在 VM `/root/Desktop/v5`；Markdown 文档、人工查看截图和必须用 Windows/Vivado 编辑的 FPGA 工程真源放在 Windows `D:/v5`。任何新版本运行、编译、部署、测试或脚本引用都不得依赖 `D:/v3` 的源码路径。

最终产品目标：v5 不是只停留在“极简 UI”。最终必须在干净 v5 主干上恢复与 v3 一致的可操作 UI 布局和每个可见按键功能：主页面布局、坐标/刀路/速度/跟随误差/CPU 等显示区域、导航与运行控制、WCS/坐标/回零/归零、点动与视图、主轴/程序/MDI、设置页及其 `设置驱动` 等按钮，都要按 `功能/7主页面按钮功能.md`、`功能/4设置页设置驱动按钮作用和目的.md` 以及 `功能/需求真源索引.md` 指向的其它 owner 文档闭合。v3 代码和画面只能作为布局、视觉和按钮清单参考；当 v3 旧行为与当前 `功能/` owner 冲突时，以 owner 文档为准，不能为了“像 v3”恢复旧 Broker/Python/JSON/fallback/假入口或旧主线路径。

现有代码只作为素材库，允许搬运经过筛选的 UI 显示、VPS/auth/download、drive profile 和少量 native/linuxcncrsh 封装；旧 Broker/Python 运动中介、JSON 状态、SHM 泛状态、自造门禁、rotary 本地算法、program SHA sidecar 和测试假入口一律不搬。

最高需求来源：

1. `AGENTS.md`
2. `功能/需求真源索引.md`
3. `功能/0开机参数入内存.md`
4. `功能/共享内存.md`
5. `功能/7主页面按钮功能.md`
6. 其它 `功能/` owner 文档

本文是重建执行指导，不替代 `功能/` owner。实现中如果发现需求不清，先查 `功能/需求真源索引.md`，再读对应 owner。

## 2. 重建总原则

1. 源码按编译/编辑/验证便利性归属真源：哪边是主要工具链和验收环境，源码就放哪边；不得为了目录整齐强行跨环境放置源码。
2. 同一个源码域只能有一份真源：Linux/native/LVGL/运行服务源码不得在 Windows `D:/v5` 镜像；Vivado/FPGA 工程不得在 VM `/root/Desktop/v5` 镜像；临时迁移包、备份副本、同步副本和双边可编辑副本必须删除。
3. 新主干优先，不在旧主干上继续包兼容层。
4. 先建立最小可编译骨架，再逐步搬入允许复用块。
5. 搬旧代码必须先过白名单；不在白名单内的旧代码默认删除或不迁移。
6. 新主干只有一条控制路径：`UI C -> command gate -> native/linuxcncrsh/microkernel`。
7. 新主干只有一条显示投影路径：`microkernel/native memory -> State Publisher -> /dev/shm/v3_status_shm -> UI display`。
8. 运行热路径不得读盘、扫目录、懒导入、写 result JSON、等待 proof 文件、计算 program SHA 或通过 Broker/Python 运动中介。
9. VM/Linux 是 native、LinuxCNC、HAL、`/dev/shm`、socket、ARM/板端相关代码的主闭合环境；后续 Linux/native 编译以 `/root/Desktop/v5` 为准。
10. VM `/root/Desktop/v5` 是 Linux/native/LVGL/运行服务源码和编译运行真源，不是文档副本；VM 内禁止 Markdown 文档，禁止 Vivado/FPGA 工程镜像，`*.md`、`功能/`、`待做工作/`、`*.xpr`、`*.srcs/` 不进入 VM v5。
11. Windows `D:/v5` 当前只保留三类内容：Markdown 文档、人工查看截图，以及必须在 Windows/Vivado 环境中编辑和验证的 FPGA 工程源码。项目工具统一放 VM `/root/Desktop/v5/tools`；不得在 Windows 保留 `tools/` 真源或 Linux/native/LVGL/app/services/config/linuxcnc/third_party/build/artifacts 副本。
12. VM 缺少编译/验证工具时允许安装所需软件；优先使用不破坏系统包的用户级安装路径，安装后必须在本文记录工具名、版本、安装路径和后续命令使用方式。缺工具不是停止推进的理由。
13. Codex 重启后应优先使用 `v5_vm` MCP server 访问 VM；直接 SSH 仍保留为 fallback。`v5_vm` MCP 配置位于 `C:/Users/Administrator/.codex/config.toml`，server 脚本位于 `C:/Users/Administrator/.codex/mcp_servers/v5_vm_server.py`，目标为 `z20-vm:/root/Desktop/v5`。MCP 不得再从 Windows `D:/v5` 覆盖 VM 真源。
14. Codex 重启后应优先使用 `v5_board` MCP server 访问开发板；直接 SSH 仍保留为 fallback。`v5_board` MCP 配置位于 `C:/Users/Administrator/.codex/config.toml`，server 脚本位于 `C:/Users/Administrator/.codex/mcp_servers/v5_board_server.py`，目标为 Windows SSH config 中的 `z20-board`，当前解析到 `root@192.168.1.221:22`。可用工具预期为 `board_exec(command, timeout_sec)` 和 `board_diagnose(timeout_sec)`；新 MCP 需要重启 Codex/会话运行环境后才会出现在工具列表。
15. 本文后续历史状态中出现的 Windows `D:/v5` 上 Linux/native/LVGL 源码、项目工具或编译证据只表示迁移早期的 Windows 临时副本证据，不再是当前执行路径。当前 Linux/native/LVGL 代码、项目工具、编译、扫描和源码验收以 VM `/root/Desktop/v5` 为准；当前 Markdown 文档、人工查看截图与 Vivado/FPGA 工程修改以 Windows `D:/v5` 为准。
16. 从本条记录生效起，`D:/v3` 全部视为冻结只读旧工程。后续不得再修改、同步、审计写入、复制覆盖或把任何状态回写到 `D:/v3`；确需查阅时只能只读参考。v5 文档真源只在 Windows `D:/v5`，Linux/native/LVGL/运行服务源码真源只在 VM `/root/Desktop/v5`。

## 3. VM `/root/Desktop/v5` 独立目录闭包

目标：VM `/root/Desktop/v5` 能在 `D:/v3` 被删除后继续作为 Linux/native/LVGL/运行服务源码、项目工具和编译根目录使用；Windows `D:/v5` 能在 `D:/v3` 被删除后继续作为 Markdown 文档、人工查看截图和 Vivado/FPGA 工程根目录使用。

### 3.0 Windows `D:/v5` 文档、截图与 Vivado 工程目录

目标：Windows `D:/v5` 只作为 v5 文档目录、人工查看截图目录和 Windows/Vivado 工程目录，方便阅读修改需求/作业指导、查看板端截图，并编辑、综合、打开 Vivado/FPGA 工程；它不是 Linux/native/LVGL 源码副本目录，也不是项目工具真源目录。

允许：

1. `*.md`
2. 只包含 Markdown 的目录，例如 `功能/`、`待做工作/`。
3. Vivado/FPGA 工程源码目录，例如 `new-vivado/z20_v1_5_hw_project/` 与 `vivado_hw_project/`，允许 `.xpr`、`.srcs/`、`rtl/`、`ip_repo/`、`hls_manual_src/`、`constraints/`、`scripts/`、`sim/`、板端输入和工程说明。
4. 人工查看证据目录 `截图/`，用于保存从板端或 relay 拉回的截图和配套 metadata。

禁止：

1. Linux/native/LVGL/app/services/config/linuxcnc/tools/third_party 源码、配置、脚本和构建入口副本。
2. `app/`、`services/`、`config/`、`linuxcnc/`、`third_party/`、`tools/`、`build/`、`artifacts/` 等 VM runtime 源码、项目工具或产物目录。
3. 任何 VM `/root/Desktop/v5/tools` 的镜像副本、同步副本、临时备用副本或可编辑副本。
4. `.git`、缓存、日志、压缩包、传输包和 sidecar；`repo_ignored/` 只允许作为 Codex/验证临时输出或证据目录，不得承载源码真源、工具真源或可编辑备份。
5. Vivado 运行产物、缓存和导出产物，例如 `.Xil/`、`*.runs/`、`*.cache/`、`*.gen/`、`*.hw/`、`*.sim/`、`*.jou`、`*.log`、`*.str`、`*.bit`、`*.ltx`、`*.dcp`、`*.rpt`、`*.wdb`。
6. VM `/root/Desktop/v5` 已经持有真源的任何 Linux/native/LVGL/运行服务源码镜像。

检查只在阶段 0 首次建立、实际修改 Windows/Vivado 布局、怀疑引入旧路径依赖或用户要求复核时运行；不要把 Windows 全树扫描当成每轮固定动作。检查时跳过 `D:/v5/vm-v5` 手动备份和 `repo_ignored/` 临时证据目录，不读取或修改其中内容。

1. 在上述触发条件成立时，确认 Windows owner 范围内没有 `app/`、`services/`、`config/`、`linuxcnc/`、`third_party/`、`tools/`、`build/`、`artifacts/` 等 VM runtime-source 或项目工具镜像目录。
2. 在上述触发条件成立时，`*.xpr` 只指向当前保留的 Vivado 工程；Vivado 运行产物只在用户明确要求保留现场证据时保留。
3. VM `/root/Desktop/v5` 的 Markdown、Vivado 工程或 Windows 手动备份检查只在 VM source layout 变更、发现镜像风险或用户要求复核时运行；不得从 Windows `D:/v5` 覆盖 VM 真源。
4. `D:/v5/待做工作/改进重建.md` 是本文档真源；不得再为了过渡一致性修改或同步 `D:/v3/待做工作/改进重建.md`。

### 3.1 目录结构硬规则

VM `/root/Desktop/v5` 根目录必须清爽，只允许放项目入口文件和一级职责目录。源码、脚本、配置、构建、工具、第三方库、运行产物不得散落在根目录；Markdown 文档不得进入该目录树。

VM 根目录只允许：

1. `CMakeLists.txt`
2. `.gitignore`
3. 一级目录。

根目录禁止：

1. 业务 `.c/.h/.py/.sh/.json/.ngc/.bmp` 文件。
2. 临时截图、编译产物、日志、result、ready、heartbeat、sidecar。
3. `__pycache__`、`.pytest_cache`、`repo_ignored`、旧备份目录。
4. 从 v3 复制来的散文件。
5. 任何 `*.md` 文档。

标准目录树：

```text
VM /root/Desktop/v5
|-- CMakeLists.txt
|-- app/
|   |-- include/
|   |-- src/
|   |-- cmake/
|   |-- assets/
|   |   |-- fonts/
|   |   |-- images/
|   |   `-- icons/
|   `-- platform/
|-- services/
|   |-- command_gate/
|   |-- state_publisher/
|   |-- auth_download/
|   |-- microkernel/
|   `-- drive_profile/
|-- config/
|   |-- drive-profiles/
|   |   |-- public/
|   |   `-- private/
|   `-- hardware/
|-- linuxcnc/
|   |-- ini/
|   |-- hal/
|   `-- components/
|-- tools/
|-- third_party/
|-- build/
`-- artifacts/
```

目录职责：

| 目录 | 职责 | 禁止 |
| --- | --- | --- |
| `app/include` | UI 与共享 ABI 头文件 | Python、脚本、临时生成头。 |
| `app/src` | LVGL UI、显示、command client、只读 SHM reader | Broker/Python 中介、旧测试入口。 |
| `app/assets` | 字体、图片、图标 | 源码、状态文件、运行数据。 |
| `app/platform` | framebuffer、touch、remote input 等平台 I/O | 产品动作、运动门禁。 |
| `services/command_gate` | native/linuxcncrsh 唯一控制 gate | JSON Broker、fallback、旧 helper wrapper。 |
| `services/state_publisher` | native/microkernel -> SHM 显示投影 | 泛状态、动作结果、诊断总线。 |
| `services/auth_download` | VPS、DNA、授权、下载身份链 | 与授权无关的产品门禁。 |
| `services/microkernel` | native 状态/API、运行闭包 manifest、native gate registry | 上层重算运动真值。 |
| `services/drive_profile` | profile schema、下载后内存快照、命令映射 | 运行热路径读文件。 |
| `config/` | 静态配置素材和 profile map | result、ready、heartbeat、运行状态。 |
| `linuxcnc/` | INI/HAL/component 原始素材 | V3 上层门禁和脚本中介。 |
| `tools/` | VM 项目工具、构建、复制、扫描工具 | 产品运行依赖；不得建立 Windows `D:/v5/tools` 二次真源。 |
| `third_party/` | 第三方源码或头文件 | 自写业务代码。 |
| `build/` | 本地构建输出 | 源码和文档。 |
| `artifacts/` | 打包、VM 编译、交付产物 | 产品运行时真值。 |

目录执行规则：

1. 新文件必须先归类到上表目录；找不到目录时先修改本文，不得先丢根目录。
2. `build/` 和 `artifacts/` 可删除重建，不能被源码或文档引用为真源。
3. `tools/` 位于 VM `/root/Desktop/v5/tools/`，只能辅助构建/扫描/迁移；产品运行不得 import 或 exec `tools/` 里的逻辑，Windows `D:/v5` 不保留项目工具真源。
4. `services/*` 之间不得互相复制状态文件；跨服务共享只通过登记 ABI、UDS/API 或启动 RAM manifest。
5. 从 v3 搬来的文件必须在搬入时改到 v5 目录结构内，不能保持 v3 的散落根目录形态。

### 3.2 VM 源码闭包

必须保留在 VM `/root/Desktop/v5`：

1. 新主干构建入口所需的最小 `CMakeLists.txt`、toolchain、LVGL 配置、字体、图片和资源。
2. 允许复用清单中的源码素材。
3. VPS/auth/download 和 drive profile 所需 schema、profile map、授权下载配置。
4. 少量已验证 native/linuxcncrsh 封装和 LinuxCNC/HAL/INI 原始素材。
5. VM 编译和运行所需的脚本、配置、第三方库。

禁止进入 VM `/root/Desktop/v5` 作为新主干：

1. `.git`、旧构建目录、缓存目录、`__pycache__`、pytest 缓存、临时截图、过程产物。
2. `repo_ignored/` 下旧运行产物和备份。
3. 旧 Broker/Python 运动中介整包。
4. 旧 tests 假入口、mock gate、fake script。
5. 旧 VM/deploy 中退役 helper、退役 action、退役 JSON 状态映射。
6. program SHA sidecar、`.sha256.json` 兼容文件。
7. 任何需要从 `D:/v3` 或 Windows `D:/v5` 读取文件才能运行或编译的路径。
8. 任何 Markdown 文档，包括 `AGENTS.md`、`README.md`、`功能/**.md`、`待做工作/**.md`、架构说明文档。

VM 真源检查：

1. `rg "D:/v3|/mnt/d/v3|v3_program_identity|sha256[.]json|program_open_cc|start_run|pause_resume_program|wcs_switch|motion_stillness_gate|re_hal_select_helper|re_hal_safety_helper"`。
2. 命中的产品路径先删，不做兼容保留。
3. `find /root/Desktop/v5 -name '*.md'` 必须无结果。
4. 文档中历史说明可以保留在 Windows `D:/v3`，但必须明确为旧来源，不得作为 v5 执行路径。

## 4. 新主干必须包含

### 4.1 启动 RAM 闭包

目标：开机一次性把运行所需内容读入 RAM，运行期热路径只消费内存。

必须包含：

1. LinuxCNC/微内核运行闭包：executable、共享库、RTAPI、HAL component、kinematics component、INI/HAL、`PARAMETER_FILE`、`tool.tbl`、G53/RTCP 几何、WCS/G92 初始 readback、native gate 表。
2. UI 运行闭包：页面、字体、图片、布局、按钮表、命令表、资源索引。
3. 产品自写运行闭包：Command Broker 最小 gate、Actions/provider 登记表、drive profile 内存快照、VPS/auth/download 所需 schema。
4. 参数 owner 映射闭包：字段、唯一 owner、唯一 writer、readback 来源、generation/readback token。

禁止：

1. 运行期临时从硬盘发现脚本。
2. 运行期动态导入未登记 action。
3. 运行期读 `settings_runtime.json`、INI、`linuxcnc.var`、`tool.tbl`、result JSON、ready/heartbeat 文件补当前状态。
4. Start/Resume 时重读 G-code 文件或重建 runtime。

### 4.2 SHM 显示投影

目标：SHM 只服务 UI 显示。

允许字段：

1. 编码器反馈机械坐标。
2. 运动学理论坐标。
3. 轨迹动态显示点。
4. `runtime_modal_text`
5. `spindle_speed_rpm`
6. `linear_velocity_mm_per_min`
7. `feedrate_override`
8. `spindle_override`
9. frame envelope、epoch、CRC、valid bits。

禁止进入 SHM：

1. WCS/G92/tool/TLO。
2. RTCP actual。
3. active driver mode。
4. safety/stillness/busy/idle。
5. 动作结果、保存证明、诊断 JSON。
6. CPU/PS IP/network 状态。
7. rotary/wcheckpoint/rebase。
8. 参数表稳定显示。

### 4.3 UI command gate

目标：UI 只发请求，不拥有 native 真值。

必须支持：

1. Start/Pause/Resume：C UI 直达 native/linuxcncrsh gate。
2. Program Open：读取程序文件，生成运行内存副本、静态刀路模型、runtime metadata；Start 不再读文件。
3. WCS/Work Zero/G92：走登记 native/linuxcncrsh gate，并由 native/interpreter readback 更新 UI。
4. RTCP：只通过 native actual provider 判定状态，通过 native/linuxcncrsh gate 下发开关。
5. Emergency stop：直达微内核/native safety owner。

禁止：

1. `program_open_cc`
2. `start_run`
3. `pause_resume_program`
4. `first_point` blocked 占位。
5. `wcs_switch` Broker/Python 中介。
6. UI 手写 JSON 给旧 Broker。

### 4.4 State Publisher

目标：常驻进程从 native/microkernel 内存采样，30ms 单向写 SHM 显示投影。

必须：

1. 启动阶段加载到 RAM。
2. 不在 steady-state fork helper。
3. 不读 result JSON、状态快照、硬盘配置补帧。
4. 状态源缺失时发布 degraded/unavailable，不用默认值或旧缓存伪造 fresh。

### 4.5 Program Open/Start

目标：程序打开和程序启动分工清楚。

Program Open 负责：

1. 文件选择。
2. 一次性读取 G-code。
3. 生成运行内存副本。
4. 生成静态刀路模型。
5. 记录当前 runtime metadata。

Start/Resume 负责：

1. 消费 Program Open 生成的内存副本。
2. 直达 native/linuxcncrsh gate。
3. 不读程序文件。
4. 不计算 SHA。
5. 不过滤 G-code。
6. 不等待 proof/result 文件。

## 5. 旧代码允许搬运清单

只允许从旧代码搬下列内容，搬运时必须去掉旧状态源、旧 gate 和旧测试假入口。

### 5.1 LVGL 页面布局和控件样式

可搬：

1. 页面尺寸、控件位置、颜色、字体、按钮布局。
2. 主页面、设置页、程序页、MDI 页、网络页、弹窗层等纯 UI 结构。
3. 无业务判断的绘制工具函数。

不可搬：

1. 按钮背后的旧 Broker/Python action 分支。
2. popup 拒绝提示替代实现。
3. UI pending visual 冒充 native 成功。
4. 第二套 BUS/Pulse 页面或旧 fallback UI。

### 5.2 坐标/刀路纯显示代码

可搬：

1. 坐标面板绘制。
2. 刀路静态显示。
3. SHM 坐标/轨迹投影消费。
4. 纯显示的缩放、平移、视角切换。

不可搬：

1. RTCP 五轴运动学本地计算。
2. rotary equivalent、unwrap、range guard、wcheckpoint/rebase。
3. UI A/C 相位包角。
4. WCS/G92/tool/RTCP actual 通过 SHM 或 UI 缓存补真源。
5. UI 刷新时按路径重读 G-code 文件。

### 5.3 VPS/auth/download

可搬：

1. Device DNA。
2. 授权下载。
3. private/public profile 下载策略。
4. OTA/package 身份链。
5. 签名、hash、授权 fail-closed。

不可搬：

1. 普通 identity 白名单。
2. 与 VPS 授权无关的产品门禁。
3. 授权 result JSON 作为 UI 稳定状态。
4. 下载动作绕过 native/drive owner。

### 5.4 drive profile schema/download/auth

可搬：

1. `driver_profile_map.json` schema。
2. public/private profile 下载。
3. profile 签名/hash 验证。
4. 标准 `drive.*` 命令映射。
5. drive-only settings 私域字段。

不可搬：

1. 动作热路径读 profile 文件。
2. 厂家 SDO 硬编码绕过 profile。
3. active mode 从 UI/SHM/旧 JSON 推断。
4. Pulse 在硬件未闭环前 runtime selectable。
5. drive result JSON 作为参数表稳定显示。

### 5.5 native/linuxcncrsh 封装

可搬：

1. 已验证的 linuxcncrsh UDS/framed command 封装。
2. 已登记 native gate wrapper。
3. 最小 request/response framing。
4. native readback provider 薄封装。

不可搬：

1. 未登记 helper。
2. `re_hal_select_helper`、`re_hal_safety_helper` 或其替代壳。
3. Python 直接 `linuxcnc.command()` 做产品控制。
4. env-gated fallback。
5. rename-only wrapper。

## 6. 旧代码一律不搬清单

以下旧路径不允许进入新主干：

1. 旧 Broker/Python 运动中介。
2. JSON 状态文件、result 文件、ready/heartbeat 文件作为运行事实。
3. SHM 泛状态池。
4. `motion_stillness_gate` 和所有上层速度/停稳门禁。
5. rotary 本地算法：equivalent、unwrap、range guard、wcheckpoint/rebase。
6. program SHA sidecar、`v3_program_identity.py`、`.sha256.json` 兼容逻辑。
7. `program_open_cc`、`start_run`、`pause_resume_program`、`wcs_switch`。
8. `first_point` 空实现或 blocked 占位。
9. 测试假模块、fake script、mock gate 作为产品路径。
10. `settings_runtime.json` 中的非 drive-only 字段。
11. `config_cache` 稳定显示路径。
12. UI dirty/default 值冒充保存成功。
13. VM/deploy/CMake 中的退役 helper 映射。

## 7. 当前 v3 代码现状盘点

本盘点用于制定 `D:/v5` 重建计划，不表示这些旧路径可以整包复制。

### 7.1 当前工作树状态

1. `D:/v5` 当前为空，应作为新版本独立根目录从阶段 0 开始建立。
2. `D:/v3/lvgl_app/src` 仍保留完整旧 UI、命令、状态、SHM、参数表和刀路实现，适合作为 UI 视觉和纯显示素材，不适合作为新主干直接继承。
3. `D:/v3/lvgl_app/scripts` 仍保留旧 Broker、product action、settings apply、状态快照、generic SHM、State Publisher、驱动和 VPS 脚本；新主干不得整包复制，只能按白名单抽取 VPS/auth/download、drive profile schema/download/auth 和少量 linuxcncrsh/native wrapper。
4. `D:/v3/config/drive-profiles`、`D:/v3/config/hardware_profile.json`、`D:/v3/lvgl_app/linuxcnc`、`D:/v3/fonts` 是新主干可参考素材；复制后必须消除 `D:/v3` 绝对路径和旧 runtime 依赖。
5. `D:/v3/待做工作/cpu.md` 当前在工作树中显示为删除状态，属于本计划外变更；实施 v5 重建时不得恢复或改动无关文件。

### 7.2 可作为素材的 v3 文件组

| 用途 | v3 素材来源 | v5 处理方式 |
| --- | --- | --- |
| LVGL 构建骨架 | `lvgl_app/CMakeLists.txt`、`lvgl_app/cmake/re_v3_lvgl_sources.cmake`、`lvgl_app/lv_conf.h` | 只抽取构建方法，重写 v5 源文件清单；不得继承旧 target、旧 helper、旧 test target。 |
| 字体和图标 | `lvgl_app/src/lv_font_zh_16.c`、`lvgl_app/src/v3_reb_icons.c/h`、`fonts/NotoSansCJKsc-Regular.otf` | 可复制，改名或保留均可；只作为资源，不承载业务状态。 |
| 页面布局 | `lvgl_app/src/v3_page_*.c`、`v3_ui_shell.c`、`v3_ui_widgets.c`、`v3_ui_keyboard.c`、`v3_ui_popup.c`、`v3_ui_press_feedback.c` | 只搬控件层级、样式、键盘和弹窗外壳；删除按钮背后的旧 Broker/action 分支和报错替代实现。 |
| 平台输入显示 | `v3_product_platform*.c/h`、触摸校准相关文件 | 只搬 framebuffer、触摸、远程输入的底层 I/O；不得搬旧自动化假入口或旧路径镜像。 |
| 坐标显示 | `v3_widget_coordinates*` | 只保留坐标面板绘制、格式化、选择和刷新；数据源改为 v5 SHM display snapshot。 |
| 刀路显示 | `v3_ui_toolpath_*` | 只保留静态显示、缩放、视角、overlay、label、screen fit；解析层只允许服务 Program Open 的静态预览，不得做 RTCP 五轴变换、rotary unwrap、G-code filter 或 Start 门禁。 |
| SHM 参考 | `include/v3_status_shm.h`、`src/v3_status_shm.c`、`scripts/v3_state_shm.py` | 只参考 SeqLock/CRC/布局写法；v5 重新定义白名单 ABI，不复制泛状态字段。 |
| native/linuxcncrsh | `v3_product_native_run.c/h`、`v3_product_native_controls.c`、`scripts/re-v3-8ax-backend-linuxcncrsh.inc.sh` | 只抽取已验证的 framed/UDS/linuxcncrsh 封装；所有 command 必须重新登记到 v5 command gate。 |
| VPS/auth/download | `device_auth_latch.py`、`device_dna_register_auth.py`、`device_vps_identity.py`、`v3_device_authorization_download.py`、`drive_profile_download_*.py`、`v3_drive_profile_download.py` | 可按授权链搬入 `v5/services/auth_download`；删除普通 identity 白名单和与授权无关的产品门禁。 |
| drive profile | `config/drive-profiles/**`、`drive_bus_command_mapping.py`、`drive_bus_profiles.py`、`drive_bus_profile_store.py` | 可搬 schema、profile map、命令映射和下载安装；运行热路径只消费开机内存快照。 |
| LinuxCNC/Pulse 素材 | `lvgl_app/linuxcnc/**` | 只作为 INI/HAL 原始素材；active mode、motion truth 和 safety 仍归 LinuxCNC/微内核/native。 |

### 7.3 不进入 v5 的 v3 文件组

1. `lvgl_app/scripts/broker_*.py` 整包不搬。
2. `lvgl_app/scripts/product_actions/**` 整包不搬。
3. `lvgl_app/scripts/product_handler_*.py` 不搬。
4. `v3_command_broker.py`、旧 `broker_dispatch`、旧 `broker_routes`、旧 `broker_jobs` 不搬。
5. `product_actions_stillness_gate.py`、`settings_apply_motion_stillness_gate.py` 和任何 `motion_stillness_gate` 配置不搬。
6. `product_actions_status_snapshot.py`、`v3_state_shm.py` 泛状态和旧 JSON 状态路径不搬。
7. rotary 本地 filter、equivalent、range guard、wcheckpoint 类脚本不搬；若当前文件已删除，也不得在 v5 恢复。
8. `v3_program_identity.py`、`.sha256.json`、program SHA sidecar 兼容逻辑不搬。
9. `tests/` 下旧 helper 契约、mock gate、fake script 和旧 action contract 不搬。
10. `repo_ignored/`、`.pytest_cache`、`__pycache__`、旧截图、旧 result、旧构建产物不搬。

## 8. 详细实施计划

### 8.0 阶段 0：建立 v5 独立真源落点

目标：先让新版本有独立落点。当前已退役“所有实现都在 `D:/v5` 内闭合”的早期口径；Linux/native/LVGL/运行服务源码和项目工具真源在 VM `/root/Desktop/v5`，Windows `D:/v5` 只承载 Markdown 文档、人工查看截图和 Windows/Vivado 工程。

执行：

1. Windows `D:/v5` 只建立文档、截图、Vivado/FPGA 工程和必要 ignored 输出目录。
2. VM `/root/Desktop/v5` 建立 `app/`、`services/`、`config/`、`linuxcnc/`、`tools/`、`third_party/`、`build/`、`artifacts/` 等 Linux/native/LVGL/运行服务源码和构建目录。
3. 从旧工程搬入的素材只能一次性落到对应 owner 真源位置，改成 v5 自有命名和路径；不得保留运行时 `D:/v3` 依赖。
4. 不复制 `.git`、旧测试、旧 Broker、旧 product actions、旧缓存和旧构建产物。
5. 建立根目录说明时只写 v5 根目录说明、构建入口和禁止运行时回读 `D:/v3`。

验收：

1. 阶段 0 首次建立或后续实际修改 Windows 目录布局时，确认 Windows `D:/v5` 不把 VM runtime-source 镜像目录作为真源：`app/`、`services/`、`config/`、`linuxcnc/`、`third_party/`、`build/`、`lvgl_app/`。
2. 只有在修改 Windows/Vivado 工程、截图证据整理脚本或怀疑引入旧路径依赖时，才对相关 Windows 文件做 `D:/v3|/mnt/d/v3` 定向扫描；不把全目录重复扫描作为每轮固定动作。
3. VM `/root/Desktop/v5` 不含 Markdown 文档、Vivado 工程和 Windows 侧手动备份。
4. 文档路径均为 v5 相对路径，不能要求从 v3 读取文件。

### 8.1 阶段 1：最小可编译 LVGL 骨架

目标：先编译一个空功能但真实 LVGL shell，后续每搬一块都能本地编译。

新建/修改：

1. VM `/root/Desktop/v5/app/CMakeLists.txt`
2. VM `/root/Desktop/v5/app/cmake/v5_lvgl_sources.cmake`
3. VM `/root/Desktop/v5/app/include/v5_app.h`
4. VM `/root/Desktop/v5/app/src/main.c`
5. VM `/root/Desktop/v5/app/src/v5_ui_shell.c`
6. VM `/root/Desktop/v5/app/src/v5_ui_model.c`
7. VM `/root/Desktop/v5/app/src/v5_platform_*.c/h`

从 v3 只参考：

1. `lvgl_app/CMakeLists.txt`
2. `lvgl_app/cmake/re_v3_lvgl_sources.cmake`
3. `lvgl_app/src/main.c`
4. `lvgl_app/src/v3_product_platform*.c/h`

不得带入：

1. 旧 `v3_command_broker`。
2. 旧 Python action。
3. 旧 SHM 泛状态。
4. 旧 tests target。

验收：

1. VM runtime 源码在 `/root/Desktop/v5` 配置通过；Windows/Vivado owner 只跑对应 Windows/Vivado 工具检查。
2. VM runtime 源码在 `/root/Desktop/v5` 构建通过；Windows/Vivado owner 只跑对应 Windows/Vivado 工具检查。

### 8.2 阶段 2：启动 RAM 闭包

目标：开机一次性加载 UI、命令表、资源、drive profile 快照、native gate 表和 Program Open 索引；运行期热路径不扫目录、不懒导入、不读旧配置补状态。

新建/修改：

1. VM `/root/Desktop/v5/app/include/v5_boot_closure.h`
2. VM `/root/Desktop/v5/app/src/v5_boot_closure.c`
3. VM `/root/Desktop/v5/app/include/v5_runtime_registry.h`
4. VM `/root/Desktop/v5/app/src/v5_runtime_registry.c`
5. VM `/root/Desktop/v5/services/command_gate/v5_command_table.*`
6. VM `/root/Desktop/v5/services/auth_download/v5_drive_profile_snapshot.*`

闭包必须包含：

1. UI 页面、字体、图标、按钮表、命令表和资源索引。
2. LinuxCNC/微内核运行闭包清单：INI/HAL、RTAPI、kinematics、`PARAMETER_FILE`、`tool.tbl`、G53/RTCP 几何和 native gate 表。
3. drive profile 内存快照和下载授权 schema。
4. 参数 owner 映射表。

不得实现：

1. 运行期脚本发现。
2. 动态 import action。
3. 从 `settings_runtime.json`、INI、var、tool 文件、result JSON、ready/heartbeat 补当前状态。

验收：

1. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
2. 搜索 `importlib`、`scan`、`settings_runtime.json`、`result.json`、`ready`、`heartbeat`，产品热路径无命中。

### 8.3 阶段 3：SHM 显示 ABI 与 State Publisher

目标：只建立 UI 显示投影，字段严格等于 `功能/共享内存.md` 白名单。

新建/修改：

1. VM `/root/Desktop/v5/app/include/v5_status_shm.h`
2. VM `/root/Desktop/v5/app/src/v5_status_shm_reader.c`
3. VM `/root/Desktop/v5/services/state_publisher/v5_status_shm_writer.c`
4. VM `/root/Desktop/v5/services/state_publisher/v5_state_publisher.c`
5. VM `/root/Desktop/v5/services/state_publisher/CMakeLists.txt`

允许字段：

1. 编码器反馈机械坐标。
2. 运动学理论坐标。
3. 轨迹动态显示点。
4. `runtime_modal_text`
5. `spindle_speed_rpm`
6. `linear_velocity_mm_per_min`
7. `feedrate_override`
8. `spindle_override`
9. frame envelope、epoch、CRC、valid bits。

从 v3 只参考：

1. `include/v3_status_shm.h` 的 ABI 写法。
2. `src/v3_status_shm.c` 的读写保护方式。
3. `scripts/v3_state_publisher.py` 只作为“旧路径反例”和字段来源盘点，不复制产品逻辑。

必须删除/不搬：

1. WCS/G92/tool/TLO。
2. RTCP actual。
3. active driver mode。
4. safety/stillness/busy/idle。
5. 动作结果、保存证明、诊断 JSON。
6. CPU/PS IP/network 状态。
7. rotary/wcheckpoint/rebase。
8. 参数表稳定显示。

验收：

1. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
2. VM 部分编译 State Publisher 通过。
3. 搜索确认禁止字段不在 `v5_status_shm.h` 和 writer/reader 中出现。

### 8.4 阶段 4：UI 页面布局和控件样式迁移

目标：搬视觉，不搬旧动作。

新建/修改：

1. VM `/root/Desktop/v5/app/src/v5_page_main.c`
2. VM `/root/Desktop/v5/app/src/v5_page_program.c`
3. VM `/root/Desktop/v5/app/src/v5_page_settings.c`
4. VM `/root/Desktop/v5/app/src/v5_page_mdi.c`
5. VM `/root/Desktop/v5/app/src/v5_page_network.c`
6. VM `/root/Desktop/v5/app/src/v5_ui_widgets.c`
7. VM `/root/Desktop/v5/app/src/v5_ui_keyboard.c`
8. VM `/root/Desktop/v5/app/src/v5_ui_popup.c`

从 v3 只参考：

1. `v3_page_*.c`
2. `v3_ui_widgets.c`
3. `v3_ui_keyboard.c`
4. `v3_ui_popup.c`
5. `v3_ui_press_feedback.c`

搬运规则：

1. 只搬控件层级、颜色、字体、坐标、键盘、弹窗 UI 外壳。
2. 所有按钮回调改接 v5 command gate 占位函数。
3. 所有旧 Broker request、JSON request、Python action 名称、blocked popup 占位直接删除。

验收：

1. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
2. 搜索 `broker`、`program_open_cc`、`start_run`、`pause_resume_program`、`wcs_switch` 不命中 v5 UI 产品路径。

### 8.4A 基于当前代码现状的 UI 恢复实施计划

目标：把当前 VM `/root/Desktop/v5` 已能显示和触摸的过渡 `v5_main_page`，推进为与 v3 同等可操作 UI 布局和每个可见按键功能。需求真源仍是 `功能/需求真源索引.md` 指向的 owner，主页面按 `功能/7主页面按钮功能.md`，设置页 `设置驱动` 按 `功能/4设置页设置驱动按钮作用和目的.md`；本文只记录实施切片和验收顺序，不复制按钮表。

当前代码基线：

1. VM 已有 `app/src/v5_main_page.c/h`、`v5_main_page_actions.c/h`、`v5_ui_shell.c`、`v5_coordinate_panel.c/h`、`v5_toolpath_display.c/h`、`v5_lvgl_touch_input.c/h`、`v5_lvgl_remote_display.c/h`。
2. 当前 `v5_main_page` 是单页面过渡实现：`1024x600` root，坐标/模态/速度倍率/简单刀路线条和 28 个按钮；按钮覆盖 Start、Pause、Resume、Home、Estop、Estop Reset、G54-G59.3、Work Zero X、G92 Clear、RTCP On/Off、Feed/Spindle 100%、X1/X10/X100、XY/XZ/YZ/3D。
3. 当前没有完整 v3 同等页面骨架：VM `app/src` 尚无 `v5_page_settings.c`、`v5_page_program.c`、`v5_page_mdi.c`、`v5_page_network.c`、`v5_ui_keyboard.c`、`v5_ui_popup.c` 等页面/控件 owner。
4. 当前设置驱动只有后端/owner 准备路径，例如 `services/command_gate/v5_settings_apply.c/h`、drive profile 和 native gate registry；没有 v3 同等设置页表格、按钮和 `设置驱动` UI 流程。
5. 当前真实触摸、remote relay、UI event 日志和非运动上板 acceptance 已证明显示/输入链路可用；这只能作为继续恢复 UI 的底座，不是最终 UI 完成。

实施切片：

1. 空壳 UI 总框架切片：先恢复 v3 同等页面布局、控件层级、颜色、字体、间距、中文资源和全部可见按钮位置；按钮只允许 press feedback、页面内选中态和 `implemented=false/layout_only` 诊断事件，不调用 command gate、native/linuxcncrsh、Broker、文件读写、继电器、触摸模拟或运动命令。该阶段验收只声明“layout_shell 可显示/可触摸”，不得声明任何按钮功能已完成。
2. 主页面 v3 布局骨架切片：从 v3 只参考 `v3_page_main.c`、`v3_widget_coordinates*`、`v3_ui_toolpath_*`、`v3_ui_model.c` 的可见布局、控件层级、颜色、字体、间距和按钮清单；在 VM 真源拆出 `v5_page_main_layout.*`、`v5_main_button_table.*` 或等价小 owner，保持每个自写业务/control-flow 文件不超过 500 行。不得搬旧 Broker/Python/JSON/action handler。
3. 主页面显示切片：把 v3 同等坐标大字、刀路区、速度面板、跟随误差面板和 CPU/诊断面板恢复到 v5 布局；坐标/模态/转速/进给/倍率只消费当前 SHM 白名单和 native/provider owner，CPU/网络等非 SHM 字段没有 provider 时显示 degraded，不从 JSON/ready/heartbeat 补真值。
4. 主页面按钮清单切片：按 `功能/7主页面按钮功能.md` 对齐 v3 可见按钮和 action 名称，补齐当前 28 个按钮之外的导航、程序、MDI、首点、点动正负、坐标点选影响、网络/IO/探测/偏置/刀补等入口；本切片仍保持按钮无功能，只做 layout-only 事件和 AI 模拟屏幕点击命中证明，不能用 popup/error/disabled 分支冒充已实现。
5. 主页面按钮功能逐步接回切片：在空壳 UI 已上板显示并通过 AI 模拟屏幕点击命中后，按 owner 分组把 `ui_local`、`native gate command` 或明确 blocker 逐个接回；已具备 native/linuxcncrsh gate 的 Start/Pause/Resume/Home/Estop/WCS/Work Zero/G92/RTCP/override 继续接 canonical v5 command，尚未具备 native owner 的运动按钮只记录 blocker 和删除条件，不恢复旧 `start_run`、`pause_resume_program`、`program_open_cc` 直绑、旧 helper 或 product action。
6. 程序页/MDI 空壳到功能切片：先新增 v5 程序页和 MDI 页空壳，恢复 v3 的文件列表、选中/二次触摸打开、MDI 输入体验和返回主页面流程但不打开文件；随后再接 Program Open，且 Program Open 只生成运行内存副本和静态预览，Start 热路径仍只消费 runtime handle，不读文件、不算 SHA、不等待 proof。
7. 设置页空壳到功能切片：先新增 v5 设置页基础导航、轴参数表、BUS/Pulse 模式显示和保存/返回/保存并重启入口空壳，按钮不写参数；随后逐项接 owner 允许的运行内存/参数 owner，不把 UI 表格 dirty、JSON 或 SHM 投影变成参数真值。
8. 设置驱动逐步接回切片：按 `功能/4设置页设置驱动按钮作用和目的.md` 先显示 `服务器下载`、`扫描从站`、`复位驱动`、`读取驱动`、`清除故障`、`设置驱动` 等按钮但保持 layout-only；再按 BUS 模式可用、Pulse 模式三层拒绝、长流程进度、读回一致和 backend restart pending 逐项接回，不得恢复 `settings_calc_bit`、`drive_calculate_bit` 或旧微动计算 bit 产品路径。
9. 共用 UI 组件切片：按 v3 视觉恢复键盘、弹窗外壳、顶部短提示、按钮 press feedback、页面导航 shell 和中文字体资源；弹窗只作为 owner 允许的提示/长流程反馈，运动动作失败不得恢复旧全屏 popup 分支。
10. 空壳模拟点击验收切片：空壳 UI 上板后，由 AI 使用 canonical remote-input 或模拟屏幕点击对主页面、设置页、程序页关键按钮逐个点击；每次点击前确认截图和目标，点击后保存截图和 UI event delta，证明每个按钮命中正确 layout-only action；该证据不等于功能完成。
11. 单按钮功能验收切片：每接回一个可见按钮或按钮组，先跑 VM focused build/smoke，再通过 AI 模拟屏幕点击和 owner 要求的最小后端证据验收；触摸校准、真实触摸硬件健康、运动能力、Start/MDI 执行和 owner 明确要求真人触摸的路径仍不能用模拟点击替代。运动能力按钮只做到非运动证据和 blocker，必须等 `V5_ALLOW_MOTION=1` 才能跑 golden。
12. 最终 UI 回归切片：所有页面和按钮组 local/VM gates 通过后，执行 full current VM_SOURCE_ROOT build、非运动 board acceptance、AI 模拟点击抽样覆盖主页面/设置页/程序页关键按钮，再在显式允许真实运动后执行 golden `cc.ngc` motion closure；未跑 motion 时只能写 `local_verified_only` 或非运动 `board_verified`，不能写整机完成。

每个切片固定禁止项：

1. 不恢复旧 Broker/Python/JSON/product action 中介、旧 helper wrapper、fallback、兼容环境变量、测试假入口或旧路径镜像。
2. 不把 v3 旧行为直接提升为需求；v3 只作布局、视觉和按钮清单参考，冲突时以 `功能/` owner 为准。
3. 不在 Windows `D:/v5` 新增 runtime 源码或项目工具；UI 源码和工具实现都在 VM `/root/Desktop/v5`。
4. UI 空壳和非运动按钮命中由 AI 使用模拟屏幕点击或 canonical remote-input 自行推进，并保存前后截图和 UI event delta；模拟点击不得替代触摸校准、真实触摸硬件健康、Start/MDI、运动闭环或 owner 明确要求真人触摸的验收。截图异常修工具链，不写新分辨率规则。
5. 空壳阶段不得把按钮无功能实现成弹窗拒绝、后端失败、假成功或隐藏 fallback；只能诚实记录 layout-only 未实现状态。

### 8.5 阶段 5：坐标和刀路纯显示迁移

目标：保留坐标面板和刀路显示能力，但所有运动/旋转/RTCP 计算交给微内核/native。

新建/修改：

1. VM `/root/Desktop/v5/app/src/v5_widget_coordinates*.c/h`
2. VM `/root/Desktop/v5/app/src/v5_toolpath_view*.c/h`
3. VM `/root/Desktop/v5/app/src/v5_toolpath_scene*.c/h`
4. VM `/root/Desktop/v5/app/src/v5_toolpath_projection*.c/h`
5. VM `/root/Desktop/v5/app/src/v5_program_preview.c`

从 v3 只参考：

1. `v3_widget_coordinates*`
2. `v3_ui_toolpath_view*`
3. `v3_ui_toolpath_scene*`
4. `v3_ui_toolpath_projection*`
5. `v3_ui_toolpath_screen_fit*`
6. `v3_ui_toolpath_labels*`

必须删除/不搬：

1. RTCP 五轴几何本地变换。
2. rotary equivalent/unwrap/range guard/wcheckpoint/rebase。
3. UI A/C 相位包角作为真值。
4. Start 时全文件扫描、G-code filter、运行副本改写。

实现口径：

1. Program Open 一次性读取 G-code 文本到内存。
2. 静态刀路模型只用于 UI 预览。
3. 动态轨迹点只消费 SHM 白名单轨迹字段。
4. 微内核/native fail-closed reason 可以显示，但不通过 SHM 泛状态传递。

验收：

1. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
2. 搜索 `rotary_equivalent`、`unwrap`、`wcheckpoint`、`range_guard`、`filter`，产品执行路径无命中。

### 8.6 阶段 6：Program Open/Start 分工

目标：Open 读文件并建内存模型，Start/Resume 只消费内存副本并直达 native/linuxcncrsh。

新建/修改：

1. VM `/root/Desktop/v5/app/include/v5_program_runtime.h`
2. VM `/root/Desktop/v5/app/src/v5_program_open.c`
3. VM `/root/Desktop/v5/app/src/v5_program_runtime.c`
4. VM `/root/Desktop/v5/app/src/v5_command_program.c`
5. VM `/root/Desktop/v5/app/src/v5_command_start.c`

执行：

1. Program Open 负责文件选择、一次性读取、内存副本、静态预览模型和 runtime metadata。
2. Start/Resume 只检查当前内存 program handle 和 native/motion gate O(1) 状态。
3. Start/Resume 不读文件、不算 SHA、不生成 sidecar、不过滤 G-code、不等待 proof/result 文件。

必须删除/不搬：

1. `v3_program_identity.py`
2. `.sha256.json`
3. `remote_ui_relay_programs.py` 中 sidecar 兼容逻辑。
4. `actions_program_open.py`、`actions_program_load.py`、`broker_action_handlers_program.py`

验收：

1. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
2. 搜索 `sha256`、`.sha256.json`、`v3_program_identity`、`program_open_cc`、`start_run`，v5 产品路径无命中。

### 8.7 阶段 7：UI command gate

目标：替代旧 Broker/Python 运动中介，形成唯一控制路径。

新建/修改：

1. VM `/root/Desktop/v5/services/command_gate/v5_command_gate.c/h`
2. VM `/root/Desktop/v5/services/command_gate/v5_linuxcncrsh_client.c/h`
3. VM `/root/Desktop/v5/services/command_gate/v5_native_readback.c/h`
4. VM `/root/Desktop/v5/app/src/v5_command_start.c`
5. VM `/root/Desktop/v5/app/src/v5_command_motion.c`
6. VM `/root/Desktop/v5/app/src/v5_command_wcs.c`
7. VM `/root/Desktop/v5/app/src/v5_command_rtcp.c`
8. VM `/root/Desktop/v5/app/src/v5_command_safety.c`
9. VM `/root/Desktop/v5/app/src/v5_command_override.c`

必须支持：

1. Start/Pause/Resume 直达 native/linuxcncrsh。
2. WCS/Work Zero/G92 走登记 native gate，并由 native/interpreter readback。
3. RTCP 通过 native actual provider 判断状态，通过 native/linuxcncrsh gate 下发。
4. Emergency stop 直达微内核/native safety owner。
5. Feed/Spindle override 走 native owner，显示投影只从 SHM 白名单读取。

不得实现：

1. Python `linuxcnc.command()` 产品控制。
2. 旧 Broker JSON request。
3. 上层速度/停稳门禁。
4. popup 报错替代功能。
5. env-gated fallback。

验收：

1. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
2. VM 部分编译 command gate 通过。
3. 搜索 `linuxcnc.command`、`broker`、`re_v3_json`、`motion_stillness_gate`，v5 产品路径无命中。

### 8.8 阶段 8：参数表和设置保存

目标：参数表只显示/保存 owner 字段；微内核/native 已有或可拥有的字段不在 V5 再造参数真源。

新建/修改：

1. VM `/root/Desktop/v5/app/include/v5_parameter_table.h`
2. VM `/root/Desktop/v5/app/src/v5_parameter_table.c`
3. VM `/root/Desktop/v5/services/command_gate/v5_parameter_owner_map.c/h`
4. VM `/root/Desktop/v5/services/command_gate/v5_settings_apply.c/h`

从 v3 只参考：

1. `v3_parameter_table.c/h` 的表结构。
2. settings apply 脚本只作为字段盘点，不复制旧 Python flow。

实现口径：

1. 参数 owner 映射开机加载入 RAM。
2. 设置保存写唯一 owner 后直接 readback。
3. `settings_runtime.json` 只保留 drive-only 私域。
4. 参数稳定显示不得读 SHM、JSON、`config_cache`、dirty、本地默认值或 result 文件。

验收：

1. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
2. 搜索 `settings_runtime.json`，只有 drive-only/auth/download 非热路径允许。
3. 搜索 `config_cache`、`dirty saved`、`read_result`，v5 参数稳定显示路径无命中。

### 8.9 阶段 9：VPS/auth/download 与 drive profile

目标：保留授权链、下载链和驱动 profile schema，不让其成为运动门禁或状态真源。

新建/修改：

1. VM `/root/Desktop/v5/services/auth_download/device_auth_latch.py`
2. VM `/root/Desktop/v5/services/auth_download/device_dna_register_auth.py`
3. VM `/root/Desktop/v5/services/auth_download/device_vps_identity.py`
4. VM `/root/Desktop/v5/services/auth_download/v5_device_authorization_download.py`
5. VM `/root/Desktop/v5/services/auth_download/drive_profile_download_*.py`
6. VM `/root/Desktop/v5/config/drive-profiles/public/driver_profile_map.json`
7. VM `/root/Desktop/v5/config/drive-profiles/private/..._driver_profile_map.json`

从 v3 只搬：

1. VPS/DNA/授权下载。
2. profile 下载、签名/hash、安装和端点。
3. drive command mapping schema。

删除/不搬：

1. 普通 identity 白名单。
2. 与 VPS 授权无关的产品门禁。
3. drive result JSON 作为 UI 稳定状态。
4. 动作热路径读 profile 文件。

验收：

1. Python 语法检查通过。
2. 本地脚本入口 `--help` 通过。
3. 搜索确认授权 result JSON 不参与 UI 稳定显示或运动准入。

### 8.10 阶段 10：LinuxCNC/微内核/native 运行闭包

目标：把 LinuxCNC/微内核运行所需文件和 native 状态/API 列入启动 RAM 闭包。

新建/修改：

1. VM `/root/Desktop/v5/linuxcnc/...`
2. VM `/root/Desktop/v5/services/microkernel/v5_microkernel_manifest.*`
3. VM `/root/Desktop/v5/services/microkernel/v5_native_status_api.*`
4. VM `/root/Desktop/v5/services/microkernel/v5_native_gate_registry.*`

执行：

1. 复制并清洗 `lvgl_app/linuxcnc` 原始素材。
2. 登记 native gate：Start/Pause/Resume、WCS/Work Zero/G92、RTCP、estop/reset、override、参数 apply。
3. 登记 native readback：WCS/G92/tool/TLO/RTCP actual、mode switch actual、rotary logical/window/generation、safety/motion owner。
4. 所有 native actual 不进 SHM，除共享内存文档白名单显示字段外全部 direct API/UDS readback。

验收：

1. VM 部分编译通过。
2. 搜索确认 RTCP actual、WCS/G92、rotary/wcheckpoint、安全状态没有写入 SHM。

### 8.11 阶段 11：旧路径强扫描和删除闭合

目标：让 v5 没有旧主干、旧中介、旧兼容和旧假入口。

必须扫描：

```powershell
rg "D:/v3|/mnt/d/v3|broker_|v3_command_broker|product_actions|product_handler_|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|motion_stillness_gate|re_hal_select_helper|re_hal_safety_helper|v3_program_identity|sha256[.]json|settings_runtime[.]json|config_cache|result[.]json|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard|re_v3_json|linuxcnc[.]command" /root/Desktop/v5/app /root/Desktop/v5/services /root/Desktop/v5/config /root/Desktop/v5/linuxcnc /root/Desktop/v5/CMakeLists.txt
```

处理规则：

1. 命中产品路径直接删除或改为唯一 canonical v5 owner。
2. 命中文档历史说明必须写明“旧 v3 反例/禁止项”，不得作为执行步骤。
3. 命中测试假入口直接删除，不用 mock gate 修绿。

验收：

1. 扫描结果无产品路径命中。
2. 按 owner 真源运行对应构建通过：VM runtime 用 VM CMake；Windows/Vivado owner 用 Windows/Vivado 工具。
3. VM 部分编译通过。

### 8.12 阶段 12：全量编译和交付前收口

目标：确认 v5 可脱离 v3 编译，并记录未闭合的 VM/板端问题。

执行：

1. 在 VM `/root/Desktop/v5` 跑 Linux/native/LVGL/runtime-service 和项目工具的对应检查；Windows `D:/v5` 只跑文档、截图证据和 Windows/Vivado owner 的对应检查。
2. 对 LinuxCNC/native/ARM 相关部分跑 VM 部分编译。
3. 若多个 native/ARM 片段互相依赖，跑 VM 全量编译。
4. 搜索 `D:/v3` 绝对路径依赖。
5. 更新本文当前状态。
6. 无法本地或 VM 闭合的问题写入 `D:/v5/待做工作/遗留.md`。

验收：

1. VM `/root/Desktop/v5` runtime 构建和项目工具检查通过；Windows `D:/v5` 文档、截图证据和 Windows/Vivado owner 检查通过。
2. VM runtime 可编译部分通过；不可闭合的板端/运动依赖记录到遗留。
3. `D:/v5` 不依赖 `D:/v3`。

## 9. 每阶段固定收尾

每阶段必须按顺序收尾：

1. `rg` 扫描本阶段禁止项。
2. 删除扫描出来的旧路径，不做 rename-only 保留。
3. 按 owner 真源跑对应构建；VM runtime 和项目工具用 VM `/root/Desktop/v5` 检查，Windows owner 只用文档、截图证据和 Windows/Vivado 检查。
4. 涉及 LinuxCNC/native/ARM 时跑 VM 部分编译。
5. 多片段互相依赖时跑 VM 全量编译。
6. 更新本文当前状态。
7. 无法闭合的 native/微内核缺口写入 `D:/v5/待做工作/遗留.md`；不得再为了过渡一致性写回 `D:/v3`。

## 10. 当前状态

当前事实：

1. Windows `D:/v5` 只保留 Markdown 文档、人工查看截图和 Windows/Vivado 工程真源；没有 Linux/native/LVGL/运行服务源码副本，没有 `app/`、`services/`、`config/`、`linuxcnc/`、`third_party/`、`tools/`、`build/` 等 VM runtime-source 或项目工具镜像目录。
2. VM `/root/Desktop/v5` 是 Linux/native/LVGL/运行服务源码真源；Markdown 文档、Vivado 工程、Windows 手动备份、`*.sha256.json` 和 `.pytest_cache` 不得作为 VM 源码真源。Python `__pycache__` 等生成缓存若由工具运行产生，只能视为可清理缓存，不能作为源码、证据或验收真源。
3. Codex 应优先通过 `v5_vm` MCP 直接操作 VM 真源，通过 `v5_board` MCP 直接操作开发板；`vm_sync_v5` 已禁用，不得从 Windows 覆盖 VM 真源。
4. `D:/v5/待做工作/改进重建.md` 是本文档真源；`D:/v3/待做工作/改进重建.md` 只可作为历史旧文档参考，不再同步或保持一致。

已闭合阶段：

1. 阶段 0-2：VM v5 根目录、LVGL 最小 shell、启动 RAM 闭包、command table、drive profile snapshot 已建立。
2. 阶段 3-4：SHM 显示 ABI、State Publisher 最小服务、UI SHM 被动读取模型已建立；SHM 字段只包含坐标/轨迹、模态、转速、进给速度、主轴倍率、进给倍率和 frame envelope。
3. 阶段 5-7：command gate、linuxcncrsh typed command、Program Open/Start、Pause/Resume、WCS/Work Zero/G92、RTCP、estop、Feed/Spindle override wrapper 已建立；不走 Python Broker、helper、JSON 或上层停稳/速度门禁。
4. 阶段 8：参数表 owner map 和 settings apply 边界已建立；参数表不从 SHM、JSON、`config_cache`、dirty/default 或 result 文件造稳定显示。
5. 阶段 9：VPS/auth/download 和 drive profile 下载/授权边界已保留并收敛到 v5 命名；该域内 result JSON 只属于授权/下载诊断，不进入运动准入或 UI 稳定状态。
6. 阶段 10：微内核 manifest、native gate registry、native status/readback API 已建立；native actual readback 不写入 SHM。
7. 阶段 11：旧 Broker/Helper/JSON/假入口/rotary 本地算法/旧 v3 命名已完成强扫描和清理；drive profile 顶层 release rollback 已改为 `supported=false`。
8. 阶段 12：VM 独立全量编译通过；Windows `D:/v5` 不承担 Linux/native 本地 CMake 编译，因为源码真源只在 VM。
9. 阶段 13：State Publisher 已建立 POSIX mmap 写入路径，支持向 `/dev/shm/v3_status_shm` 发布一次帧，也支持 30ms 固定周期 loop；smoke 使用临时 `/dev/shm/v5_status_shm_smoke` 路径验证写入、读回和删除，不污染板端实际 SHM 文件。
10. 阶段 14：UI model 已建立 SHM 被动刷新入口 `v5_ui_model_refresh_status_from_shm()`；`v5_ui_shell` 启动时从默认 `/dev/shm/v3_status_shm` 读取一次显示投影，读取不到时保持无有效显示字段，不用默认值、JSON、Broker 或旧缓存伪造 fresh。
11. 阶段 15：坐标面板和刀路显示模型已建立：`v5_coordinate_panel.*` 只从 `V5UiStatusView` 生成 MCS、commanded/theory、跟随误差、模态、转速、进给速度和倍率文本；`v5_toolpath_display.*` 只从 `V5UiStatusView` 的 SHM 轨迹点、MCS 点和 commanded/theory 点生成屏幕显示点，不搬旧 G-code filter、RTCP、WCS/G92、rotary、wcheckpoint 或本地运动学算法。
12. 阶段 16：LVGL 主页面显示控件已建立：`v5_main_page.*` 创建坐标标签、速度/倍率标签、模态标签、刀路线条和 MCS/CMD 显示标记；控件只应用 `V5CoordinatePanelSnapshot` 与 `V5ToolpathDisplaySnapshot`。`v5_lvgl_headless.*` 仅提供 VM smoke 用的 headless display driver，不是产品状态源。
13. 阶段 17：LVGL 主页面 typed command 按钮第一段已建立：`v5_main_page_actions.*` 定义 12 个非 Start 页面动作并只调用现有 typed command wrapper；`v5_main_page.*` 创建 Pause、Resume、Estop、Estop Reset、G54、G55、Zero X、G92 Clear、RTCP On/Off、Feed 100%、Spindle 100% 按钮并保存最后一次 typed action report。该阶段不生成 JSON、不调用 Broker/helper、不走旧 Python action、不引入停稳/速度门禁。
14. 阶段 18：Program Open/Start 页面侧内存 handle 连接已建立：`v5_main_page.*` 持有 `V5ProgramController` 指针，`v5_main_page_open_program()` 只把 G-code 一次读入 Program Runtime 内存副本；Start 按钮只从当前 Program Runtime 准备 typed `V5_COMMAND_START`，并由 command gate 归属到 `native_linuxcncrsh`。`v5_ui_shell` 持有常驻 ProgramController 并绑定到主页面；该阶段不生成 program SHA、sidecar、JSON、Broker request 或旧 Python action。
15. 阶段 19：State Publisher daemon lifecycle 已建立：新增产品入口 `v5_state_publisher`，默认向 `/dev/shm/v3_status_shm` 以 30ms 周期循环发布，支持 `--once`、`--frames N`、`--path PATH`、`--interval-ms 30`；服务 loop 支持 SIGTERM/SIGINT 请求停止。当前已收敛为 init.d 生命周期；安装统一走 `tools/deploy/install_v5_runtime.sh`，目标为 `/usr/libexec/8ax/v5_state_publisher`，由 `/etc/init.d/v5-state-publisher` 管理启动/重启/停止。
16. 阶段 20：板端部署准备已建立：新增 `config/deploy/v5_runtime_deploy_manifest.tsv` 和 `tools/deploy/install_v5_runtime.sh`，dry-run 会逐项列出 `v5_lvgl_shell`、`v5_state_publisher`、`v5_linuxcncrsh_probe`、State Publisher init.d 脚本、LinuxCNC ini/hal/component contract、硬件 profile 与 drive profile 的源路径、板端目标路径和权限。LinuxCNC 冷备素材已从 `v3_pulse.ini/hal` 改为 `v5_pulse.ini/hal`，microkernel manifest 与硬件 profile 已改指向 v5 路径；未被构建引用且内部仍保留 `re_v3` 命名的旧 `v5_reb_icons.*` 已删除。`v3_step_ip_hw` 和 `/dev/shm/v3_status_shm` 仍保留，因为前者是当前 HAL 组件合约名，后者是既定 SHM ABI 路径。
17. 阶段 21：板端安装前只读检查入口已建立：新增 `tools/deploy/precheck_v5_board.sh`，默认只校验 VM 真源部署清单和源文件；设置 `V5_BOARD_SSH` 后只读检查板端 SSH、`install`、目标目录 `/usr/libexec`、`/etc/init.d`、`/opt`、`/dev/shm` 和当前相关进程。新增 `tools/deploy/push_v5_runtime_to_board.sh`，默认只 dry-run 远端部署包；真正执行必须显式 `--apply` 且设置 `V5_BOARD_SSH`，并复用同一份 manifest 和 `install_v5_runtime.sh`，不增加第二套安装逻辑。State Publisher 专用安装脚本已删除，统一到 `tools/deploy/install_v5_runtime.sh`。
18. 阶段 22：板端安装后单功能验收入口已建立：新增 `tools/deploy/verify_v5_board_runtime.sh`，要求 `V5_BOARD_SSH` 后只读/低风险检查板端已部署文件、`/etc/init.d/v5-state-publisher status` 状态、State Publisher 一次性 SHM 写入、默认 SHM 文件、`v5_lvgl_shell` 启动输出、linuxcncrsh 端口可达性、截图工具可用性、输入事件设备和相关进程。该脚本不发送运动命令、不触发触控、不运行 `cc.ngc`，只为后续单功能板端截图/触控验证提供固定入口。
19. 阶段 23：linuxcncrsh 只读协议探测已建立：新增 `v5_linuxcncrsh_probe` 二进制，使用 canonical `v5_linuxcncrsh_client` 与 linuxcncrsh 建立握手，只发送 `Get Machine` 与 `Quit`，以返回 `MACHINE ON/OFF` 作为协议可读证明；不发送 `Set`、MDI、Start、Pause、WCS、RTCP 或任何运动命令。部署清单和远端打包脚本已加入 `/usr/libexec/8ax/v5_linuxcncrsh_probe`；`verify_v5_board_runtime.sh` 已从端口探测改为调用该二进制做真实只读协议探测。
20. 阶段 24：板端验收串联入口和 golden `cc.ngc` 已建立：新增 VM 真源 `tools/deploy/run_v5_board_acceptance.sh`，dry-run 只打印 precheck、deploy、verify 和 golden 程序路径；真正执行必须显式 `--apply` 且设置 `V5_BOARD_SSH`。新增 `gcode/golden/cc.ngc`，内容为 LinuxCNC 原生 G-code，不做本地 RTCP、旋转展开、SHA sidecar 或格式转换。带 `--motion` 的 golden 运动闭环仍要求 `V5_ALLOW_MOTION=1` 和可用板端后再进入下一段，不在本地或 dry-run 中伪造通过。
21. 阶段 25：golden `cc.ngc` 命令发送工具已建立：新增 VM 真源 `services/command_gate/v5_linuxcncrsh_golden_run.c` 和目标 `v5_linuxcncrsh_golden_run`，只复用 canonical `v5_command_gate_prepare()`、`v5_linuxcncrsh_format_line()`、`v5_linuxcncrsh_send_line()`。它对指定板端程序路径发送 `Set Open <path>`；只有传入 `--start` 且环境变量 `V5_ALLOW_MOTION=1` 时才发送 `Set Run 0`。部署清单把该二进制安装到 `/usr/libexec/8ax/v5_linuxcncrsh_golden_run`；`run_v5_board_acceptance.sh --apply --motion` 在完成 precheck、deploy、verify、golden 上传和只读 probe 后调用该二进制，不再保留“命令发送尚未绑定”的提示分支。
22. 阶段 26：部署打包单源化已建立：`tools/deploy/push_v5_runtime_to_board.sh` 不再维护第二份硬编码 tar 文件列表；`--apply` 打包时从同一份 `config/deploy/v5_runtime_deploy_manifest.tsv` 读取所有部署源文件，并额外加入 `install_v5_runtime.sh` 与 manifest 自身。这样新增 `v5_linuxcncrsh_golden_run` 后不会出现 manifest 已列出但真实远端包漏文件的分叉。
23. 阶段 27：板端 UI 截图单步验收入口已建立：新增 VM 真源 `tools/deploy/capture_v5_board_ui.sh`，默认 dry-run 只打印目标板、远端截图路径、本地拉回路径和执行命令；真正截图必须显式 `--apply` 且设置 `V5_BOARD_SSH`。脚本只尝试 `fbgrab`、`grim`、`weston-screenshot` 或 `import -window root` 捕获一帧并 scp 拉回本地，不发送 touch/key/mouse，不发送 motion、linuxcncrsh、MDI 或 Start 命令。`verify_v5_board_runtime.sh` 同步增加 `/usr/libexec/8ax/v5_linuxcncrsh_golden_run` 安装检查，避免部署清单与板端验收盲区分叉。
24. 阶段 28：acceptance 固定截图顺序已建立：`tools/deploy/run_v5_board_acceptance.sh --apply` 的固定顺序现在是 precheck、deploy、verify、capture，截图步骤调用 `capture_v5_board_ui.sh --apply`，仍不发送输入和运动。golden `cc.ngc` 只在追加 `--motion` 且 `V5_ALLOW_MOTION=1` 时进入上传、只读 probe、`Set Open`、`Set Run 0` 流程；普通 `--apply` 不再停在“后续另跑截图”的手工分叉。
25. 阶段 29：板端 SSH 只读诊断入口已建立：新增 VM 真源 `tools/deploy/diagnose_v5_board_ssh.sh`，支持 `--host`、`--port` 或 `V5_BOARD_SSH`，只做解析、ping、tcp 22 和 `ssh true` 检查；不部署、不截图、不发送输入、不访问 linuxcncrsh、不发运动命令。该脚本用于把板端不可达拆成网络、端口、SSH batch login 三层证据，避免只保留 precheck 的笼统失败。
26. 阶段 30：UI 截图证据默认落点已收敛：`tools/deploy/capture_v5_board_ui.sh` 的本地截图默认路径从覆盖式 `/tmp/v5_board_ui.png` 改为 VM 真源下的 ignored 输出目录 `artifacts/board_ui/v5_board_ui_<UTC>.png`。`artifacts/` 已在 VM `.gitignore` 中声明为本地输出；dry-run 不创建该目录，真实截图时才创建父目录并保留带时间戳的 UI 帧证据。
27. 阶段 31：板端 SSH 端口配置已统一：`precheck_v5_board.sh`、`push_v5_runtime_to_board.sh`、`verify_v5_board_runtime.sh`、`capture_v5_board_ui.sh`、`run_v5_board_acceptance.sh` 和 `diagnose_v5_board_ssh.sh` 统一支持 `V5_BOARD_SSH_PORT`；所有 SSH 调用使用 `-p`，所有 SCP 调用使用 `-P`，串联脚本会把端口继续传给下游 precheck、deploy、verify、capture 和 motion 步骤。未设置时默认 22，不新增第二套板端入口或本地临时源码副本。
28. 阶段 32：VPS/auth/download 兼容 fallback 命名已清理：VM 真源 `services/auth_download/drive_profile_download_endpoints.py` 与 `v5_device_dna_register.py` 删除 `fallback_urls/fallbacks` endpoint 配置兼容键，仅保留 owner 允许的主/备域名键；`drive_profile_download_transport.py`、`drive_profile_download_install.py`、`drive_profile_download_auth.py` 和 C 侧 `parse_u32` 默认值参数已从 `fallback*` 命名改为 artifact/default/detail 语义。该阶段不改变 VPS 授权/DNA/challenge/signature/hash 主链路，不新增下载兜底入口。
29. 阶段 33：VPS/auth/download 板端部署闭环已补入单源 manifest：`config/deploy/v5_runtime_deploy_manifest.tsv` 现在安装 `v5_device_dna_register.py`、`v5_device_authorization_download.py`、`v5_drive_profile_download.py` 到 `/usr/libexec/8ax/auth_download/` 且模式为 `0755`，并安装同目录下所有运行依赖模块为 `0644`。`verify_v5_board_runtime.sh` 同步检查三入口和关键模块已安装，避免 VM 源码有 auth/download 但板端部署包漏文件。VM 真源的 auth/download 源文件权限已收敛为入口 `0755`、模块和 smoke `0644`；验证生成的 `__pycache__` 已删除。
30. 阶段 34：State Publisher 板端安装生命周期已收紧：`tools/deploy/install_v5_runtime.sh --apply` 在安装 manifest 后执行 `/etc/init.d/v5-state-publisher restart`，确保部署后长驻服务立即进入当前版本。`tools/deploy/verify_v5_board_runtime.sh` 不再把 init.d 状态异常作为 warning 放过，而是把 running 状态作为安装后验收失败/通过条件之一。
31. 阶段 35：板端 verify 硬验收继续收紧：`tools/deploy/verify_v5_board_runtime.sh` 现在把默认 `/dev/shm/v3_status_shm` 存在、`v5_linuxcncrsh_probe` 只读 machine probe 成功、截图工具可用都作为失败/通过条件；不再以 warning 继续进入后续 acceptance。触控 event 设备仍保留 warning，因为触控脚本按当前作业顺序必须在真实截图证据之后再补。
32. 阶段 36：真实手指触控证据采集入口已建立：新增 VM 真源 `tools/deploy/collect_v5_board_touch_evidence.sh`。该脚本不发送 touch/key/mouse、不访问 linuxcncrsh、不发 MDI 或运动命令；`--apply` 必须提供 `V5_BOARD_SSH` 和已有截图证据 `V5_UI_SCREENSHOT_EVIDENCE` 或 `--screenshot`，先校验 `/opt/8ax/safe_ui/re_touch_calibration.json`，确认旧 `/opt/8ax/ui/re_touch_calibration.json` 不存在，再启用触摸诊断并等待操作者真实手指点击，最后把 `/run/8ax_v5_product_ui/touch_events.jsonl` 拉回 VM ignored 输出目录。`run_v5_board_acceptance.sh` dry-run 和普通 `--apply` 完成提示现在显式指向该触控证据采集步骤，但不自动执行，避免无人值守时伪造输入或卡死。
33. 阶段 37：开发板 MCP 入口已建立：新增 Codex 本机 MCP server 脚本 `C:/Users/Administrator/.codex/mcp_servers/v5_board_server.py`，并在 `C:/Users/Administrator/.codex/config.toml` 注册 `[mcp_servers.v5_board]`，目标为 Windows SSH config 中的 `z20-board`。该入口只通过 SSH 调用真实开发板，不承载源码副本；当前提供 `board_exec` 和 `board_diagnose` 两个工具。新 MCP 需要重启 Codex/会话运行环境后生效。

当前验证证据：

1. VM 执行 `cmake --build /root/Desktop/v5/build/linux` 通过。
2. VM smoke 通过：`v5_microkernel_manifest_smoke`、`v5_command_wrappers_smoke`、`v5_parameter_table_smoke`、`v5_program_command_smoke`、`v5_state_publisher_smoke`、`v5_lvgl_shell`。
3. Python 入口通过：`v5_device_authorization_download.py --help`、`v5_device_dna_register.py --help`、`v5_drive_profile_download.py --help`、`v5_auth_download_boundary_smoke.py`。
4. VM 产品源码扫描无命中：`D:/v3|/mnt/d/v3|broker_|v3_command_broker|product_actions|product_handler_|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|motion_stillness_gate|re_hal_select_helper|re_hal_safety_helper|v3_program_identity|sha256[.]json|settings_runtime[.]json|config_cache|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard|re_v3_json|linuxcnc[.]command`。
5. SHM 泄漏扫描无命中：RTCP actual、WCS/G92、tool/TLO、safety、motion owner、rotary/wcheckpoint/rebase 未写入 SHM ABI、reader 或 State Publisher。
6. Windows `D:/v5` 非 md 文件对 `D:/v3` 和 `/mnt/d/v3` 无命中；`D:/v5` 没有 Linux/native 源码副本。
7. `D:/v5/待做工作/改进重建.md` 是当前工作文档真源；`D:/v3/待做工作/改进重建.md` 不再作为同步目标或当前证据来源。
8. State Publisher mmap 验证通过：`v5_state_publisher_smoke --path /dev/shm/v5_status_shm_smoke --frames 1 --unlink-after` 通过；`--frames 3 --interval-ms 30 --unlink-after` 通过；输出 `valid_mask=0x000000f8`、`flags=0x00000001`、`bytes=936`，并确认临时 shm 文件已删除。
9. UI SHM 被动刷新验证通过：`v5_ui_shm_refresh_smoke` 先发布临时 SHM，再由 `V5UiModel` mmap 读回，输出 `valid_mask=0x000000f8`、`flags=0x00000001`、`modal=UNAVAILABLE`；短暂向默认 `/dev/shm/v3_status_shm` 发布一帧后运行 `v5_lvgl_shell`，输出 `status_refresh=1 status_mask=0x000000f8 status_flags=0x00000001`，随后已删除默认 SHM 文件。
10. UI 显示模型验证通过：`v5_ui_display_models_smoke` 输出 `axis=X mcs=+00001.250 err=+00000.250 modal=G0 G17 G54 points=3 feed=110.0%`；同轮 `v5_microkernel_manifest_smoke`、`v5_command_wrappers_smoke`、`v5_parameter_table_smoke`、`v5_program_command_smoke`、`v5_ui_shm_refresh_smoke`、`v5_state_publisher_smoke` 和 `v5_lvgl_shell` 均通过。
11. LVGL 主页面控件验证通过：`v5_main_page_smoke` 输出 `mcs=+00002.500 modal=G0 G17 points=3 feed=100.0%`；`v5_lvgl_shell` 输出 `main_page=1 applied=1`，证明 shell 启动已创建主页面控件并应用当前 `V5UiStatusView`。同轮旧路径扫描和 SHM 禁止字段扫描无命中。
12. LVGL 主页面非 Start typed command 按钮验证已并入当前 `v5_main_page_actions_smoke`；Pause/Resume、Estop、WCS、Work Zero、G92、RTCP 和倍率按钮仍只走 typed command wrapper。针对新增按钮路径扫描 `json|broker|helper|product_actions|product_handler|re_v3_json|linuxcnc[.]command|motion_stillness_gate|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|sha256[.]json|settings_runtime[.]json|config_cache|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard` 无命中。
13. Program Open/Start 页面侧内存 handle 验证通过：`v5_main_page_actions_smoke` 输出 `buttons=13 open_generation=1 start=start last=spindle_override_set owner=native_linuxcncrsh`；同轮 `v5_main_page_smoke` 与 `v5_lvgl_shell` 通过。VM 默认构建 `cmake --build /root/Desktop/v5/build/linux` 通过；新增页面/Start 路径扫描 `json|broker|helper|product_actions|product_handler|re_v3_json|linuxcnc[.]command|motion_stillness_gate|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|sha256[.]json|settings_runtime[.]json|config_cache|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard` 无命中；smoke 临时 G-code 和临时 SHM 文件未残留。
14. State Publisher daemon lifecycle 验证通过：`v5_state_publisher --path /dev/shm/v5_state_publisher_once --once` 输出 `frames=1 interval_ms=30 valid_mask=0x000000f8 flags=0x00000001`；`--frames 3 --interval-ms 30` 输出 `frames=3 interval_ms=30`；后台启动后发送 SIGTERM 可正常退出并输出 `frames=5 interval_ms=30`。VM 默认构建通过；`services/state_publisher` 旧路径扫描 `json|broker|helper|product_actions|product_handler|re_v3_json|linuxcnc[.]command|motion_stillness_gate|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|sha256[.]json|settings_runtime[.]json|config_cache|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard` 无命中；主链路 smoke 批跑通过，临时 SHM 与临时 G-code 未残留。
15. 板端部署准备验证通过：`tools/deploy/install_v5_runtime.sh` dry-run 当前列出 10 个部署项，源文件均存在；VM 默认构建通过；`v3_pulse|re_v3_linuxcncrsh|re_v3_init|rollback path|lvgl_app/linuxcnc|D:/v3|/mnt/d/v3|broker_|v3_command_broker|product_actions|product_handler_|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|motion_stillness_gate|re_hal_select_helper|re_hal_safety_helper|v3_program_identity|sha256[.]json|settings_runtime[.]json|config_cache|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard|re_v3_json|linuxcnc[.]command` 扫描无命中；`v5_reb_icons|re_v3_reb_icon` 扫描无命中；`v5_microkernel_manifest_smoke`、`v5_state_publisher --once`、`v5_state_publisher_smoke`、`v5_command_wrappers_smoke`、`v5_program_command_smoke`、`v5_main_page_actions_smoke`、`v5_lvgl_shell` 均通过，临时 SHM 与临时 G-code 未残留。
19. MCP 直连 VM 真源验证通过：`v5_vm` MCP 已直接读取 `/root/Desktop/v5`；默认全量构建通过并输出 `ninja: no work to do`。`tools/deploy/run_v5_board_acceptance.sh` dry-run 通过；`--apply` 未设置 `V5_BOARD_SSH` 时按预期拒绝并输出 `V5_BOARD_SSH is required`。同轮 `v5_linuxcncrsh_probe --help`、`v5_main_page_actions_smoke`、`tools/deploy/precheck_v5_board.sh`、`tools/deploy/push_v5_runtime_to_board.sh` 通过；旧路径扫描 `install_v5_state_publisher|v3_pulse|re_v3_linuxcncrsh|re_v3_init|D:[\\/]|/mnt/d/v3|D:/v3|broker_|v3_command_broker|product_actions|product_handler_|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|motion_stillness_gate|re_hal_select_helper|re_hal_safety_helper|v3_program_identity|sha256[.]json|settings_runtime[.]json|config_cache|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard|re_v3_json|linuxcnc[.]command|v5_reb_icons|re_v3_reb_icon` 在 `app services config linuxcnc tools CMakeLists.txt gcode` 中无命中。
20. golden 命令发送工具验证通过：显式构建 `v5_linuxcncrsh_golden_run` 通过；VM 默认全量构建通过。`v5_linuxcncrsh_golden_run --help` 通过；`--program /tmp/v5_golden/cc.ngc --start` 未设置 `V5_ALLOW_MOTION=1` 时按预期拒绝并输出 `V5_ALLOW_MOTION=1 is required for --start`。`tools/deploy/install_v5_runtime.sh` dry-run 当前列出 11 个部署项并包含 `v5_linuxcncrsh_golden_run`；`tools/deploy/run_v5_board_acceptance.sh` dry-run 仍只打印命令，`--apply` 未设置 `V5_BOARD_SSH` 时仍拒绝。`v5_command_wrappers_smoke`、`v5_program_command_smoke`、`v5_main_page_actions_smoke`、`v5_linuxcncrsh_probe --help`、precheck dry-run、push dry-run 均通过；旧路径和旧“未绑定发送器”提示扫描在 `app services config linuxcnc tools CMakeLists.txt gcode` 中无命中。
21. 部署打包单源化验证通过：按 `v5_runtime_deploy_manifest.tsv` 生成本地测试 tar，包内包含 `build/linux/app/v5_linuxcncrsh_golden_run`、`v5_linuxcncrsh_probe`、`v5_lvgl_shell`、`v5_state_publisher`、init.d scripts、LinuxCNC ini/hal/component contract、硬件 profile、drive profile、`tools/deploy/install_v5_runtime.sh` 和 manifest 自身。`tools/deploy/push_v5_runtime_to_board.sh` dry-run 通过；`--apply` 未设置 `V5_BOARD_SSH` 时仍拒绝。VM 默认全量构建通过，`v5_command_wrappers_smoke`、`v5_program_command_smoke`、`v5_main_page_actions_smoke`、`v5_linuxcncrsh_golden_run --help`、`install_v5_runtime.sh` dry-run、`precheck_v5_board.sh` dry-run、`run_v5_board_acceptance.sh` dry-run 通过；旧路径扫描无命中。
22. 板端 UI 截图入口验证通过：VM 默认全量构建通过；`tools/deploy/capture_v5_board_ui.sh --help` 通过，dry-run 输出 `input: no touch, key, mouse, or motion command is sent`；`--apply` 未设置 `V5_BOARD_SSH` 时按预期拒绝并输出 `V5_BOARD_SSH is required for --apply`。`verify_v5_board_runtime.sh` 未设置 `V5_BOARD_SSH` 时仍拒绝，脚本内已包含 `v5_linuxcncrsh_golden_run installed executable` 检查。`install_v5_runtime.sh` dry-run、`push_v5_runtime_to_board.sh` dry-run 通过；旧路径和 fake/simulated click 扫描在 `app services config linuxcnc tools CMakeLists.txt gcode` 中无命中。
23. acceptance 截图串联验证通过：`tools/deploy/run_v5_board_acceptance.sh` dry-run 当前列出 precheck、deploy、verify、capture、golden 和 motion 命令；`--apply` 未设置 `V5_BOARD_SSH` 时仍拒绝。`capture_v5_board_ui.sh` dry-run 仍声明不发送 touch/key/mouse/motion。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke` 通过；旧路径和 fake/simulated click 扫描在 `app services config linuxcnc tools CMakeLists.txt gcode` 中无命中。`V5_REMOTE_STAGING_ROOT` 仅在 `push_v5_runtime_to_board.sh` 中作为合法远端 staging 配置存在，不属于旧路径。
24. 文档单源审计通过：在 `D:/v3` 执行 `python tools/v3_doc_single_source_audit.py --repo-root . --json-out repo_ignored/doc_single_source/audit.json`，输出 `errors=0 warnings=0`；说明当前 `功能/需求真源索引.md` 与 owner 文档单源关系未被本轮作业指导更新破坏。
26. UI 截图证据路径验证通过：`capture_v5_board_ui.sh` dry-run 当前输出默认本地路径 `/root/Desktop/v5/artifacts/board_ui/v5_board_ui_<UTC>.png`，并继续声明不发送 touch/key/mouse/motion；`--apply` 未设置 `V5_BOARD_SSH` 时仍拒绝。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke` 通过；旧路径扫描无命中。dry-run 未创建 `artifacts/` 目录，真实截图时才会创建 ignored 输出目录。
27. 板端 SSH 端口统一验证通过：在 VM 真源直接执行 `V5_BOARD_SSH_PORT=2222` 的 `push_v5_runtime_to_board.sh`、`capture_v5_board_ui.sh` 和 `run_v5_board_acceptance.sh` dry-run，输出均包含 `port=2222` 且下游命令继续传递 `V5_BOARD_SSH_PORT=2222`；三者在 `--apply` 但未设置 `V5_BOARD_SSH` 时仍按预期拒绝。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke` 通过；旧路径扫描在 `app services config linuxcnc tools CMakeLists.txt gcode` 中无命中；VM 真源没有 Markdown/Vivado 残留。
28. VPS/auth/download fallback 兼容清理验证通过：`python3 -m py_compile services/auth_download/*.py` 通过；`python3 services/auth_download/v5_device_authorization_download.py --help`、`python3 services/auth_download/v5_device_dna_register.py --help`、`python3 services/auth_download/v5_drive_profile_download.py --help` 和 `python3 services/auth_download/v5_auth_download_boundary_smoke.py` 通过。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke`、`v5_state_publisher_smoke --path /dev/shm/v5_status_shm_smoke --frames 1 --unlink-after` 通过；定向扫描 `fallback_urls|fallbacks|fallback_name|auth_error.*fallback|parse_u32.*fallback|broker_|v3_command_broker|product_actions|product_handler_|program_open_cc|start_run|pause_resume_program|wcs_switch|first_point|motion_stillness_gate|re_hal_select_helper|re_hal_safety_helper|v3_program_identity|sha256[.]json|settings_runtime[.]json|config_cache|ready|heartbeat|rotary_equivalent|rotary_program_filter|wcheckpoint|range_guard|re_v3_json|linuxcnc[.]command` 无命中。剩余 `fallback` 文本仅存在于 LVGL 第三方字体字段和 drive profile 数据中“不是 runtime fallback/不能自动 fallback”的禁用说明。
29. VPS/auth/download 部署闭环验证通过：三入口脚本可直接执行 `--help`，不再因源文件可执行位失败；`python3 -m py_compile services/auth_download/*.py`、`v5_auth_download_boundary_smoke.py` 通过，验证生成的 `services/auth_download/__pycache__` 已删除。`install_v5_runtime.sh` dry-run 当前包含 `/usr/libexec/8ax/auth_download/v5_device_dna_register.py mode=0755`、`v5_device_authorization_download.py mode=0755`、`v5_drive_profile_download.py mode=0755` 和运行模块 `mode=0644`；`precheck_v5_board.sh` dry-run 能检查这些 manifest 源文件；按 manifest 生成测试 tar 时包含三入口、`drive_profile_download_flow.py` 和 `device_vps_identity.py`。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke`、`v5_state_publisher_smoke --path /dev/shm/v5_status_shm_smoke --frames 1 --unlink-after` 通过；`verify_v5_board_runtime.sh` 未设置 `V5_BOARD_SSH` 时仍拒绝；旧路径扫描、Windows 绝对路径扫描、VM cache 扫描和 VM Markdown/Vivado 扫描均无命中。
30. State Publisher 安装生命周期收紧验证通过：`install_v5_runtime.sh` dry-run 文案已更新为 install/restart init.d services；`precheck_v5_board.sh` dry-run 通过；`verify_v5_board_runtime.sh` 未设置 `V5_BOARD_SSH` 时仍拒绝；`run_v5_board_acceptance.sh` dry-run 通过。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke`、`v5_state_publisher_smoke --path /dev/shm/v5_status_shm_smoke --frames 1 --unlink-after` 和三条 auth/download 入口 `--help` 通过；定向旧路径扫描无命中，VM cache/Markdown/Vivado 扫描无命中。
31. 板端 verify 硬验收收紧验证通过：`verify_v5_board_runtime.sh` 未设置 `V5_BOARD_SSH` 时仍拒绝；`precheck_v5_board.sh` dry-run 和 `run_v5_board_acceptance.sh` dry-run 通过；`verify_v5_board_runtime.sh` 当前 112 行且未使用旧 `check_remote_warn` helper。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke`、`v5_state_publisher_smoke --path /dev/shm/v5_status_shm_smoke --frames 1 --unlink-after` 和三条 auth/download 入口 `--help` 通过；定向旧路径扫描无命中，VM cache/Markdown/Vivado 扫描无命中。
32. 真实手指触控证据采集入口验证通过：`collect_v5_board_touch_evidence.sh --help` 和 dry-run 通过；`--apply` 未设置 `V5_BOARD_SSH` 时拒绝；设置 `V5_BOARD_SSH` 但缺少截图证据时拒绝；`run_v5_board_acceptance.sh` dry-run 已列出 `collect_v5_board_touch_evidence.sh --apply --screenshot <captured-png>`。VM 默认全量构建通过，`v5_main_page_actions_smoke`、`v5_program_command_smoke`、`v5_command_wrappers_smoke`、`v5_state_publisher_smoke --path /dev/shm/v5_status_shm_smoke --frames 1 --unlink-after` 和三条 auth/download 入口 `--help` 通过；定向旧路径扫描追加 `simulated click|fake click|xdotool|ydotool|sendevent|uinput` 后仍无命中，VM cache/Markdown/Vivado 扫描无命中。
33. 开发板 MCP 配置验证通过：Windows 端 `ssh -o BatchMode=yes -o ConnectTimeout=5 root@192.168.1.221 true` 通过；`python -m py_compile C:/Users/Administrator/.codex/mcp_servers/v5_board_server.py` 通过；直接调用 `board_diagnose(timeout_sec=30)` 返回板端 `hostname=z20_plnx`、kernel `5.4.0-rt1-xilinx-v2020.2`、`linuxcncrsh=/usr/bin/linuxcncrsh`、`/dev/shm` 存在、`/usr/libexec` 存在，同时确认当前板端缺少 `systemctl`、`/etc/systemd/system`、`fbgrab`、`fbcat` 和 `import`。该结果说明 MCP 连接本身可用，Codex 工具列表需重启后刷新。
34. 板端非 systemd 生命周期收敛验证通过：VM 真源删除 `services/state_publisher/systemd/v5-state-publisher.service`，新增 `services/state_publisher/init.d/v5-state-publisher` 和 `services/command_gate/init.d/v5-linuxcnc-command-gate`；`config/deploy/v5_runtime_deploy_manifest.tsv` 统一安装 init.d 脚本，`tools/deploy/install_v5_runtime.sh` 部署后调用 init.d `restart`。`precheck_v5_board.sh` 与 `verify_v5_board_runtime.sh` 已改为当前 PetaLinux/BusyBox 板端真实可用的 init.d 检查，不再要求 `systemctl` 或 `/etc/systemd/system`。
35. 板端 ARM 构建与部署包验证通过：新增 VM 真源 `tools/cmake/arm-linux-gnueabihf.toolchain.cmake`，板端部署清单改用 `build/board/app/v5_lvgl_shell`、`v5_state_publisher`、`v5_linuxcncrsh_probe`、`v5_linuxcncrsh_golden_run`；`file` 确认四个部署二进制均为 ARM EABI5 hard-float ELF，解释器为 `/lib/ld-linux-armhf.so.3`，不再把 VM x86 `build/linux` 产物部署到板端。
36. 板端真实硬件 LinuxCNC 路径收敛：当前板端实际 HAL 组件为 `/usr/lib/linuxcnc/modules/zynq_stepgen_hw.so`，只导出 0-5 共 6 轴，且需要 `/dev/uio0` 运行权限；VM 真源 `linuxcnc/hal/v5_pulse.hal`、`linuxcnc/ini/v5_pulse.ini`、`linuxcnc/components/step_ip_v1_5.contract.json` 和 `config/hardware/hardware_profile.json` 已从旧 `v3_step_ip_hw`/8 轴假定收敛为板端真实 `zynq_stepgen_hw`/6 轴 `XYZABC`。该阶段不伪造 U/V 两轴闭环，后续 8 轴能力必须由真实板端微内核/HAL 模块支持后再登记。
37. 板端 command gate 启动路径已改为 LinuxCNC 原生 headless：`v5_pulse.ini` 的 `DISPLAY` 改为带 `--connectpw EMC`、`--enablepw EMCTOO`、`--path /opt/8ax/v5/linuxcnc/nc` 的 `linuxcncrsh`；缺失实现文件且会阻断 interpreter 初始化的本地 `M128/M129` remap 入口已删除，RTCP 后续必须通过微内核/native 实际 owner 补全，不保留本地 remap 胶水。`v5-linuxcnc-command-gate` 只探测 LinuxCNC 自己启动的 `linuxcncrsh`，不再额外 fork 一个 root 侧第二 gate。
38. 板端 UI 截图入口已改为 framebuffer 原始帧：`tools/deploy/capture_v5_board_ui.sh` 不再依赖板端缺失的 `fbgrab/fbcat/import`，改为读取 `/dev/fb0`、sysfs 分辨率/stride/bpp 元数据并在 VM 生成 PPM 后通过 `pnmtopng` 或 ImageMagick `convert` 转 PNG；该脚本仍不发送 touch/key/mouse、linuxcncrsh、MDI、Start 或运动命令。
39. linuxcncrsh 只读 machine probe 握手已按真实协议修正：`services/command_gate/v5_linuxcncrsh_client.c` 先发送 `Hello` 并读取 `HELLO ACK`，再发送 `Get Machine`，并持续读取到 `MACHINE ON/OFF` 或超时；同一 canonical client 的命令发送也先读取 ACK 后再发送 typed command，不新增第二套 probe 或 fallback。
40. 板端 source/deploy/verify/framebuffer capture 串联已闭合到非运动诊断截图阶段：`V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 已完成 ARM manifest 源检查、板端 precheck、部署、init.d State Publisher、headless LinuxCNC command gate、`v5_linuxcncrsh_probe` 只读 `Get Machine`、默认 SHM、`v5_lvgl_shell` 启动和 framebuffer 截图；截图证据落在 VM ignored 输出目录 `artifacts/board_ui/v5_board_ui_20260705T040615Z.png`，并已复制到 Windows `D:/v5/截图/v5_board_ui_20260705T040615Z.png`。该帧显示的是板端启动画面 `Please wait: booting...`，因此只能证明 framebuffer 抓取链路可用，不能证明真实 v5 产品 UI/operator path 已显示或闭合。该步骤未发送 touch/key/mouse、MDI、Start 或 motion 命令。
41. v5 规则已记录主干真源边界：`AGENTS.md` 新增 `V5_REBUILD_SOURCE_TRUTH_RULE` 和 `V5_D3_REFERENCE_ONLY_RULE`，明确 `D:/v3` 不再作为继续修补源码主线，只能只读参考或一次性素材来源；Linux/native/LVGL/运行服务源码真源在 VM `/root/Desktop/v5`，Markdown 与 Windows/Vivado 工程真源在 Windows `D:/v5`，后续运行/编译/部署/测试不得依赖 `D:/v3` 路径。
42. v5 规则已记录板端不可达恢复路径：`AGENTS.md` 新增 `V5_BOARD_UNREACHABLE_RECOVERY_RULE`，要求 `v5_board` MCP、`z20-board` SSH 或配置板端 IP 不能连接时，先打开 COM 串口监控，再使用 v5 项目内继电器工具断电重启，并记录 U-Boot、kernel、网卡、IP、SSH、init.d、LinuxCNC 相关启动输出。该工具的当前真源已在阶段 75 迁到 VM `/root/Desktop/v5/tools/v5_board_power_cycle.py`。
43. 抓图工具已从只读旧工程搬入 v5 工具目录：`D:/v3/tools/v3_remote_frame_capture.py` 已复制为 v5 自有工具入口维护；阶段 75 已把当前真源迁到 VM `/root/Desktop/v5/tools/v5_remote_frame_capture.py`。该工具走远程 relay `/remote/frame/full` 抓 BGRA32 帧并输出 BMP；抓屏图片统一放到 Windows `D:/v5/截图/` 便于人工查看，VM `artifacts/board_ui/` 只作为中间/ignored 输出；后续运行不得引用 `D:/v3`。首次实测超时证明当时 relay 抓帧链路尚未闭合；直接 `/dev/fb0` PNG 只能保留为诊断截图。
44. v5 常驻 UI/relay 路径已建立并上板验证：VM 真源新增 `app/src/v5_lvgl_remote_display.*`，`v5_lvgl_shell --serve --port 18080` 注册 LVGL BGRA32 frame buffer 并提供 `/remote/frame/full`；默认 `v5_lvgl_shell --once` 仍作为一次性启动检查，不让 verify 卡住。新增 `services/ui/init.d/v5-ui-relay` 并加入 `config/deploy/v5_runtime_deploy_manifest.tsv`、`install_v5_runtime.sh`、`precheck_v5_board.sh` 和 `verify_v5_board_runtime.sh`。`V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 已完成部署、`v5-ui-relay` 启动、HTTP full-frame probe、`v5_linuxcncrsh_probe`、默认 SHM、framebuffer 诊断抓图；板端早期 `wget http://127.0.0.1:18080/remote/frame/full` 得到 `1536087` 字节 packet。Windows 抓图工具曾生成 `D:/v5/截图/v5_remote_frame_20260705T122428.bmp/json`，metadata 为 `width=800 height=480 stride=3200 format=bgra32 source=v5_lvgl_shell`；该尺寸与当前现场确认的实际屏幕 `1024x600` 不一致，只能作为 relay buffer 配置历史异常和修复前证据，不能作为当前 operator path 截图证据。该步骤未发送 touch/key/mouse、MDI、Start 或 motion 命令。
45. Windows 侧手动备份目录已纳入保护规则：用户明确 `D:/v5/vm-v5` 是手动备份，不得删除、移动、清理、格式化、改写、同步覆盖或写入新源码改动。`AGENTS.md` 已新增 `V5_MANUAL_VM_BACKUP_PROTECTION_RULE`：该目录不属于当前运行、编译、部署、测试或编辑真源；AI 和自动化必须跳过它。后续 Linux/native/LVGL/运行服务源码真源仍是 VM `/root/Desktop/v5`，但发现 `D:/v5/vm-v5` 与真源不一致时只能记录“手动备份可能过期”，不得修改该目录。
46. v5 真实触摸诊断链路已建立到等待真人触摸阶段：`功能/-触摸校准与输入链路说明.md` 已补充 v5 触摸诊断 owner 路径，v5 事件日志固定为 `/run/8ax_v5_product_ui/touch_events.jsonl`，启用 marker 固定为 `/run/8ax_v5_product_ui/enable_touch_diagnostics`，不得用旧 `/run/8ax_v3_product_ui/touch_events.jsonl` 作为 v5 验收真源。VM 真源新增 `services/ui/v5_touch_diagnostics.c` 和 `services/ui/init.d/v5-touch-diagnostics`，读取真实 `/dev/input/by-path/z20-touchscreen` evdev 事件，套用 `/opt/8ax/safe_ui/re_touch_calibration.json` 的 `raw-evdev-cal-v2` 矩阵，写入 v5 JSONL，不发送任何模拟输入。该服务已加入 CMake、deploy manifest、install、precheck、verify 和 `collect_v5_board_touch_evidence.sh`。Linux/ARM 构建通过，ARM 产物为 EABI5；定向扫描 `sendevent|uinput|xdotool|ydotool|fake click|simulated click|/run/8ax_v3_product_ui/touch_events|/run/8ax_v3_product_ui/enable_touch` 无命中。`run_v5_board_acceptance.sh --apply` 已部署并验证 `v5-touch-diagnostics` 运行、safe_ui 校准文件可读、旧 `/opt/8ax/ui/re_touch_calibration.json` 不存在、remote relay 和 linuxcncrsh probe 仍通过。采集入口实测 `collect_v5_board_touch_evidence.sh --apply --screenshot artifacts/board_ui/v5_remote_frame_20260705T122428.bmp --seconds 3` 在无人真实触摸时诚实失败并输出 `no new touch diagnostics were recorded`，证明当前未伪造触摸证据；真实手指日志仍待操作者现场点屏。
47. v5 新增命名禁止继续使用 `z20` 已纳入规则和 owner：`AGENTS.md` 新增 `V5_NEW_NAME_NO_Z20_RULE`，要求 v5 新增工具、脚本、服务、日志、运行目录、文档入口、部署 manifest 条目和对外命令名称不得再使用 `z20`；旧硬件 udev 路径、SSH host、hostname 等外部既成事实只能作为诊断事实出现。`功能/-触摸校准与输入链路说明.md` 已把 v5 校准工具入口改为 `tools/v5_touch_calibration/` 与板端 `/opt/8ax/tools/v5_touch_calibration/`，旧 `D:/v3/tools/z20_touch_calibration/**` 只作为只读参考来源。
48. v5 触摸校准工具已移植并完成真实手点校准：VM 真源新增 `tools/v5_touch_calibration/v5_touch_calibration.py`、`v5_touch_core.py`、`v5_touch_evdev.py`、`v5_touch_window*.py`、`v5_apply_touch_calibration.py`、`v5_run_touch_calibration.sh`、`v5_restart_after_touch_calibration.sh`，并加入 `config/deploy/v5_runtime_deploy_manifest.tsv` 安装到板端 `/opt/8ax/tools/v5_touch_calibration/`。所有新增文件名为 `v5_*`；扫描 `z20_touch|Z20_|/home/cnc/z20|/opt/8ax/tools/z20|re_run_z20|z20_apply|z20_launch|z20_run|z20_touch_test` 在 v5 工具和 manifest 中无命中。板端真实手点校准后 `/opt/8ax/safe_ui/re_touch_calibration.json` 已更新，SHA256 为 `95f5622be6337aa978d6a240acf64ef39536e71969ded7a3c1f7eb51d74436f8`，内容为 `raw-evdev-cal-v2`、`display=[1024,600]`、`sx=1.0020218703375101`、`sxy=0.018919001924444488`、`syx=-0.007394956755422606`、`sy=1.0026072650528595`、`ox=-9.872099616931486`、`oy=-5.195156360248589`；旧 `/opt/8ax/ui/re_touch_calibration.json` 仍不存在。v5 诊断日志 `/run/8ax_v5_product_ui/touch_events.jsonl` 已记录 8 行真实手指事件，且每行 `calibration_path=/opt/8ax/safe_ui/re_touch_calibration.json`、`calibration_enabled=true`，未使用模拟输入。
49. 校准界面切回 v5 UI 的 framebuffer 残影已修复：手点校准完成后第一次截图 `D:/v5/截图/v5_board_ui_20260705T045106Z.png` 暴露 PyQt 校准界面残影叠在 v5 UI 上。VM 真源 `services/ui/init.d/v5-ui-relay` 已新增 `clear_framebuffer()`，启动 `v5_lvgl_shell --serve` 前按 `/sys/class/graphics/fb0/stride` 和 modes 高度清零 `/dev/fb0`；脚本 `sh -n` 通过并已上板重启。清屏后截图 `D:/v5/截图/v5_board_ui_20260705T045243Z.png` 显示干净 v5 极简 UI，无校准残影；该截图和所有采集步骤未发送 key/mouse/linuxcncrsh/MDI/Start/motion 命令。
50. framebuffer 抓屏颜色格式已按真实屏幕修正：用户现场反馈实际屏幕为黄色/橙色按钮，但旧截图 `D:/v5/截图/v5_board_ui_20260705T045243Z.png` 显示蓝色按钮，根因是 `tools/deploy/capture_v5_board_ui.sh` 把 24bpp framebuffer 当 `bgr24` 做了红蓝通道交换。板端真实 `/sys/class/graphics/fb0/modes` 为 `U:1024x600p-0`，`bits_per_pixel=24`，`stride=3072`，物理显示按 `rgb24` 解释；VM 真源抓屏脚本已改为 metadata `format=rgb24` 且 PPM 转换直接写原始 row，不再交换通道。重新抓图得到 `D:/v5/截图/v5_board_ui_20260705T045529Z.png`，尺寸 `1024x600`，按钮颜色与肉眼黄色/橙色一致；该抓图未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。后续截图颜色若再与现场不一致，直接修抓屏工具链。
51. v5 LVGL 真实触摸输入已接入 UI shell：VM 真源新增 `app/src/v5_lvgl_touch_input.c/h`，读取真实 `/dev/input/by-path/z20-touchscreen` evdev，加载 `/opt/8ax/safe_ui/re_touch_calibration.json` 的 `raw-evdev-cal-v2` 仿射矩阵后注册 `LV_INDEV_TYPE_POINTER`；若旧 `/opt/8ax/ui/re_touch_calibration.json` 存在则拒绝启用输入，不保留旧路径 fallback。`app/src/v5_ui_shell.c` 在 framebuffer/remote shell 初始化后调用 `v5_lvgl_touch_input_setup()`，`app/CMakeLists.txt` 已把新源加入 `v5_lvgl_shell`。`app/src/v5_main_page.c` 在真实 LVGL `LV_EVENT_CLICKED` 后追加事件诊断日志 `/run/8ax_v5_product_ui/ui_events.jsonl`，记录 `button_clicked`、action、prepared/local_only、command kind/name/owner；该日志只作验收证据，不作为产品状态真源或命令结果来源。Linux 构建、`v5_main_page_actions_smoke`、`v5_lvgl_shell --once`、ARM `v5_lvgl_shell` 构建均通过，新增 `v5_lvgl_touch_input.c` 为 224 行。
52. 板端部署和验收入口已覆盖真实触摸 UI action：`services/ui/init.d/v5-touch-diagnostics` 的 stop/restart 已修复为 TERM 后必要时 KILL，避免 input read 阻塞导致部署失败；板端 restart 验证通过。`V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 已完整通过 source/deploy/verify/ui-capture，板端日志包含 `v5 touch input enabled device=/dev/input/by-path/z20-touchscreen calibration=/opt/8ax/safe_ui/re_touch_calibration.json`，`v5-ui-relay`、`v5-touch-diagnostics`、State Publisher、LinuxCNC command gate、remote frame probe、linuxcncrsh machine probe 和 `rgb24` framebuffer capture 均通过。最新截图 `D:/v5/截图/v5_board_ui_20260705T050230Z.png` 为 `1024x600` 正确颜色。新增 VM 真源 `tools/deploy/collect_v5_board_ui_action_evidence.sh`，要求已有截图证据，等待真实手指点击，并同时拉回新增 `/run/8ax_v5_product_ui/touch_events.jsonl` 与 `/run/8ax_v5_product_ui/ui_events.jsonl` delta；`--help`、dry-run、缺截图拒绝路径均通过，`run_v5_board_acceptance.sh` dry-run 已列出该入口。首次 30 秒等待窗口未新增真实触摸或 UI action 事件，因此本轮尚不能声明某个按钮 operator path 已闭合。
53. 真实手指到 LVGL 本地按钮的板端日志证据已固定：后续板端日志快照显示 `/run/8ax_v5_product_ui/touch_events.jsonl` 共有 59 行、`/run/8ax_v5_product_ui/ui_events.jsonl` 共有 9 行；其中 `3D` 按钮事件为 `time_monotonic_s=5480.259619`、`local_only=true`、`command_name=view_3d`、`command_owner=ui_local`，对应真实触摸按下/松开为 `time_monotonic_s=5480.058375` 和 `5480.234501`，且触摸日志记录 `calibration_path=/opt/8ax/safe_ui/re_touch_calibration.json`、`calibration_enabled=true`。完整日志快照已保存到 VM ignored 证据 `artifacts/board_touch/v5_board_ui_action_manual_20260705T051216Z_touch_all.jsonl` 和 `artifacts/board_touch/v5_board_ui_action_manual_20260705T051216Z_ui_all.jsonl`；同轮校验证实 safe_ui 校准 SHA256 仍为 `95f5622be6337aa978d6a240acf64ef39536e71969ded7a3c1f7eb51d74436f8`，旧 `/opt/8ax/ui/re_touch_calibration.json` 不存在。两次 `collect_v5_board_ui_action_evidence.sh --apply --screenshot artifacts/board_ui/v5_board_ui_20260705T050230Z.png` 等待窗口未在窗口内收集到新 delta，因此当前可声明真实日志证明 3D 本地 operator path 已触发，但脚本自动 delta 收尾仍待下一次窗口内手点闭合；本阶段未使用模拟触摸、远程点击、key/mouse、MDI、Start 或 motion 命令。
54. v5 触摸校准工具 fallback/legacy 分支已清理并重新上板：`功能/-触摸校准与输入链路说明.md` 已补充 v5 校准工具只允许 raw evdev 输入域、只接受 `raw-evdev-cal-v2`、helper 固定同目录 v5 入口、旧 `/opt/8ax/ui/re_touch_calibration.json` 只能删除不得读取的 owner 要求。VM 真源 `tools/v5_touch_calibration` 删除 Qt pointer 替代输入、`raw-evdev-cal-v1` 兼容读取、旧 `/opt/z20`/`/usr/local/share/z20` helper/font 候选和 systemd restart path 分支；保留 Python `evdev` 包读取与原始 Linux `input_event` 解析，二者同属真实 raw evdev 设备输入域。`python3 -m py_compile tools/v5_touch_calibration/*.py`、`sh -n v5_run_touch_calibration.sh v5_restart_after_touch_calibration.sh`、Linux 构建目标 `v5_lvgl_shell v5_touch_diagnostics` 均通过；扫描 `fallback|Fallback|compat|legacy|raw-evdev-cal-v1|/opt/z20|/usr/local/share/z20|z20-linuxcnc|qt_pointer` 在 VM v5 校准工具中无命中，旧 `/opt/8ax/ui/re_touch_calibration.json` 只在 restart 脚本删除动作中出现。`V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 已重新部署并通过非运动 source/deploy/verify/ui-capture，板端 `/opt/8ax/tools/v5_touch_calibration/*.py` 语法检查通过，板端同类 fallback/legacy/旧工具名扫描无命中。新 VM framebuffer 证据为 `artifacts/board_ui/v5_board_ui_20260705T051833Z.png`，Windows relay 截图证据为 `D:/v5/截图/v5_remote_frame_20260705T051833Z_after_acceptance.bmp/json`，metadata 为 `width=1024 height=600 stride=4096 format=bgra32 source=v5_lvgl_shell`；这些步骤未发送远程输入、模拟触摸、key/mouse、MDI、Start 或 motion 命令。
55. post-cleanup 真实触摸/UI 日志快照已固定：清理部署后板端日志增长到 `/run/8ax_v5_product_ui/touch_events.jsonl` 83 行、`/run/8ax_v5_product_ui/ui_events.jsonl` 20 行，新增真实 LVGL `button_clicked` 包含 `3D`、`S 100%`、`G59.3`、`G59.2`、`Estop Reset`、`Estop`、`Home`、`Resume`、`Pause`、`Start` 和 `G54`；对应触摸 tail 仍记录真实 `/dev/input/by-path/z20-touchscreen`、`calibration_path=/opt/8ax/safe_ui/re_touch_calibration.json`、`calibration_enabled=true`。完整 post-cleanup 快照已保存到 VM ignored 证据 `artifacts/board_touch/v5_board_ui_action_post_cleanup_20260705T054059Z_touch_all.jsonl` 和 `artifacts/board_touch/v5_board_ui_action_post_cleanup_20260705T054059Z_ui_all.jsonl`。这些新增点击来自现场真实手点日志，不是自动脚本发送；由于不是 `collect_v5_board_ui_action_evidence.sh` 窗口内 delta 收尾，本条仍不能替代下一次脚本自动 delta 验收，也不能替代 motion/golden。
56. 窗口内真实手指 UI action delta 已闭合：在服务在线且基线为 touch 83 行、UI 20 行后，执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/collect_v5_board_ui_action_evidence.sh --apply --screenshot artifacts/board_ui/v5_board_ui_20260705T051833Z.png --seconds 90`，操作者在窗口内真实手点 `3D`，脚本收集到 `before_touch=83 after_touch=85 before_ui=20 after_ui=21`。delta 已保存到 VM ignored 证据 `artifacts/board_touch/v5_board_ui_action_20260705T054347Z_touch.jsonl` 和 `artifacts/board_touch/v5_board_ui_action_20260705T054347Z_ui.jsonl`；touch delta 为真实 `/dev/input/by-path/z20-touchscreen` 按下/松开，`time_monotonic_s=7750.214575/7750.405380`，`calibration_path=/opt/8ax/safe_ui/re_touch_calibration.json`，`calibration_enabled=true`，映射坐标 `x=443.27 y=428.66`；UI delta 为 `time_monotonic_s=7750.425266`、`event=button_clicked`、`action=3D`、`local_only=true`、`command_name=view_3d`、`command_owner=ui_local`。该脚本明确未发送 synthetic touch、key、mouse、linuxcncrsh、MDI 或 motion 命令；因此 v5 本地无运动风险按钮 operator path 已有窗口内真实手指自动 delta 证据。
57. golden motion 前置只读检查已完成但未执行运动：`tools/deploy/run_v5_board_acceptance.sh --help` 明确 `--motion also requires V5_ALLOW_MOTION=1`，`v5_linuxcncrsh_golden_run --help` 明确 `--start also requires V5_ALLOW_MOTION=1`，因此本轮未擅自设置该环境变量也未运行 motion。板端只读 `/usr/libexec/8ax/v5_linuxcncrsh_probe --host 127.0.0.1 --port 5007` 返回 `linuxcncrsh machine probe ok`、`Get Machine`、`MACHINE OFF`；板端可读日志位置包括 `/run/8ax/v5_linuxcnc.log`、`/home/petalinux/linuxcnc_debug.txt`、`/home/petalinux/linuxcnc_print.txt`。定向旧路径扫描仍无 `D:/v3`、旧 Broker/helper/action、模拟点击、`sendevent/uinput` 等产品执行路径命中；残留命中为 auth/download 域允许的 result JSON 和变量名 `ready`，不构成 motion/operator product fallback 入口。
58. 板端实际屏幕尺寸按现场反馈固定进工具门禁：Windows v5 抓帧工具 `tools/v5_remote_frame_capture.py` 默认新增 `--expected-width 1024 --expected-height 600` 检查；只有显式 `--allow-any-size` 才允许诊断性抓取非当前屏幕尺寸。最新 Windows relay 证据 `D:/v5/截图/v5_remote_frame_20260705T051833Z_after_acceptance.json` 为 `width=1024 height=600 stride=4096 format=bgra32 source=v5_lvgl_shell`。阶段 63 已把抓屏/分辨率从规则细则收敛为工具链问题。
59. v5 UI/测试画布旧 `800x480` 默认已收敛到真实 `1024x600`：VM 真源 `app/src/v5_main_page.c` 将 `page->root` 从 `800x480` 改为 `1024x600`，`app/src/v5_lvgl_headless.c` 将 headless LVGL 测试驱动和 draw buffer 改为 `1024x600`，`tools/v5_touch_calibration/v5_touch_evdev.py` 在无法读取 ABS 范围时的 raw max 默认从旧 `800/480` 改为 `1023/599`。`python3 -m py_compile tools/v5_touch_calibration/*.py`、Linux `v5_lvgl_shell v5_main_page_smoke v5_main_page_actions_smoke` 构建、`v5_main_page_smoke`、`v5_main_page_actions_smoke`、`v5_lvgl_shell --once` 均通过；固定字符串扫描 `lv_obj_set_size(page->root, 800, 480)`、`hor_res = 800`、`ver_res = 480`、`g_v5_headless_buffer[800`、`max_x/max_y = 800/480` 在相关 UI/校准文件无命中。随后执行 `cmake --build build/board --target v5_lvgl_shell ...` 重编 ARM 产物，并运行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 完成非运动上板部署、verify、remote frame probe、linuxcncrsh 只读 `MACHINE OFF` 和 `/dev/fb0` `1024x600 rgb24` 截图；新 VM framebuffer 证据为 `artifacts/board_ui/v5_board_ui_20260705T055507Z.png`。Windows relay 抓图工具默认 1024x600 门禁通过，生成 `D:/v5/截图/v5_remote_frame_20260705T055507Z_1024x600.bmp/json`，metadata 为 `width=1024 height=600 stride=4096 format=bgra32 source=v5_lvgl_shell`；视觉确认 UI 画布已占满 `1024x600`，不再是旧 `800x480` 面板残留。本阶段未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。
60. acceptance 部署前 board 构建门禁已补齐：阶段 59 暴露 `run_v5_board_acceptance.sh --apply` 过去只部署 `build/board` 现有产物，不会先按当前 VM 真源重编 ARM，可能把 stale binary 部署上板。VM 真源 `tools/deploy/run_v5_board_acceptance.sh` 现在定义 `V5_BOARD_BUILD_DIR` 和 `V5_BOARD_BUILD_TARGETS`，dry-run 第一行明确显示 `cmake --build <build/board> --target v5_lvgl_shell v5_state_publisher v5_touch_diagnostics v5_linuxcncrsh_probe v5_linuxcncrsh_golden_run`；真实 `--apply` 在 precheck/deploy/verify/capture 前强制执行该 board build，缺少 build 目录时 fail-closed，不再静默部署旧产物。`sh -n tools/deploy/run_v5_board_acceptance.sh`、dry-run、`wc -l` 通过；重新执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 开头出现 `ninja: no work to do.`，随后完成非运动部署、verify、remote full-frame probe、linuxcncrsh 只读 `MACHINE OFF` 和 `/dev/fb0` `1024x600 rgb24` 截图，新 VM framebuffer 证据为 `artifacts/board_ui/v5_board_ui_20260705T055900Z.png`。Windows relay 抓图 `D:/v5/截图/v5_remote_frame_20260705T055900Z_auto_build.bmp/json` 通过默认 1024x600 门禁，metadata 为 `width=1024 height=600 stride=4096 format=bgra32 source=v5_lvgl_shell`。本阶段未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。
61. VPS/auth/download 旧环境变量兼容入口已清理：扫描发现 VM 真源 `services/auth_download` 仍读取 `AX8_*` 旧环境变量、`RE_V5_DRIVE_PROFILE_PRIVATE` 布尔覆盖和 `legacy_effective` 命名。按 `REQ-DRIVE-PROFILE-AUTH-CHAIN` 保留授权/DNA/challenge/signature/hash 主链路，删除这些兼容入口：当前只读 `RE_V5_*` 命名的 endpoint、token、device auth key、private mode、timeout、openssl 和设备树配置；private 下载开关只允许 `--private-mode` 或 `RE_V5_DRIVE_PROFILE_PRIVATE_MODE`，不再有旧布尔环境变量覆盖；旧 `effective` 目录清理函数/结果字段改名为 `retired_effective_removed`，只表示删除退役缓存，不是 fallback。`v5_auth_download_boundary_smoke.py` 扩展为扫描整个 `services/auth_download/*.py`，禁止 `AX8_`、旧 private 布尔 env、`legacy_effective` 和旧产品运动/Broker marker 重新出现。`python3 -m py_compile services/auth_download/*.py`、`v5_auth_download_boundary_smoke.py`、三条入口 `v5_device_authorization_download.py --help`、`v5_device_dna_register.py --help`、`v5_drive_profile_download.py --help` 通过；VM 和板端 `/usr/libexec/8ax/auth_download` 扫描 `AX8_|RE_V5_DRIVE_PROFILE_PRIVATE([^_A-Z]|$)|legacy_effective|remove_legacy_effective_cache|RE_v5_` 无命中。`run_v5_board_acceptance.sh --apply` 已重新部署并通过非运动 build/deploy/verify/ui-capture，VM framebuffer 证据为 `artifacts/board_ui/v5_board_ui_20260705T060617Z.png`，Windows relay 证据为 `D:/v5/截图/v5_remote_frame_20260705T060617Z_auth_cleanup.bmp/json`，metadata 为 `width=1024 height=600 stride=4096 format=bgra32 source=v5_lvgl_shell`。本阶段未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。
62. deploy tar 未来时间戳噪声已收敛：上板部署时曾因 VM UTC 比板端 UTC 快约 2 分 39 秒，远端 GNU tar 解包保留归档 mtime 时输出 `time stamp ... is in the future`，污染 acceptance 证据。确认板端 `tar --help` 为 GNU tar 后，VM 真源 `tools/deploy/push_v5_runtime_to_board.sh` 的远端解包改为 `tar -m -C '$remote_root' -xf -`，不再按归档 mtime 设置临时 staging 文件时间；最终安装权限和内容仍由 `install_v5_runtime.sh` 按 manifest 执行。dry-run 新增 `extract: remote tar -m ignores archive mtimes`，`sh -n tools/deploy/push_v5_runtime_to_board.sh`、dry-run、`wc -l` 通过。重新执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 完成非运动 build/deploy/verify/ui-capture，保存日志 `/tmp/v5_acceptance_tar_m_20260705T0609.log`，扫描 `time stamp|timestamp` 无命中；VM framebuffer 证据为 `artifacts/board_ui/v5_board_ui_20260705T060940Z.png`，Windows relay 证据为 `D:/v5/截图/v5_remote_frame_20260705T060940Z_tar_m.bmp/json`，metadata 为 `width=1024 height=600 stride=4096 format=bgra32 source=v5_lvgl_shell`。本阶段未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。
63. 抓屏/分辨率规则已收敛为工具问题：按最新现场反馈，不再在 `AGENTS.md` 和 `功能/自动闭环测试方式.md` 保留独立的抓屏/分辨率细则；截图与现场不一致时直接修工具链。VM 真源 `app/src/v5_lvgl_remote_display.c` 已把 remote relay 的 frame bytes 改为匹配物理屏幕颜色的顺序，重新构建 Linux/board `v5_lvgl_shell` 并执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 通过非运动 build/deploy/verify/ui-capture。Windows 抓图 `D:/v5/截图/v5_remote_frame_20260705T141722_relay_color_fixed.bmp/json` 显示按钮已从错误蓝色变为现场一致的橙黄色；metadata 仍为 `width=1024 height=600 stride=4096 format=bgra32 source=v5_lvgl_shell`。本阶段未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令；SSH known-host 噪声仍是下一步 cleanup 候选，不影响本次颜色修复结论。
64. acceptance SSH known-host 噪声已清理：VM 真源 `tools/deploy/precheck_v5_board.sh`、`verify_v5_board_runtime.sh`、`push_v5_runtime_to_board.sh`、`capture_v5_board_ui.sh`、`collect_v5_board_touch_evidence.sh`、`collect_v5_board_ui_action_evidence.sh`、`diagnose_v5_board_ssh.sh` 和 `run_v5_board_acceptance.sh` 的 SSH/SCP 调用已加入 `LogLevel=ERROR`，避免每个远端命令污染验收输出。`sh -n` 覆盖上述脚本通过；重新执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 完成非运动 build/deploy/verify/ui-capture，日志 `/tmp/v5_acceptance_ssh_quiet_20260705T0622.log` 扫描 `Permanently added|Warning:` 无命中。该阶段未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令，golden motion 仍未请求。
65. 继电器端口映射文件已改为 v5 命名：扫描发现 `tools/v5_board_power_cycle.py` 仍读取 `tools/z20_port_map.json`，该文件名不属于外部既成事实，违反 v5 新增命名不得继续使用 `z20`。端口映射文件已改为 `tools/v5_port_map.json`，同步更新 `PORT_MAP_PATH`、result metadata source 和 `--help` 文案；`tools/z20_port_map.json` 已删除。阶段 75 已把当前工具真源整体迁到 VM `/root/Desktop/v5/tools/`。本阶段未触发继电器、未连接 COM、未重启板端、未发送输入或运动命令。
66. AGENTS 下半部旧 VM 同步口径已改为 v5 真源边界：扫描发现 `AGENTS.md` 仍保留旧 `VM_SYNC`、`/home/sj/.../v3`、`PROJECT_ROOT` 单真源、`PRODUCT_UI=lvgl_app` 和 `/tmp/v3_test_tools` 文字。当前已改为 `VM_SOURCE_ROOT=/root/Desktop/v5`、`PRODUCT_UI=app`，并明确 Linux/native/LVGL/runtime-service 和项目工具源码真源在 VM_SOURCE_ROOT，Markdown、截图和 Windows/Vivado owner 源在 Windows PROJECT_ROOT；板端 `/opt/8ax/...`、staging、artifacts、old builds 和 temp clean copies 都不是源码真源；临时板端 probe 目录改为 `/tmp/v5_test_tools` 或 `/run/v5_test_tools`。定向扫描 `VM_SYNC|8ax-native-linuxcnc-current/v3|PROJECT_ROOT as canonical source|/tmp/v3_test_tools|/run/v3_test_tools|PRODUCT_UI=lvgl_app` 在 `AGENTS.md` 无命中。本阶段未部署、未发送输入或运动命令。
67. 当前状态中的 D:/v3 过渡同步口径已退役：`AGENTS.md` 的 `PROJECT_PATH_RULE` 已从 “whole v3 folder” 改为 “whole v5 folder”。`待做工作/改进重建.md` 当前收尾和当前事实已同步改口径：无法闭合项只写 `D:/v5/待做工作/遗留.md`，不得再临时写回 `D:/v3`；`D:/v5/待做工作/改进重建.md` 是当前文档真源，`D:/v3/待做工作/改进重建.md` 只可作为历史旧文档参考；Windows `D:/v5` 当前真源范围明确为 Markdown 文档、截图和 Windows/Vivado 工程，不包含 Linux/native/LVGL/运行服务源码或项目工具镜像。定向扫描 `whole v3 folder|临时写入 D:/v3|保持一致|同步 SHA` 只剩上述“不得/不再”语义和 SHM owner 的 `同步目标` 术语，无当前执行冲突。本阶段未部署、未发送输入或运动命令。
68. 项目工具真源和临时板端 probe 路径已同步到 owner：`待做工作/改进重建.md` 前置作业目的、总原则和 Windows 目录目标已改为项目工具属于 VM `/root/Desktop/v5/tools` 真源范围，避免后续在 Windows/VM 之间保留双边副本；`功能/4设置页设置驱动按钮作用和目的.md` 的旧测试/诊断微动采样临时板端目录已从 `/tmp/v3_test_tools` 改为 `/tmp/v5_test_tools`。定向扫描 `/tmp/v3_test_tools|/run/v3_test_tools|whole v3 folder|VM_SYNC|PRODUCT_UI=lvgl_app` 在当前规则/owner 中无执行性残留，旧命中只剩阶段 66/67 的历史说明。本阶段未部署、未发送输入或运动命令。
69. Windows 根目录真源边界复核通过：当前 `D:/v5` 顶层不存在 `app/`、`services/`、`config/`、`linuxcnc/`、`third_party/`、`tools/`、`build/` 或 `lvgl_app/` 这些 VM runtime-source 或项目工具镜像目录；排除 `vm-v5/` 和 `repo_ignored/` 后，非 Markdown 文件扫描 `D:/v3|/mnt/d/v3` 无命中。该证据支撑当前 Windows 真源范围为 Markdown 文档、截图和 Windows/Vivado 工程，不包含 Linux/native/LVGL/运行服务源码或项目工具副本。本阶段未部署、未发送输入或运动命令。
70. 详细实施计划中的旧本地 CMake 口径已快速收敛：阶段 8.1-8.12 和“每阶段固定收尾”中原先要求 Windows `D:/v5` 本地 CMake configure/build 的条目，已统一改为按 owner 真源运行对应构建：VM runtime 和项目工具用 VM `/root/Desktop/v5` 对应检查，Windows `D:/v5` 只跑文档、截图证据和 Windows/Vivado owner 的对应检查。定向扫描 `Windows 本地 CMake|在 D:/v5 跑本地 CMake|D:/v5.*本地编译` 无执行性残留；剩余 “Windows 不承担 Linux/native 本地 CMake” 是当前正确事实。本阶段未部署、未发送输入或运动命令。
71. State Publisher 与触摸日志历史口径已同步到当前主干：本文档阶段 19-22、30 和部署/验收说明已从旧 systemd 叙述收敛为当前板端真实使用的 init.d 生命周期；阶段 36 的 v5 触摸证据路径已改为 `/run/8ax_v5_product_ui/touch_events.jsonl`，不再把旧 `/run/8ax_v3_product_ui/touch_events.jsonl` 写成 v5 验收真源。当前 `systemd/systemctl` 命中只保留板端缺失事实、非 systemd 收敛记录和已删除旧分支说明，不作为执行路径。本阶段只改工作文档，未改 VM 源码、未部署、未发送输入或运动命令。
72. 文档执行门禁已按“少重复检查、快推进”收敛：阶段 8.0 的 Windows 根目录与旧路径扫描验收改为阶段 0 首次建立、实际修改 Windows 目录布局、修改 Windows/Vivado 工程或怀疑引入旧路径依赖时触发；不再把 Windows 全目录反复扫描作为每轮固定动作。当前缺口中的截图说明也收敛为 relay/framebuffer 证据已一致、异常修工具链，不再重复强调分辨率细节。本阶段只改工作文档，未部署、未发送输入或运动命令。
73. Windows `tools/` 目录口径已按最新要求改为迁出：用户明确要求把 `tools` 移动到 VM 下，当前 3.0 已撤销 Windows `tools/` 白名单，恢复 `tools/` 不得存在于 Windows `D:/v5` 的检查项；项目工具统一归属 VM `/root/Desktop/v5/tools`。Windows `D:/v5` 只保留 Markdown 文档、人工查看截图和 Windows/Vivado 工程。本阶段先改规则/工作文档，随后迁移实际文件；未部署、未发送输入或运动命令。
74. Windows 允许目录白名单已保留截图但不再保留工具：3.0 的允许项现在只补充人工查看证据目录 `截图/`，用于保存从板端或 relay 拉回的截图和配套 metadata；抓图、COM/继电器和项目辅助工具源码不再放 Windows `D:/v5/tools`。本阶段只改工作文档，未改 VM 源码、未部署、未发送输入或运动命令。
75. Windows `tools/` 已实际迁移到 VM 真源：`D:/v5/tools/v5_board_power_cycle.py`、`v5_remote_frame_capture.py` 和 `v5_port_map.json` 已上传到 VM `/root/Desktop/v5/tools/`；VM 侧 SHA256 分别为 `83ad97eadc58af3787f3b57d389f33e67c2f0d4c887ba1cbd2fb643941ac7677`、`679040e7e9e98e17cfc7c5f6ac80cfa408933b1f5b7cf514b316bad7b090248c`、`c9c73e16af94d8f5636b00b67ccd3c4271e58bd2f3a7ed437739b9879d55cc63`，与迁移前 Windows 哈希一致。VM `python3 -m py_compile tools/v5_board_power_cycle.py tools/v5_remote_frame_capture.py`、`python3 tools/v5_remote_frame_capture.py --help`、`python3 tools/v5_board_power_cycle.py --help` 均通过，`tools/v5_port_map.json` 可读出 `relay_port=COM3`、`console_port=COM4`。确认后已删除 Windows `D:/v5/tools` 目录，避免双边真源。本阶段未触发继电器、未连接 COM、未重启板端、未发送输入或运动命令。
76. 文件放置 owner 已同步 tools 迁移：`功能/需求真源索引.md` 的 `REQ-FILE-CLASSIFIED-PLACEMENT` 已改为项目工具真源进 VM `/root/Desktop/v5/tools/`，人工查看截图证据进 Windows `D:/v5/截图/` 或 owner 指定证据目录，避免 AGENTS、工作文档和 owner 索引对 tools 归属分叉。本阶段只改文档，未部署、未发送输入或运动命令。
77. AGENTS 当前执行规则已清掉旧 v3 工具闭环入口：`AGENTS.md` 的 `LOCAL_GATES`、`LOCAL_FIRST_FAST_PROGRESS`、`BOARD_CLOSURE_ENTRY` 和 `BOARD_CLOSURE` 不再要求 `tools/v3_microkernel_param_audit_runner.py`、`tools/v3_vm_full_current_sync.py` 或 `tools/v3_board_deploy_product_ui.py`；C/LVGL/board-shipped runtime 变更改为从 VM_SOURCE_ROOT `/root/Desktop/v5` 构建，并通过 `/root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply` 或 owner 要求的 focused v5 deploy/verify 脚本闭合。VM 校验 `tools/deploy/run_v5_board_acceptance.sh --help` 通过，输出 `--motion also requires V5_ALLOW_MOTION=1`；定向扫描 `v3_vm_full_current_sync|v3_board_deploy_product_ui|v3_microkernel_param_audit_runner|full current PROJECT_ROOT sync/build` 在 `AGENTS.md` 无命中。本阶段只改规则/工作文档和只读校验，未部署、未发送输入或运动命令。

78. AGENTS 其他当前闭环规则已同步为 VM_SOURCE_ROOT：`INCREMENTAL_UI_AUTOMATION_GATE`、`FINISH_LINE_MATRIX`、`BOARD_UNREACHABLE_RECOVERY_RULE` 和 `VM_ACCESS` 中残留的 `full current PROJECT_ROOT sync/build`、`PROJECT_ROOT sync/build/deploy` 和 `Windows-tool source` 口径已改为从 full current VM_SOURCE_ROOT 构建/部署，Windows PROJECT_ROOT 只作为 Markdown、截图和 Windows/Vivado source。定向扫描 `PROJECT_ROOT sync|full-current PROJECT_ROOT|full current PROJECT_ROOT|sync/build full current PROJECT_ROOT|Windows-tool|Windows tools|Windows 侧项目工具|v3_vm_full_current_sync|v3_board_deploy_product_ui|v3_microkernel_param_audit_runner` 在 `AGENTS.md` 无命中。本阶段只改规则/工作文档，未部署、未发送输入或运动命令。

79. 工具真源残留措辞已继续收紧：`AGENTS.md` 的 `TOOLING` 与 `TOOL_GAP_FIRST` 不再写泛化 `tools/`，改为可复用项目工具只放 `VM_SOURCE_ROOT/tools/`，并明确不得创建或保留 Windows `D:/v5/tools` 二次真源；本文标准目录职责、目录执行规则和“每阶段固定收尾”已同步为 VM `/root/Desktop/v5/tools/` 工具检查，Windows `D:/v5` 只做文档、截图证据和 Windows/Vivado owner 检查。本阶段只改规则/工作文档，未部署、未发送输入或运动命令。

80. owner 编译口径已避免双边源码误读：`AGENTS.md` 的删除优先规则不再写泛化 `local/VM compile`，改为用 owning truth location 的对应 compile/check gate 暴露真实依赖；`功能/需求真源索引.md` 中 `REQ-DELETE-FIRST-RETIREMENT` 及顶部速查同步为“owner 真源对应编译/检查”。这只改变规则措辞，不改变删除优先级，也不新增 Windows/VM 镜像或工具副本。本阶段未部署、未发送输入或运动命令。

81. Windows 目录检查规则已从“每轮全树扫描/误删风险”收敛为条件触发：3.0 现在明确 Windows 布局检查只在阶段 0 首次建立、实际修改 Windows/Vivado 布局、怀疑旧路径依赖或用户要求复核时运行；检查时跳过 `D:/v5/vm-v5` 手动备份和 `repo_ignored/` 临时证据目录，不读取或修改其中内容。`repo_ignored/` 只允许作为 Codex/验证临时输出或证据目录，不得承载源码真源、工具真源或可编辑备份；Windows owner 范围仍禁止 VM runtime-source 和项目工具镜像。本阶段只改工作文档，未部署、未发送输入或运动命令。

82. 当前“下一步”工具路径已同步到 VM 真源：后续触控按钮验收和 golden motion 闭环命令不再写相对 `tools/deploy/...`，改为在 VM `/root/Desktop/v5` 使用 `/root/Desktop/v5/tools/deploy/collect_v5_board_ui_action_evidence.sh` 或 `/root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply --motion`。这避免 Windows `D:/v5` 被误认为项目工具执行根。本阶段只改工作文档，未部署、未发送输入或运动命令。

83. 交接入口和当前缺口路径已按 owner 真源收口：本文顶部上下文压缩恢复说明不再写 Windows 绝对 `D:\v5\AGENTS.md`，改为项目内 `AGENTS.md` -> `功能/需求真源索引.md` -> 本文当前缺口；当前缺口中的非运动 acceptance 也改为 VM 绝对 `/root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply`，避免把 Windows 文档根误作运行工具根。本阶段只改工作文档，未部署、未发送输入或运动命令。

84. 最终 UI 目标已按最新要求写入本文：v5 不能只停留在过渡极简 UI，最终必须在干净 v5 主干上恢复与 v3 一致的可操作 UI 布局和每个可见按键功能；主页面按 `功能/7主页面按钮功能.md`，设置页 `设置驱动` 按 `功能/4设置页设置驱动按钮作用和目的.md`，其它页面/按钮/状态按 `功能/需求真源索引.md` 指向的 owner 文档闭合。本文只记录重建目标和推进顺序，不复制按钮表作为第二需求真源；v3 只作为布局、视觉和按钮清单参考，若 v3 旧行为与当前 owner 冲突，以 `功能/` owner 为准，不恢复旧 Broker/Python/JSON/fallback/假入口。本阶段只改工作文档，未部署、未发送输入或运动命令。

85. 基于当前 VM 代码现状补入 UI 恢复实施计划：盘点确认当前 VM 真源已有 `v5_main_page.c/h`、`v5_main_page_actions.c/h`、`v5_ui_shell.c`、坐标/刀路显示模型、真实触摸和 remote relay，但仍只有单页面过渡实现；`app/src` 尚无完整 `v5_page_settings.c`、`v5_page_program.c`、`v5_page_mdi.c`、`v5_ui_keyboard.c`、`v5_ui_popup.c`，`设置驱动` 目前只有后端/owner 准备路径没有 UI 页面。本文新增 8.4A，把恢复 v3 同等 UI 拆成空壳 UI、主页面布局、显示、按钮清单、按钮功能、程序/MDI、设置页、设置驱动、共用 UI、空壳模拟点击验收、单按钮功能验收和最终回归十二个切片，并写明禁止恢复旧 Broker/Python/JSON/fallback/假入口。同步修正当前事实：VM 中 Python `__pycache__` 只能视为生成缓存，不是源码、证据或验收真源。本阶段只改工作文档，未改 VM 源码、未部署、未发送输入或运动命令。

86. UI 恢复顺序已按“先空壳、后功能”优化：8.4A 的第一切片改为 layout-only 空壳 UI，先恢复 v3 同等页面布局、控件层级、中文资源和全部可见按钮位置；按钮只允许 press feedback、页面内选中态和 `implemented=false/layout_only` 诊断事件，不调用 command gate、native/linuxcncrsh、Broker、文件读写、继电器、触摸模拟或运动命令。后续主页面、程序/MDI、设置页和 `设置驱动` 都先显示空壳，再按 `功能/` owner 分组逐步接回功能；空壳阶段不得用弹窗拒绝、后端失败、假成功或 hidden fallback 冒充功能。本阶段只改工作文档，未改 VM 源码、未部署、未发送输入或运动命令。

87. 主页面按钮触发路径已切到 layout-only 空壳模式并完成非运动上板：VM 真源 `app/src/v5_main_page.c` 的 `v5_main_page_trigger_action()` 当前不再调用 `v5_main_page_action_prepare()`，不准备 command gate/native 请求；所有按钮只返回 `command.name=layout_only`、`command.owner=ui_layout_shell`、`prepared=false`、`local_only=true`、`accepted=false`，UI 事件日志写入 `implemented=false` 和 `layout_only=true`。`v5_main_page_actions_smoke` 已改为遍历当前 28 个按钮并验证全部 layout-only。VM Linux focused build `cmake --build build/linux --target v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过，随后 `v5_main_page_actions_smoke`、`v5_main_page_smoke`、`v5_lvgl_shell --once` 通过；静态扫描确认 `v5_main_page.c` 和 `v5_main_page_actions_smoke.c` 不再调用 `v5_main_page_action_prepare(...)`。随后执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 /root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply`，完成 board build、deploy、verify、remote relay probe、linuxcncrsh 只读 `MACHINE OFF` 和 `/dev/fb0` 抓图，VM 证据为 `/root/Desktop/v5/artifacts/board_ui/v5_board_ui_20260705T072713Z.png`，已复制到 Windows `D:/v5/截图/v5_board_ui_20260705T072713Z_layout_only.png`。该 acceptance 明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令；layout-only 按钮真实手指 delta 仍是下一步。

88. v3 风格主页面和设置页空壳已上板：按最新要求，VM 上已建立只读 v3 参考 `/root/Desktop/v3_reference_readonly`，顶层 58 项、约 378M，去掉写权限，仅用于查看布局/图标/颜色，不作为 v5 源码真源。VM v5 真源新增/修改 `app/src/v5_main_page.c/h`、`v5_layout_icons.c/h`、`v5_layout_icon_masks.c/h`、`v5_settings_page.c/h`、`v5_ui_shell.c`、`v5_main_page_actions.c/h`、`v5_lvgl_remote_display.c` 和 `app/CMakeLists.txt`：主页面按 v3 右侧竖向导航、深蓝色系、速度/误差/CPU 面板、刀路区、坐标区和 35 个主页面可见按钮显示；图标掩码从 v3 资源复制为 v5 当前用途命名，未保留 `v5_reb_icons.*` 旧未用文件名；设置页空壳显示标题、运动模型、G53、机械坐标、轴参数表和 `下载授权/服务器下载/扫描从站/复位驱动/读取驱动/清除故障/设置驱动/登记本机码/返回` 按钮。所有主页面和设置页按钮仍只生成 `command.name=layout_only`、`command.owner=ui_layout_shell`，不调用 command gate、native/linuxcncrsh、Broker、文件写参数、驱动、MDI、Start 或 motion。颜色按板端实屏确认正确；若后续截图颜色与实屏不一致，只修抓屏/relay 工具链。VM Linux focused build `cmake --build build/linux --target v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过，`v5_main_page_actions_smoke` 输出 `buttons=35 last=layout_only owner=ui_layout_shell`，`v5_main_page_smoke` 和 `v5_lvgl_shell --once` 通过；静态扫描 `D:/v3|D:\\v3|start_run|pause_resume_program|program_open_cc|settings_calc_bit|drive_calculate_bit` 在新增/修改 UI 文件无命中。`V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 /root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply` 已完成非运动 build/deploy/verify/ui-capture，VM 主页面证据为 `/root/Desktop/v5/artifacts/board_ui/v5_board_ui_20260705T075635Z.png`，已复制到 Windows `D:/v5/截图/v5_board_ui_20260705T075635Z_v3_like_icons_color_fixed.png`；操作者真实点一次 `系统设置` 后收集到 touch/UI delta：`/root/Desktop/v5/artifacts/board_touch/v5_board_ui_action_20260705T075732Z_touch.jsonl` 和 `_ui.jsonl`，随后设置页截图为 `/root/Desktop/v5/artifacts/board_ui/v5_board_ui_20260705T075834Z.png`，已复制到 Windows `D:/v5/截图/v5_board_ui_20260705T075834Z_settings_layout_shell.png`。本阶段 acceptance 未发送 key/mouse/linuxcncrsh/MDI/Start/motion 命令；后续 UI 空壳/非运动按钮点击改由 AI 模拟屏幕点击推进并记录前后截图和 UI event delta。

89. 设置页轴参数表已扩为 8 轴宽表空壳：用户要求轴参数增加到 `X/Y/Z/A/B/C/龙门/刀库`，参数列较多时轴名固定、右侧参数列可平移。`功能/4设置页设置驱动按钮作用和目的.md` 已记录该 owner 要求。VM 真源 `app/src/v5_settings_page.c` 已把设置页机械坐标和轴参数表扩为 8 轴；轴名列固定在左侧，右侧参数列放入 LVGL 横向滚动区域，当前列包括轴模式、从站、bit、目标精度、螺距/角度、电机、负载、电子齿轮分子/分母、回零、软限位、零位和状态。所有单元格仍是 layout-only 显示，不写参数、不调用设置驱动、不发送运动命令。VM Linux focused build `cmake --build build/linux --target v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过，`v5_main_page_actions_smoke`、`v5_main_page_smoke`、`v5_lvgl_shell --once` 通过；定向扫描 `D:/v3|D:\\v3|start_run|pause_resume_program|program_open_cc|settings_calc_bit|drive_calculate_bit` 在相关 UI 文件无命中，`v5_settings_page.c/h` trailing whitespace 检查无命中。`V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 /root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply` 已完成 board build/deploy/verify/ui-capture，部署后主页面截图为 `/root/Desktop/v5/artifacts/board_ui/v5_board_ui_20260705T084448Z.png`，已复制到 Windows `D:/v5/截图/v5_board_ui_20260705T084448Z_after_8axis_settings_table_deploy.png`；该 acceptance 明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion。设置页 8 轴表的页面内截图、横向滚动截图和 UI event delta 仍待后续补齐 canonical 模拟点击/remote-input 后闭合，不能用本条主页面截图替代设置页视觉验收。

90. 受限 remote-input 工具已建立并用于设置页 8 轴表证据：AGENTS 已明确模拟点击/拖动/滚动前必须先截屏确认当前画面和目标位置，禁止裸发坐标。VM 真源新增 `app/src/v5_lvgl_remote_input.c/h`，`v5_lvgl_shell --serve` 在 `V5_UI_REMOTE_INPUT=layout_only` 下提供 `/remote/input/layout-click` 和 `/remote/input/layout-drag` 诊断端点；`services/ui/init.d/v5-ui-relay` 默认显式传入 `remote_input=layout_only`。该端点只驱动 LVGL 本地 pointer 状态并写 `/run/8ax_v5_product_ui/remote_input_events.jsonl`，scope 固定为 `non_motion_ui_layout`，不写参数、不访问 command gate、不调用 linuxcncrsh、不发送 MDI/Start/motion。VM 真源新增 `tools/deploy/collect_v5_remote_input_evidence.py`，流程固定为先抓 `/remote/frame/full` before 帧，再发送一次 layout-only click/drag，再抓 after 帧并拉取 UI/remote-input delta。Linux 构建 `v5_lvgl_shell` 通过，`python3 -m py_compile tools/deploy/collect_v5_remote_input_evidence.py`、`sh -n services/ui/init.d/v5-ui-relay`、`v5_lvgl_shell --once` 通过；上板 acceptance 通过并显示 `v5_ui_relay started ... remote_input=layout_only`，截图证据 `/root/Desktop/v5/artifacts/board_ui/v5_board_ui_20260705T085145Z.png`。随后工具按规则先截图再点击 `系统设置`：summary `/root/Desktop/v5/artifacts/board_remote_input/v5_remote_input_20260705T085201Z_summary.json`，UI delta 为 `action=Settings`、`implemented=false`、`layout_only=true`、`command_name=layout_only`，remote-input delta 为 `kind=click x1=970 y1=330 ok=true`；after 图已复制到 Windows `D:/v5/截图/v5_remote_input_20260705T085201Z_after_settings_click.bmp`。再按规则先截设置页再横向拖动轴参数区：summary `/root/Desktop/v5/artifacts/board_remote_input/v5_remote_input_20260705T085214Z_summary.json`，remote-input delta 为 `kind=drag x1=850 y1=365 x2=260 y2=365 ok=true`；before 图显示 8 行 `X/Y/Z/A/B/C/龙门/刀库` 且右侧从 `轴模式/从站/bit` 开始，已复制到 Windows `D:/v5/截图/v5_remote_input_20260705T085214Z_before_settings_8axis.bmp`；after 图显示轴名仍固定、右侧平移到 `软限位/零位/状态` 等后续列，已复制到 Windows `D:/v5/截图/v5_remote_input_20260705T085214Z_after_settings_8axis_drag.bmp`。这些证据只证明非运动 layout-only UI 导航和横向滚动，不替代触摸校准、真实手指硬件、Start/MDI、设置驱动功能或 motion 闭环。

91. 页面切换速度已改为 RAM 预渲染缓存路径并上板闭合：用户要求页面切换 0.2 秒内刷出。VM 真源 `app/src/v5_lvgl_remote_display.c/h` 增加主页面和设置页两份驻留帧缓存，分别保存远程 BGRA 帧和 `/dev/fb0` 原生帧；`app/src/v5_ui_shell.c` 开机预渲染主页面/设置页并在导航时先 `cache_blit` 到 framebuffer，再让 LVGL 常规刷新跟上，同时把耗时写入 `/run/8ax_v5_product_ui/navigation_perf.jsonl`。这避免切页时依赖 LVGL 整屏逐像素重绘，目标是先快速出界面。VM focused build `v5_lvgl_shell v5_main_page_actions_smoke v5_main_page_smoke` 通过，`v5_main_page_actions_smoke` 和 `v5_main_page_smoke` 通过；`V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 /root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply` 完成非运动 build/deploy/verify/ui-capture。按规则先截图确认主页后使用受限 remote-input 单击 `系统设置`，summary 为 `/root/Desktop/v5/artifacts/board_remote_input/v5_remote_input_20260705T100207Z_summary.json`；板端真实导航日志显示 `target=settings cache_blit=true elapsed_us=12189`。随后先确认设置页截图 `D:/v5/截图/v5_cache_settings_after_20260705T180236.png`，再单击 `返回`，板端日志显示 `target=main cache_blit=true elapsed_us=12932`，后续重复记录约 `12399us` 和 `12529us`。该性能证据证明页面先出界面路径约 0.012-0.013 秒，低于 0.2 秒；远程证据工具抓两张全帧本身仍会耗时数秒，不能用来衡量物理屏切页速度。本阶段未发送 key/linuxcncrsh/MDI/Start/motion，仍只属于非运动 UI 切页性能闭合。

92. WinRemote 鼠标输入已改为 v3 兼容 WebSocket pointer 协议并上板闭合：用户反馈 Win 软件点击鼠标无法操作板端，确认根因是 v5 板端只保留旧 HTTP `layout-click/layout-drag` 诊断入口，`/remote/info` 仍报告 `view_only=true/input_enabled=false`，而 v3 WinRemote 真实路径是 `WS /remote/input` 上的 `control_request -> control_grant` 和 `pointer_event -> pointer_ack`。VM 真源新增 `app/src/v5_remote_ws.c/h` 和 `app/src/v5_remote_input_socket.c/h`，`app/src/v5_lvgl_remote_display.c` 同时监听 frame HTTP 与 input WebSocket，输入 socket 增量处理，不阻塞 `/remote/frame/full`；`app/src/v5_lvgl_remote_input.c/h` 改为 down/move/up pointer 状态机；`tools/deploy/collect_v5_remote_input_evidence.py` 已从旧 HTTP 诊断端点迁到同一 v3 WebSocket pointer 协议，`verify_v5_board_runtime.sh` 开始检查 `view_only:false` 与 `input_enabled:true`。Linux focused build、两个 main page smoke、board `v5_lvgl_shell` 构建、部署和 `verify_v5_board_runtime.sh` 均通过。板端 `/remote/info` 当前返回 `view_only=false,input_enabled=true`；按规则先截图确认主页右侧 `系统设置` 目标后，通过 WebSocket pointer 点击 `(970,330)`，summary 为 `/root/Desktop/v5/repo_ignored/winremote_mouse_input/evidence/v5_remote_input_20260705T101005Z_summary.json`，ACK 包含 `control_grant`、`pointer_ack down`、`pointer_ack up`，remote-input delta 为 `protocol=v3_pointer_ws phase=down/up ok=true`，UI delta 为 `event=button_clicked action=Settings`；点击后截图 `D:/v5/repo_ignored/winremote_mouse_input/after_click.bmp` 显示板端已进入 `参数设置` 页。该证据证明 WinRemote 使用的 v3 输入协议在板端真实 LVGL 页面生效；它不替代真实手指触摸校准、Start/MDI、设置驱动或 motion 闭环。
93. WinRemote v3 源码已复制到 v5 并完成路径相对化：按最新要求，`D:/v3/8ax-win` 的完整源码目录 `src/`、`tests/`、`tools/` 和工程文件已复制到 `8ax-win/`，排除 `bin/obj` 构建缓存并保留 v5 现有 `publish/`。清理后 WinRemote 运行代码、测试、发布脚本和说明文档不再包含 `D:\...`、`D:/...`、`/root/Desktop`、`RE_V3_ROOT`、`RE_V3_ARCHIVE_ROOT`、`v3_archive`、`lvgl_app/` 或退役 `v3_remote_ui_relay.py` 路径；`tools/publish_winremote_update.ps1` 默认输出已改到工程内 `repo_ignored/release/winremote_update`，测试改用 `FindWinRemoteRoot()` 和相对文件读取，不再从 WinRemote 自检读取 VM/板端旧 v3 relay 源码。删除从 v3 带入但当前 v5 不支持的 `8ax-win/tools/v3_remote_ui_relay.py` wrapper。验证通过：`rg` 绝对/旧路径扫描无命中，`dotnet run --project tests/8ax.WinRemote.Tests/8ax.WinRemote.Tests.csproj` 输出 `Relay protocol, evidence, mock relay, and read-only client checks passed.`，`dotnet build 8ax.WinRemote.sln -c Release` 为 0 warning/0 error，`git diff --check` 通过。本阶段未改板端 runtime、未部署、未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。

94. 设置页轴参数表已按 owner 归属重建并上板闭合：按最新纠正顺序，先补齐轴参数表，再继续设置页按钮功能。VM 真源新增 `app/src/v5_settings_axis_table.c/h` 和 `app/src/v5_settings_axis_table_smoke.c`，设置页改为调用独立轴表模块；表格保持 v3 同等列语义，包含 8 轴 `X/Y/Z/A/B/C/龙门/刀库` 和 18 列 `轴模式/从站/目标精度/螺距/电机/负载/回零次序/回零方向/-软限位/零位/+软限位/速度/加速/反向间隙/bit/分子/分母/写入状态`，左侧轴名固定，右侧列横向滚动。字段归属不自造第二真源：`axis_mode/precision/pitch/motor_rev/load_rev/home_order/home_direction/soft_minus/soft_plus/max_velocity/max_acceleration/backlash` 走 runtime INI/native readback，`zero` 走 native runtime/API，只有 `slave/encoder_bits/egear_numerator/egear_denominator/write_status` 属于设置驱动私域/drive provider；UI 初始值只显示 `待读` 或动作标签，不把 layout 占位值当参数真值。同步修正 `app/src/v5_parameter_table.c` 中无效的双通配 `axis_*_home_*`、`axis_*_egear_*`，替换为明确字段，避免 owner 表漏接。验证通过：`v5_settings_axis_table_smoke` 输出 `rows=8 cols=18 native_runtime_cells=104 drive_only_cells=40`；`v5_parameter_table_smoke` 输出 `parameter table: fields=33`；`v5_main_page_actions_smoke`、`v5_main_page_smoke`、`v5_lvgl_shell --once` 通过。随后执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 /root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply` 完成 board build/deploy/verify，板端 `v5_lvgl_shell` 启动日志显示 `parameter_owners=33`。按规则先截图确认当前设置页后，Windows 截图 `D:/v5/截图/v5_axis_current_before_click_20260705T183808.png` 显示完整 8 轴表首屏；再执行一次受限 remote-input 横向拖动，summary 为 `/root/Desktop/v5/artifacts/board_remote_input/v5_remote_input_20260705T103832Z_summary.json`，拖动后截图 `D:/v5/截图/v5_axis_after_hscroll_20260705T183855.png` 显示轴名列仍固定，右侧已滚到 `零位/+软限位/速度/加速/反向间隙/bit/分子/分母/写入状态`。本条只闭合轴参数表视觉、滚动和字段 owner 归属；实际 provider 读值、编辑保存、设置驱动写入和运动闭环仍未完成。
95. WinRemote 顶部升级按钮已改为“有更新才显示”：按最新反馈，`8ax-win/src/8ax.WinRemote/MainWindow.xaml` 中 `UpgradeButton` 默认 `Visibility=Collapsed`，顶部右侧控件组改为自适应宽度，隐藏时不再留下固定按钮位；`MainWindow.xaml.cs` 的 `RefreshUpgradeButtonText()` 固定显示 `升级`，仅当 `_updateAvailable=true` 时设为 `Visible/Enabled`，无更新或 VPS 检查失败时保持隐藏。README 同步为“服务器版本更新时显示 `升级`，没有更新则隐藏按钮”。WinRemote 测试中不再读取中文文件名或乱码文案断言，改用稳定代码 token 和 endpoint 断言，避免路径相对化后自检假失败。验证通过：`dotnet run --project tests/8ax.WinRemote.Tests/8ax.WinRemote.Tests.csproj` 输出 `Relay protocol, evidence, mock relay, and read-only client checks passed.`；`dotnet build 8ax.WinRemote.sln -c Release` 为 0 warning/0 error；`git diff --check -- 8ax-win/src/8ax.WinRemote/MainWindow.xaml 8ax-win/src/8ax.WinRemote/MainWindow.xaml.cs 8ax-win/tests/8ax.WinRemote.Tests/ScaffoldTests.cs 8ax-win/README.md` 通过；已重新发布 `8ax-win/publish/win-x64/8ax.WinRemote.exe`。本阶段只改 Windows WinRemote UI 和本地测试/文档，未改板端 runtime、未部署、未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。
96. Factory/Dealer Windows 客户端源码已复制到 v5 并完成独立编译：按最新要求，从冻结 v3 参考复制完整 `8ax-factory-client-source/` 与 `8ax-dealer-client-source/` 源码到 v5，排除生成缓存 `bin/obj`，保留 v5 已有 `publish/`。Factory 源码现包含 `8ax.FactoryClient/`、`vps-admin-api/`、`start-vps3-admin-tunnel.ps1` 和发布记录；Dealer 源码现包含 `8ax.DealerClient/`。`start-vps3-admin-tunnel.ps1` 已从固定 Windows 盘符密钥根改为从 v5 项目根相对定位 `vpn/...`，两个目录的源码/文档扫描 `Windows absolute roots、old v3/re archive roots、/root/Desktop` 无命中。验证通过：`dotnet build .\8ax-factory-client-source\8ax.FactoryClient\8ax.FactoryClient.csproj -c Release` 和 `dotnet build .\8ax-dealer-client-source\8ax.DealerClient\8ax.DealerClient.csproj -c Release` 均为 0 warning/0 error；`python -m py_compile` 覆盖 Factory `vps-admin-api` 模块；`python -m pytest .\8ax-factory-client-source\vps-admin-api\tests` 通过 6 tests；已发布 `8ax-factory-client-source/publish/win-x64/8ax.FactoryClient.exe`，SHA256 `12E074AFD7385875AB9780D1F7498DBEBC187C83F07B2FF0915AC00DBAAAD326`，以及 `8ax-dealer-client-source/publish/win-x64/8ax.DealerClient.exe`，SHA256 `C26476D348450F054B10FBA45B6CEA33D53903AAEE2CF55D60B3F9014B2BA67F`；发布目录已清理 PDB sidecar，只保留 exe；`git diff --check -- 8ax-factory-client-source 8ax-dealer-client-source` 通过。Factory `发布记录.md` 已记录本次 v5 local source migration check。本阶段只改 Windows 客户端源码/文档/本地产物，未改板端 runtime、未部署、未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。

97. 设置页轴参数 UI 已按三来源口径补本地读取闭环，并修正第 7、8 轴显示：先按最新反馈修正 `功能/0开机参数入内存.md`、`功能/需求真源索引.md` 和 `功能/4设置页设置驱动按钮作用和目的.md`，明确只禁止重复承载微内核/native 已有参数和驱动参数映射表已有参数；微内核和驱动映射表都没有的设置页轴参数必须进入自建参数表并由 UI 自动读取。龙门和刀库是第 7、8 轴，不是占位行；没有 native/runtime owner 的轴模式、精度、回零、限位、零位、速度、加速度、反向间隙等字段已按单元格 owner 落入 `self_parameter_table`，显示和编辑状态按普通轴处理，不再灰色禁用或显示 `NAT`。VM 真源 `app/include/v5_parameter_table.h`、`app/src/v5_parameter_table.c`、`services/command_gate/v5_parameter_owner_map.c` 增加 `self_parameter_table` owner/readback；`app/src/v5_settings_axis_table.c` 现在先读自建表，再用 runtime INI/native 覆盖已有 owner 字段，再读 drive profile/provider，并用 bit、精度、螺距和 `motor_rev/load_rev` 派生电子齿轮分子/分母显示值。验证通过：VM focused build `v5_parameter_table_smoke v5_settings_axis_table_smoke v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过；运行 smoke 逐单元检查 8x18 表格值均为真实值，不能是 `NAT` 或 `--`，输出 `v5 settings axis table: rows=8 cols=18 native_runtime_cells=60 self_table_cells=52 drive_only_cells=32 x_pitch=5 x_velocity=166.666667 x_bit=18 gantry_velocity=166.666667 toolmag_bit=18 x_egear=16384/3125`，`v5_lvgl_shell --once` 通过；定向扫描旧参数/缓存/result/旧 v3 路径在本次触及产品文件无命中。本阶段未部署板端、未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令；结论为 `local_verified_only`，不能替代设置页上板截图、编辑保存、设置驱动写入或 motion 闭环。

98. 设置页轴参数归类与上板显示已按最新反馈收敛：`功能/参数归类.md` 按 `微内核参数 / 自建参数 / 驱动映射表参数` 三类重写设置页字段，明确 B 中心、`轴模式=直线/旋转/虚拟`、`方向=cw/ccw`、`脉冲/总线=总线`、`bit` 默认驱动表且可人工覆盖、`从站` 自建参数表和扫描候选来源。VM 真源同步实现：`linuxcnc/ini/v5_pulse.ini` 增加 B 中心和 `DIRECTION_MODE=cw`，`app/src/v5_settings_axis_table.c/h` 增加 19 列轴表、`方向` 列、B 中心行、左侧默认滚动归零、龙门/刀库第 7/8 轴普通显示、自建参数扫描快照读取；`services/drive_profile/v5_drive_bus_action.py` 在 `扫描从站` 成功时把扫到的 `slave_options` 和轴从站值写入 `/run/8ax_v5_drive/settings_self_parameter_table.json`。验证通过：`git diff --check` 覆盖相关功能文档；VM focused build `v5_parameter_table_smoke v5_settings_axis_table_smoke v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过；smoke 输出 `fields=48`、`rows=8 cols=19 native_runtime_cells=112 self_table_cells=32 drive_only_cells=8`，并检查 `axis_X_direction_mode owner=runtime_ini`、`X direction=cw`、扫描自建参数快照候选 `10/11`；Python monkey patch 模拟 `ethercat slaves` 后确认生成 `v5.self_parameter_table.v1`、`slave_options=[0,1]`。已上板两轮 `run_v5_board_acceptance.sh --apply`，最终截图 `D:/v5/repo_ignored/settings_axis_direction/board_frame_settings_final.png` 显示设置页 B 中心在 A/C 中间、`脉冲/总线` 为 `总线`、轴表首屏为 `轴模式 / 方向 / 从站 / 目标精度...`，8 轴含 `龙门/刀库`，无底部重叠、无灰色禁用、无 `NAT`。板端只读执行真实 `扫描从站` 返回 `DRIVE_SCAN_NO_SLAVES`，因此本次板端未生成扫描自建参数快照；有从站时的写入链路已由本地模拟闭合。本阶段未发送 key/mouse/linuxcncrsh/MDI/Start/motion，remote-input 只用于非运动设置页导航截图。

99. 设置页轴参数下拉列已按 v3 控件形态改为真实 LVGL dropdown 并上板确认：按最新反馈参考 v3，但采用 v5 owner 语义，`轴模式` 下拉为 `直线/旋转/虚拟`，`方向` 下拉为 `cw/ccw`，`从站` 下拉只使用 `扫描从站` 写入自建参数表的候选，`回零次序` 下拉为 `禁用/0/1/2/3/4/5/6/7`，`回零方向` 下拉为 `0/+/-`，`bit` 下拉为 `NAT/24/23/22/21/20/19/18/17/16`。文档已同步到 `功能/4设置页设置驱动按钮作用和目的.md` 和 `功能/参数归类.md`，禁止这些列继续用普通输入框或红字模拟下拉。VM focused build `v5_settings_axis_table_smoke v5_parameter_table_smoke v5_lvgl_shell` 通过，smoke 检查 19 列中 6 列为 select：`axis_mode/direction_mode/slave/home_order/home_direction/encoder_bits`。已执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 完成非运动 build/deploy/verify；按截图门禁先确认主页面，再 remote-input 点击 `系统设置`，左侧设置页截图 `D:/v5/repo_ignored/settings_dropdown/board_frame_settings_left.png` 显示 `轴模式/方向/从站/回零次序/回零方向` 均为带箭头下拉；横向拖动后截图 `D:/v5/repo_ignored/settings_dropdown/board_frame_settings_right.png` 显示 `bit` 列为带箭头下拉；再点击首行 `bit` 后截图 `D:/v5/repo_ignored/settings_dropdown/board_frame_bit_dropdown_open.png` 显示下拉列表真实展开。该阶段只闭合下拉显示和控件形态；下拉选项变更后的保存、owner 写入、驱动写入和运动闭环仍未完成。

100. 设置页轴参数读写归属文档已补“读一处、写两处”规则：按最新确认，`功能/参数归类.md` 和 `功能/4设置页设置驱动按钮作用和目的.md` 已明确读取来源和写入目标分开记录。`从站` 读取自建参数表，候选来自 `扫描从站` 写入自建参数表；用户改变后必须同时写入自建参数表和微内核参数，自建参数表保存 UI 选择/候选，微内核参数保存运行态轴-从站绑定。`bit` 默认读取驱动参数表/驱动映射表；用户改变后必须同时写入驱动参数表和微内核参数表，驱动参数表保存驱动侧人工覆盖，微内核参数表保存运行态比例链/编码器分辨率使用值。双写不得形成双真值；写入成功必须以持久 owner 写入成功、微内核参数写入成功、微内核/native readback 一致三者同时满足为准，任一目标失败时 `写入状态` 不能显示成功。本阶段只改文档，未改 VM 源码、未部署、未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。

101. 设置页参数改变写硬盘参数表和右上角按钮语义已先改文档：按最新要求，本阶段只修改 `功能/参数归类.md` 和 `功能/4设置页设置驱动按钮作用和目的.md`，明确 UI 中任意可修改参数控件发生改变时，必须直接写入该字段对应硬盘持久参数表，再从对应 owner/readback 链路回读刷新 UI；不得只改 LVGL 文本、RAM 临时变量或本地显示缓存。`从站` 改变后写硬盘自建参数表和微内核参数表，`bit` 改变后写硬盘驱动参数表和微内核参数表，成功条件为硬盘 owner 写入、受控重载/运行态写入、微内核/native readback 三者一致。设置页右上角按钮文案和语义改为 `保存并重启`，不得保留普通 `返回` 或只切主页面语义。已撤回本轮开始时未验证的 VM 源码草稿，当前阶段不实现源码、不部署、不发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。

102. 轴参数表软件盘控件规则已记录到文档：按最新要求，`功能/参数归类.md` 和 `功能/4设置页设置驱动按钮作用和目的.md` 已明确轴参数表控件类型：`轴模式`、`方向`、`从站`、`回零次序`、`回零方向`、`bit` 使用下拉；`分子`、`分母`、`写入状态` 是自动计算/动作结果只读；除此之外的轴参数单元格点击必须弹出软件盘输入。软件盘确认后必须立即写入对应硬盘参数表并按 owner/readback 回读刷新，不能只改本地显示，也不能等 `保存并重启` 才首次写表。本阶段只改文档，未改 VM 源码、未部署、未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion 命令。
103. 设置页轴参数即时写硬盘表、v3 样式软件盘和 `保存并重启` 已实现并上板验证：按最新要求，`功能/参数归类.md` 和 `功能/4设置页设置驱动按钮作用和目的.md` 已补充软件盘必须保持 v3 设置页同款数字键盘布局和视觉风格，用 LVGL 面板/按键/标签自绘实现，不得用 LVGL 默认 `lv_keyboard` 成品键盘替代。VM 真源新增/修改 `app/src/v5_settings_parameter_store.c/h`、`app/src/v5_settings_axis_table.c/h`、`app/src/v5_settings_axis_table_smoke.c`、`app/src/v5_settings_page.c`、`app/src/v5_main_page_actions.c` 和 `app/CMakeLists.txt`：设置页右上角按钮显示 `保存并重启`；轴参数可编辑单元格点击弹出 v3 同款 20 键数字软件盘；确认后按字段 owner 写入对应硬盘参数表并从目标硬盘表读回校验，`从站`/`bit` 双写路径按自建/驱动表加微内核表处理；下拉变更也走同一 commit/readback 路径；`分子`、`分母`、`写入状态` 保持只读。验证通过：VM focused build `v5_settings_axis_table_smoke v5_parameter_table_smoke v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过；运行 smoke 输出 `fields=48`、`rows=8 cols=19 ... x_pitch=6 ... x_bit=19 ...`，`v5_lvgl_shell --once` 通过；源码扫描确认 `v5_settings_axis_table.c` 无 `lv_keyboard`/`lv_textarea` 残留。已执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 上板部署，最终 relay 为 `remote_input=layout_only project_root=/opt/8ax/v5`。按增量 UI 门禁先截图再点击：`/root/Desktop/v5/artifacts/board_remote_input/v5_remote_input_20260705T133709Z_summary.json` 进入设置页，`v5_remote_input_20260705T133715Z_summary.json` 打开 `axis_X_pitch` 软件盘，Windows 截图 `D:/v5/repo_ignored/settings_param_edit_board/frame_keyboard_v3_style_final.png` 显示 v3 同款 `/ x - +`、数字、`BKSP/C/OK/ESC` 20 键布局；`v5_remote_input_20260705T133721Z_summary.json` 点击 OK 后，板端 `/opt/8ax/v5/config/settings/self_parameter_table.tsv` 写入 `X\tpitch\t5`，UI 事件 delta 为 `settings_axis_param_commit field=axis_X_pitch value=5 ok=true`，最终截图 `D:/v5/repo_ignored/settings_param_edit_board/frame_after_ok_clicked_final.png` 回到设置页且右上角为 `保存并重启`，未穿透重开其它单元格。本阶段未发送 key/mouse/linuxcncrsh/MDI/Start/motion 命令；结论为非运动设置页参数编辑路径 `board_verified`，不替代真实手指触摸校准、设置驱动实际写驱动或 motion 闭环。
104. G53 机床坐标目标位置表非红线格已接入同款软件盘编辑并上板验证：按最新截图要求，`功能/参数归类.md` 和 `功能/4设置页设置驱动按钮作用和目的.md` 已明确 G53 表中 `A中心 X`、`B中心 Y`、`C中心 Z` 三个图示红线 owner 格保持 WCS/native owner 只读，不从设置页软件盘写入；除此之外的 `A中心 Y/Z`、`B中心 X/Z`、`C中心 X/Y`、`对刀仪 X/Y/Z`、`5方向监测仪 X/Y/Z` 必须像轴参数普通输入格一样点击弹出 v3 同款 LVGL 自绘数字软件盘，确认后写对应硬盘参数表并回读刷新。VM 真源修改 `app/src/v5_settings_axis_table.c/h`、`app/src/v5_settings_axis_table_smoke.c`、`app/src/v5_settings_page.c`：G53 单元格共用轴参数的 v3 20 键软件盘；非红线 G53 几何格写硬盘微内核参数表，`对刀仪`/`5方向监测仪` 写硬盘自建参数表；红线三格不可编辑；页面创建时统一重置单元格引用，避免 G53 引用被后续轴表覆盖。验证通过：VM focused build `v5_settings_axis_table_smoke v5_parameter_table_smoke v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过；smoke 覆盖红线三格不可编辑、`B中心 X` 写 `microkernel_parameter_table.tsv`、`对刀仪 Z` 写 `self_parameter_table.tsv` 并回读；`v5_lvgl_shell --once` 通过；扫描确认无 `lv_keyboard`/`lv_textarea` 残留。已执行 `V5_BOARD_SSH=192.168.1.221 V5_BOARD_SSH_PORT=22 tools/deploy/run_v5_board_acceptance.sh --apply` 上板部署；按增量 UI 门禁先截图再点击：`/root/Desktop/v5/artifacts/board_remote_input/v5_remote_input_20260705T134449Z_summary.json` 进入设置页，`v5_remote_input_20260705T134455Z_summary.json` 点击非红线 `B中心 X`，Windows 截图 `D:/v5/repo_ignored/settings_g53_keyboard/frame_g53_keyboard.png` 显示标题 `g53_g53_b_x` 和 v3 同款 20 键软件盘；`v5_remote_input_20260705T134534Z_summary.json` 点击 OK 后，板端 `/opt/8ax/v5/config/settings/microkernel_parameter_table.tsv` 写入 `G53\tg53_b_x\t0`，UI 事件 delta 为 `settings_axis_param_commit field=g53_g53_b_x value=0 ok=true`，最终截图 `D:/v5/repo_ignored/settings_g53_keyboard/frame_g53_after_ok.png` 回到设置页且未重开其它单元格。本阶段未发送 key/mouse/linuxcncrsh/MDI/Start/motion 命令；结论为 G53 非红线单元格软件盘编辑路径 `board_verified`，不替代真实手指触摸校准、设置驱动实际写驱动或 motion 闭环。

105. 设置页 `登记本机码` 已按 v3 长动作提示和板端硬盘登记状态显示闭合一段：按最新反馈，`功能/4设置页设置驱动按钮作用和目的.md` 已明确登记成功后顶部 `本机码` 必须从板端硬盘 `/opt/8ax/drive-profiles/device_register_status.json` 回读 VPS 返回的 6 位 `vpsDistributionId`，不得继续显示 `未登记`，也不得显示或落盘 raw PL DNA/DNA hash。VM 真源 `app/src/v5_settings_page.c/h` 增加设置页本机码 label、硬盘登记状态读取和登记完成后刷新；已有 `v5_device_dna_register.py` 仍负责写硬盘登记状态。验证通过：VM focused build `v5_settings_axis_table_smoke v5_parameter_table_smoke v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过，`v5_lvgl_shell --once` 通过；上板 `run_v5_board_acceptance.sh --apply` 通过。板端登记状态文件当前含合法 `vpsDistributionId=359764`；按增量 UI 门禁先截图确认主页，再 remote-input 点击 `系统设置`，设置页截图 `D:/v5/repo_ignored/settings_machine_code/settings_machine_code.png` 显示右上角 `本机码 359764 已登记`。随后 remote-input 点击 `登记本机码`，actiond 完成 `device_dna_register` 并返回 `DNA_REGISTER_UPLOADED_PENDING_AUTH`，最终截图 `D:/v5/repo_ignored/settings_machine_code/dna_register_final.png` 显示同一弹窗保留最终成功结果且顶部仍显示 `本机码 359764 已登记`。本阶段未发送 key/mouse/linuxcncrsh/MDI/Start/motion 命令；结论为登记本机码 UI 显示/弹窗路径 `board_verified`，不替代 `下载授权/服务器下载/复位驱动/读取驱动/清除故障/设置驱动` 的真实后端闭合。

106. `下载授权` 和 `服务器下载` 已按真实 VPS/板端链路闭合一段：板端公钥已用 Windows `D:/授权私钥/device_auth_public.pem` 替换到 `/etc/6x-cnc/device_auth_public.pem`，替换前后 SHA256 均为 `5608ee95805979e3a9936a5803716fd65afeb8b64fc72e56acb0dcfc8835ac1f`，板端备份为 `/etc/6x-cnc/device_auth_public.pem.bak_20260705T141040Z`。VPS `vps3` 已确认授权文件存在于新设备目录 `/opt/8ax-auth/storage/private/359764-535e661e9ea313143fed0d86e9d982368ca9a70c7062823e25560f34ceef7f9d/device_authorization.json`，大小 1334，权限链路使用 6 位设备 ID `359764` 和 DNA 摘要目录；旧 `/opt/8ax-auth/storage/private/359764/device_authorization.json` 仍只是历史兼容文件，不作为本轮下载真源。VM 真源 `services/auth_download/v5_device_authorization_download.py` 已把授权下载 URL 修正为 VPS 当前 `GET /api/v1/device/authorization`；`services/auth_download/drive_profile_download_auth.py` 已去掉签名 payload 中服务端不验收的 `vps_distribution_id`，并把静态公共映射表下载时的 challenge 基地址解析到授权 API 域名，避免向 `updates` 静态域名请求 challenge。已把 VM/板端 v5 schema 驱动映射表发布到 VPS 公共、静态和新设备私有目录，并修正私有 `.sha256` 权限；板端 UI 点击 `下载授权` 返回 `DEVICE_AUTH_DOWNLOAD_OK`，点击 `服务器下载` 返回 `SERVER_DOWNLOAD_OK`，截图 `D:/v5/repo_ignored/settings_drive_buttons/server_download_fresh_ok.png` 显示 v3 风格进度弹窗和顶部 `本机码 359764 已登记`。板端 `/run/8ax_v5_auth_download/drive_profile_server_download_result.json` 回读 `ok=true`、`fresh_download_ok=true`、`cache_used=false`、公共/私有 profile 数均为 2、总下载 profile 数 4；板端 `/opt/8ax/drive-profiles/public/driver_profile_map.json` SHA256 为 `6EA575FAC19B44D4845AD9D98D68217755A3709D5F1FE8FB464305B62B61A4FA`，私有映射表 SHA256 为 `669DCA796BA3E456E44A1BE569503F0079200ADA8A30553B40C17C02CECF62A8`。本阶段未发送 key/mouse/linuxcncrsh/MDI/Start/motion 命令；结论为授权下载和服务器映射表下载 UI/后端路径 `board_verified`，不替代 `扫描从站/复位驱动/读取驱动/清除故障/设置驱动` 的真实闭合。

107. 主页面按钮从全 `layout_only` 恢复到分层 action，并恢复 UI 轨迹显示视图状态：按最新目标，VM 真源 `app/src/v5_main_page.c/h`、`app/src/v5_main_page_actions.c/h`、`app/src/v5_ui_shell.c`、`app/src/v5_toolpath_display.c/h`、`app/src/v5_main_page_actions_smoke.c` 已改为主页面按钮点击调用 `v5_main_page_action_prepare()`，不再统一返回 `command.name=layout_only`/`owner=ui_layout_shell`。右侧页面入口、`打开程序`、`手动输入`、`全轴`、`X1/X10/X100`、`XY/XZ/YZ/3D` 作为 `ui_local` 本地动作记录真实 command name；WCS、暂停、回零、急停、归零等已登记 command gate 的按钮返回对应 `native_linuxcncrsh` 或 `native_safety` owner，并在产品 shell 中启用 linuxcncrsh 发送路径，使用 `127.0.0.1:5007`、connect password `EMC` 格式化并发送 canonical command line。测试创建的裸 `V5MainPage` 默认不启用发送，只验证 command line 能格式化且 `executed=false`，避免本地测试发运动命令。`Start` 在未加载程序时 fail-closed，`RTCP toggle`、`点动+/-`、`首点` 因当前缺少对应 native actual/gate 仍不伪造成功。UI 轨迹投影新增 `V5_TOOLPATH_DISPLAY_3D`，主页面默认 3D，`XY/XZ/YZ/3D` 按钮实际更新 `page->view_plane`，后续 SHM trajectory/MCS/CMD 投影按当前视图重算。验证通过：VM focused build `v5_main_page_actions_smoke v5_main_page_smoke v5_ui_display_models_smoke v5_command_wrappers_smoke v5_program_command_smoke v5_lvgl_shell` 通过；运行 smoke 输出 `native_lines=prepared missing_gates=5`，轨迹/坐标/跟随误差模型仍通过。已执行两轮 `run_v5_board_acceptance.sh --apply` 完成非运动上板部署，脚本均明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion；最终部署后只 remote-input 点击本地视图按钮 `XZ` 和 `3D`，板端 `/run/8ax_v5_product_ui/ui_events.jsonl` 新增事件分别为 `view_xz/ui_local`、`view_3d/ui_local`，均 `implemented=true`、`layout_only=false`、`executed=false`、`send_status=0`。早一轮在发送路径未启用前曾点击 `G55` 验证 prepared report 为 `wcs_select/native_linuxcncrsh`；最终启用发送路径后未再点击任何 WCS/Start/Home/Jog/MDI/急停等会进入 linuxcncrsh 或运动链路的按钮。最终截图 `D:/v5/repo_ignored/main_page_buttons_trajectory/main_after_execute_path_deploy.png` 保持主页面可见。本阶段没有实际移动测试，没有发送 MDI/Start/motion；结论为主页面非运动 UI action/report、native command line 准备和轨迹视图状态 `operator_path_verified`，运动按钮真实运行效果和 motion golden loop 仍未闭合。

108. 主页面非运动页面入口已从只记 action 推进到实际驻留页面导航：VM 真源 `app/src/v5_ui_shell.c` 增加驻留页面容器和统一导航映射，`刀具补偿/探测/偏置/IO设置/网络/打开程序/手动输入` 在开机时创建对应页面，主页面按钮点击后实际隐藏主页面并显示目标页，目标页右上角 `返回主页面` 可回到主页面；这些页面当前只闭合“主页面按钮进入对应页/返回”功能，不把刀补编辑、探测流程、偏置编辑、IO provider、网络 provider、程序文件列表/运行内存副本生成或 MDI 文本执行冒充完成。验证通过：VM build `v5_lvgl_shell v5_main_page_actions_smoke v5_main_page_smoke` 通过，`v5_main_page_actions_smoke` 和 `v5_lvgl_shell --once` 通过；上板 `run_v5_board_acceptance.sh --apply` 通过且脚本明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion。按增量 UI 门禁先截图再点击：`打开程序` 实际进入程序驻留页，截图 `D:/v5/repo_ignored/main_page_buttons_trajectory/resident_program_page.png` 显示标题 `打开程序` 和 `返回主页面`；随后返回主页面，截图 `D:/v5/repo_ignored/main_page_buttons_trajectory/resident_pages_return_main.png`。继续逐项点击 `刀具补偿/探测/偏置/IO设置/网络/手动输入` 并返回主页面，板端 `/run/8ax_v5_product_ui/ui_events.jsonl` 记录 `nav_tool/nav_probe/nav_offset/nav_io/nav_network/nav_mdi` 均为 `implemented=true`、`layout_only=false`、`local_only=true`、`executed=false`；`/run/8ax_v5_product_ui/navigation_perf.jsonl` 记录 `tool/probe/offset/io/network/mdi` 页面切换约 68-69ms，返回 `main` 约 12-13ms。最终截图 `D:/v5/repo_ignored/main_page_buttons_trajectory/resident_pages_final_main.png` 保持主页面可见。本阶段没有实际移动测试，没有发送 MDI/Start/motion；结论为主页面非运动页面入口 operator path `operator_path_verified`，各页面内部业务功能仍待后续按 owner 接入。

109. `打开程序` 页面已从驻留占位推进到程序选择、二次点击打开和主页面黄线预览恢复：VM 真源 `services/program_runtime/v5_program_runtime.c/h` 在 Program Open 阶段从已读入内存的 G-code 文本抽取 G0/G1 预览点，存入 `V5ProgramRuntime.preview_trajectory`；`app/src/v5_main_page.c/h` 优先使用已打开程序的预览点绘制主页面黄色程序层，并显示程序名、行数和点数，未打开程序时仍使用 SHM/status 轨迹；`app/src/v5_ui_shell.c` 的程序页列出项目内 `gcode/golden/cc.ngc`，第一次点击选中，第二次点击调用既有 `v5_main_page_open_program()` 读入内存并切回主页面；`config/deploy/v5_runtime_deploy_manifest.tsv` 安装该程序到板端 `/opt/8ax/v5/gcode/golden/cc.ngc`。验证通过：VM build `v5_program_runtime_smoke v5_program_command_smoke v5_main_page_smoke v5_main_page_actions_smoke v5_ui_display_models_smoke v5_lvgl_shell` 通过，smoke 输出 `preview=3`、`preview=2` 和主页面 `points=5`；`run_v5_board_acceptance.sh --apply` 已重新上板并安装 `cc.ngc`，脚本明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion。按增量 UI 门禁先截图再 remote-input：点击 `打开程序` 进入程序页，切页约 67ms；第一次点击程序行写入 `/run/8ax_v5_product_ui/program_events.jsonl` 的 `select`，第二次点击同一行写入 `open`，路径 `/opt/8ax/v5/gcode/golden/cc.ngc`、`ok=true`、`bytes=195`、`lines=11`、`display=cc.ngc`，随后自动回主页面，返回约 12ms。该步骤只验证程序选择、打开入内存和 UI 黄线预览链路，没有发送 Start/MDI/motion；Start 的 runtime handoff、真实 LinuxCNC Open/Run 和 `cc.ngc` golden motion 仍未闭合。

110. `手动输入` 页面已从驻留占位推进到 MDI 文本载入内存和 Start 准备链路：VM 真源 `services/program_runtime/v5_program_runtime.c/h` 增加 `PROGRAM/MDI` runtime mode，`v5_program_runtime_set_mdi_line()` 将可见 MDI 单行写入同一个运行内存 owner；`services/command_gate/v5_command_gate.*`、`v5_command_table.c` 和 `v5_linuxcncrsh_client.c` 增加 `mdi_run/native_linuxcncrsh`，格式化为 `Set MDI <line>`；`app/src/v5_main_page.c/h` 增加 `v5_main_page_set_mdi_text()` 并让 Start 在 MDI mode 下准备 `mdi_run`，未载入程序或 MDI 时仍 fail-closed；`app/src/v5_ui_shell.c` 的 MDI 页显示默认非运动 MDI 行 `G4 P0`，点击 `载入MDI` 只写入内存并返回主页面，不执行 MDI。验证通过：VM build `v5_command_gate_smoke v5_program_runtime_smoke v5_program_command_smoke v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过，smoke 输出 `mdi=G4 P0`、`line=Set MDI G4 P0`、主页面 `mdi=start ... missing_gates=4`；扫描未发现 `program_open_cc`、旧 start handler、RTCP target 中介、`BROKER_START_RUN` 或主页面新路径硬编码项目绝对地址。已执行 `run_v5_board_acceptance.sh --apply` 上板，UI shell 报告 `commands=14`，脚本明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion。按增量 UI 门禁先截图再 remote-input：点击 `手动输入` 进入 MDI 页，切页约 67ms；点击 `载入MDI` 后 `/run/8ax_v5_product_ui/mdi_events.jsonl` 记录 `load line=G4 P0 ok=true`，随后回主页面约 12ms，`v5_linuxcncrsh_probe` 仍回读 `MACHINE OFF`。该阶段没有点击 Start，没有执行 MDI，也没有真实运动；结论为 MDI 页面载入内存和 Start 准备链路 `operator_path_verified`，真实 MDI 执行仍需后续按运动/操作员门禁验证。

111. 主页面 `点动+/-` 已从缺 gate 推进到 native jog increment 命令准备和上板部署：VM 真源 `services/command_gate/v5_command_gate.*`、`services/command_gate/v5_command_table.c`、`services/command_gate/v5_linuxcncrsh_client.c` 增加 `jog_increment/native_linuxcncrsh`；`app/src/v5_command_motion.c/h` 增加 `v5_command_jog_increment_prepare()`，按当前步距 `X1/X10/X100` 生成增量点动请求；`app/src/v5_main_page_actions.c/h` 和 `app/src/v5_main_page.c` 将 `点动+/-` 接到该 native owner，当前默认轴为 `X`，正向格式化为 `Set Jog_Incr X 100.000 <step>`，反向格式化为 `Set Jog_Incr X -100.000 <step>`。验证通过：VM `v5_command_wrappers_smoke` 输出 `jog_plus=Set Jog_Incr X 100.000 10.000`、`jog_minus=Set Jog_Incr X -100.000 10.000`，`v5_main_page_actions_smoke` 输出 `jog=native native_lines=prepared missing_gates=2`，`v5_lvgl_shell --once` 报告 `commands=15`；定向扫描未发现旧 `BROKER_JOG`、旧 start handler、硬编码项目绝对路径或新 fallback 路径。已执行 `run_v5_board_acceptance.sh --apply` 上板部署并通过非运动 source/deploy/verify，板端只读 `v5_linuxcncrsh_probe` 回读 `MACHINE OFF`。本阶段按用户“不用实际移动测试”要求，没有点击 `点动+/-`，没有发送 jog、MDI、Start 或 motion 命令；结论为点动命令准备和部署 `local_verified_only`，不是运动闭环验证。后续还需把坐标/轴选择接入 `点动+/-`，否则默认 X 轴只能作为当前最小可验证路径。

112. 主页面 `RTCP` toggle 已从无条件缺 gate 推进到“native actual 有效时才准备反向 M-code”的严格路径：VM 真源 `app/src/v5_command_rtcp.c/h` 增加 `v5_command_rtcp_toggle_prepare()`，只消费 `V5NativeReadback.rtcp_actual_available/rtcp_enabled`，actual 缺失时清空 prepared/request 并 fail-closed；`app/src/v5_main_page.c/h` 增加页面级 `V5NativeReadback` 和 `v5_main_page_set_native_readback()`；`app/src/v5_main_page_actions.c/h` 在 `V5_MAIN_PAGE_ACTION_RTCP_TOGGLE` 中使用 native readback 反向选择 `M128/M129`，不得用按钮文字、默认 OFF、SHM modal 文本、`target_rtcp_active` 或旧 Python/Broker 中介推断目标。验证通过：VM focused build `v5_command_wrappers_smoke v5_main_page_actions_smoke v5_main_page_smoke v5_lvgl_shell` 通过；`v5_command_wrappers_smoke` 输出 `rtcp_toggle_off=Set MDI M129`、`rtcp_toggle_on=Set MDI M128`，并覆盖 actual unavailable 时 toggle 不准备；`v5_main_page_actions_smoke` 输出 `rtcp=toggle native_lines=prepared missing_gates=1`，当前只剩 `首点` 缺 gate；定向扫描未发现 `target_rtcp_active`、`target_mcode`、旧 start/jog Broker、硬编码项目绝对路径或新 fallback 路径。已执行 `run_v5_board_acceptance.sh --apply` 完成非运动上板部署，板端 `v5_lvgl_shell` 报告 `commands=15`，只读 `v5_linuxcncrsh_probe` 回读 `MACHINE OFF`，acceptance 明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion。当前产品 shell 还没有把真实板端 `rtcp_actual` native status provider 接入 `v5_main_page_set_native_readback()`，因此本条只证明 RTCP toggle 的严格命令准备逻辑和部署，不能声明板端点击 `RTCP` 已可执行；后续必须接真实 `rtcp_actual` provider 后再按增量门禁验证，不得用 SHM `runtime_modal_text` 反推控制 actual。

113. `首点` 的 Program Open 前置 runtime metadata 已补进内存，但未接假 first-point gate：VM 真源 `services/program_runtime/v5_program_runtime.c/h` 在 `v5_program_runtime_open_file()` 成功后保存 `source_path` 和 `loaded_epoch`，并提供 `v5_program_runtime_source_path()`、`v5_program_runtime_loaded_epoch()`、`v5_program_runtime_has_first_point_metadata()`；`V5ProgramOpenResult` 同步返回 `source_path/loaded_epoch`。这些数据随 Program Open 读入内存建立，Start/后续按钮不需要重新读文件或扫描目录。验证通过：VM focused build `v5_program_runtime_smoke v5_program_command_smoke v5_main_page_actions_smoke v5_lvgl_shell` 通过；smoke 输出 `epoch=1 source=v5_program_runtime_smoke.ngc preview=3` 和 `epoch=1 source=v5_program_command_smoke.ngc preview=2`，`v5_main_page_actions_smoke` 仍为 `missing_gates=1`，说明 `首点` 没被假装接通；定向扫描未发现旧 `program_open_cc`、旧 start handler、`target_rtcp_active/target_mcode`、旧 Broker jog/start 或硬编码项目绝对路径。已执行 `run_v5_board_acceptance.sh --apply` 非运动上板部署，板端 `v5_lvgl_shell` 仍报告 `commands=15`，只读 `v5_linuxcncrsh_probe` 回读 `MACHINE OFF`，acceptance 明确未发送 touch/key/mouse/linuxcncrsh/MDI/Start/motion。当前仍缺 `sha256` 和 v5 first-point native gate，所以 `首点` 按钮保持真实缺口，不能恢复 v3 空函数或旧 Broker/Python/JSON 中介。

当前缺口：

1. 板端当前已从先前 SSH 不可达状态恢复，`192.168.1.221:22` 可达；不再需要先执行继电器重启作为下一步。
2. VM `/root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply` 已完成非运动 build/source/deploy/verify、常驻 v5 UI relay 和 framebuffer 诊断抓图：脚本现在先构建 `build/board` 产物，再做部署包传输、init.d 服务启动、State Publisher 默认 SHM、`v5_lvgl_shell --once`、`v5-ui-relay --serve`、真实 `v5_linuxcncrsh_probe`、remote full-frame probe 和 `/dev/fb0` 抓图。relay 和 framebuffer 证据已能证明真实 v5 主页面可显示；`/dev/fb0` 图仍只作为诊断，不能替代触控或 motion。
3. golden `cc.ngc` 的板端命令发送工具已经建立并已默认使用 `EMC` connect password；motion 前置只读 probe 返回 `MACHINE OFF`，但尚未在真实板端执行 `--motion`。运动闭环仍未完成，当前状态不能标记为整机 motion `board_verified`。
4. v5 真实触摸校准、诊断链路、LVGL pointer 输入接入和校准工具 fallback/legacy 清理已完成并上板；当前主页面已从全 `layout_only` 推进为非运动 UI action/report 可验证状态，`XY/XZ/YZ/3D` 轨迹视图状态已恢复，WCS、暂停/恢复、回零、急停、工件零点、Start、MDI、`点动+/-` 和 RTCP toggle 等已登记 gate 按钮能准备真实 owner/name/command line，其中 `点动+/-` 当前只闭合到 X 轴 jog increment 命令准备和上板部署，RTCP toggle 只闭合到 native readback 注入条件下的反向 `M128/M129` 准备，二者均未做点击/运动验证；产品 shell 尚未接真实 `rtcp_actual` provider，`首点` 已有 Program Open 的 `source_path/loaded_epoch` 前置 metadata，但仍缺 `sha256` 和 v5 first-point native gate。右侧非运动页面入口和 `打开程序/手动输入/网络` 已能实际进入驻留页并返回主页面。设置页轴参数表已按 `v5_parameter_table` owner 归属拆出独立模块并上板验证：8 轴、19 列、固定轴名列、右侧横向滚动和字段 owner 校验均已闭合；三来源读取已经上板截图确认：runtime INI/native、自建参数表和 drive provider 均能在设置页显示，龙门/刀库作为第 7、8 轴按普通轴显示，首屏包含 `轴模式/方向/从站`，无 `NAT`/空占位/异常灰色禁用。编辑保存、设置驱动写入和运行态回读仍未完成。页面切换已改为 RAM 预渲染缓存 blit，板端真实日志显示主页面/设置页互切约 0.012-0.013 秒，新增驻留占位页切换约 0.068-0.069 秒，均满足 0.2 秒内先出界面。后续 UI 空壳/非运动按钮命中由 AI 模拟屏幕点击推进并记录截图/UI event delta；触摸校准、真实触摸硬件健康、Start/MDI、运动和 owner 明确要求真人触摸的路径仍不能用模拟点击替代。运动相关按钮真实下发和 motion 闭环仍未闭合。
5. 远程 relay 产品 UI 截图和 `/dev/fb0` 诊断截图均已证明真实 v5 v3 风格主页面和设置页空壳可显示；页面切换性能也已用板端 `navigation_perf.jsonl` 证明低于 0.2 秒。这些只是显示、layout-only 导航和切页速度闭合证据，不是最终 UI 功能完成。最终 UI 仍要恢复 v3 同等布局和每个可见按钮功能，并按 `功能/7主页面按钮功能.md`、`功能/4设置页设置驱动按钮作用和目的.md` 及其它 owner 文档验收。截图显示异常时直接修工具链，不再把抓屏格式或分辨率细节写成独立规则。

下一步：

1. 页面切换速度已满足 0.2 秒内先出界面，后续不要再围绕远程全帧抓取耗时误判物理屏切页速度；若切换再次变慢，先看 `/run/8ax_v5_product_ui/navigation_perf.jsonl` 的 `cache_blit` 和 `elapsed_us`。
2. 继续使用受限 remote-input 证据工具推进非运动 UI 空壳页面导航；每次点击/滚动前必须先截图确认当前画面和目标，动作后保存截图和 UI event delta。不得把模拟点击当成触摸校准、真实触摸硬件健康、Start/MDI、运动或 owner 明确要求真人触摸的验收。
3. 下一步继续做主页面剩余缺口：先接真实 `rtcp_actual` native status provider 到 `v5_main_page_set_native_readback()`，再在不实际运动前提下只验证 RTCP 按钮 prepared/report；随后补 `首点` 所需 `sha256` 和 v5 native gate，禁止恢复 v3 空函数、旧 Broker/Python/JSON 中介或假成功提示。
4. 设置页按钮功能仍需继续：`扫描从站/复位驱动/读取驱动/清除故障/设置驱动` 逐个按 owner 分组接回；已验证的 `登记本机码/下载授权/服务器下载` 只代表登记显示、授权文件下载和映射表同步闭合，不代表后续驱动总线按钮完成。每接回一组都单独跑 focused build/smoke、AI 模拟点击和 owner 要求的最小后端证据。
5. `扫描从站` 的板端真实候选快照还缺真实从站环境证据：当前板端返回 `DRIVE_SCAN_NO_SLAVES`，所以未生成 `/run/8ax_v5_drive/settings_self_parameter_table.json`；后续接入真实 EtherCAT 从站后，必须复测扫描成功、确认自建参数快照含 `slave_options`，并确认 `从站` 列下拉只能从这些候选选择。
6. 若后续触控脚本发现 `/opt/8ax/safe_ui/re_touch_calibration.json` 缺失、旧 `/opt/8ax/ui/re_touch_calibration.json` 仍存在，或真实触摸 owner 验收要求 `touch_events.jsonl` 新增真实手指事件，先修 canonical 触摸校准/诊断链路，不得扩大按钮热区或用远程输入替代真实触摸硬件证据。
7. 如果板端再次不可达，按 `AGENTS.md` 的 `V5_BOARD_UNREACHABLE_RECOVERY_RULE`：先打开 COM 串口监控，再用 VM 真源 `/root/Desktop/v5/tools/v5_board_power_cycle.py` 继电器断电重启，记录 U-Boot、kernel、网卡、IP、SSH、init.d、LinuxCNC 证据。
8. golden `cc.ngc` 运动闭环只在显式允许真实运动并设置 `V5_ALLOW_MOTION=1` 后，在 VM `/root/Desktop/v5` 运行 `/root/Desktop/v5/tools/deploy/run_v5_board_acceptance.sh --apply --motion`；执行前继续扫描并删除旧 Broker/action/helper/JSON/rotary 本地算法、停稳/速度门禁和假入口残留。
9. 如果后续 LinuxCNC 再失败，优先读取 `/run/8ax/v5_linuxcnc.log`、`/run/8ax/v5_linuxcncrsh.log`、`/home/petalinux/linuxcnc_debug.txt`、`/home/petalinux/linuxcnc_print.txt` 和 COM 启动日志，修 canonical `v5_pulse.ini/hal/init.d` 路径，不新增 fallback gate 或报错提示替代实现。
