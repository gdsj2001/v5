# 启动顺序最高要求：Linux -> LinuxCNC/微内核 -> UI -> 远程界面

简写次序：Linux-linuxcnc微内核-ui-远程界面

# 开机参数入内存与微内核参数真源要求

最高口径：LinuxCNC/微内核/native 到 UI 的轻量数据，以及 LinuxCNC/微内核自身运行所需闭包，必须在开机阶段一次性进入常驻 RAM。运行期 UI、Broker、动作脚本和显示 provider 不得反复读取硬盘、JSON、INI、var、tool 文件、脚本快照或 result 文件来补当前状态。

这里的“全读进内存”指 LinuxCNC/微内核运行闭包、UI 运行闭包和所有产品自写运行代码闭包，不是把 SHM 扩展成通用状态池，也不是把 UI 变成控制真源。

```text
硬盘/闪存持久 owner
-> 开机一次性读取 LinuxCNC/微内核运行闭包、UI 运行闭包、产品自写运行闭包和参数 owner 映射到 RAM
-> LinuxCNC/微内核/native 常驻内存与原生 readback
-> State Publisher 30ms 写 UI 显示 SHM 投影
-> UI 只读显示
```

硬盘/闪存只允许作为开机装载、设置保存落盘、重启/重载前的持久 owner；运行期控制、运动准入、状态 readback、UI 显示 provider 和动作证明必须来自 LinuxCNC/微内核/native 常驻内存或原生 API。

开机常驻内存闭包必须至少包含四类：

1. LinuxCNC/微内核运行闭包：executable、共享库、RTAPI/实时模块、HAL component、kinematics component、INI/HAL 配置、`PARAMETER_FILE`、`tool.tbl`、G53/RTCP 几何、WCS/G92 初始 readback、驱动映射和已登记 native gate 表。
2. 参数 owner 映射闭包：参数表字段、唯一 owner、唯一 writer、源位置回读字段、generation/readback token、drive-only 例外字段和删除旧路径条件。
3. UI 显示投影闭包：只允许坐标/轨迹、模态、主轴转速、进给速度、主轴倍率、进给倍率进入 `/dev/shm/v3_status_shm`。
4. 产品自写运行闭包：C UI、Command Broker、Actions、provider、native gate wrapper、运行期需要的资源索引、Python 字节码和动作登记表必须在启动阶段加载到 RAM。运行期 UDS 调用不得临时从硬盘发现脚本、动态导入 action、扫描目录补入口、读取 UI 资源文件补状态或靠 fallback wrapper 恢复旧路径。

运行期允许触发硬盘/闪存的场景只有：设置保存写回唯一持久 owner、程序文件选择/打开并生成运行内存副本、日志/诊断写入、受控重启或重载。除此之外，UI 显示、控制动作、运动准入、保存成功、参数稳定显示和安全判断都不得读硬盘兜底；Start/Resume 等热路径不得再读程序文件、脚本、配置或资源。

引用需求真源：`REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-DOC-SINGLE-SOURCE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-NATIVE-OWNER-FIRST`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION`、`REQ-LINUXCNC-COMMAND-GATE`、`REQ-G92-NATIVE-RUNTIME`、`REQ-WCS-NATIVE-OWNER`、`REQ-ROTARY-UNWRAP-ABSOLUTE-PROOF`、`REQ-ROTARY-REBASE-NATIVE-GATE`、`REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-GCODE-RUN-HOT-PATH`、`REQ-MOTION-STILLNESS-TOLERANCE`
## AI 阅读入口

- 本文定位：开机参数、LinuxCNC/微内核运行闭包、常驻内存、`settings_runtime.json` drive-only、比例链、V3 上层运动门禁退役和 G-code 运行热路径的 owner。
- 先读顶部最高口径和 `REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-SCALE-CHAIN-HISTORY-POLLUTION`、`REQ-MOTION-STILLNESS-TOLERANCE`、`REQ-GCODE-RUN-HOT-PATH`，再读具体字段章节。
- 修改代码时只允许按“持久 owner -> 开机全量入 RAM -> 微内核/native readback -> UI/动作直接消费”闭环落地。
- SHM 例外只允许 30ms UI 显示投影，承载坐标/轨迹、`runtime_modal_text`、`spindle_speed_rpm`、`linear_velocity_mm_per_min`、`feedrate_override`、`spindle_override`；不得用 UI dirty、默认值、旧 JSON、`config_cache`、`/run` result、SHM 投影或硬盘热路径补真值。
本文同时是 `REQ-SCALE-CHAIN-HISTORY-POLLUTION` 的 owner；比例链、`SCALE`、电子齿轮和历史 JSON/cache 污染规则在本文维护，`待做工作/改进.md` 只记录尚未闭合的实施卡，不再复制 REQ 正文。
比例链与 raw 限位规则：设置页修改螺距、每转角度、`motor_rev/load_rev`、减速比、目标精度或轴方向等会改变 runtime INI `SCALE` 的字段时，如果该轴已启用 BUS count-domain `zero_model`，同一次 settings apply 必须用新 `SCALE` 重新计算硬盘 raw 限位并写入 runtime INI/native owner。公式固定为 `raw_zero_position = zero_counts / new_SCALE`，`raw_min_limit = raw_zero_position + ui_min_limit_distance`，`raw_max_limit = raw_zero_position + ui_max_limit_distance`。UI 限位距离仍是相对 0 位的物理距离，不得被 raw 坐标覆盖；`MIN_LIMIT/MAX_LIMIT` 是最终重启装载用的硬件 raw 坐标。该保存动作只证明硬件 owner 已更新，LinuxCNC/HAL/EtherCAT live raw 限位必须等最后统一重启后的 native 内存/API 回读证明；SHM/UI 只作显示投影。
## P0 CPU 热路径要求：慢投影只能缓存后发布

### P0 微内核运动安全 owner 与上层速度门禁退役

`REQ-MOTION-STILLNESS-TOLERANCE` 的最终口径是退役 V3 上层速度门禁。运动直接由 LinuxCNC/微内核/HAL/EtherCAT 控制；停稳、速度安全、busy/idle、队列状态、驱动写入前安全窗口、Home/Work Zero/Rotary/Rebase 等运动准入都属于微内核/native 内部 owner。V3/UI/Broker/product action 不得维护 `motion_stillness_gate` 参数组，不得硬编码 `速度为 0`、`current_vel == 0`、`linear_velocity == 0`、`0.001`、`0.1` 或等价私有阈值，也不得把 SHM、JSON、状态快照、动作结果或环境变量当作停稳证明。
上层只允许发起请求、显示微内核返回的状态/错误码，以及在真正需要时直连读取微内核/native 原生状态做诊断展示；不得在请求前增加自造速度门禁或把微内核拒绝包装成 V3 预检失败。旧共享函数、旧参数、旧 runtime INI `[MOTION_STILLNESS_GATE]` 投影字段、测试白名单、环境变量、fallback 和文档示例都应按“先删再补”原则删除；如果某动作确实需要底层安全判断，只接入微内核/native 的 canonical gate，不在 V3 重新实现判定。

- State Publisher 写入 `/dev/shm/v3_status_shm` 时必须保证常驻内存，以 30ms 为目标周期。采用 SeqLock 奇偶校验、CRC 校验、retry 和 degraded/fail-closed 逻辑。产品路径在应用参数时，为避免同步/flush/seqlock 冲突，如果需要设计 flush 双页实验，必须先更新本地 owner 并在同一个 slice 中提供验证证据。
最高执行口径：microkernel/LinuxCNC/HAL/EtherCAT/native 已有或可拥有 owner 的字段，V3 自造参数、兼容 JSON、cache、var、snapshot、状态文件和前端 UI 字段必须从产品路径直接删除；不得作为来源、长期兼容层、fallback、配置生成输入、保存成功依据或运行判断。任何文档与此冲突时，以本段为准。
自造参数准入门：只有在完成微内核 LinuxCNC/HAL/EtherCAT/native owner 盘点后，确认没有现有字段、没有可登记 native helper/gate、没有可扩展的运行内存 API readback，也没有等价持久 owner 能承载该语义时，才允许新增或保留 V3 自造参数。新增前必须在本文件记录 owner 卡：为什么微内核没有、目标语义、唯一 writer、readback/SHM 投影边界、删除或下沉条件。凡是微内核/native 已有或可拥有的参数，V3 自造字段必须删除；不得扩展调用面、不得新增写路径、不得把旧字段继续作为配置生成输入、运行门禁、保存成功依据、fallback 或诊断兜底。每个 slice 只要碰到这类重复参数，就先把仍依赖它的产品路径迁到微内核/native owner，再删除该 slice 内可删的 V3 自造入口。
微内核优先使用规则：微内核/native 已经有的参数、状态和运行数据，除 `功能/共享内存.md` 白名单允许的 UI 显示投影外，产品路径必须尽量直接使用微内核/native 常驻内存、原生 API 或登记 native memory block。禁止自建参数表的范围只限于两类：微内核/native 已有或可直接拥有的参数，以及驱动参数映射表/drive provider 已有的驱动私域参数；这些字段不得再造第二套参数表、JSON/cache、状态文件、SHM 字段或前端字段。微内核和驱动参数映射表都没有承载的设置页轴参数，必须进入自建参数表，由自建参数表作为唯一 owner 并在开机进入 RAM；设置页 UI 必须自动从自建参数表读取这些字段，不得用 UI 默认值、旧 JSON、`/run` result 或 SHM 投影补值。`设置驱动`/驱动参数表是明确例外：驱动 profile、SDO/PDO 对象字典、encoder raw/multiturn、电子齿轮、count-domain 校准、驱动写入/读回证据和授权下载状态可以使用项目自有 schema 和 drive-only 参数表，但只能服务驱动私域，不能反向成为 LinuxCNC/微内核运动参数、坐标基、刀补、WCS/G92、RTCP actual、模态或安全真源。

本文定位：本文件是 V3 开机参数入内存、微内核/native 真源和第二真源禁止边界的最高功能需求，也是 `REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE` 和 `REQ-SETTINGS-RUNTIME-DRIVE-ONLY` 的 owner。其他功能文档只应引用这个 REQ，不应复制本文件的可变政策全文。
## P0 最高规则：参数在内存闭环

必须遵守的链路：

```text
开机：
硬盘/闪存持久 owner
-> 开机一次性读取 LinuxCNC/微内核运行闭包、参数 owner 映射、UI 运行闭包和产品自写运行闭包到 RAM
-> LinuxCNC/微内核/native 常驻内存与原生 readback
-> UI/provider 直连微内核 native 运行内存或登记 API 显示
-> SHM 显示例外：State Publisher 只把编码器反馈机械坐标、运动学理论坐标、模态、转速、进给速度和主轴/进给倍率写入 30ms 显示投影

UI 修改：UI dirty/request
-> 保存请求构建，提交瞬间从当前微内核/native UI 状态 provider 取最新 owner generation/readback token；UI 显示 freshness 才使用 SHM `status_epoch`
-> Broker UDS
-> settings_apply / native helper / 受控 apply
-> 写回该字段唯一 owner
-> 源位置回读确认 -> UI 可清该字段 dirty / 显示已保存，待重启或待运行态刷新 -> 需要重启生效的字段保留 restart pending
-> 右上角保存并重启前台只做轻量 restart precheck + backend/UI handoff + 恢复主页面；后台在新 UI 恢复/进入主页面后必须通过正式 `estop_or_reset` product action 发起上使使能，machine-on 和驱动 Operation Enabled 结果只作为后台恢复诊断 -> 重启后的最终运行态，运动准入/刀路真值仍以微内核/native 内存或同 owner 运行态直接回读为准；SHM 只允许承载 30ms UI 显示投影。
```

### P0.1 执行口径

- 参数表里的全部参数都必须走同一条闭环，不按字段另开显示/保存捷径。开机链路是：硬盘参数或原生 owner -> 对应 owner/backend apply 读入微内核 native 运行内存 -> UI/provider 直连显示。参数表不得再通过 SHM 稳定显示。 - UI 参数表稳定显示只能来自 fresh 微内核 native 运行内存或登记只读 API。SHM 投影、Broker/read-only 结果、`/run` JSON、`config_cache`、诊断快照、UI 本地缓存及 dirty 值都不能作为稳定显示兜底；它们只能作为一次请求输入、动作证据或离线诊断。 - 微内核 native 常驻内存是运行态事实和运动安全判断的 owner；除编码器反馈机械坐标和运动学理论坐标外，不得把这些事实投影为 typed SHM 或诊断状态块用于稳定显示。普通产品路径不得通过 SHM、JSON 或上层缓存访问替代下层内存事实，也不得重建自造速度门禁。唯一 owner 的读取必须是启动加载、保存事务、重启/重载、generation/签名变化触发或显式诊断，不能成为每帧同步机制。 - UI 更改链路是：UI dirty/request（保存请求构建，提交瞬间从当前微内核/native 状态 provider 取最新 generation/status，坐标 SHM epoch 不能作为保存准入证明。 -> Broker UDS -> product action -> 已登记 native owner/helper 或受控 settings_apply 写入唯一硬盘/native 参数。 -> 源位置回读确认 -> UI 可清该字段 dirty、显示“已保存/待重启”并进入下一步。需要 backend reload 的字段保留 restart pending；右上角 `保存并重启` 的前台边界是 restart precheck + backend/UI restart handoff + 恢复主页面，不等待完成 SHM、machine-on 和 Operation Enabled。settings 轻量保存重启链路必须在新 UI 恢复/进入主页面后，通过正式 `estop_or_reset` product action 发起上使使能；machine-on 和驱动 Operation Enabled 结果只作为后台恢复诊断，不作为前台 handoff 完成条件。 - “写到参数表”只能写到该字段的唯一 owner：微内核/native 字段如 runtime INI、`PARAMETER_FILE`、`tool.tbl`、HAL/EtherCAT/native helper 或登记 owner；`设置驱动` 私域字段才允许写 `settings_runtime.json` drive-only/驱动证据。任何字段都不能因为 UI 需要显示而新建 V3 第二套落盘参数表。 - 文件写入、UI 输入值、`config_cache`、Broker/result JSON、`/run` 快照、诊断 JSON、SHM 投影或默认模型值都不是保存成功。字段级“已保存/待重启”必须有唯一 owner 写入/持久化结果和源位置回读；需要跨重启恢复的字段还必须保留 restart pending，运行态最终生效和刀路显示仍必须以后续微内核/native 内存或同 owner 运行态直接回读为准。 - 运行期 actual 与持久参数分域：G-code/MDI/按钮造成的解释器模态、G92 actual、RTCP/switchkins ON/OFF、倍率、HAL/IO actual、运动状态等只改当前微内核 native 运行内存/actual，并由 UI/provider 直连显示，不反写硬盘默认值、runtime INI 默认、`settings_runtime.json`、SHM 或 V3 私有参数文件。 - UI 机械坐标、UI 轨迹动态当前位置、绿色机械点、实际刀尖/刀柄反馈点和所有以 MCS 为基础的显示值，源头必须是编码器反馈或 LinuxCNC/HAL/EtherCAT/native owner 提供的关节实际反馈；该反馈先进入后端运行内存，再由 State Publisher 写入同一个 typed SHM/受控内存投影，UI 只显示这一帧内存值。UI 不得从参数表、`settings_runtime.json`、`/run` JSON、Broker result、旧 SHM 快照、模型默认值、命令坐标 `cmd_mcs` 或本地缓存推算，兜底机械坐标。 - SHM freshness 与 `status_epoch` 异常不得静默重置：`age_ms < 0`、状态时间戳向前跳、`status_epoch` 回退、monotonic 基准异常或系统时钟突变，必须标记 degraded/time anomaly 并让控制请求、保存请求和状态 freshness 判断 fail-closed。UI 显示层最多可用本地 monotonic 时间做短暂视图 grace，但不得 reset epoch、改写系统时钟、伪造 fresh、复用异常 epoch 放行，或把时间轴自动归零当成链路恢复。
### P0.2 核心口径
- 除 `设置驱动` 的驱动私有域外，能由 LinuxCNC runtime INI、`PARAMETER_FILE`、`tool.tbl`、HAL/EtherCAT actual 或已登记的非 Python native gate/helper 承载的参数，必须进入对应 native owner。 - 开机入内存是读取 native owner/唯一硬盘 owner 后形成微内核/native 运行内存，不是再造一份可写参数文件，也不是再造一个 SHM 参数投影。 - 常驻脚本/daemon 的职责是将 native/runtime 坐标 actual、commanded/theory、模态文字、转速、进给速度和倍率显示字段读入内存并按 30ms 单向投影到 SHM；它不是新的参数 owner，也不得将 SHM 改造成控制写入通道、诊断总线或通用状态总线。状态源缺失、过期、结构错误或回读失败时必须发送 unavailable/degraded 或让动作 fail-closed，不能用旧缓存、默认值、硬盘文件或 `/run` result 补成有效状态。 - UI dirty 值、一次性 Broker request payload、Broker/result JSON、`/run` 快照、诊断文件和 `config_cache` 只允许作为瞬时请求输入、动作证据或离线诊断，不是稳定显示、保存成功、落盘参数、配置生成输入或最终真源。 - 可持久字段必须先写入对应硬盘/native owner，再刷新 native/runtime 内存并由该 owner 直接回读确认；不得靠 SHM 证明。热 apply 场景也必须证明 owner持久化结果和微内核 native 内存一致。运行期 actual、解释器瞬态模态、RTCP 运行期开关只进微内核/native 内存，不写回硬盘默认值或 SHM。 - 微内核 native 已经拥有或可拥有的字段，不允许保留任何 V3 自造落盘参数、兼容 JSON、cache、var 或状态文件；历史兼容字段不得作为产品输入、待迁移来源或诊断兜底；同一字段/slice 必须接回 native owner 并删除旧入口。 - V3 自造参数文件按字段迁移和删除；审 owner 卡时只能盘点和同步文档，不能实现写路径、保留兼容路径或声称迁移闭环。
### P0.2.1 参数表字段 owner 清单

代码 owner：`lvgl_app/include/v3_parameter_table.h` 和 `lvgl_app/src/v3_parameter_table.c`。脚本侧只能通过 `lvgl_app/scripts/v3_parameter_table.py` 解析这张 C 表，不得在 Python 中再维护参数字段白名单。设置页只允许通过这张表判断字段 owner、下拉选项、只读状态、立即应用状态、动作字段和运行内存 readback；不得在 UI、Broker、State Publisher、脚本或测试里再写第二套字段白名单。

| 字段范围 | 唯一 owner | UI/代码入口 | 回读来源 | 禁止路径 |
| --- | --- | --- | --- | --- |
| `motion_driver_mode`、`bus_pulse_active_mode` | 微内核/native active mode | 参数表 `RE_V3_PARAMETER_OWNER_NATIVE_RUNTIME` | `native:active_motion_driver_mode` | `settings_runtime.json`、SHM、UI 默认值、旧模式文件单点判断 |
| `axis_X/Y/Z/A/C_axis_mode`、`precision` | active runtime INI `[AXIS_*]`/`[JOINT_*]` 元数据和 SCALE 相关 owner | 参数表动态轴字段 | runtime INI/native axis readback | drive-only incremental owner、旧 V3 JSON、`config_cache` |
| `axis_*_pitch`、`axis_*_motor_rev`、`axis_*_load_rev`、`axis_*_slave` | 自建参数表；仅在微内核/native 和驱动参数映射表都没有该字段时允许 | 参数表动态轴字段 | 自建参数表开机 RAM 快照/readback | UI 默认值、旧 V3 JSON、`config_cache`、SHM、驱动映射表中不存在的伪字段 |
| `axis_*_max_velocity`、`axis_*_max_acceleration`、`axis_*_backlash`、`axis_*_ferror`、`axis_*_min_ferror` | LinuxCNC runtime INI 原生运动 owner | 参数表动态轴字段 | `[AXIS_*]`/`[JOINT_*]` readback | UI 模型默认值、SHM、`/run` result |
| `axis_*_home_order`、`axis_*_home_direction`、`axis_*_home_offset`、`axis_*_home_search_vel`、`axis_*_home_latch_vel`、`axis_*_home_final_vel` | LinuxCNC runtime INI Home owner | 参数表动态轴字段 | `[JOINT_*] HOME*` readback | 设置页缓存、旧回零配置、SHM |
| `axis_*_soft_minus`、`axis_*_soft_plus`、`soft_limits` | LinuxCNC runtime INI limit owner | 参数表动态轴字段 | `[AXIS_*]`/`[JOINT_*] MIN_LIMIT/MAX_LIMIT` readback | drive-only 文件、旧软限位 JSON、SHM |
| `axis_*_zero` | 模式相关真实 owner：BUS 为 drive-only `zero_model` 证据并接 native/runtime readback；Pulse 为 runtime INI `HOME_OFFSET` | 参数表 `RE_V3_PARAMETER_OWNER_MODE_DEPENDENT_AXIS_ZERO` action | 当前模式 owner 直接回读 | 显示坐标种子、SHM/MCS、旧快捷设零字段 |
| `axis_*_slave`、`axis_*_encoder_bits`、`axis_*_profile_id`、`drive_profile_id` | Settings Drive private domain | 参数表 drive-only owner | drive-only settings/runtime drive readback | runtime INI 运动真源、SHM |
| `axis_*_egear_numerator`、`axis_*_egear_denominator`、`axis_*_feedback_error`、`axis_*_write_status` | 驱动 SDO 写入/读回证据 owner | 参数表只读字段 | drive owner/electronic gear readback | UI 手填、默认成功状态、SHM |
| `feedback_counts_per_motor_rev`、`rotary_load_counts_per_rev`、`drive_zero_model` | Settings Drive count-domain 证据 owner | 参数表 drive-only owner | drive-only settings/runtime readback | LinuxCNC 坐标/运动基真源、SHM |
| `wcs_offsets` | LinuxCNC `PARAMETER_FILE` | native/LinuxCNC owner | `linuxcnc:PARAMETER_FILE`、interpreter readback | `settings_runtime.json`、SHM |
| `g92_offset` | LinuxCNC interpreter/native G92 runtime owner | native/LinuxCNC owner | `linuxcnc:interpreter.g92_offset` | V3 私有坐标补丁、SHM |
| `tool_table`、`tool_offset`、`display_tool_length_mm` | LinuxCNC tool table / tool native owner | native/tool owner | `tool.tbl`、`stat.tool_offset`、`DISPLAY.T0_TOOL_HOLDER_LENGTH_MM` | SHM、旧 tool display JSON |
| `rtcp_ac_geometry`、`g53_geometry`、`G53_A_Y`、`G53_A_Z`、`G53_C_X`、`G53_C_Y` | kinematics/native geometry owner | 参数表 kinematics owner | runtime INI `[RTCP]`/native geometry readback | SHM、settings runtime、UI 默认几何 |

参数表不得包含 UI 显示 SHM 字段。`mcs`、`cmd_mcs`、`runtime_modal_text`、`spindle_speed_rpm`、`linear_velocity_mm_per_min`、`feedrate_override`、`spindle_override` 只属于 `/dev/shm/v3_status_shm` 显示投影表，不属于参数表。VPS 授权、设备 DNA、下载身份链也不属于参数表；它们按 VPS owner 执行。

### P0.3 字段接入要求

1. 每个字段必须有 owner 卡：禁止来源、唯一 owner、唯一 writer、微内核/native 运行内存刷新路径、直接内存 API readback 字段、旧路径删除条件、板端证据。 2. 必须先具备微内核 native 内存直接 readback，再允许接入写入路径。没有 fresh readback 字段时，不得用 UI 的 dirty、显示成功或把 `/run`/`config_cache`/SHM 投影当成兜底。 3. `settings_apply`、native helper 或受控 apply 只能返回分层结果：已写入 owner/source、等待 runtime refresh、已由微内核/native 内存 fresh 直接回读确认。只有最后一类可触发 UI 稳定成功。 4. 参数表稳定显示只能消费微内核/native 常驻内存或登记只读 API；SHM 只服务坐标/轨迹、模态、转速、进给速度和倍率显示白名单。action 运行判断必须读微内核/native 内存事实或登记 native gate readback。dirty、本地缓存、Broker result、`/run` JSON 和 `config_cache` 只保留为编辑态、动作证据或离线诊断。 5. 禁止旧入口：启动 `--inject-shm`、`settings_read_result.json` 显示兜底、通用 `config_cache` 运动/安全 fallback、V3 自造字段、测试 allowlist 和文档示例。 6. 本地 gate 只能证明 `local_verified_only`；涉及 UI、Broker、Home、运动、安全、State Publisher 或开机链路时，必须 full current build/deploy 并走完 UI/operator 路径闭环后才能声明板端结果。
## 0. 最终口径：进微内核与废弃文档
最终口径：除 `设置驱动` 的驱动私有域外，机器运动/控制参数能由 LinuxCNC runtime INI、`PARAMETER_FILE`、`tool.tbl`、HAL/EtherCAT 状态或已登记的非 Python native gate/helper 承载的，都必须使用微内核/native 真源。V3 不得为这些参数另造或保留任何落盘 JSON、var、cache、状态文件或等价自造参数表。
### 0.1 总表：必须迁入微内核/native owner 的参数项
| 参数项 | 禁止来源/读回要求 | 目标真源 | V3 文件/缓存处理 |
| --- | --- | --- | --- |
| 全关节比例和运动参数 | 历史 `settings_runtime.json` 残留不得进入产品路径；runtime INI 生成/校验结果必须由微内核/native 内存和 LinuxCNC/runtime INI owner 直接回读证明；不得进入 SHM | LinuxCNC runtime INI 和 native apply 路径，包含轴/所有关节映射、方向、虚拟轴、`SCALE`、螺距、减速比、编码器计数、`MAX_VELOCITY`、`MAX_ACCELERATION`、`ferror`、`min_ferror`、`[TRAJ]` 默认和最大线性/旋转速度；G0/rapid 只能使用设置页每轴最大速度换算后的 active runtime INI/native `[TRAJ]`/`[DISPLAY]` 速度上限和每个 `[AXIS_*]`/`[JOINT_*]` `MAX_VELOCITY`/`MAX_ACCELERATION`；写入后必须落到 runtime INI/native持久化位置并经微内核/native 内存与重启回读证明。比例链字段改变 `SCALE` 时必须同步重算已启用轴的硬盘 raw 限位。目标精度只能作为一次 UI 请求或 Settings Drive 电子齿轮私域输入/证据，不能作为微内核运动比例持久真源 | `settings_runtime.json` 不得保留该类微内核字段；`config_cache` 不得作为 UI 显示、运行安全 fallback 或配置生成输入 |
| `[DISPLAY]` 显示默认速度/长度 | 历史 `settings_runtime.json` 残留不得进入产品路径；runtime INI `[DISPLAY]` 和 native display owner 直接回读 | runtime INI/native display owner；只用于显示默认值，不是真运动控制真源；不得进入 SHM | 不能落成 V3 JSON/cache 真源；不能用于证明运动约束、安全互锁、刀补、RTCP 或回零已生效 |
| 回零和软限位 | 历史 `settings_runtime.json` 残留不得进入产品路径；runtime INI 生成/校验结果只作读回证明 | LinuxCNC runtime INI / homing 配置 / native apply 路径，包含 `home_offset`、`home_direction`、`home_sequence`、`home_search_vel`、`home_latch_vel`、`min_limit`、`max_limit`；Pulse `HOME_SEARCH_VEL`/`HOME_LATCH_VEL` 必须使用设置页最大速度的同幅值，BUS Home 必须从微内核/native 回读的 `max_velocity_units_per_s` 和 `max_acceleration_units_per_s2` 生成运动 timeout；不得进入 SHM | `settings_runtime.json` 不得保留回零/限位字段；不再扩展 V3 私有回零/限位参数文件 |
| WCS / G54-G59.3 偏置 | LinuxCNC `PARAMETER_FILE` / `linuxcnc.var`，微内核/native 直接回读 `stat.g5x_offset` | LinuxCNC `PARAMETER_FILE` 和解释器状态；写入后必须落到 `linuxcnc.var`/native 偏置 owner 并经原生内存/API 与重启回读证明；不得进入 SHM | 不允许新建 V3 WCS 参数文件；UI 直连显示回读值，不从 SHM 消费 WCS |
| G53 / RTCP / A-C 旋转中心几何和运动模态 | 历史 `settings_runtime.json` 残留不得进入产品路径；runtime INI 的 `[RTCP]`、受控 switchkins/RTCP helper 只作回读证明；RTCP 开机策略目标为 OFF，未从微内核/native fresh 回读前只返回 unknown/degraded | LinuxCNC kinematics/runtime INI、HAL/native geometry gate、固定范围 native helper 和 actual RTCP/switchkins 状态；启动归一化和运行期 RTCP 开/关只改当前微内核/native 运行内存/actual | `settings_runtime.json` 不得保留 G53/RTCP/A-C 几何字段；UI/刀路直连微内核/native 消费 actual 和几何；SHM 只承载 UI 显示白名单字段，不承载几何或动作证明 |
| 坐标基、零点、总线设零后的机器基准 | 驱动读回只作动作证据，历史设置缓存不得进入产品路径；后端生成配置必须由 native owner 回读确认 | LinuxCNC/已登记的非 Python native gate/helper 确认后的坐标/零点真源 | 驱动读回只作为证据，不能成为 V3 坐标真源文件 |

### 0.1.1 自造参数表的字段级收敛要求

字段要求按字段执行，不按整个 JSON 文件搬迁。V3 自造兼容参数表中，只要字段语义已经属于 LinuxCNC runtime INI、`PARAMETER_FILE`、`tool.tbl`、HAL/EtherCAT actual 或已登记 native gate/helper，就必须使用对应 native owner，并由微内核/native 内存或原生 API 直接回读证明；除编码器反馈机械坐标、运动学理论坐标、`runtime_modal_text`、`spindle_speed_rpm`、`linear_velocity_mm_per_min`、`feedrate_override`、`spindle_override` 这些 UI 显示白名单字段外，不得同步到 SHM。不能因为字段放在 `machine_settings.json`、`settings_runtime.json` 或 `config_cache` 里，就继续把这些文件当第二真源，也不能把它们包装成“过渡参数”长期保留。
| 自造兼容项 | 必须使用微内核 native 的字段 | 不进入运动微内核真源的字段 | 禁止保留要求 |
| --- | --- | --- | --- |
| `machine_settings.json` | 所有关节映射和启用状态：`axis`、`joint`、`enabled`、`linuxcnc_axis_letter`、`axis_type`、`unit`；SCALE/比例链：`pulse_equivalent_counts_per_unit`、`cia402_pos_scale_counts_per_unit`、`lead_pitch_mm_per_rev`、`screw_pitch_mm_per_rev`、`rotary_degrees_per_rev`、`motor_revs_per_load_rev`；运动约束：`min_limit`、`max_limit`、`max_velocity_units_per_s`、`max_velocity_per_min`、`max_acceleration_units_per_s2`、`backlash`；回零：`home_sequence`、`home_offset`、`home_search_velocity`、`home_latch_velocity`；RTCP/G53 几何：`rtcp.model`、`rtcp.ac_geometry.*_rot_point`、`rtcp.ac_geometry.*_offset` | `ethercat` 厂商/VID/PID/ESI/PDO/target mode/commissioning gate、`slave_index`、`drive_command_counts_per_motor_rev` 等属于拓扑、驱动映射或驱动读回证据，进入 EtherCAT/backend/设置驱动 owner，不反向成为 WCS、刀补、RTCP actual、模态或坐标基真源。设置页 `max_velocity_per_min` 是最终物理输出速度：X/Y/Z 为 mm/min，A/C 为 deg/min；写入 runtime INI 时只除以 60，不再把 UI 值当电机 RPM。电机 RPM 只能由输出速度、螺距/每转角度和减速比反算；只有缺少该字段的旧数据才可由 legacy `mechanical_max_velocity_rpm` 换算。 | 迁入后产品路径不得再读取它生成 runtime INI，不得复制它恢复 `settings_runtime.json`。旧文件不得作为产品迁移证据或输入；部署/init/测试/文档示例里的读写入口必须同步删除 |
| `settings_runtime.json` | 若历史残留仍含速度、加速度、限位、回零、SCALE、RTCP/G53 几何、显示默认、刀补、WCS、G92 或模态字段，全部按字段迁入 runtime INI、`PARAMETER_FILE`、`tool.tbl` 或 native gate/helper | 允许持久化的只有设置驱动私域和深度清洗后的证据：`drive_profile_id`、encoder raw/multiturn、电子齿轮、驱动内部计数、`feedback_counts_per_motor_rev`、`rotary_load_counts_per_rev`、count-domain 校准、驱动写入/读回、`rotary_range_guard` 等 | drive-only schema 只能承载设置驱动域。不得重新添加运行坐标/刀具/模态字段；不得在 `zero_model`、`drive_position`、readback 或诊断子对象中嵌入整套 SHM/LinuxCNC/HAL 快照；迁出的字段必须从写入 owner、read-only 投影、preflight allowlist、测试和文档示例中移除；不能保留为“配置生成输入” |
| `driver_profile_map.json`、`driver_profiles/*.json`、`device_register_status.json`、`download_status.json` | 无直接运动微内核参数迁移目标；它们只给设置驱动、对象字典、SDO/PDO、授权下载和驱动读回提供输入/证据 | 驱动型号匹配、对象字典、SDO 命令、PDO、encoder raw/multiturn、profile 发布/下载状态、device authorization 都留在设置驱动私域 | 可保留，但不得被 UI/Broker/action 当作 LinuxCNC 运动参数、坐标基、刀补、RTCP actual 或运行模态真源 |

字段迁移完成条件：每个字段都审 owner 卡，包含旧入口删除对象、目标 native owner、唯一 writer、微内核/native 内存直接回读字段、旧文件读写入口删除条件和板端证据。迁移测试优先用现有工具；现有工具不能证明字段迁移、旧入口删除、native readback、SHM 仅投影、禁止路径扫描或 board/motion 需求时，先补最小可复用工具并验证工具，再推进产品代码。字段或 slice 先逐个本地验证 `local_verified_only`，多个字段本地稳定后，再从完整当前 worktree 做一次集中 VM/board 编译、部署和 UI/operator/motion 闭环。缺 owner 卡或缺工具证据时，只能做盘点 and 文档同步；不得实现写路径、保留兼容路径或声称迁移闭环。
### 0.1.2 微内核 native owner 对照表
下表是自造参数表删除和源码改造 of 硬口径。只要 native/microkernel 已有 owner 或能由已登记 owner/helper 承载，V3 就只能发请求、显示 SHM/内存投影和保留动作诊断证据，不得再把这些字段写入 `settings_runtime.json`、`machine_settings.json`、`current_tool_state.json`、`linuxcnc_modal_defaults.var`、`io_params` 或等价 JSON/cache/var。
| 类别 | 微内核 native 已有 owner | V3 正确用法 | 必须删除的自造参数入口 |
| --- | --- | --- | --- |
| 全关节运动参数 | LinuxCNC runtime INI：`[TRAJ]`、`[AXIS_*]`、`[JOINT_*]` | UI 只发设置请求；保存后等微内核/native 内存或原生 API 回读 runtime owner，不靠 SHM；G0/rapid 速度和实际加速度只使用轴参数最大速度/最大加速度落到 runtime owner 后的回读值；螺距、每转角度、`motor_rev/load_rev`、减速比、目标精度或轴方向改变 `SCALE` 时，若存在 BUS `zero_model`，同事务按新 `SCALE` 和 UI 限位距离重写硬盘 `MIN_LIMIT/MAX_LIMIT` raw 坐标 | `settings_runtime.json`、`machine_settings.json`、`config_cache` 中的轴映射、SCALE、速度、加速度、限位、反向方向和等价字段。 |
| 回零参数 | LinuxCNC homing / joint home 配置：`HOME_OFFSET`、`HOME_SEQUENCE`、`HOME_SEARCH_VEL`、`HOME_LATCH_VEL` 等 | 设置请求受控 apply/native owner 写入；Home 只看微内核 native 回读，不靠 SHM；Pulse 回零速度同幅值匹配 `MAX_VELOCITY`，BUS 回零速度/加速度只取 native 的 `max_velocity_units_per_s`/`max_acceleration_units_per_s2` | `settings_runtime.json` 里的 home/limit 字段和 V3 私有回零参数文件 |
| WCS/G54-G59.3 | LinuxCNC `PARAMETER_FILE` / `linuxcnc.var` + `stat.g5x_offset` | Work Zero 走原生 MDI 命令/登记 gate；UI 和刀路直连显示原生内存 API 读回值，成功证明也必须来自原生内存 API 直接回读 | V3 WCS/G54-G59.3 JSON、active WCS cache、`wcs_offset_params`、SHM WCS 字段 |
| G92 | LinuxCNC interpreter 原生 G92 actual 状态 | UI/provider 直连显示 native actual；需要写入时走明确 native G92 策略和原生内存 API 直接回读，不靠 SHM | V3 G92 JSON、坐标补丁、默认 G92 文件、SHM G92 字段 |
| 模态状态 | LinuxCNC interpreter：G90/G91、G17/G18/G19、G93/G94/G95、G40/G41/G42/G49、G54、G61/G64、RTCP actual 等 | UI 可从 SHM `runtime_modal_text` 显示当前模态；运行期变化只进 interpreter/native 内存，动作证明、门禁和 RTCP actual 仍必须直接回读 native | `linuxcnc_modal_defaults.var`、`settings_runtime.json` 模态字段、UI 默认模态缓存、把 SHM 模态当动作证明 |
| 刀长/刀径 | LinuxCNC `tool.tbl`、TLO、刀半径/刀补、`stat.tool_offset` | 当前刀号、刀长、TLO、刀半径由 native/tool table 直接回读表达；不靠 SHM | `current_tool_state.json`、`tool_params`、V3 刀长/TLO JSON、SHM tool/TLO 字段 |
| 当前刀号 | LinuxCNC/native tool owner | UI 直接显示 LinuxCNC/tool table/native owner 回读值；不进 SHM | 当前刀号 V3 状态文件或掉电恢复 JSON、SHM 当前刀号字段 |
| G53/RTCP/运动学几何 | runtime INI `[RTCP]` / kinematics / registered native geometry owner | 设置页不提供写入入口；UI/刀路直连 INI/native actual；RTCP ON/OFF 是运行期 actual，不靠 SHM | `settings_runtime.json` RTCP/G53/A-C 几何字段、RTCP actual 默认文件、SHM 几何/RTCP 字段 |
| 急停/使能/安全 | LinuxCNC/微内核 HAL/EtherCAT realtime/native safety emergency latch/gate | V3 只能触发急停或恢复请求，并看微内核 native 内存回读；不得同步到 SHM。急停实时生效必须由微内核 realtime safety owner 完成 | V3 安全状态 JSON、把 `/run` result 和 SHM 当安全 truth 的路径、让 UI/Python/linuxcncrsh 普通命令链承担急停热路径 |
| IO/限位/安全互锁 | HAL/EtherCAT/native IO helper | UI 只显示请求；互锁事实来自 native 直连，不靠 SHM | `io_params`、`settings_runtime.json` IO/限位/互锁字段 |
| Feed/Spindle 倍率 | LinuxCNC/HAL runtime actual | UI 可从 SHM 显示 `feedrate_override` 和 `spindle_override`；运行期 truth、请求结果和门禁只看 native/HAL actual | V3 私有倍率默认文件、`settings_runtime.json` 倍率字段、把 SHM 倍率当动作证明 |
| 程序打开/运行 | LinuxCNC interpreter：program open / AUTO_RUN / MDI | Start/Pause UI 直达 native command gate 的返回和 native audit 只能作动作诊断；最终状态看 interpreter/native 内存；SHM 只可显示模态/速度/倍率 | 把 UI/Broker result、旧 `start_run_result.json`、SHM 或其他 `/run` 文件当程序状态或参数真源的路径 |
| homed/all_homed | LinuxCNC joint homed 状态 | Home result 只能作动作证据；最终 homed/all_homed 来自微内核 native，不靠 SHM | 把 `home_result.json`、SHM 或 V3 home cache 当最终真源的路径 |
| 坐标/速度/行号 | 编码器反馈机械坐标、运动学理论坐标、`linear_velocity_mm_per_min`、`spindle_speed_rpm` 和倍率显示字段进入后端运行内存后由 State Publisher 做 30ms 显示投影写入 SHM；行号来自 LinuxCNC/HAL/EtherCAT/native runtime actual，直连读取，不进 SHM | UI 机械坐标、UI 轨迹机械点、实际刀尖/刀柄反馈点、理论点、进给速度、转速和倍率可以消费同一套 SHM 显示投影；运动证明、行号、解释器状态和动作结果必须来自微内核 native 内存或原生 API 直接回读；`cmd_mcs` 只能作理论跟随误差显示，不能替代 actual 机械坐标 | 位置/速度/行号 JSON/cache 参数表；用命令坐标、参数表、`/run` result 或旧缓存补机械坐标；把行号或其它未列入显示例外的状态继续同步到 SHM |

### 0.2 唯一长期例外：设置驱动域

以下内容属于驱动能力、驱动写入或驱动证据，不按“全部进微内核参数文件”处理：
- SDO 对象表、电子齿轮、驱动内部计数、编码器 raw bits、multiturn raw 解释。- `feedback_counts_per_motor_rev`、`rotary_load_counts_per_rev`、count-domain 校准和驱动写入/读回证据。- 驱动扫描、设置驱动、复位、参数下发的结果快照和进度文件。
这些文件/字段可以作为驱动配置输入或证据，但不能反向成为 LinuxCNC 运动参数、坐标基、刀补、WCS、RTCP 或运行模态的最终真源。
涉及 A/C、BUS、回零、G-code Start/run、等效角、坐标比例、电子齿轮或电机转动圈数的运动入口，必须证明 `设置驱动` 私域已经完成驱动写入和读回：`drive_parameters_valid` 不能为 false，`electronic_gear.write_status` 必须为 `write_verified_readback`，目标分子/分母必须与驱动读回分子/分母一致。缺少这些证据、A/C count-domain 为 0 证据缺失，或微内核 native fresh 内存回读未确认时，Home、Start、G-code 过滤、等效角运行文件生成和运动黄金测试都必须 fail-closed，不得用旧比例、默认比例、SHM 投影或 `/run` 结果继续驱动电机。
### 0.3 必须删除的 V3 自造参数文件/缓存真源

删除口径：凡是 V3 自己造出来、用于承载 LinuxCNC/微内核可管理参数的文件，都不能保留为产品参数、兼容真源或配置生成真源。处理动作是迁移到 LinuxCNC/runtime INI、`PARAMETER_FILE`、`tool.tbl`、HAL/EtherCAT 原生状态或已登记的非 Python native gate/helper 后，同步删除旧文件、字段、读写 owner、runtime manifest、init、CMake/注册表、部署脚本、预检 allowlist、测试入口和文档示例，并用工具扫描证明旧入口已清理。若当前代码仍强依赖某个自造字段，只能记为未闭环迁移债务；不得新增字段、不得扩展调用、不得在最终目标里保留。
| 文件/缓存 | 处理口径 |
| --- | --- |
微内核已有的字段一律迁入 runtime INI、`PARAMETER_FILE`、`tool.tbl` 或已登记的非 Python native gate/helper，并从该文件、写入 owner、读回判定和文档示例中删除。
| `system_params`、`axis_params`、`tool_params`、`wcs_offset_params`、`io_params` 等 V3 自造参数文件名 | 不允许落地为长期参数文件；若历史代码或文档已引入，必须改成微内核/native 直连 provider 或删除，真实 owner 分别回到 runtime INI、`PARAMETER_FILE`、`tool.tbl`、HAL/IO/已登记的非 Python native gate/helper |
| V3 自造 WCS/G92/刀长/RTCP/模态/坐标补丁 JSON、var、cache | 发现即删除或迁出产品路径；不得作为兼容层继续写入，不得以迁移证据名义保留为产品输入。 |
| SHM `config_cache` | 必须从产品路径删除；不得作为产品 SHM 初值、UI 参数 fallback、运行安全 fallback、保存成功、板端 live 或参数真源证据。 |
| `/run/8ax_v3_product_ui/settings_read_result.json`、`settings_apply_result.json`、Broker result snapshot | 动作证据/离线诊断快照；不能作为参数表稳定显示、保存成功、Home 成功、运动安全、配置生成输入或最终参数真源。 |
| UI dirty 值、UI 内存值、临时 scratch JSON | 只表示用户正在编辑或一次请求输入；不得持久化为参数真源。 |

## 1. 开机进入内存的参数

### 设置页轴参数

旧入口删除对象：`/opt/8ax/phase0_bus5/settings_runtime.json` 中的微内核字段。目标上它只能保存 `设置驱动` drive-only 私域和驱动证据，不得继续作为微内核字段的落盘参数表或配置生成输入。
目标真源：轴/关节映射、比例、速度、加速度、限位、回零、RTCP/G53 几何等字段最终进入 runtime INI、LinuxCNC/native owner 或已登记的非 Python native gate/helper；`settings_runtime.json` 只保留设置驱动私域和驱动证据。
历史不合规过渡路径（不得恢复）：
- `lvgl_app/scripts/re-v3-lvgl-ui.init` 在 UI 拉起前调用 `v3_settings_read_only.py --inject-shm /dev/shm/v3_status_shm`。- `v3_settings_read_only.py` 只读一下 `settings_runtime.json`，生成 SHM JSON payload 里的 `config_cache`。- `v3_state_publisher.py` 每帧通过 `_settings_runtime_config_cache()` 把同套 `config_cache` 发布到 `/dev/shm/v3_status_shm`，UI/运动/安全再从该字段 fallback。
上述路径不满足本文最高规则：它绕过唯一 owner -> 微内核 native 运行内存/API 直接回读的闭环，且会导致 `config_cache` 和 `settings_projection` 成为 UI/运动/安全 fallback 的第二真源。产品路径必须保持删除启动启用 `--inject-shm` 链路；State Publisher 不发布通用 `config_cache`、`settings_projection` 或任何白名单的 JSON payload；参数表 UI 需要显示时必须直连微内核 native 运行内存、runtime INI readback 或登记 API provider。`settings_read_only`、`settings_read_result.json` 和历史 `config_cache` 必须从产品路径删除，不能作为兼容证据。
历史 `settings_projection` 不再作为过渡 SHM payload。若仍有调用面，必须按待删迁移债务处理：先接入微内核 native 直连 provider，再删除对应 SHM 字段、测试口径和文案；不得恢复开机一次性注入、不得让 UI 直接读硬盘，也不得在 provider 缺失时用默认值或 `/run` result 文件兜底。
历史 `config_cache.axes[]` 曾包含的主要每轴设置页参数：
- 轴标识 and 映射：`axis`、`axis_mode`、`slave_index`。- 精度 and 比例：目标精度只允许作为一次请求或 Settings Drive 电子齿轮私域输入；实际运动比例以 active runtime INI/native `SCALE` 和微内核/native 内存或原生 API 直接回读为准，不得进入 SHM。历史倒数比例字段不得恢复到 `settings_runtime.json` 持久字段或配置生成输入。- 编码器/驱动计数：`profile_id`、`drive_profile_id`、`encoder_bits`、`raw_encoder_bits`、`drive_internal_counts_per_motor_rev`、`encoder_multiturn_limit`、`feedback_counts_per_motor_rev`、`rotary_load_counts_per_rev`。- 运行限制 and 回零：`mechanical_max_velocity_rpm`、`max_velocity_units_per_s`、`max_acceleration_units_per_s2`、`backlash`、`min_limit`、`max_limit`、`home_offset`、`home_direction`、`home_sequence`；BUS 轴只保存 drive count-domain 证据，显示运行位置从 active runtime INI/native owner 直接回读推导，不得进入 SHM。
这些字段必须按语义拆分后进入各自的 owner，不得继续依赖 `config_cache` 聚合模型。- 运动配置字段迁入 runtime INI/native owner，并用微内核/native 内存或原生 API 直接回读和重启回读证明；不得进入 SHM。- 驱动 profile、编码器 raw/multiturn、电子齿轮、count-domain 校准和驱动读回证据留在设置驱动私域。- UI 显示必须由 owner 拆分为直连微内核/native 和 typed display provider；`settings_projection` 不得继续承载参数 SHM 投影，不能反向成为参数 owner；`config_cache` 不得重新定义一套参数模型，不得作为显示兜底、保存成功、board live、运行安全 fallback 或参数真源证据。
- 设置页显示的整数运动字段必须在直连 provider 返回 UI 前按字段精度归一化；例如速度/加速度回读从 runtime INI 秒单位换算产生 `10000.000000004999` 这类浮点尾差时，显示 provider 值必须收敛为 UI 等价的 `10000`，不得只在 UI 文本层遮蔽。
### 显示/刀路辅助参数
来源：当前 runtime INI 的 `[DISPLAY]` / `[RTCP]` 和微内核/native 内存或原生 API 直接回读；不得同步到 SHM。`settings_runtime.json` 中的旧显示几何字段不得作为 `settings_apply` 的持久配置生成输入、显示长度最终真源、待迁移来源或离线诊断兜底。
UI 状?provider / 微内核直连读取：
- runtime INI `[DISPLAY]` 下的 `T0_TOOL_HOLDER_LENGTH_MM`，作为 T0 显示刀柄长度直接回读；若 `tool_display` / `toolpath_display` / `tool` 下的 `t0_tool_holder_length_mm` 必须从产品路径删除，不得作为输入、兜底、过渡来源或离线诊断真源。- runtime INI 的 `[RTCP]`、kinematics 选择和 G53/A-C 几何，作为 `rtcp_ac_geometry` 及其有效标志直接回读。- HAL pin/native component、常规 native/helper daemon 登记 native memory block 或 native 内存/API 读回 RTCP/switchkins actual 状态；开机策略目标是 RTCP OFF，但 UI 状态 provider 从同套 native 内存/API fresh 回读前只能返回 unknown/degraded，不得返回有效 OFF，也不能把按钮默认文案、旧 SHM 帧、一次性 helper 输出或旧 `/run` 结果当作已关闭。启动自动 enable/backend ready 后必须执行一次 RTCP OFF 归一化：先通过 native 状态源取 `motion.switchkins-type`，若实际为 ON，只能通过已登记固定范围 native gate/helper 将当前运行期 actual 设 OFF，并等待微内核 native 内存 fresh 直接回读确认；失败时记录启动归一化失败并保持 unknown/fail-closed。运行期 RTCP 开/关只改变当前微内核 native 运行内存/actual，不写回 runtime INI、`settings_runtime.json`、V3 JSON/cache 或其他硬盘参数；`stat.kinematics_type` 只能作能力诊断，不能代表 actual active。State Publisher 不得保留 `RTCP_STATUS_HELPER_FALLBACK`、`RE_V3_RTCP_STATUS_HELPER_FALLBACK` 或等价环境变量重新打开短生命周期 helper 查询。- LinuxCNC `PARAMETER_FILE` / `linuxcnc.var` 的 G54-G59.3 全表偏置，结合 `stat.g5x_index` / `stat.g5x_offset` 直接提供 `active_wcs`、`wcs_offsets` 和当前活动 offset。- LinuxCNC 解释器模态和 G92 原生状态，直接提供 `runtime_modal_text`、活动几何平面/进给/刀具路径模式、`g92_offset[5]`/valid mask 和显示 G92 策略状态；常规开机默认模态为 `G90`、`G17`、`G94`、`G40`、`G49`、`G54`、`G64`、`G80`，RTCP 策略目标为 OFF 但必须由 native 内存/API fresh 回读确认，G92 默认 inactive/zero 或已明确采用的 native G92 策略。运行期 MDI/程序/按钮改变这些模态时，只改变当前 interpreter/native 内存状态；State Publisher 可把 `runtime_modal_text` 写入 SHM 给 UI 显示，但不得把 SHM modal当动作证明、门禁或回写来源，也不得写回 `RS274NGC_STARTUP_CODE`、runtime INI、`settings_runtime.json`、`linuxcnc_modal_defaults.var` 或任何 V3 默认模态文件。G92 未登记时只显示诊断并作为 Work Zero 等动作的 fail-closed 门禁，不作为 V3 坐标补丁。缺少 fresh native `g92_offset` readback 时，Work Zero 等坐标写入动作必须拒绝，不能把 0 偏移默认放行。- LinuxCNC `stat.tool_offset`、`stat.tool_in_spindle`，必要时由 `tool.tbl` / native tool owner 的当前刀长、刀半径和 `[DISPLAY] T0_TOOL_HOLDER_LENGTH_MM` 显示刀柄长度兜底，直接提供 `tool_offset`、`active_tlo_z_mm`、`display_tool_length_mm`。- LinuxCNC/HAL/EtherCAT 运行事实，例如 `estop`、enabled、task/interp state、homed/all_homed、限位和 IO 互锁状态，只作为当前运行事实直接提供，不从 V3 文件恢复。
除 `runtime_modal_text`、`spindle_speed_rpm`、`linear_velocity_mm_per_min`、`feedrate_override`、`spindle_override` 等 UI 显示投影外，这些值不得由 `v3_state_publisher.py` 写入 `/dev/shm/v3_status_shm`。UI 消费微内核 native 状态 provider 或登记 API，不走 UI 热路径；SHM 只保留 30ms UI 显示投影。
### 驱动映射
来源：`/opt/8ax/drive-profiles/public/driver_profile_map.json` 和 `/opt/8ax/drive-profiles/private/driver_profile_map.json`。
UI 不直接读取驱动映射表；驱动映射属于开机常驻内存闭包，不是 UI 运行期热路径文件。State Publisher、Broker 或产品 action 在启动阶段必须加载并校验映射表内存快照；运行期驱动动作和旋转轴 count-domain 判断只消费该快照或刷新后的 native/drive provider。- `v3_drive_bus_command.py::load_profiles()` 只能作为启动装载或受控重载入口读取 public/private 映射表，匹配 `settings_runtime` 中每轴的 `profile_id` / `drive_profile_id`。- State Publisher 的旋转轴计数域也按映射表签名与 `settings_runtime` 签名缓存结果。
映射表提供 SDO 命令对象、编码器分辨率、多圈限制、旋转负载单圈计数等驱动能力信息。`feedback_counts_per_motor_rev`、`rotary_load_counts_per_rev`、`zero_abs_counts64` 和 split raw basis 属于设置驱动/count-domain 例外快照；可以开机入内存供等效角、unwrap、checkpoint、Rebase 启动检查使用，但不能反向成为 LinuxCNC 运动参数、WCS、RTCP、刀补、运行模态或坐标基真源。倒数比例只能由 active runtime INI/native `SCALE` 在内存中即时反算用于诊断显示，不得作为 `settings_runtime.json` 持久字段或配置生成输入恢复。
## 2. 后续如何调用

### 设置页显示
设置页字段读取顺序是：
`正在编辑的 UI dirty 值（仅编辑请求态） -> fresh 微内核/native 常驻内存或登记只读 API readback -> 自建参数表中唯一 owner 的轴参数 RAM 快照/readback -> 驱动参数映射表或 drive provider readback -> unavailable/degraded`

对应代码：- `v3_page_settings.c` 和 `re_v3_settings_current_value_for_field()` 只能把 dirty 值作为当前编辑待提交输入；字段级“已保存/待重启”不等于运行态已生效，保存后的稳定显示必须在重启或运行内存刷新后回到 fresh native/provider readback。- `v3_page_settings_readback.c` 必须从微内核/native 常驻内存或 typed/fresh owner readback 字段取值；不得从 `/dev/shm/v3_status_shm` 读取参数表字段，除非该字段本身属于 SHM 显示白名单。历史 `config_cache.axes[]` 不得作为参数表稳定显示来源；旧入口必须从产品路径删除。- `/run/8ax_v3_product_ui/settings_read_result.json`、`drive_read_result.json`、`drive_factory_reset_result.json` 只允许作为一次动作证据或离线诊断；不得作为显示刷新辅助、首屏主路径、SHM 缺失 fallback 或硬盘兜底。
UI C 不允许直接读 `/opt/8ax/phase0_bus5/settings_runtime.json`、runtime INI、`linuxcnc.var`、`tool.tbl` 或其他硬盘参数文件。内存 readback 缺失时必须显示 unavailable/degraded 或 blocked，不能用静态默认值、旧参数表、旧 `/run` 结果或模型默认值冒充当前生效参数。
### 设置保存

设置页修改后：- UI 组装 `settings_save_return` / `settings_apply` 请求时，必须在保存请求构建，提交瞬间从当前微内核/native UI 状态 provider 取最新 owner generation/readback token，不得使用进入设置页或开始编辑时缓存的旧 token；UI 显示 freshness 才读取 SHM `status_epoch`；请求再通过 Command Broker UDS 发送。- Broker 调用 `v3_settings_apply.py` 或后续等待 product action。- product action 可以在一次事务中使用完整内存请求 payload 来生成 runtime INI、校验源输入和执行 readback，但这个 payload 不能从旧 V3 落盘参数表恢复，也不能原样保存成新的 V3 参数表。- 属于微内核 native 的字段必须写入其唯一硬盘/native owner，再刷新或重载运行内存并直接回读；属于 `设置驱动` 私域的字段可以写入 drive-only `settings_runtime.json`，但仍必须完成驱动 owner 写入/读回 UI provider 直连显示刷新，不得依赖 SHM。- 写入 `settings_runtime.json` 时必须经过 drive-only 提取器，只保留设置驱动私域字段和深度清洗后的驱动证据；运动、坐标、刀具、模态、RTCP/G53 几何、显示默认、安全互锁和倍率字段不得落入该文件，也不得藏在 `zero_model`、`drive_position`、readback 或诊断子对象中。- UI 本地值或 `immediate_persist` 只能表示编辑态请求态已更新；State Publisher 发布 generic `config_cache` 和 `/run` result 更新不能替代微内核 runtime fresh typed 回读证明。- 需要 LinuxCNC 生效的字段必须走后端重启、受控 apply 或已登记 native gate/helper；字段级“已保存/待重启”以 native owner 持久化结果和源位置回读为准，右上角 `保存并重启` 以前台 restart precheck + backend/UI restart handoff + 恢复主页面为准；运行态最终生效以微内核 native 运行内存直接回读和必要的重启回读为准，不得进入 SHM。
### 运行状态和刀路显示
LinuxCNC 运行状态分两条读取链路： - UI 坐标值、UI 轨迹变化、模态文字、转速、进给速度和主轴/进给倍率的统一显示链路固定为：微内核 native 对应 owner -> 后端运行内存 -> State Publisher -> `/dev/shm/v3_status_shm` typed SHM -> UI SHM reader -> `ReV3StatusView` -> 坐标面板、刀路动态点和主页面显示。缺 fresh 显示投影时只能显示 degraded/unavailable，不能用参数表、文件快照或本地旧值假装当前显示有效。 - WCS 偏置、刀具长度、RTCP/G53 几何、旋转轴计数域、急停/使能、安全、行号、诊断和系统指标必须由 `lvgl_app/src/v3_ui_state.c`、`v3_ui_toolpath_*`、主页面/辅助页面通过微内核 native 状态 provider 或登记 API 直连读取，不进入 SHM。模态、进给速度、转速和主轴/进给倍率允许作为 SHM UI 显示投影，但不能作为动作证明或门禁。 - Product action 和 Broker 结果不得读取 typed SHM 作为动作依据、准入、保存证明或安全证明；需要状态时必须读微内核/native 内存/API 或登记 native memory block。
### 驱动动作

设置驱动、驱动扫描、驱动读回、驱动复位等 设置驱动 域动作不走 UI 缓存直接推断驱动能力。 - 动作入口只消费启动阶段已加载的 drive-only 轴到从站/profile 绑定、驱动证据和驱动映射内存快照；`settings_runtime.json` 和 profile 映射表只能在启动装载、设置保存后刷新或受控重载时读取。 - EtherCAT 从站扫描结果进入驱动/native provider 内存，动作匹配只消费该内存视图。 - 结果写入 Broker/action 响应和状态文件只能作为动作证据；后续 UI 稳定显示来自驱动/native provider readback，SHM 只显示坐标/轨迹、模态、转速、进给速度和倍率白名单字段。运行判断和成功证明必须来自微内核 native 内存或驱动 owner 直接回读。总线设零使用这些驱动读回作为证据，但零点/坐标基的长期 owner 应迁入微内核/已登记的非 Python native gate/helper 或受控 runtime apply 路径，不再扩展 V3 参数文件真源。
## 3. `settings_runtime.json` drive-only 收口要求

`/opt/8ax/phase0_bus5/settings_runtime.json` 必须收敛为 `schema=re.v3.settings_runtime.drive_only.v1`。该文件只允许持久化设置驱动私域和深度清洗后的最小驱动 count-domain 证据： - 轴到从站/profile 绑定。 - 电子齿轮输入和读回。 - encoder raw/multiturn 参数。 - drive SDO 写入/读回证据。 - A/C count-domain 校准。 - BUS 设零 `zero_model` 证据。
`zero_model`、`drive_position`、readback 或诊断子对象只能保存驱动对象、电子齿轮、encoder raw/multiturn、count-domain 零点锚点、驱动读回和必要验收时间状态码；不得保存 `pre_status`、`linuxcnc_snapshot_before`、`runtime_scale_check_before`、WCS/G92/TLO/tool/RTCP/modal/status 帧或 runtime INI 回读行。这些运行事实必须留在微内核/native 内存 owner，并由 UI provider 直连显示；离线诊断证据只作诊断；Broker `/run` 结果只能作动作证据，不能落进 `settings_runtime.json`，也不能作为参数表稳定显示或保存成功真源。
该文件禁止持久化： - runtime INI 运动约束：HOME/HOME_OFFSET/HOME_SEQUENCE、软限位、速度、加速度、FERROR、BACKLASH、`[TRAJ]`、`[AXIS_*]`、`[JOINT_*]`。 - `[DISPLAY]` 显示默认、tool/TLO/T0 显示默认。 - WCS、G92、RTCP 运行期开关、G53/kinematics 几何。 - HAL/IO 安全互锁、feed/rapid/spindle override truth。
`settings_apply` 可以继续在内存中使用一次完整请求 payload 来生成 runtime INI 并在同一事务里做源读回，但该 payload 不能从落入 V3 自造参数表恢复，也不能原样写回任何 V3 参数文件。写入 `settings_runtime.json` 的内容必须经过 drive-only 提取器裁剪。UI 参数稳定显示必须来自 native/provider readback；SHM 投影只来自 State Publisher 拆分后的 typed fresh display 白名单字段。真正成功证明来自 owner/native 直接回读。`config_cache` 必须从产品路径删除，不能合并 active runtime INI 和 drive-only 字段后继续充当 UI/SHM 投影或参数真源。
`settings_runtime.json` 不得再由 `machine_settings.json` 回填或恢复。drive-only 提取器必须删除运行时标注型字段（例如 `drive_axis_role`）和非必要派生显示字段；schema、role、truth-boundary 等说明只能作为结果诊断字段或字段卡分类，不得成为新的参数数据模型。
## 4. 设置页字段接口 owner 规则

设置页每个数据必须按“唯一 owner -> 微内核 native 运行内存/API -> UI 直连 provider”显示，UI 修改必须走 “Broker -> settings_apply/native owner -> owner 写入 -> 微内核 native 运行内存刷新 -> native/API fresh
直接回读 -> UI 可清该字段 dirty”闭环。下列字段不得再由 `settings_runtime.json`、`machine_settings.json`、`config_cache`、`/run` result 或 UI 默认值充当真源：
- 轴显示语义元数据：`axis_mode`、目标精度 precision、`mechanical_max_velocity_rpm/max_velocity`、`soft_minus/soft_plus`、`zero`
  可以写入 active runtime INI `[AXIS_*]` 和 `V3_SETTINGS_*` 元数据，用于 UI 从运行内存读回。`pitch`、`motor_rev`、`load_rev`、`slave` 这类微内核/native 和驱动参数映射表都没有承载的设置页轴参数，进入自建参数表并由 UI 自动读取；它们只作为设置页参数和后续受控 apply 的输入 owner，不得冒充 native 已生效 readback。 - 运动实际真源：`SCALE`、`MAX_VELOCITY`、`MAX_ACCELERATION`、`BACKLASH`、`FERROR`、`MIN_FERROR`、`HOME_SEQUENCE`、Pulse `HOME/HOME_OFFSET/HOME_SEARCH_VEL/HOME_LATCH_VEL`
  必须写入 LinuxCNC runtime INI `[JOINT_*]`/`[AXIS_*]` 原生字段并从 owner 回读。- BUS/Pulse 零位边界：模式差异、切换闭环、Pulse 设零/回零 UI 禁用口径以 `REQ-BUS-PULSE-MODE-DIFFERENCE` / `功能/总线脉冲区别.md` 为准；本文只维护字段 owner。BUS 设零保留 drive-only `zero_model` 证据并通过 native/runtime 直接回读显示；Pulse 设置 `zero` 显示和保存机械零点到 `HOME_OFFSET`；两者都不能把旧 V3 JSON 当长期 owner。- 驱动私域：`profile_id/slave_index`、电子齿轮、encoder raw/multiturn、SDO 写入/读回证据，A/C count-domain 校准仍属于 Settings Drive private domain；只允许在 drive-only
  `settings_runtime.json` 或驱动映射 profile owner 中持久化。- `settings_projection` 是历史 SHM 设置页投影债务；设置页必须改为 active runtime INI、drive-only owner 和微内核/native 直连 provider 显示，不得把它作为保存成功、运行安全门禁、board live 或配置生成真源。

## P0 启动预加载与实时常驻内存锁定规则

本章节是顶部最高口径的执行拆解。目标是消除运行时闪存/磁盘 I/O 抖动，确保微内核与上层控制系统的实时响应性能。

### 1. UI 与产品自写代码启动预加载（自写代码常驻）
- **C 语言交互层（C UI）**：
  - 前端进程编译为静态/预链接的单体二进制文件；运行期必须使用启动阶段已加载的页面、字体、图片、布局、按键表、命令表和资源索引。
  - 开机启动后立即调用 `mlockall(MCL_CURRENT | MCL_FUTURE)`，锁定前端交互所有代码页及资源页在 RAM 中，杜绝运行时内存页面置换。
  - UI 运行期不得为了按钮动作、页面刷新、参数显示、图标/字体、状态解释或动作结果再读取硬盘资源兜底；需要可变内容时必须来自微内核/native 常驻内存、SHM 显示白名单或受控程序文件选择。
- **Python 业务层（Command Broker 与 Actions）**：
  - 后台守护进程（如 `v3_command_broker.py`）在引导阶段通过 `broker_inprocess_registry.py` 初始化。
  - 自动遍历并导入（`importlib.import_module`）全部业务动作处理器（包括 `settings_apply`、`settings_axis_zero`、`drive_bus` 等），完成 Python 字节码在 RAM 中的常驻编译，使后续 UDS 指令调用全部在内存中进行，避免运行态临时读取 Python 脚本。
  - Broker、Actions、provider、native gate wrapper 和 action registry 必须在启动时形成固定内存登记表；运行期不得按请求再扫描脚本目录、懒导入模块、按文件名执行未登记脚本或从旧路径 wrapper 兜底。
  - 产品自写守护进程同样必须预加载业务模块、配置 schema、协议表和最小资源索引；运行期只消费内存快照或受控刷新后的 provider。

### 2. 微内核与实时层物理锁定（LCNC / RTAPI 强实时）
- **微内核运行闭包全量入 RAM**：
  - LinuxCNC/微内核启动所需的 executable、共享库、RTAPI/实时模块、HAL component、kinematics component、INI/HAL 配置、`PARAMETER_FILE`、`tool.tbl`、G53/RTCP 几何、WCS/G92 初始 readback、驱动映射和已登记 native gate 表，必须在开机阶段一次性读入 RAM 或由 initramfs/RAM disk 提供。
  - 开机装载完成后，运行期控制、运动准入、状态 readback、UI 显示 provider 和动作证明必须来自 LinuxCNC/微内核/native 常驻内存或原生 API，不得再临时读取硬盘文件作为当前真源。
  - 设置保存允许写回唯一持久 owner；写入后必须刷新或重载对应 native 常驻内存，并用该 owner 的内存/API readback 证明结果。单纯落盘成功不等于运行态已生效。
- **物理内存锁死（Memory Locking）**：
  - 底层实时装载器（`rtapi_app` 及相关的伺服/总线线程）必须无条件启用物理内存锁死。在内核空间与用户空间强制锁定所有实时内存页面（`mlockall`），杜绝页面换出（Page-Out）。
- **动态链接符号立即绑定（LD_BIND_NOW）**：
  - 守护进程或实时进程的拉起环境变量中，必须显式定义 `LD_BIND_NOW=1`。
  - 指示动态链接器在程序启动的第一时间解析所有关联共享库（`.so`）的动态符号并全量载入 RAM，消除由于懒加载（Lazy Loading）导致运行态突发闪存读取的隐患。
- **物理共享内存限制（tmpfs）**：
  - 所有的进程间同步（SHM，例如 `/dev/shm/v3_status_shm`）必须在 RAM 内存文件系统中创建，完全避免物理闪存写入。
  - SHM 只保留 UI 显示投影：坐标/轨迹、模态、主轴转速、进给速度、主轴倍率、进给倍率。RTCP actual、WCS/G92、tool、驱动模式、安全、停稳、动作成功和参数真源不进入 SHM。
- **RAM 盘根文件系统镜像（RAM Disk / initramfs）**：
  - 板端最终运行形态必须在引导阶段将 LinuxCNC/微内核运行闭包和业务运行闭包打包为只读镜像或等价 manifest，并一次性释放到 `initramfs`/RAM disk 或等价 RAM 驻留路径中。
  - 闪存介质在运行期应只读挂载或从热路径隔离；除明确的设置保存、程序文件选择、日志/诊断写入和受控重载外，控制与显示热路径不得触发闪存读写。
