# 设置页触摸闭环测试作业指导书

## 适用范围

本文只用于设置页链路闭环测试，不用于主页 `cc.ngc` 运动黄金链路。主页测试另按 `待做工作/测试主页.md` 执行；全自动流程只能在本文和主页测试都单步闭合后再运行。

控制链路必须保持：触摸屏幕/remote input -> UI C -> Command Broker UDS -> product action -> LinuxCNC/HAL/EtherCAT/native owner。状态和验收只能来自 State Publisher/SHM、Broker 结构化结果、驱动读回、截图和事件日志；不得用固定延时、旧 `/run` 快照或 UI 视觉成功替代读回。

## 前置条件

- 正式开始前先用继电器重启开发板，并记录断电/上电、SSH/relay/remote input/Broker/SHM 就绪证据。
- 重启后重新抓屏，确认进入主页面且无急停、无运动、无遮挡；不能沿用重启前截图、SHM 或事件日志。
- 进入设置页前只确认 UI/Command Broker/remote input 可用；不得用 fresh SHM、速度容差或 JSON 快照做设置页运动安全门禁。
- 上使能不是危险动作。测试中某个后端步骤需要上使能时，必须由该 product action 自动发起正式上使能，并以 microkernel/native enable gate 返回和原生回读作为结果；不得要求操作员手动上使能，也不得把上使能弹成危险确认。
- 只有转动轴相关动作按危险动作处理。A/C 旋转轴设零、A/C 轴坐标写入、会导致旋转轴实际运动或相位重定的动作必须有二次危险确认；X/Y/Z 直线轴设零、下载、读取、普通保存、普通上使能都不是危险动作。
- 测试必须从真实触摸屏幕路径推进；每个点击前抓屏确认页面、按钮位置和遮挡状态，点击后立刻抓屏并读取 Broker/SHM/事件/结果文件。
- 任一步未闭合时停止后续步骤，先定位和修复该单点，再从设置页链路开头重新复测。

## 黄金操作顺序
总线模式下
1. 下载授权
2. 服务器下载
3. 扫描从站
4. 复位驱动
5. 读取驱动
6. 清除故障
7. 设置驱动
8. X/Y/Z/A/C /7/8每个轴逐个设零
9. 设置 A/b/C 轴坐标
10. 保存并重启

顺序不得跳过、合并或换成主页 `Home -> Open Program -> Start` 链路。`登记本机码` 不是本文黄金顺序步骤；若下载授权因本机码或授权状态缺失失败，只记录该失败并按授权 owner 修复，不能改顺序绕过。

## 每步执行规则

- 每一步只允许一个触摸动作；只有转动轴危险动作需要确认弹窗，确认按钮也作为独立一步执行，必须有确认前截图和确认后截图。
- 每一步必须保存最小证据：点击前截图、点击后截图、事件日志或 Broker 结果、SHM/驱动读回摘要。
- 长流程按钮必须看到进度弹窗进入对应阶段，并最终得到结构化结果；弹窗只显示固定文案、顶部黄字或长期无阶段推进时，该步不算通过。
- 下载授权必须返回授权下载/验签成功或已有效状态；失败必须显示结构化原因。
- 服务器下载必须证明 public/private profile 映射表下载、校验和板端缓存结果。
- 扫描从站必须证明当前从站、OP 状态和 profile 匹配可被后续动作消费。
- 复位驱动必须证明恢复厂家默认参数、必要 fault 清除、自动读回和最终读回校验；复位成功后旧电子齿轮、设零和 count-domain trust 视为失效。
- 读取驱动必须证明必读 SDO、电子齿轮、statusword 和驱动私域读回完整；读回不完整时不得继续设置驱动。
- 清除故障只闭合驱动报警/fault 清除，不得冒充复位驱动或设置驱动。
- 设置驱动必须证明逐轴电子齿轮、模式写入、OP 恢复、必读 SDO 读回和 `DRIVE_SET_OK`；成功后右上角必须进入 `保存并重启` 状态。
- 每个轴逐个设零必须按 X、Y、Z、A、C 单轴执行；X/Y/Z 直线轴不要求危险确认，但需要本轴请求、自动上使能证明（若该动作需要上使能）、raw/count-domain 读回、硬盘 raw 限位写入和结果码；A/C 转动轴还必须有转动轴危险确认。未完成当前轴不得进入下一轴。
- 设置 A/B/C 中心坐标只允许编辑 `G53_A_Y`、`G53_A_Z`、`G53_B_X`、`G53_B_Z`、`G53_C_X`、`G53_C_Y`；A 中心 X、B 中心 Y、C 中心 Z 来自当前模态加工坐标系偏移值，不得进入 dirty 或保存链路。G53 表显示顺序为 A 中心、B 中心、C 中心、对刀仪、5 方向检测仪。
- 保存并重启必须证明 dirty 字段已写入唯一 owner 并源位置回读，restart precheck 通过，backend + UI restart handoff 成功，并恢复主页面。

## 反应时间硬指标

- 软键盘：触摸可编辑单元格按下到软键盘出现并显示可输入数字，必须不大于 `0.2s`。数字键按下到输入框显示该数字，也必须不大于 `0.2s`。
- 单字段保存：软键盘 `OK` 或下拉提交按下，到本字段唯一 owner 写入完成、源位置回读完成、UI 清 dirty 或显示结构化失败，必须不大于 `0.5s`。
- 设置 A/C 轴坐标属于单字段保存；每个字段都按 `0.5s` 保存回读时限验收。
- 驱动长流程和保存并重启不按 `0.5s` 等待最终硬件动作完成，但按下后 `0.2s` 内必须出现进度窗口或明确已提交反馈，随后必须持续显示阶段进度和耗时。
- 任一反应超过时限，先判定是 UI 触摸/键盘本地路径、Broker/UDS、State Publisher/SHM 回读、驱动读写、还是自动化采集工具问题；修当前问题后重测，不得继续后续步骤掩盖慢点。

## 停步规则

- 页面不对、按钮不可见、目标被遮挡、急停不可见或截图不清楚时，不点击。
- 点击未命中、事件日志无对应动作、Broker 返回失败、SHM/驱动读回不更新、进度弹窗无阶段推进时，不继续下一步。
- 需要上使能的步骤如果未自动上使能、未取得 fresh SHM `enabled=true` 回读，或要求操作员手动补上使能，视为该单步失败，先修自动前置链路。
- 任一步触发急停、忙碌、LinuxCNC NAK、驱动 fault、EtherCAT 非 OP、授权/DNA degraded、状态 stale 或反应超时，立即停步修复。
- 诊断可使用直接 Broker/UDS 或临时单文件同步，但最终闭合必须回到完整当前 PROJECT_ROOT 构建部署后的原始触摸路径。

## 当前状态记录

- 当前作业指导以本文黄金操作顺序为准；旧主页 `cc.ngc` 顺序已从本文移除。
- 未完成本文全部步骤前，只能说“当前单步通过”或记录具体阻塞，不能说设置页链路通过。
