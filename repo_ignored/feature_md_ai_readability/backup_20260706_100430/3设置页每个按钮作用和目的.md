# 设置页每个按钮作用和目的

引用需求真源：`REQ-BUS-PULSE-MODE-DIFFERENCE`、`REQ-DOC-SINGLE-SOURCE`、`REQ-PARAM-MEMORY-LIGHTWEIGHT-SAVE`、`REQ-NATIVE-OWNER-FIRST`、`REQ-SETTINGS-RUNTIME-DRIVE-ONLY`、`REQ-LINUXCNC-COMMAND-GATE`、`REQ-DRIVE-PROFILE-AUTH-CHAIN`、`REQ-G92-NATIVE-RUNTIME`、`REQ-WCS-NATIVE-OWNER`、`REQ-ROTARY-UNWRAP-ABSOLUTE-PROOF`、`REQ-ROTARY-REBASE-NATIVE-GATE`、`REQ-RTCP-G53-NATIVE-ACTUAL`、`REQ-MOTION-STILLNESS-TOLERANCE`。

## AI 阅读入口

- 本文同样遵守 `功能/0开机参数入内存.md` 的最高启动内存架构：LinuxCNC/微内核、UI 和产品自写运行代码闭包开机全量入 RAM；运行期热路径不得临时读盘、懒导入、扫描目录、读取旧 JSON/result 或用 SHM 显示投影冒充 owner 真源。
-  本文定位：设置页按钮清单、当前源码入口和按钮到后台动作的索引；它不是跨功能政策 owner。
- 先用本文找到按钮、C dispatch、Broker/action 路径，再回到 `功能/需求真源索引.md` 查对应 REQ owner。
- 不要把本文里的按钮现状扩展成参数、WCS、G92、RTCP、BUS/Pulse、驱动 profile 或运动安全真源。

本文只记录当前源码中设置页按钮的实际连接情况。不要把本文当成完成验收报告；涉及 VPS、驱动映射、BUS 设置、设0、回零等细节，只引用已有专门文档。BUS/Pulse 模式差异、模式切换闭环、Pulse 禁用 BUS 驱动动作和 Pulse 设零/回零口径以 `功能/总线脉冲区别.md` 为准。

## 1. 代码事实来源

| 项 | 当前源码 |
| --- | --- |
| 设置页按钮清单 | `/root/Desktop/v5/app/src/v5_settings_page.c` 的设置页按钮创建与 action 映射 |
| 按钮分发 | `/root/Desktop/v5/app/src/v5_settings_page.c::v5_settings_page_trigger_action()` |
| UI 请求组包 | `/root/Desktop/v5/app/src/v5_settings_actions.c`，通过 `/run/8ax_v5_product_ui/settings_actiond.sock` 发送 framed JSON |
| 设置页右上角机械坐标面板 | 当前 v5 坐标显示按 `/root/Desktop/v5/app/src/v5_coordinate_panel.c` 和设置页布局代码现状执行 |
| 旧 C 产品命令白名单/直连 Broker | 已删除：旧 `lvgl_app/src/v3_product_command*` 不再是当前入口，不得恢复为设置页或运动控制中介层 |
| 设置页 action 守护进程 | `/root/Desktop/v5/services/drive_profile/v5_settings_actiond.py` |
| 驱动命令脚本映射 | `/root/Desktop/v5/services/drive_profile/v5_drive_bus_action.py`、`drive_bus_command_mapping.py`、`drive_bus_profiles.py` |

已有文档归属：

| 主题 | 只引用这里 |
| --- | --- |
| BUS 设置总体规则、按钮顺序、阶段验收 | `功能/3设置页总线模式设置项作用说明.md` |
| BUS/Pulse 模式差异、模式切换、Pulse 禁用 BUS 驱动动作 | `功能/总线脉冲区别.md` |
| VPS endpoint、DNA、授权、gateway API | `功能/VPS登录与项目对接说明.md` |
| 驱动映射表 schema、public/private、运行期 profile 选择、标准 `drive.*` 命令、profile 到软件接口边界 | `功能/1驱动命令映射表命令需求.md` |
| 设置页 `设置驱动` 按钮 | `功能/4设置页设置驱动按钮作用和目的.md` |
| 设置页 `设0/零位` | `功能/4轴设0软件偏置方案.md` |
| 主页面 `回零/归零` 与设置页 `设0` 边界 | `功能/8回零按钮开机强制回零与双模式真实回零方案.md` |

授权/DNA latch 消费边界：本文只记录设置页按钮怎样触发和显示授权/DNA 状态；Device DNA 读取、授权文件验签、VPS challenge/signature、profile private scope 和开机/上使能前 latch 语义只引用 `REQ-DRIVE-PROFILE-AUTH-CHAIN` 及 `功能/VPS登录与项目对接说明.md`。设置页 `登记本机码`、`下载授权`、`服务器下载` 可以发起后台非实时校验、下载和状态刷新，但不得把 raw PL DNA、DNA hash、`pl_dna_hash`、token、私钥或授权原文写入 UI、日志、SHM、本地状态或仓库。`登记本机码` 必须现读本机 PL DNA，并把该 DNA 和设备公钥发给 VPS；VPS 先验证公钥，公钥通过后才登记 DNA、计算并保存 6 位 `vpsDistributionId`，同时在 VPS 数据管理中登记 ID-DNA-公钥关系并创建该设备 private 文件夹；同一 PL DNA 已登记时，VPS 必须直接返回已保存 ID 给板端，不得重新生成 ID。private 文件夹名统一为 `<vpsDistributionId>-<pl_dna_hash>`，但该文件夹名只允许作为 VPS 服务端内部存储路径，不得暴露给板端 URL、UI、日志或下载接口。板端只回读 VPS 返回的 6 位 ID 和校验状态，不得本地推导最终 ID；回读成功后本地登记状态只允许保存 `vpsDistributionId`、登记结果码和公钥摘要，设置页顶部 `本机码` 只显示该 6 位 ID 和登记状态。`DNA_REGISTER_UPLOADED_PENDING_AUTH` 表示公钥已通过 VPS 校验、DNA 已上传、VPS 已算出/返回 ID、VPS 已建立登记关系且板端已保存本地 ID 状态，只是授权文件还等待工厂端生成/上传；设置页不得把它当登记失败，也必须显示已登记 ID。`下载授权` 和 `服务器下载` 必须先从本地登记状态读取 `vpsDistributionId`；本地继续前置顺序固定为先校验 6 位 ID 存在，再现读本机 live DNA 并在内存中计算 DNA hash，之后才允许发起 VPS 请求。下载请求 URL/path 只能携带保存的 6 位 `vpsDistributionId`；本次临时读取的 DNA/DNA hash 必须作为 header 或 body 校验字段随请求发送给 VPS，不得放入 query，并且动作结束后不得落盘保存。VPS 对自带 ID 的访问必须用临时 DNA/DNA hash 校验 ID-DNA 关系，校验通过后在服务端解析到 `<vpsDistributionId>-<pl_dna_hash>` private 文件夹并放行下载；客户端不得直接用 `id-dna`、hash-only 或旧 ID-only private 目录当本地真源。没有已登记 ID 时必须提示先登记本机码，不得只按 DNA hash 访问 VPS。运动、Home、Start、Jog、设0、设置驱动和 `estop_or_reset` 只能消费后台已经锁存的只读授权/DNA 状态；热路径不得每次点击都重新读取硬件 DNA、验签文件或访问 VPS。若 latch 缺失、过期、三次 DNA 读取不一致、授权 hash 不匹配、授权过期或状态不可用，设置页只显示 degraded/失败原因，运动和上使能前置必须 fail-closed，返回 `MOTION_GATE_DNA_NOT_VERIFIED` 或对应授权失败码。

参数域归属：

最新交互边界（2026-06-27，按当前 v5 代码收敛）：设置页每个可编辑字段在软键盘 `OK` 或下拉选择提交时必须确认保存到该字段唯一 owner，并完成本字段源位置回读后才清该字段 dirty；这包括 `G53_A_Y/G53_A_Z/G53_B_X/G53_B_Z/G53_C_X/G53_C_Y`，它们只写 active runtime INI `[RTCP]` owner，不写 `settings_runtime.json`。右上角 `保存并重启` 不再负责重新保存这些已确认字段，也不在前台等待完整 native 状态恢复、machine-on 或驱动 Operation Enabled 恢复；它只做轻量 restart precheck，发起微内核 + UI restart handoff，收到 handoff 成功后关闭进度窗口并返回主页面。settings 页轻量保存重启链路必须在新 UI 恢复/进入主页面后通过正式 `estop_or_reset` 产品动作发起上使能；machine-on 和驱动 Operation Enabled 结果只作为后台恢复诊断，微内核重启后的最终运行态真值仍以后续 native/运行内存直接回读为准；SHM 只更新允许的 UI 显示投影。

以下名称只表示 运行内存/UI 显示投影或 UI 分区，不允许落地成 V3 自造参数文件。除 `设置驱动` 的驱动私有域外，可由 LinuxCNC/runtime INI、`PARAMETER_FILE`、`tool.tbl`、HAL/IO 或登记 native gate 承载的字段必须迁入对应 owner；历史上已经造出的 `system_params`、`axis_params`、`tool_params`、`wcs_offset_params`、`io_params` 文件或写入 owner 要删除。

最高参数表闭环：
- 设置页参数表所有字段都遵守 `开机硬盘/native owner -> native/运行内存 -> UI显示；坐标/模态/速度倍率再投影到 SHM`，以及 `UI dirty -> Broker/settings_apply/native owner -> 写唯一 owner -> 刷新 native/运行内存 -> UI回读；允许字段同步 SHM 显示投影`。
- UI dirty 值只表示编辑/请求态；按钮弹窗里的“写入参数表”必须理解为写入该字段唯一 owner，不能理解为新增或继续使用 V3 自造参数文件。
- 保存按钮、即时保存、读取驱动后的表格刷新都必须以 fresh native/运行内存回读或受控 read-only 内存回读快照更新 UI；坐标、模态、速度和倍率可同步刷新允许的 SHM 显示投影。`G53_A_Y/G53_A_Z/G53_B_X/G53_B_Z/G53_C_X/G53_C_Y` 在软键盘 `OK` 或下拉/单元格提交时就是确认保存，必须立即通过 `settings_apply` 单字段写 active runtime INI `[RTCP]` owner 并完成源位置回读；它们不得落入 `settings_runtime.json`，也不得把 UI dirty 值当运行态真值。A 中心 X 显示当前模态加工坐标系 X 偏移值，B 中心 Y 显示当前模态加工坐标系 Y 偏移值，C 中心 Z 显示当前模态加工坐标系 Z 偏移值，`G53_A_X` / `G53_B_Y` / `G53_C_Z` 不得进入 dirty 或保存链路；B 中心 X/Z 参考 A/C 中心坐标体系，使用 `G53_B_X/G53_B_Z` 或等价 native geometry owner。旧 `settings_runtime.json`、`machine_settings.json`、默认值、`/run` 结果或 dirty 值不得冒充当前生效参数。

| 参数域 | 写法 |
| --- | --- |
| 系统设置 | 轴配置、运动模式、回零、软限位经各自 owner 写入；`motion_driver_mode` 写 `motion_driver.conf` owner；`G53_A_Y/G53_A_Z/G53_B_X/G53_B_Z/G53_C_X/G53_C_Y` 写 runtime INI `[RTCP]` owner 后由 native/readback 显示；A 中心 X、B 中心 Y、C 中心 Z 来自当前模态加工坐标系偏移值，`G53_A_X` / `G53_B_Y` / `G53_C_Z` 不进 dirty 或保存链路；`system_params` / `axis_params` 只能作内存视图名，不得建文件 |
| 刀具补偿 | 真实 TLO、刀长、刀半径来自 LinuxCNC tool table / native tool owner / `stat.tool_offset`；`tool_params` 不得建成 V3 刀补文件，T0 显示刀柄长度只作显示 |
| 偏置 | G54-G59.3 来自 LinuxCNC `PARAMETER_FILE` / 参数接口或登记 native readback，直连显示；不得经 SHM `wcs_offsets` 发布，`wcs_offset_params` 不得建成 V3 偏置文件 |
| IO设置 | IO 映射、极性、滤波和互锁归 HAL/IO/native owner；`io_params` 只能作内存视图名，不得和普通显示参数混表保存或新建 V3 文件 |

设置页右上角小“机械坐标/MCS”面板是当前状态显示，不是参数表字段。它必须和主页机械坐标大字使用同一个 UI 显示域：基础来自同一帧 SHM `status->mcs`，A/C 在 `rotary_phase` 有效时显示同一等效角。不得在设置页改用未套用 `rotary_phase` 的原始 `mcs`，否则 A/C 会出现设置页和主页读数不一致。数值字体和布局也必须和主页一致：使用同一套七段数码字画布绘制函数和固定宽度数值区域，设置页不得对坐标值使用普通 label 字体，避免数值变化时宽度和位置跳动。

设置页 `设0/零位` 的软键盘不得把 SHM 坐标显示投影自动写成参数输入：Pulse/StepDir 模式下点击轴 `设0` 打开 `HOME_OFFSET` 输入键盘时，只能显示当前字段 owner/readback 初值或空值，由操作员明确输入当前轴 0 位坐标。右上角 MCS 面板和主页机械坐标只作显示参考，不得被 UI 自动复制为 `HOME_OFFSET`、不得作为保存准入或成功证明。

## 2. 当前源码按钮清单

当前 VM 真源 `/root/Desktop/v5` 中，设置页入口以 `app/src/v5_settings_page.c`、`app/src/v5_settings_actions.c`、`services/drive_profile/v5_settings_actiond.py` 和对应 `services/*/v5_*` 动作为准；旧 `lvgl_app/src/v3_*` 只可作视觉或历史参考，不再作为代码 owner。

### 2.1 顶部按钮

| 当前显示 | 坐标 | action | shell_action | 当前代码实际路径 | 作用和目的 |
| --- | --- | --- | --- | --- | --- |
| 登记本机码 | `x=806 y=8 w=94 h=34` | `settings_dna_register` | `RE_V3_SHELL_DNA_REGISTER_GATE` | `execute_dna_register_gate_command()` 组包 `action=settings_dna_register`、`native_action=device_dna_register`，再调用 `g_ui.command_handler("device_dna_register", ...)` | 现读本机 PL DNA 和设备公钥并提交 VPS 登记；VPS 是 6 位 `vpsDistributionId` 的生成和保存 owner，板端不得用 DNA 本地推导值当最终 ID。VPS 必须先验证设备公钥，公钥通过后才允许登记 DNA、计算/冻结 ID、在数据管理中记录 ID-DNA-公钥关系、创建 `<vpsDistributionId>-<pl_dna_hash>` private 文件夹；同一 PL DNA 已登记时直接返回已保存 ID 给板端，不得重新生成 ID。该 folder 只作为 VPS 内部存储路径，不得暴露到客户端 URL。VPS 只把 ID 和登记校验状态返回板端，板端本地登记状态只允许写入 `vpsDistributionId`、登记结果码和公钥摘要，不得保存 raw DNA、DNA hash、`pl_dna_hash` 或 id-dna folder；刷新设置页顶部 `本机码` 标签时只显示该 6 位 ID 和登记状态，不显示 DNA 片段或 hash。`DNA_REGISTER_UPLOADED_PENDING_AUTH` 属于登记成功/等待授权文件，不得显示成登记失败。VPS 未返回合法 ID、公钥校验失败、ID-DNA 关系不能确认或本地状态写入失败时必须 fail-closed。 |
| 保存并重启 | `x=902 y=8 w=92 h=34` | `settings_save_return` | v5 settings save/restart handoff | 当前代码以 `app/src/v5_settings_page.c` 创建 `保存并重启` 按钮；触发 `V5_MAIN_PAGE_ACTION_SETTINGS_SAVE_RETURN`，现有 UI 状态提示为“参数已落盘，等待受控重启”。 | 设置页面里任何参数发生改动后，右上角都保持 `保存并重启`，不保留 `保存` / `保存并返回` 中间态。当前 UI 更改任意参数值时已经直接写入对应参数表/owner 并按该字段回读刷新；因此右上角 `保存并重启` 不再承担首次保存字段值，实际语义是触发微内核重启与 UI 重启 handoff，让已落盘参数重新进入运行内存。软键盘 `OK`、下拉选择、设0/零位写入、驱动读回刷新、表内派生值刷新等任何设置页参数变更都必须进入这个重启待处理状态。每个字段仍只允许写入其唯一 owner；除 `设置驱动` 的驱动私有域外，不得扩展成 V3 自造参数文件、过渡配置文件或微内核第二真源。`保存并重启` 的前台完成边界是已落盘字段完成本字段源位置回读、restart precheck 通过、微内核 + UI restart handoff 发起成功并恢复主页面；不在按钮响应里同步等待完整 native 状态恢复、machine on 或驱动 Operation Enabled。后台恢复失败必须记录诊断，但不得把 handoff 写成运行态已经完全恢复。 |

注意：当前源码中 `下载授权` 不在顶部，也不在 `登记本机码` 左侧；它在 y=236 的驱动动作行最左。y=236 驱动动作行的 `下载授权`、`服务器下载`、`扫描从站`、`复位驱动`、`读取驱动`、`清除故障`、`设置驱动` 必须保持单行显示，不得再用 `\n` 拆成两行。

参数表清 dirty 的最高解释：上表任何“成功后清 dirty”都只能理解为该字段唯一 owner 写入成功并完成本字段源位置回读；脚本返回、文件写入、UI dirty 值、`settings_apply_progress.json`、`config_cache` 或旧 `/run` 快照都不能单独触发清 dirty 或显示保存成功。需要重启生效的字段，其最终运行态真值仍等微内核重启后的 native/运行内存 fresh 回读。

软键盘输入边界：打开软键盘和数字、`.`、`+/-`、`BKSP`、`C` 输入只更新软键盘上方输入框的 UI 进程内临时值，不强制同步刷新整屏、不写 dirty、不发 Broker、不写事件日志热路径、不触发保存或参数表刷新；`ESC`/取消必须恢复打开软键盘前的原值，不得产生 dirty、Broker 请求或硬盘写入。只有 `OK` 才把当前输入值提交到对应字段并触发该字段确认保存。`OK` 后仍按单字段闭环执行：只提交本字段，只写本字段唯一 owner，只回读/校验本字段，不得因为一次软键盘提交全量读取或重建整张设置参数表。

软键盘可见期间必须进入 modal 输入态：键盘与设置页参数表、右上角保存/返回按钮、y=236 驱动动作行之间必须有全屏可点击的透明/不可见 blocked mask，或等效的全屏 keyboard layer click-swallow 机制，拦截所有非键盘目标的触摸事件。数字键、`OK`、`ESC`、`BKSP`、`C` 只能由键盘层处理；点击键盘外区域不得穿透触发底层单元格、保存并重启、设置驱动、扫描从站、复位驱动、读取驱动、清除故障或其它动作按钮。键盘关闭后先隐藏 blocked mask/keyboard modal，再释放底层输入；现有短输入过渡保护仍必须保留，用于阻断同一次触摸释放穿透到底层控件。

右上角 `保存并重启` 的交互边界：字段 owner 写入和源位置回读已在各字段提交时完成；按钮只发起一次微内核重启与 UI 重启 handoff，不在弹窗里等待完整 native 状态恢复。handoff 成功返回后立即关闭进度窗口、返回主页面，顶部状态显示微内核和界面正在重启。新的 v5 UI 进程再从 native/运行内存回读显示参数；允许的坐标/模态/速度倍率同时来自 SHM 显示投影。handoff 不是 native 状态已恢复，也不是 machine-on/Operation Enabled 已确认；不能把它写成参数运行态已经生效的证明。

### 2.2 y=236 驱动动作行

BUS/EtherCAT 可用状态下，y=236 这一排驱动动作按钮（`下载授权`、`服务器下载`、`扫描从站`、`复位驱动`、`读取驱动`、`清除故障`、`设置驱动`）按下后必须立即弹出同一格式的进度窗口。窗口格式固定为首行 `进度 n/m: <当前阶段>` 或当前代码等价阶段文案，末行 `耗时 <秒> 秒`；后端返回 `code`、`failed_stage` 或 `message_cn` 时继续在同一个窗口追加显示，不允许只在顶部黄字提示，也不允许长时间停留在固定“正在执行”文案。按当前 v5 代码，`服务器下载` 优先读取 `/run/8ax_v5_auth_download/drive_profile_server_download_progress.json`；驱动读/写类动作以 `/run/8ax_v5_drive/*_result.json` 和 UI 本地阶段/倒计时为准，不能要求不存在的 v3 progress 文件。没有专用进度 JSON 的 `下载授权`、`扫描从站`、`读取驱动`、`复位驱动`、`清除故障`、`设置驱动` 必须由 UI progress owner 提供本地阶段兜底，并持续显示耗时，直到 actiond/后台返回最终结果。长流程弹窗的 `关闭` 必须立即隐藏弹窗并释放触摸界面；若后台 job 仍在运行，UI 只能通过正式 `job_cancel` 请求停止后台动作，不能在弹窗里继续等待取消结果。后台后续返回取消、失败或成功时，若该弹窗已被操作员关闭，不得重新弹出覆盖设置页，只能记录事件和顶部状态。

按 `REQ-BUS-PULSE-MODE-DIFFERENCE`，Pulse/StepDir 模式下 `服务器下载`、`扫描从站`、`复位驱动`、`读取驱动`、`清除故障`、`设置驱动` 必须 Broker/product action、C dispatch、UI 视觉三层拒绝，也不进入本节进度弹窗；`下载授权` 仍按授权/DNA owner 判断。

| 当前显示 | 坐标 | action | shell_action | 当前代码实际路径 | 作用和目的 |
| --- | --- | --- | --- | --- | --- |
| 下载授权 | `x=410 y=236 w=82 h=34` | `settings_auth_download` | `RE_V3_SHELL_AUTH_DOWNLOAD_GATE` | `execute_auth_download_gate_command()` 组包 `action=settings_auth_download`、`native_action=device_authorization_download`，再调用 `g_ui.command_handler("device_authorization_download", ...)` | 发起授权下载和验签请求。执行前必须读取本地保存的 `vpsDistributionId`，再现读本机 live DNA 并在内存中计算 DNA hash；请求 URL 只带 6 位 ID，同时把临时读取的 DNA/DNA hash 作为校验字段发给 VPS，由 VPS 校验 ID-DNA 绑定。DNA 和 hash 只允许本次动作内存使用，不得落盘、不得写 UI/日志/SHM。VPS 校验 ID-DNA 通过后，在服务端解析到 `<vpsDistributionId>-<pl_dna_hash>` private 文件夹并下载 `device_authorization.json`；客户端不得把 id-dna 放进 URL。没有已登记 ID 时提示先登记本机码，不得只按 DNA hash 查找授权文件。具体授权文件、DNA 匹配、VPS API 规则只引用 `功能/VPS登录与项目对接说明.md`。 |
| 服务器下载 | `x=494 y=236 w=82 h=34` | `settings_server_download` | v5 action `drive_profile_server_download` | `v5_settings_actions.c` 发送 `drive_profile_server_download` 到 `v5_settings_actiond.py`，由 `services/auth_download/v5_drive_profile_download.py` 执行。 | 发起驱动 profile/映射表下载请求。执行前必须读取本地保存的 `vpsDistributionId`，再现读本机 live DNA 并在内存中计算 DNA hash；私有映射表请求 URL 只带 6 位 ID，同时把临时读取的 DNA/DNA hash 作为校验字段发给 VPS，由 VPS 校验 ID-DNA 绑定。DNA 和 hash 只允许本次动作内存使用，不得落盘、不得写 UI/日志/SHM。VPS 校验 ID-DNA 通过后，在服务端解析到 `<vpsDistributionId>-<pl_dna_hash>` private 文件夹并下载 `driver_profile_map.json`；客户端不得把 id-dna 放进 URL。private 缺失可按规则记录 absent/skipped 后处理 public fallback，但 private 存在却 ID-DNA、授权、hash 或签名不匹配时必须 fail-closed。等待期间弹窗按当前代码读取 `/run/8ax_v5_auth_download/drive_profile_server_download_progress.json` 显示分步进度，最终结果来自 `/run/8ax_v5_auth_download/drive_profile_server_download_result.json`；若进度文件暂未出现，弹窗只能显示正在等待后端阶段进度并显示耗时秒数。当前 UI 成功提示写“公共和私有映射表已同步到板端”，但实际 public/private、absent、校验、运行期 profile 选择规则以 `功能/1驱动命令映射表命令需求.md` 为准。 |
| 扫描从站 | `x=578 y=236 w=82 h=34` | `settings_scan` | `RE_V3_SHELL_DRIVE_SCAN_GATE` | `execute_drive_bus_gate_command(..., "drive_scan_slaves", "扫描从站")`；请求为 `action=settings_scan`、`native_action=drive_scan_slaves` | 发起 EtherCAT 从站扫描请求。Broker 合同接受 `settings_scan/drive_scan/drive_scan_slaves`，脚本侧映射为 `scan`。成功弹窗按两列显示最多 8 个从站条目，格式为 `slave_index name state/未匹配`，两列之间用 `|`；不读取 JSON `axis` 字段，也不把扫描结果定义成轴，轴定义仍由设置页“从站”列决定。 |
| 复位驱动 | `x=662 y=236 w=82 h=34` | `settings_drive_reset` | `RE_V3_SHELL_DRIVE_RESET_GATE` | v5 当前 action 为 `drive_factory_reset`，经 `settings_actiond.sock` 转给 `services/drive_profile/v5_drive_bus_action.py`。 | 业务语义必须是恢复驱动厂家默认参数，通过映射表 `drive.restore_factory_defaults` 执行。按钮内部必须使用本次动作实时 `scan_slaves()` 的从站结果匹配当前轴表 `slave_index` 和 profile；不能要求用户先手动点 `扫描从站`，也不能使用旧缓存或上次扫描结果执行写入。任一目标轴无当前从站、从站非 OP、profile 未匹配或 profile 缺 `drive.restore_factory_defaults` 时，本次复位必须整体 fail-closed，不得部分写入其他驱动。恢复后必须自动执行一次驱动参数读取，把读回值写入 drive-only 设置驱动私域，并确认每轴最终读回 `ok=true` 且无 fault/error；自动读回或校验失败时不能返回成功。它不是清除故障、不是逐轴 `drive.software_reset`，执行后会清除驱动内部参数，使电子齿轮写入状态、设0基准、编码器校准和 count-domain trust 全部失效。复位驱动成功属于设置页参数/驱动私域状态改动，右上角必须保持 `保存并重启`，由操作员统一触发微内核重启与 UI 重启 handoff；不得再写成 `保存并返回`。后续若要恢复运动可信状态，必须单独执行 `设置驱动`、重新设0/验证；设置驱动成功后再按设置驱动自己的生效流程处理。 |
| 读取驱动 | `x=746 y=236 w=82 h=34` | `settings_read` | v5 action `drive_parameter_read` | `v5_settings_actions.c` 发送 `drive_parameter_read`，`v5_settings_actiond.py` 转为 `v5_drive_bus_action.py` 的 `read`。 | 发起驱动参数读取请求。当前代码结果文件为 `/run/8ax_v5_drive/drive_read_result.json`；它只能作为本次动作证据或诊断，禁止使用旧 `settings_read_result.json`、`settings_read_only` 或 v3 `/run` 路径作为产品路径。设置页刷新下方轴参数表时只消费 fresh native/运行内存回读中的同轴读回对象；SHM 显示投影不得作为驱动读回对象，不得按 `/run` 文件更新时间选择结果快照。轴参数表显示 `bit`、电子齿轮分子/分母和写入状态；`bit` 默认来自映射表/本机参数表，用户通过三角下拉人工选择时优先于映射表默认值。总线轴参数表不再显示 `驱动count/圈`、`多圈周期`、`count/圈` / `counts_per_rev`，也不再显示 `actual_precision` / `count/单位` 列；这些 count 域和内部比例只保留在后端校准、运动配置、旋转 count-domain 和诊断对象中。 |
| 清除故障 | `x=830 y=236 w=82 h=34` | `settings_fault_reset` | `RE_V3_SHELL_DRIVE_FAULT_RESET_GATE` | `execute_drive_bus_gate_command(..., "drive_fault_reset", "清除故障")`；请求为 `action=settings_fault_reset`、`native_action=drive_fault_reset`，并带 `confirm_drive_fault_reset=true` 和 `confirm_drive_write_one_shot=true` | 只发起驱动报警/fault 清除请求。Broker 接受显式全局写门禁，或当前设置页按钮发出的精确 UI 单次确认；通过后由驱动映射表的 `drive.reset_fault`/控制字对象执行。成功只表示报警、错误码或 fault bit 已清除，不改变轴参数、不恢复出厂、不证明电子齿轮已写入。 |
| 设置驱动 | `x=914 y=236 w=82 h=34` | `settings_set_drive` | v5 action `drive_set_parameters` | `v5_settings_actions.c` 发送 `drive_set_parameters`，`v5_settings_actiond.py` 转为 `v5_drive_bus_action.py` 的 `set-drive`。 | 发起设置驱动入口：当前 v5 代码结果文件为 `/run/8ax_v5_drive/drive_set_result.json`。完整目标流程仍是按轴 `profile_id` 取 profile 默认 `encoder_bits`，若本机参数表存在人工 bit 覆盖则优先使用人工 bit，并由最终 bit 派生 `drive_internal_counts_per_motor_rev` 后再恢复出厂、计算电子齿轮、写入并读回；不得引用已删除的 `驱动count/圈` 表列、旧 profile count 或 UI 默认 count。该按钮不执行上使能或正反微动。当前代码写类动作尚未闭合真实写入/读回时必须继续 fail-closed，不能为了 UI 可点而伪造成成功；完整动作和失败规则只看 `功能/4设置页设置驱动按钮作用和目的.md`。 |

Latency rule: editable setting cells and the numeric settings keyboard handle press-down events, not click-release events. Opening the keyboard and showing typed digits must stay in the local UI layer and must not wait on background MDI/runtime projection refresh, Broker, or touch/event diagnostic logging. 可编辑单元格按下到软键盘出现并显示可输入数字不得超过 `0.2s`；数字键按下到输入框显示该数字也不得超过 `0.2s`。软键盘 `OK` 或下拉提交按下后，单字段唯一 owner 写入、源位置回读、UI 清 dirty 或结构化失败显示不得超过 `0.5s`。驱动长流程和 `保存并重启` 不按 `0.5s` 等最终硬件动作完成，但按下后 `0.2s` 内必须出现进度窗口或明确提交反馈，随后持续显示阶段进度和耗时。超过时限必须定位并修复慢点，不能继续后续步骤掩盖。

### 2.3 需要自动安全前置的设置页按钮

以下设置页按钮会写驱动内部参数，不能再要求用户先手动急停或手动下使能后重试：

| 按钮 | 自动前置动作 | 成功后继续 | 失败处理 |
| --- | --- | --- | --- |
| `复位驱动` / `settings_drive_reset` | Broker 在执行 `drive_factory_reset` 前自动调用正式 `drive_write_machine_off` 产品前置：清空运动意图、切手动、请求下使能或急停安全窗口，并由 microkernel/native drive-write gate 返回是否允许继续；`task_state=4/machine_off`、SHM 速度、JSON 状态或 V3 私有阈值都不能单独放行；机器已急停/下使能时可由 native gate 直接确认满足，不要求用户手动急停；native gate/helper 异常或慢返回后必须重新读取 fresh native 状态，若 native gate 已确认安全窗口成立，则安全准备成功，不允许继续报 `BROKER_DRIVE_WRITE_SAFETY_PREP_FAILED` | 进入驱动脚本后必须先实时扫描从站并匹配 profile，再执行 `drive.restore_factory_defaults`；如果恢复出厂后驱动短暂进入 fault/error，必须只用当前 profile 的 `drive.reset_fault` 按 `retry/timeout_ms` 清除并再次读回；随后自动读取驱动参数并刷新轴参数表；只有扫描匹配、复位写入、fault 清除和最终读回校验都成功才算成功 | 返回 `BROKER_DRIVE_WRITE_SAFETY_PREP_FAILED`、`DRIVE_RESET_PRECHECK_FAILED` 或 `DRIVE_RESET_PARTIAL`，不把未扫描匹配、未写入或未校验读回的复位动作当成功 |
| `设置驱动` / `settings_set_drive` | 设置驱动入口；先保存 dirty，尤其是 `bit` 人工选择，再由 Broker 在 `drive_set_parameters` 前自动执行写驱动安全前置 | 继续执行 `drive_set_parameters`；使用最终 bit 派生的 `drive_internal_counts_per_motor_rev` 和螺距/减速比/目标精度计算电子齿轮，只做恢复出厂、电子齿轮计算/写入/读回 | 详见 `功能/4设置页设置驱动按钮作用和目的.md` |
| `计算bit` / `settings_calc_bit` | 禁止 | 不再执行 `drive_calculate_bit`、不再微动、不得通过 `log2(count)` 或样本计算 bit | bit 默认直接读取映射表/驱动参数表；轴参数表在分子左侧显示 bit 列，允许人工下拉选择且人工选择优先。多圈周期、驱动内部一圈 count 不作为轴参数表列显示 |

`读取驱动` 和 `扫描从站` 是只读入口，不自动改变机器安全状态；`清除故障` 只清驱动报警，当前只要求运动内核空闲，不把它升级成强制急停/下使能动作。`设置驱动` 只引用 `功能/4设置页设置驱动按钮作用和目的.md`。后续若某个设置页按钮新增“必须急停/下使能/上使能微动”的后端门禁，必须同时加入本节的自动前置表，而不是新增要求用户手动操作的弹窗。

上使能不是危险动作。设置页任一动作需要上使能时，必须由后端自动调用正式 product action，并以 microkernel/native enable gate 的返回和原生回读作为结果；不得要求操作员手动上使能，也不得把上使能做成危险确认弹窗。V3 不再用解释器 idle、速度容差、queued MDI 或 SHM/JSON 状态拼上层门禁。只有转动轴相关动作按危险动作处理：A/C 旋转轴设零、A/C 轴坐标写入、会导致旋转轴实际运动或相位重定的动作必须二次确认；X/Y/Z 直线轴设零、普通保存、读取、下载和上使能本身都不是危险动作。

显式急停按钮不属于上述写驱动安全前置表。急停只能走 Broker 到 native/microkernel 急停 gate/helper 的薄适配，成功以 microkernel/native 回读确认；不得把设置页驱动写入前置、EtherCAT `0x6041` 状态字扫描或 V3 私有 `estop_loop` 逻辑复用成急停成功真源。

`复位驱动` 是长耗时写入动作，等待弹窗不能只显示静态“正在执行”。按钮触发后必须保持弹窗打开，并按阶段刷新进度：提交复位请求、扫描从站并匹配驱动资料、请求 native drive-write safety gate、恢复厂家默认参数、清除恢复后的报警并读回驱动参数、等待后端完成复位与读回校验。复位驱动必须使用专用长流程 job 等待上限，不能和普通驱动写入共用短超时；后台仍在恢复出厂或读回校验时，UI 不得先报 `DRIVE_WRITE_JOB_TIMEOUT`。最后一阶段不能伪装成已经在刷新轴参数表；只有 Broker 返回成功并完成读回写入后，才允许刷新设置页表格。最终成功或失败弹窗仍以 Broker 返回码和读回校验结果为准。

`服务器下载` 和 `读取驱动` 虽然不是写驱动参数的高风险动作，也必须使用和长流程按钮一致的阶段反馈。按钮触发后弹窗保持打开，先显示请求已提交；`服务器下载` 按当前代码从 `drive_profile_server_download_progress.json` 刷新真实后端阶段；`读取驱动` 当前以本地阶段/倒计时和 `/run/8ax_v5_drive/drive_read_result.json` 最终结果为准，不得写成依赖当前代码不存在的专用 read progress 文件。阶段文本必须显示 `进度 n/m` 或当前代码等价阶段文案和 `耗时 x 秒`；失败时同一弹窗显示 `code/message_cn/failed_stage`，不得长时间停在固定“正在从服务器下载驱动映射表，等待本机授权校验”或只显示最终“读取驱动完成”。

`设置驱动` 也是长耗时写入动作，等待弹窗必须显示当前代码可取得的阶段进度：提交请求、扫描/匹配目标轴、逐轴恢复出厂、逐轴计算电子齿轮、逐轴写入电子齿轮和模式、逐轴读回校验、最终读回和运行参数刷新。当前 v5 代码以 UI 本地阶段/倒计时和 `/run/8ax_v5_drive/drive_set_result.json` 最终结果为准，不得写成依赖当前代码不存在的专用 set-drive progress 文件。`drive_set_parameters` 必须使用专用长流程 job 等待上限，避免后台已返回 `DRIVE_SET_OK` 时 UI 先报 `DRIVE_WRITE_JOB_TIMEOUT`。后台最终返回 `DRIVE_SET_OK` 且轴参数表显示 `已写入` 后，右上角按钮必须保持 `保存并重启`。

驱动长任务一旦进入 `BROKER_JOB_ACCEPTED` 异步模式，UI 主线程只允许按短周期提交 `job_status` 快查询并刷新弹窗，不能同步等待最终写入结果。`BROKER_JOB_RUNNING` 表示通道正常并清零连续失败计数；`JOB_STATUS_BROKER_SOCKET_UNAVAILABLE`、`JOB_STATUS_BROKER_DIRECT_FAIL`、handler 返回失败、状态结果无法解析或缺失状态码都计入通信失败。连续 5 次失败后，UI 必须结束当前异步任务并显示“后台通道连续不可用”的结构化失败，不得继续等到长超时才让操作员看到失败。若 `job_status` 返回业务最终失败码，则保持原始业务 `code/message/detail`，不要改写成通道丢失。

Broker 异步长任务必须携带 job ownership 元数据：`job_id`、`job_action`、`state`、`started_ns/submitted_ns/completed_ns`、`deadline_ns`、`cancel_requested`、`cancel_reason` 和 `final_result_owner`。取消只能通过正式 `job_cancel` 请求进入，先把任务置为 `cancelling`，由 worker 短周期检查 cancel token/deadline 后进入 `cancelled` 或 `failed_closed`；UI 不得本地伪造取消完成。取消、超时或异常后的最终 `job_status` 仍必须保留原始 `job_id` 和 `final_result_owner`，防止后台结果丢失或串到其它请求。

## 3. 容易写错的点

1. `读取驱动` 走 `drive_parameter_read`；禁止 `settings_read_only` 产品路径作为设置页按钮、Broker route、UI 刷新兜底或 `/run` 读回入口。
2. `下载授权` 当前不在顶部；它在 y=236 的驱动动作行，且该行动作按钮必须保持 82px 宽的单行文字，不允许再拆成两行。
3. `复位驱动`、`清除故障`、`设置驱动` 当前 UI 发起写类请求并带 action confirm 与 `confirm_drive_write_one_shot=true`；Broker 只接受该精确 UI 单次确认或显式全局写门禁。`复位驱动`、`设置驱动` 是长流程，不再把 UI 请求里的旧 `status_epoch` 当成访问驱动前的门禁；安全性由执行时重新读取微内核/native 内存状态、请求身份和映射表对象支持情况保证。`设置驱动` 的写类请求边界只引用 `功能/4设置页设置驱动按钮作用和目的.md`。
4. `设置驱动` 的按钮功能已剥离出本文；详见 `功能/4设置页设置驱动按钮作用和目的.md`。
5. `复位驱动` 就是恢复驱动厂家默认参数，必须走 `drive.restore_factory_defaults`，并在弹窗中写清会清除驱动内部参数、恢复后需要单独重新写电子齿轮和重新设0/验证；不能再把它写成清故障。复位驱动只做驱动复位和回读校验，读回结果只保存为设置驱动私域证据；成功后右上角保持 `保存并重启`，用于统一触发微内核重启与 UI 重启 handoff，不得再写成 `保存并返回`。
6. `清除故障` 只是清除驱动报警；它和 `设置驱动` 的边界详见 `功能/4设置页设置驱动按钮作用和目的.md`。
7. 轴参数表任意可编辑字段在软键盘 `OK` 或下拉选择变化后，成功只代表本字段唯一 owner 已按当前 v5 受控路径写入，并只完成本字段源位置回读；运行内存/允许的 SHM 显示投影刷新仍等右上角 `保存并重启` 触发微内核重启与 UI 重启 handoff。除 `设置驱动` 的驱动私有域外，这不表示 V3 参数表成为长期真源，最终仍以 LinuxCNC/native 内存/API 回读为准。
8. 顶部按钮按当前 v5 代码为 `保存并重启`；设置页任何参数已改动时，它只负责微内核重启与 UI 重启 handoff。`设置驱动` 前若需要使用最新字段，必须消费已写入对应 owner 的字段读回，不得从 UI dirty 或旧 v3 source 名称推导。
9. `保存并重启` 的前台完成边界是字段已逐项源回读确认、restart precheck 通过、微内核 + UI restart handoff 已发起并能恢复主页面；它不再在按钮响应里同步等待完整 native 状态恢复、machine-on 或驱动 Operation Enabled。微内核重启、UI restart、新 UI 进入后上使能和运行态恢复仍由后台流程和 native/运行内存真值链路继续完成。
10. 如果后台重启恢复阶段后续发现 `status_shm_stale`、`STATE_PUBLISHER_RESTARTING`、machine on 或 BUS/EtherCAT Operation Enabled 未恢复，必须保留诊断状态，不能把这些后台状态冒充为已恢复运行态；但它们不再阻断右上角按钮的轻量 handoff 返回主页面。settings 页轻量保存重启必须在新 UI 恢复/进入主页面后通过正式 `estop_or_reset` 产品动作发起上使能；后续运行准入仍以独立 native 真值和运动门禁判断。
10. `settings_save_return` 无 dirty 且无 restart pending 时只是本地返回主页，不发 `settings_apply`。
11. 从轴、S轴、刀库三个按钮当前都是 blocked，不是待验收的真实功能。
12. `settings_save_return` 的进度弹窗是操作员在等待后端期间的可见反馈，必须持续刷新阶段文本；失败必须在弹窗里显示结果，顶部黄色字不能替代弹窗内容；restart handoff 成功按当前代码关闭进度窗口、返回主页面，并用状态消息提示后端和界面正在重启。

## 4. 本文不重复维护的内容

- DNA 上传、授权下载、VPS gateway、主备域名和文件路径：看 `功能/VPS登录与项目对接说明.md`。
- BUS/Pulse 模式差异、模式切换、Pulse 禁用 BUS 驱动动作：看 `功能/总线脉冲区别.md`。
- 服务器下载 public/private、schema、运行期 profile 选择和标准命令：看 `功能/1驱动命令映射表命令需求.md`。
- `设置驱动` 的完整动作、安全前置、电子齿轮、读回验证、开机内存加载和 fail-closed：看 `功能/4设置页设置驱动按钮作用和目的.md`。
- 设置页 `设0/零位`：看 `功能/4轴设0软件偏置方案.md`。
- 主页面 `回零/归零`：看 `功能/8回零按钮开机强制回零与双模式真实回零方案.md`。
## 2026-06-21 轴设0按钮语义修订

设置页每个轴行上的“设0”按钮是单轴按钮，不是全局重启或全轴设零入口。

点击某个轴的“设0”时，UI -> Broker -> product action 只处理该轴：
- 只采集该轴驱动当前位置并写入该轴 `zero_model.zero_counts`、绝对/拆分计数和读写证据；不得恢复或写入顶层 `旧浮点零位字段`、`zero_model.旧浮点零位字段`、`zero_model.旧倒数比例字段` 作为产品输入。
- 不扫描全轴、不构建全轴 target、不改其它轴参数。
- 不在按钮动作中自动重启 backend/LinuxCNC、不执行 runtime rebase；BUS 设0本次动作只证明零点证据和 hard/raw limit owner 写入，UI 等效坐标清零只能作为最后统一保存重启后的显示闭环，不作为本次按钮通过门禁。
- 成功后设置页保持可操作，用户可以继续点其它轴设0或修改其它设置。
- 右上角按钮保持/切换为“保存并重启”，最后一次性执行统一生效和返回。

设置页 `设0` 的 UI 安全包装必须按驱动模式分流，但不能绕过 `功能/4轴设0软件偏置方案.md` 的产品语义：
- BUS/EtherCAT 或绝对总线模式下，X/Y/Z 直线轴点击单轴 `设0` 后直接发送本次单轴 `settings_axis_zero` / `bus_count_domain_zero` 请求，不弹危险确认；若后端需要上使能，必须自动上使能并用 fresh microkernel/native 回读证明。A/C 转动轴点击单轴 `设0` 后必须先显示二次危险确认弹窗，只有操作员在该弹窗内确认后，UI 才能组包发送请求；取消、关闭弹窗或触摸穿透都不得发送 Broker 请求。确认弹窗只授权本次 A/C 单轴请求，不得作为全局写门禁复用到其它按钮或后续点击。
- Pulse/StepDir 模式下，点击轴参数表单轴 `设零` 仍打开 `HOME_OFFSET` 软键盘；键盘不得用 SHM/MCS/`rotary_phase` 显示投影自动种子化输入，只能显示当前字段 owner/readback 初值或空值并等待操作员输入。Pulse 轴参数表另有 `回零方向` 列，通过下拉选择回零方向并写 `home_direction` owner；`设零` 键盘本身不得静默启用或提交 `home_direction=current`，X/Y/Z 也不得提交 `current`。
- A/C 转动轴 `设0` 的确认弹窗和 Pulse 软键盘都必须使用 modal blocked mask 或等效全屏输入吞噬机制；弹窗/键盘关闭时继续使用短输入过渡保护，避免同一次触摸释放落到底层轴表、右上角按钮或 y=236 驱动动作行。

BUS `设0` 成功结果代表当前轴零点证据已落盘、active raw 限位已同步回读；live raw 限位装载和 UI 等效坐标归零要在最后统一保存重启后闭合，不能把 SHM 显示投影写成本次动作成功证明。Pulse `设零` 成功边界按 Pulse owner 的 `HOME_OFFSET` 写入和后续回零/native 证明处理。
