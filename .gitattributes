# Push scope notes:
# - D:/v5 tracks project files by default, including docs, screenshots,
#   evidence/artifacts, Vivado-owned files, and published Windows client executables.
# - Only cache, temporary, and generated local build/run outputs are excluded
#   by .gitignore and by the desktop push script before commit.

8ax-win/publish/win-x64/*.exe filter=lfs diff=lfs merge=lfs -text
8ax-factory-client-source/publish/win-x64/*.exe filter=lfs diff=lfs merge=lfs -text
8ax-dealer-client-source/publish/win-x64/*.exe filter=lfs diff=lfs merge=lfs -text
