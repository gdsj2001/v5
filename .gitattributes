# Push scope notes:
# - D:/v5 tracks source, docs, configs, Vivado-owned files, and published
#   Windows client executables by default.
# - Cache, temporary files, generated build/run outputs, compressed packages,
#   and test proof/evidence captures are excluded by .gitignore and by the
#   desktop push script before commit.

8ax-win/publish/win-x64/*.exe filter=lfs diff=lfs merge=lfs -text
8ax-factory-client-source/publish/win-x64/*.exe filter=lfs diff=lfs merge=lfs -text
8ax-dealer-client-source/publish/win-x64/*.exe filter=lfs diff=lfs merge=lfs -text
board/third_party/petalinux-source-packages/** filter=lfs diff=lfs merge=lfs -text
board/third_party/petalinux-source-packages/v5_source_packages.json -filter -merge text diff
